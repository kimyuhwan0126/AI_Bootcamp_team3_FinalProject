# 모이머 실서비스 — 통합 결함·그릴링 원장 (2026-08-11)

에이전트 4개가 전수 조사한 것을 중복 제거해 합쳤다.
🔴 = **실제로 재현함**(추측 아님). ⚠️ = 코드 근거는 확실하나 실행 재현은 안 함. ❔ = 추측.

---

## A. 지금 당장 고쳐야 하는 결함 — 22건

### A-1. 신원·권한 (7)

| id | 심각 | 결함 | 상태 |
|---|---|---|---|
| B01 | 치명 | **GET 응답에 `pin_hash` 가 그대로 나간다** — 좌표·participantId 도 함께 | 🔴 재현 |
| B02 | 치명 | **강퇴자 재참여에 PIN 검사가 없다** — 이름만 알면 그 사람 쿠키를 받는다(사칭) | ⚠️ |
| B03 | 치명 | **PIN 이 저장만 되고 검증 0회** — 쿠키 잃으면 영구 퇴출, 방장이면 모임이 벽돌 | ⚠️ |
| B04 | 치명 | **이미 멤버가 다시 join 하면 참가자가 는다** — 표 부풀리기 | 🔴 재현 (4명) |
| B05 | 치명 | **kick/approve 가 소속 모임을 안 본다** — 남의 모임 사람을 강퇴 | 🔴 재현 |
| B06 | 치명 | **approve(false) 로 방장을 잠글 수 있다** — cannot_kick_host 우회 | 🔴 재현 |
| B07 | 높음 | 쿠키에 서명이 없다 — participantId 를 알면 그 사람으로 행세 | ⚠️ |

### A-2. 데이터 무결성 (6)

| id | 심각 | 결함 | 상태 |
|---|---|---|---|
| B08 | 치명 | **남이 찍어 준 내 후보를 두고 나가면 500** — 표는 이미 지워진 뒤 | 🔴 재현 |
| B09 | 치명 | **confirm 이 candidateId 를 검증하지 않는다** — 없는 id·남의 모임·kind 불일치 전부 통과 | 🔴 재현 |
| B10 | 높음 | **unping 에 단계 가드가 없다** — 확정된 후보를 지울 수 있다 | 🔴 재현 |
| B11 | 높음 | **refId 없는 ping 이 후보를 무한 증식** | 🔴 재현 (3곳) |
| B12 | 높음 | `winner_*` · `parked_under` 에 FK 없음 — 댕글링·영구 은닉 후보 | ⚠️ |
| B13 | 높음 | **트랜잭션이 한 곳도 없다** — createMeeting 중단 시 손댈 수 없는 모임 | ⚠️ |

### A-3. 외부 API (5)

| id | 심각 | 결함 | 상태 |
|---|---|---|---|
| B14 | 치명 | **Nominatim 초당 1회가 동시 요청에서 무력화** — 각자 재고 각자 잔다 | ⚠️ |
| B15 | 치명 | **`/api/places/search` 는 레이트리밋 자체가 없다** | ⚠️ |
| B16 | 치명 | **외부 fetch 에 타임아웃이 없다**(ai.ts 제외) — 화면이 영원히 로딩 | ⚠️ |
| B17 | 치명 | **AI 가 성공하면 환각을 검증 없이 후보로 등록** | ⚠️ |
| B18 | 높음 | **모든 catch 가 빈 블록** — 401(키 만료)과 "결과 없음"이 로그에서 같다 | ⚠️ |

### A-4. 화면 (4)

| id | 심각 | 결함 | 상태 |
|---|---|---|---|
| B19 | 치명 | **강퇴된 사람이 계속 본다** — 차단이 서버 렌더 1회뿐, SSE 로 갱신 계속 | ⚠️ |
| B20 | 높음 | **비참여자에게 '나가기' 버튼이 보인다** | ⚠️ |
| B21 | 높음 | **읽기 전용에서 지도 핀이 눌린다** — 핀은 `canPing` 을 안 봄 | ⚠️ |
| B22 | 높음 | **`quota_kakao` 가 영문 그대로 토스트에 뜬다** | ⚠️ |

---

## B. 그릴링으로 정해야 하는 것 — 재집계

| 갈래 | 건수 |
|---|---|
| 원장 381건 중 남은 것 (논의21~36 반영 후) | **46** |
| 에이전트가 새로 올린 **결정이 필요한** 항목 | **31** |
| **합계** | **77** |

### 새로 올라온 결정거리 31건

**규칙을 정해야 하는 것 (11)**
1. 강퇴자의 표를 지울 것인가 (leave 는 지우는데 kick 은 안 지움 — 규칙 3개가 서로 다름)
2. 0표 후보가 확정될 수 있는가 (지금은 조용히 됨)
3. 동점일 때 지도에 1위를 몇 개 크게 그릴 것인가 (지금 1개, 시트는 "2곳 동점")
4. reopen 이 winner 를 지울 것인가 (지금 남겨서 "아무것도 확정 안 된 모임"을 마무리할 수 있음)
5. 닫힌 모임을 지우거나 되돌릴 길을 줄 것인가 (지금 둘 다 불가)
6. voters(누가 어디 찍었는지)를 전원에게 공개할 것인가
7. 비참여자에게 참가자 이름·출발지를 보여줄 것인가
8. `region_type` 이 H 없이 B 만 올 때 법정동 코드를 쓸 것인가
9. 429(한도 초과)일 때 OSM 으로 폴백할 것인가, 멈출 것인가
10. `RADIUS_M` 700 하드코딩 vs `meetings.radius_m` 사용
11. AI 후보를 방장의 표로 셀 것인가 (지금 그럼)

**화면을 정해야 하는 것 (10)**
12. 외부 장애를 "여긴 고를 수 없어요"로 말하는 문구 고치기
13. 가게를 못 불러온 것을 "등록된 가게가 없어요"로 말하는 문구 고치기
14. 출발지 검색 실패를 "찾는 곳이 없어요"로 말하는 문구 고치기
15. 모임 만들기 실패 시 아무 반응이 없는 것
16. 모임이 삭제됐을 때 남의 화면이 어떻게 될 것인가
17. 승인 대기 화면이 수락되면 저절로 바뀔 것인가
18. 지도 둘 다 실패했을 때 안내를 줄 것인가
19. 모달에 Esc·포커스 트랩·`role="dialog"` 를 넣을 것인가
20. `.mini`(내보내기)·`.seg` 초점 표시
21. 375×667 에서 방장 도구 스크롤이 실제로 닿는지 (실측 필요)

**폴백을 어디까지 맞출 것인가 (5)**
22. OSM 폴백에도 겹친 핀 벌리기를 넣을 것인가
23. OSM 폴백에서 지점 단계에 지역으로 지도를 당길 것인가
24. OSM 1위 핀이 버튼을 덮는 z 순서
25. 되돌린 뒤 지점 단계로 다시 오면 지도를 다시 당길 것인가
26. 카카오가 4초 뒤 늦게 뜨면 지도 두 개가 공존하는 문제

**운영 (5)**
27. `/api/geo`·`/api/places` 에 인증·레이트리밋을 둘 것인가
28. ODsay·TMAP 를 실제로 붙일 것인가 (지금 환경변수만 있고 코드 0)
29. `OLLAMA_FALLBACK_*` 2순위 자동 전환을 구현할 것인가
30. 외부 실패를 로그에 남길 것인가 (지금 전부 빈 catch)
31. `.env.local` 이 작업 트리에 평문으로 있는 것

---

## C. 테스트 현황

| 갈래 | 있음 | 부분 | 없음 | 합계 |
|---|---|---|---|---|
| API·권한 | 30 | 10 | 126 | 166 |
| DB·스키마 | ~40 | ~15 | ~205 | ~260 |
| 화면 상태 | **0** | 0 | ~200 | ~200 |
| 외부 API·폴백 | 0 | 0 | ~90 | ~90 |

- `flowtest.mjs` 61개는 **행복 경로**만 덮는다.
- **UI 테스트가 하나도 없다.**
- `schematest.js` 13개는 **현 스키마로는 통과할 수 없다** (host_id FK, 지점 1인1표 폐기) — 재작성 필요.
- 자동화의 발목: 외부 URL 3개 하드코딩 · 실패 주입점 없음 · `geo.ts`/`db.ts` 가 모듈 로드 시 `neon()` 호출
