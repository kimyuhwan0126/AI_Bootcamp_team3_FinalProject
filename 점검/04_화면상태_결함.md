# 화면 상태·흐름 전수 조사 (에이전트 4) — 2026-08-11

**지금 저장소에 UI 테스트가 하나도 없다.** flowtest.mjs 는 API 만 훑어 화면 코드 경로는 0%.

## 심각 — 그릴링 규칙이 실제로 깨지는 곳

| # | 결함 | 근거 |
|---|---|---|
| 1 | **강퇴된 사람이 강퇴된 줄 모르고 계속 본다** | page.tsx 차단은 **서버 렌더 1회뿐**. 이미 열고 있으면 SSE 로 계속 갱신 → 논의29 위반 |
| 2 | **비참여자에게 '나가기' 버튼이 보인다** | ui.tsx 442-444 가 방장 아니면 무조건 그림. 누르면 not_member 토스트 |
| 3 | **읽기 전용 화면에서 지도 핀이 눌린다** | 목록은 static 으로 바뀌는데 핀은 카카오·OSM 둘 다 `canPing` 을 안 봄 |
| 4 | **OSM 폴백에서 '지점 단계는 확정 지역이 한가운데' 가 안 지켜짐** | `useState(center)` 로 초기값으로만 씀 → 논의32 위반 |
| 5 | **오류 코드가 그대로 노출** | `flash('quota_kakao')` — 번역표는 `send()` 안에만 |

## 중간

| # | 결함 |
|---|---|
| 6 | OSM 1위 핀(z 6)이 지도 위 버튼(z 5)을 덮는다 |
| 7 | **겹친 핀 벌리기가 카카오에만 있다** — OSM 폴백에선 논의21 이 깨짐 |
| 8 | **0표 후보가 조용히 확정될 수 있다** — 동점 시트도 안 뜨고 1위 표시도 없이 |
| 9 | 되돌린 뒤 지점 단계로 와도 지도가 안 당겨짐 (`placed`/`fitted` 리셋 안 함) |
| 10 | 키보드 선정 후 초점이 사라짐 (`disabled={busy}` + 재정렬) |
| 11 | 모달 4종에 Esc·포커스 트랩·`role="dialog"` 없음 |
| 12 | 모임이 삭제돼도 남의 화면은 살아 있음 (`reload` 가 404 무시) |
| 13 | 승인 대기 화면이 정적 — 수락돼도 안 바뀜 |
| 14 | 카카오 4초 타임아웃 뒤 늦게 로드되면 **지도 두 개가 공존** |
| 15 | `.mini`(내보내기)·`.seg` 에 focus-visible 없음 |
| 16 | 실패를 '없음'으로 말함 (출발지 검색) / 모임 만들기 실패는 **아무 반응 없음** |
| 17 | 375×667 에서 방장 도구가 넘침 (224px 자리에 328px) — 스크롤 도달 실측 필요 |

## 낮음
18. 700m 규칙에 300m 새는 구멍 (탭 좌표만 검사 후 r=300 검색)
19. `RADIUS_M` 하드코딩 (meetings.radius_m 무시)
20. 동점인데 지도엔 1위가 하나만 크게 — 시트와 어긋남
21. `iReopened` 가 실패한 reopen 에서도 남아 다음 알림을 삼킴
22. 만료된 대기자에게 '방장이 내보냈어요' 로 표시
23. 핀 클릭에 busy 가드 없음
24. 비참여자가 참가자 이름·출발지를 전부 봄 (설계 확인 필요)

## 권한 매트릭스에서 확인된 것
- B12: 비참여자에게 버튼이 없어야 하는데 `나가기` 가 뜬다
- B2/B4: 후보 0곳이면 확정 버튼이 감춰진다 (논의30 반영 확인)
- B6/B11: closed 면 `.acts`·`.acts-fix` 둘 다 미렌더

## 자동화 여건
API 시딩으로 모든 상태를 만들 수 있고, 쿠키 항아리 패턴은 flowtest.mjs 에 이미 있다.
카카오 성공 경로만 CI 에서 불가(스텁 SDK 필요). 나머지는 Playwright 로 전부 자동화 가능.
