# DB·스키마 무결성 전수 조사 (에이전트 2) — 2026-08-11

테스트 후보 **약 260개** (스키마 제약 66 · db.ts 함수 155 · 시간대 28 · 삭제연쇄 21 · 동시성 6).
정적 분석이며 실행 재현은 안 함.

## 치명

| # | 결함 | 근거 |
|---|---|---|
| R1 | **남이 찍어 준 내 후보를 두고 leave 하면 FK 위반 500** | `created_by` 에 ON DELETE 없음. 표는 이미 지워진 뒤라 **표만 잃고 모임에 남는다** |
| R2 | **`schematest.js` 가 현 스키마로는 통과 불가** | 32행 host_id='u1'(users id인데 FK는 participants) · 65행 "지점 1인1표"는 이미 폐기. **13개 통과 기록은 갱신 이전 것** |
| R3 | **winner_region_id·winner_place_id·parked_under 에 FK 가 없다** | 댕글링 우승자, 꺼낼 수 없는 영구 은닉 후보 |

## 높음

| # | 결함 |
|---|---|
| R4 | result 단계에서 우승 후보를 unping 으로 지울 수 있다 (unping 에 단계 가드 없음) |
| R5 | confirm 이 candidateId 를 검증하지 않는다 (없는 id·남의 모임·kind 불일치 전부 통과) |
| R6 | kick/approve 가 대상의 소속 모임을 안 본다 |
| R7 | `kstToInstant` 범위 검증 없음 — `2026-13-45T99:99` 통과, 형식 오타 하나로 **기존 시간이 지워진다** |
| R10 | PIN 이 저장만 되고 검증 0회. 방장은 `pin_hash` 자체가 없고 게스트 `upsertUser` 는 매번 새 users 행 |
| R12 | **트랜잭션이 한 곳도 없다** — createMeeting 중단 시 손댈 수 없는 모임이 남음 |
| R17 | reopen 이 우승자를 안 지운다 → 아무것도 확정 안 된 모임을 마무리할 수 있다 |

## 중간

| # | 결함 |
|---|---|
| R8 | pending 인 사람이 다시 참여하면 `name_taken` (사실과 다른 안내) |
| R9 | ref_id 가 null 이면 중복 후보 무한 생성 / 후보 이름을 나중 사람이 덮어씀 |
| R11 | 강퇴자의 표가 1위를 정한다 (leave 는 표를 지우는데 kick 은 안 지움 — 규칙 3개가 서로 다름) |
| R14 | geo_cache upsert 가 region_code·source 를 안 고침 → 같은 동이 `kakao 코드`와 `osm:이름` 두 줄로 갈림 |
| R15 | `normalize` 가 created_at·joined_at 등을 안 덮음 → RSC 경로에서 Date 객체가 그대로 감 |
| R16 | requested_at 이 null 인 pending 은 영원히 안 만료 |
| R18 | 주석 "남은 사람이 없으면 모임째 사라진다" 가 코드에 없음 |

## 추측
- R13 `voters` 가 배열이 아닐 수 있음 (`'{}'` 무타입 리터럴) → `.includes` 가 문자열 검색이 될 위험

## 미검증 큰 덩어리
시간대 경계 27건(자정·09:00·연말·서버 TZ 비의존) · 되돌리기 심화 12건 · nowKind 8조합 중 5 · CHECK 제약 다수 · 동시성 6건
