# 여기서 멈췄다 — 2026-08-12

토큰이 다 되어 **전부 일시정지**. 코드는 성한 상태다: `npx tsc --noEmit` 깨끗, `node flowtest.mjs` **85 · 실패 0**.

---

## 끝난 갈래 3 — 코드에 들어가 있다

| 갈래 | 고친 것 | 시험 |
|---|---|---|
| **들어오는 길** | A49 A18 A19 A22 A45 A59 A8 | `tests/entry.mjs` 15 → **33 · 실패 0** |
| **입력 검사** | A21 A17(검사만) A25 A37 A38 A39 A40 A41 A42 A43 A46 A16 + `transport` 무검증 | `tests/api.mjs` 15 → **63 · 실패 0** |
| **데이터 무결성** | A20(DB몫) A35 A13 A14 A15 A57 A60 + `setOrigin()` 신설 | `tests/db.mjs` 8 → **19 · 실패 1** |

`tests/db.mjs` 의 남은 실패 1건은 **route.ts 한 줄**이면 닫힌다 (아래 "먼저 할 것" ①).

## 중간에 멈춘 갈래 5 — 파일은 성하지만 일이 안 끝났다

| 갈래 | 어디까지 | 이어서 |
|---|---|---|
| **지도** `osmmap.tsx` | 다시 씀 (A11 배율·화면 밖 핀 제거·타일 죽으면 핀 숨김). 이름표 20개 빽빽할 때 52% 가 덮이는 게 진짜 한계인지 재던 중 | `tests/map.mjs` 를 돌려 지금 상태부터 확인 |
| **모임 화면** `ui.tsx` | 고침 28건 초록. **고치기 전 빨간불을 증명하려고 원본을 되돌리려던 참** | `ui.tsx` 가 고쳐진 판인지 원본인지 **먼저 확인**할 것 |
| **스타일** `globals.css` | 지도 파일이 바뀌어 시험을 맞추던 중 | `tests/css.mjs` |
| **바깥 서비스** | `maps.mail.ru` 만 살아 있고(1.8초) 나머지 둘은 죽음 — **지금 순서면 그 전에 9초를 버린다**. 순서 바꾸기 전 | `lib/places.ts` 거울 순서 |
| **운영 데이터** | 읽기 전용 점검기를 짜던 중. `점검/05_*` 아직 안 나옴 | 다시 띄우면 된다 |

---

## 다시 시작하면 먼저 할 것

1. **`route.ts` 에 세 줄** — 앞 갈래들이 남긴 것
   - `db.join` 이 던지는 `db.DbError` 를 잡아 400 (`name_taken`) — 이러면 `tests/db.mjs` 가 20/20
   - `route.ts:115` 에 `await db.setOrigin(prior.id, {...})` — A17 마무리
   - `app/api/m/route.ts` 에 `transport` 검사 + 이름 40자 상한
2. **`ui.tsx` 상태 확인** — 화면 갈래가 원본을 되돌리려다 멈췄다. 고쳐진 판인지 확인하고, 원본이면 되살려야 한다
3. 멈춘 갈래 5 를 다시 띄운다 (지시문은 `%CLAUDE_JOB_DIR%\tmp\FIX_BRIEF.md`, 결함 원문은 `verdict_full.md`)

## 그다음

4. **논의44~55 를 코드에 넣는다** — `07_그릴링_결정.md` 에 넣을 자리를 다 적어 뒀다. 8갈래가 끝난 뒤에 한꺼번에
5. **논의52(카카오 로그인)는 결함 수정이 아니라 새 기능**이다 — 따로 계획을 세워야 한다
6. 그릴링 이어서 (다음 물음은 아래)

---

## 다음 그릴링 회차에 물을 것

- AI 후보는 0표 삭제 예외인데 **그럼 언제 사라지나** (논의53 의 남은 칸)
- 카카오 로그인한 방장도 출발지를 내야 하나 · 카카오가 죽으면 모임을 못 만드나 (논의52 의 남은 칸)
- 다른 지역에서 되살아난 후보의 **옛 표가 사라진다는 것을 화면이 말해야 하나**
- 모임 이름 상한 40자 · 약속 시간 2000~2100년 을 그대로 굳힐 것인가
- 레거시 `vote` 액션을 언제 없앨 것인가

---

## 안건 집계

**남은 안건 65건** (69 − 이번 회차 결정 4건).
갈래별 목록은 `06_그릴링_안건.md`, 결정 기록은 `07_그릴링_결정.md`.
멈춘 갈래 5개가 새 안건을 더 올릴 것이므로 이 숫자는 다시 셀 때 늘어난다.
