# 모이머(Moimer) — 프로젝트 상태 (status.md)

_최종 업데이트: 2026-08-09 · 작성: Claude(AI, v19 구현 세션)_
_저장소: `kimyuhwan0126/AI_Bootcamp_team3_FinalProject` (통합 개발) · 버전 `0.2.0`_

---

> ## 🔷 지금 국면 — v19 설계 구현 (2026-08-09 ~)
>
> **멘토 지적("세부 기초 설계가 부족하다")에 따라 그릴링을 돌렸고, 그 결과가 v19 설계 확정판이다.**
> 이 국면의 작업은 전부 **v19 를 코드로 옮기는 것**이다.
>
> | | |
> |---|---|
> | 작업 브랜치 | **`claude/moimer-app-foundation-design-ac8zwj`** (`develop-grilled-session` 에서 분기) |
> | 기준 문서 | ⭐ **[`docs/설계_v19.md`](../docs/설계_v19.md)** — 순서도 원본은 [`docs/설계도/`](../docs/설계도/) 5개 |
> | 발표 목표 | **로컬 실행 + 팀원 각 기기가 IP 로 접속** (Vercel 아님) → [`docs/발표_로컬시연.md`](../docs/발표_로컬시연.md) |
> | 실행 | **`npm run dev:lan`** (`next dev -H 0.0.0.0 -p 3100`) |
>
> ### 시작 지점
> 이 브랜치는 `85ba6d2`(= `develop-grilled-session`)에서 출발했다. **그릴링 이전 코드**라
> v19 가 폐기한 것들이 그대로 살아 있다: `Meeting.password` · `Reservation`·`depositPerHead` ·
> `/votes`·`/members` 라우트. **폐기 = 즉시 삭제가 아니다** — 화면에서 내리는 게 먼저고
> 컬럼 삭제는 마이그레이션으로 나중에 한다(돌아가는 데모를 깨지 않기 위해).
>
> ### 진행
> | # | 작업 | 상태 |
> |---|---|---|
> | 1 | v19 스펙 레포 고정 (`설계_v19.md` + 순서도 5개) | ✅ 완료 |
> | 2 | LAN 시연 런북 + 접속 구조 정비 (`dev:lan`·포트 3100 정정) | ✅ 완료 |
> | 3 | 타입·DB 스키마 v19 정렬 (`scope`·`purposeCategory`·`meetTime`·`pin` …) | ⬜ |
> | 4 | 확정 범위 분기축 (지역까지 / 지점까지) | ✅ 완료 |
> | 5 | 단계 규칙 (핑 잠금 · 후보 0/1/2+ · 동점 · reopen 사다리) | ✅ 완료 |
> | 6 | 지점 단계 (반경 700m · 카테고리 5종 · POI 탭 등록 · 별점) | ✅ 완료 |
> | 7 | 참여·인증 (PIN 복구 · 정원 8명 · 이름 중복 · 강퇴·삭제) | ✅ 완료 |
> | 8 | 결과 · 신호등 · 지난 모임 · 재모임 | ✅ 완료 |
> | 9 | 하단 3탭 통합 (`/votes`·`/members` 폐기) | ✅ 완료 |
>
> ### ✅ 팀 결정 완료 — 별점: **스크래핑 복원** (2026-08-09)
> v19 문구대로 간다. 발표가 **로컬 시연**이라 크롬이 있어, 원래 걷어냈던 사유
> (Vercel 서버리스에 크롬 없음)가 해당하지 않는다. 단 **공식 API(`api-kakao.ts`)는
> 폴백으로 남긴다** — 스크래핑 실패 시 `rating: 0`(정보 없음)으로 조용히 떨어져
> 시연이 멈추지 않게. 감수한 대가(느림·발표 중 실패 가능)는 리허설에서 실측한다.
> 상세: [`docs/설계_v19.md` §13-A](../docs/설계_v19.md)
>
> ### 🔴 발표 전 반드시 — 카카오 콘솔 등록
> LAN 접속은 **사이트 도메인**(지도 SDK)과 **Redirect URI**(로그인) 두 곳에
> `http://<노트북IP>:3100` 을 등록해야 동작한다. IP 가 바뀌면 전부 무효다.
> v14 가 이미 경고한 항목 — 절차·대비책은 `docs/발표_로컬시연.md` §3·§7.

---

> **⚡ 이전 국면 스냅샷 (2026-08-06 · PR #46 머지 직후)**
>
> | | |
> |---|---|
> | `main` | `5862751` · **`v0.2.0`**(태그 부착) · 프로덕션 서빙 중 |
> | `develop` | **`d76c48b`** · main 대비 **37커밋** — 팀원 브랜치 3개(#40·#41·#42) + 검증 리포트(#45) + 출처 문구 수정(#46) |
> | 프로덕션 | `odsay: false`(의도됨) · `db.ready: true` · **홈 화면**은 출처를 정직하게 밝힌다(옛 문구 `거리 추정으로 계산`). ⚠️ **모임 상세 화면은 같은 추정값에 `실시간` 이 붙어 있다** — 아래 결함 행 참고 |
> | ODsay 터널 | ✅ **양방향 검증 성공**(8/5). 발표 투입 여부는 미결(노트북 의존) |
> | 열린 PR | **#14**(날씨 스코어러, CEO 지시 보류) 하나뿐 |
> | 🔴 다음 릴리스 조건 | 속도 게이트 2건 수정 — **팀원에게 수정 요청 프롬프트 전달됨**(8/5, 할 일 5건: 게이트 위치·게이트 범위·포트 3100·`env.ts` trim·`/api/diag` 하드코딩). PR 이 오면 CI 확인 후 머지 |
> | ✅ **신뢰도 결함 1건 — `develop` 머지 완료** | 모임 상세 화면이 추정값에 `실시간` 배지를 무조건 붙이던 문제(실키·**프로덕션 양쪽에서 재현 확인**, 2026-08-05). **PR #46 머지(`d76c48b`, merge commit, 2026-08-06)** — ① 칩을 **사람 단위 `real`** 에 연결 ② 문구를 **`경로 기준` / `거리 추정`** 으로 통일. **`실시간` 이라는 단어는 이제 앱에 없다.** 전문: **[`docs/검증_실키_2026-08-05.md`](../docs/검증_실키_2026-08-05.md)** |
> | ⚠️ **`main` 릴리스 전까지 프로덕션은 그대로다** | 위 수정은 **`develop` 에만 들어갔다.** `main` `5862751` 은 옛 코드라 **지금도 추정값에 `실시간` 이 붙어 있다.** 아래 "프로덕션" 행의 문구도 옛 표기 기준이다. 릴리스 조건은 §1 참고 — **속도 게이트 수정이 먼저다** |
> | 🔴 **ODsay 키 투입 보류 사유 추가** | 실키에서 `regions` 가 **13.87초**(4인 콜드). 프로덕션은 `odsay: false` 라 1.55초로 끝나고 있을 뿐이다 — **속도 게이트 수정 전에는 프로덕션에 키를 넣지 않는다** |

> ## ✅ PR #46 머지 완료 — `docs/verify-live-keys` → `develop` (2026-08-06)
>
> **merge commit `d76c48b`** (squash 아님 — 커밋 7개의 주제가 달라 이력을 남겼다.
> 특히 `cfa7978`·`5abc5f0` 은 *"실측하지 않은 숫자를 근거처럼 쓴 것을 정정"* 한 이력이다).
>
> **머지 전 확인** (통합 세션 실측): `develop` 이 분기 이후 **0커밋** → 충돌 없음 ·
> CI **4/4 green** · `mergeable_state: clean` · 🔒 소유 파일 3개는 **선택 필드
> `real?: boolean` 추가뿐 로직 0줄** · `db/`·`package.json`·`app/api/meeting/route.ts`·
> `lib/flags.ts`·`lib/scoring/index.ts` **무변경** · DB 마이그레이션 **0건**.
>
> ### 무엇이 들어갔나
> 1. `실시간` 배지를 **사람 단위 `real`** 에 연결 (`perParticipant[].real`)
> 2. **CEO 결정으로 문구를 `경로 기준` / `거리 추정` 두 단어로 통일** — 근거: 우리는
>    ODsay·TMAP 요청에 **시각을 안 보낸다.** 그러니 실 API 값도 "실시간"이 아니다.
>    (`실시간`·`ODsay 실시간`·`TMAP 실시간`·`실 이동시간 기준`·`추정값`·`직선 근사` 전부 사라짐)
> 3. 추정 신호등 분리 · 금액 줄 `(모의)` · `getIdentities` 모양 검사
> 4. **46-에이전트 병렬 감사** 로 42건 지적 → 31건 확정. 그중 코드 결함 7건 반영 완료
>    (자차 툴팁 분기 · "시간표 기준" 주장 철회 · 거리순 출처 표시 · 홈 범위 표기 ·
>     홈 결과 카드 출처 · `verified:false` 차단 · 지도 `직선 근사` 통일). **CHANGELOG 기입 완료.**
>
> ### ✅ 문서 정합성 정리도 완료 (2026-08-06)
> 감사가 확정한 어긋남을 전부 손봤다. **원칙: 과거 실측 기록은 그대로 두고, 현재형 서술과
> 사람이 따라 하는 절차만 고쳤다.**
>
> | 위치 | 처리 |
> |---|---|
> | 이 파일 **§⚡ 스냅샷** | 결함 행을 ✅ 해결(머지 대기)로. **프로덕션은 옛 코드라 그대로**라는 행 추가 |
> | 이 파일 **§ 종단 확인 절차** | 기대 문자열을 `전원 경로 기준`/`일부 거리 추정` 으로. 옛 배포용 문구도 병기 |
> | 이 파일 **§ 후속 7** | "문구는 팀 판단"을 취소선 처리 + 해소 근거 |
> | 이 파일 **§3-0-A 신 1** · 리포트 §B-2·§5 | "라벨을 고쳐도 남는 결함"임을 명시 |
> | `docs/검증_실키_*.md` | §4 에 시점 표기 · **§4-2 신설**(문구 통일 근거·감사 7건·화면 투어 7장) · 잔여 4건 기록 |
> | `docs/검증_develop_*.md` | 머리말에 안내 배너 · §4 표에 1·2·8·9 해결 표기(**1번은 처방과 다르게 간 이유**까지) |
>
> ⚠️ 커밋 `ae194fb` 의 메시지에 *"스크린샷은 아직 커밋하지 않았다"* 라고 적혀 있는데 **틀렸다** —
> 같은 커밋에 7장이 들어가 있다(`git show --stat ae194fb`). 푸시된 메시지라 고치지 않고 여기 남긴다.
>
> ### 🟡 머지하면서 알고 있어야 할 것 3건 (차단 사유 아님 — 통합 세션 확인)
>
> 1. **머지 이전에 저장된 모임은 `real` 을 얻지 못한다.** `setRegionCandidates` 가
>    (a) 확정된 모임은 후보를 갱신하지 않고 (b) 후보 이름이 같으면 쓰기를 생략한다.
>    → 옛 모임은 화면에 **전부 `거리 추정`** 으로 뜬다. **지금은 그게 정확하다**
>    (프로덕션 `odsay: false` 라 실제로 전부 추정). 나중에 키를 넣어도 옛 모임은
>    안 올라오지만 **과소평가 방향이라 안전**하다(CLAUDE.md §6).
>    ⚠️ **Preview 확인은 반드시 새 모임으로 할 것** — 옛 모임으로 보면 수정이 안 된 것처럼 보인다.
> 2. `lib/routing.ts` **393줄** — 400줄 규칙까지 7줄 남았다.
> 3. `app/page.tsx` 1,134줄 · `lib/store.ts` 659줄 — 이미 넘던 파일이 조금 더 길어졌다.
>
> ### 💰 예산 (이 작업 누적)
> ODsay **~146/150** · 카카오 로컬 **~21/60** · TMAP **~16/20**.
> ⚠️ **ODsay 는 거의 소진됐다.** 추가 실호출 전에 날짜가 바뀌어 한도가 초기화됐는지 확인할 것.
> 재촬영·재검증은 **L2 캐시(`.cache/api-cache.jsonl`)로 0콜** 가능하다 —
> 방법은 `docs/검증_실키_2026-08-05.md` §4 "수정 검증" 참고.

> **새 세션은 이 파일을 가장 먼저 읽는다.**
> 여기 적힌 숫자·주장은 전부 실측했다. 확인 안 된 것은 "미검증"이라고 적었다.
> 이 원칙을 깨지 말 것 — v8 에서 `CLAUDE.md` 가 코드와 달라 잘못된 전제로
> 작업한 적이 있다(쓰지도 않는 Tailwind·shadcn 이 적혀 있었다).

> ⚠️ 옛 저장소 `kimyuhwan0126/Moimer` 는 **지우지 말 것.**
> `main` 의 조상이 아닌 갈래는 이관 때 따라오지 않았다(예: 알고리즘 8종이 있던 `5ff50ee`).
> 필요한 건 `docs/legacy-algo/` 로 꺼내 왔지만 원본은 보험으로 남긴다.

---

## 1. 지금 어디까지 왔나

**팀원 4명이 동시에 개발할 수 있는 상태.** 기능 개발은 이제부터다.

**✅ 릴리스 `v0.2.0` 완료** (2026-08-04, main `b92ac70` · merge commit · 12커밋 · 23파일 · +1,023/−121).

프로덕션 `/api/status` 실측값 (릴리스 직후):

```
deploy { env "production" · branch "main" · commit "5862751" }  ← 문서 릴리스(#33)까지 반영
store "neon" · db.ready true · kakao true · kakaoJs true · tmap true
odsay false · odsayProxy false · odsayProxyAuth false
              ← 의도된 상태. 터널은 Preview 에서만 검증했고 프로덕션엔 안 넣었다.
                대중교통 시간은 전부 추정값이고 화면이 "거리 추정으로 계산"이라 밝힌다.
                투입 여부는 §1 "구조적 한계" 참고 — CEO·멘토님 판단 사안
ai.ok false ← 의도된 상태 (Ollama 1순위가 팀 내부망 주소라 Vercel 에서 접근 불가.
              `NEXT_PUBLIC_FF_AI_CHAT=0` 이라 화면 영향 없음)
```

**카카오 지도는 프로덕션에서 정상 렌더**된다(릴리스 후 실측 — 지도·핀·후보 목록 확인).
Preview 에서만 안 뜨는데 **도메인 화이트리스트**가 원인으로 보인다(§ 후속 5).

**릴리스 후 프로덕션 화면 실측 (2026-08-04, 참가자 4명: 부발역·노원역·에버랜드·부천역)**

| 관찰 | 판정 |
|---|---|
| 중간지점 **`잠실`** | ✅ 4명 분포에 맞는 위치. `옥천군 안남면 도덕리` 류의 사고 없음 |
| 주변 가게 **`파리바게뜨 잠실역점`·`명동할머니국수 잠실지하상가점`** | ✅ 카카오 로컬 **실 데이터** |
| **`거리 추정으로 계산`** 표시 | ✅ #28 정직 판정 동작 |
| **`최대 220분 · 편차 136분`** | ⚠️ 과대 — 부발↔부천이 실제 대중교통 2시간 안팎이다. §3 의 `estMinutes` 오차 문제 |

> 🔎 **"가짜 mock 가게" 문제의 진짜 원인이 드러났다.** 가게 후보는
> `app/api/meeting/route.ts:266` 이 **카카오 실검색**(`recommendPlaces`)으로 받고,
> **검색 결과가 0건일 때만** `generatePlaces()` mock 으로 떨어진다(`lib/routing.ts:275`).
> 예전에 "존재하지 않는 가게"가 뜬 건 mock 이 기본이라서가 아니라 **좌표가 시골이라
> 주변 검색이 0건**이었기 때문이다 — **#26 이 좌표를 고치면서 함께 완화됐다.**
> 다만 구조는 그대로라 시골 좌표가 다시 나오면 또 mock 이 뜬다.
> ⚠️ **지방 모임에서 가게 후보가 실제인지는 미검증** (수도권 4인만 확인했다).

> ✅ **태그 `v0.2.0` 부착 완료 — `5862751`** (CEO가 GitHub 웹에서 생성. 실측 확인).
> 릴리스 머지(`b92ac70`)가 아니라 **문서 릴리스까지 포함한 main 최신**을 가리키는데,
> 두 커밋의 **코드 차이는 0줄**(`CHANGELOG.md`·`status.md` 뿐)이라 문제 없다.
>
> ⚠️ **태그는 이 컨테이너에서 만들 수 없다.** git 프록시가 태그 푸시를
> **`HTTP 403`** 으로 거부한다(브랜치 푸시는 같은 경로로 정상. 5회 시도 확인).
> **릴리스 태그는 사람이 GitHub 웹 Releases 에서 만들어야 한다.**
> 기존 태그: `v0.1.0`(`4a6e794`) · `v0.2.0`(`5862751`).

> ⚠️ **외부 API 호출에 타임아웃이 한 곳도 없다**(실측: `AbortSignal` 0곳,
> `maxDuration` 미설정). 프록시·터널이 응답을 끌면 그 시간이 함수 실행시간에 그대로
> 쌓인다. 죽은 Cloudflare 터널은 엣지가 빠르게 502/1033 을 주므로 큰 지연은 없을
> 것으로 보이나 **미검증**이다.

> DB 는 **Supabase → Neon 으로 이관됐다** (`lib/db.ts` · `db/schema.sql` · `DATABASE_URL`).
> 로컬·프로덕션 **양쪽에서 `db.ready: true` 실측**했고, 키 없는 인메모리 경로도
> verify(브라우저 스모크 포함)로 확인돼 있다. Neon 스키마는 콘솔에서 3테이블
> (`meetings`·`participants`·`votes`) 생성 확인.

**카카오 로그인 — 프로덕션에서 동작 확인 완료.** 배포 도메인이 생기거나 바뀌면
**두 곳을 같이** 고쳐야 한다. 한쪽만 하면 조용히 실패한다:

| 고칠 곳 | 값 | 빠뜨리면 |
|---|---|---|
| Vercel 환경변수 `KAKAO_REDIRECT_URI` | `https://<도메인>/api/auth/kakao/callback` | 기본값(`lib/env.ts:8`)인 **localhost 로 튕긴다** — 배포 화면에서 로그인했는데 내 PC 로 돌아온다 |
| developers.kakao.com → 카카오 로그인 → Redirect URI | 위와 **글자 단위로 같은 값** | 카카오가 **KOE006**(앱 관리자 설정 오류)으로 거부 |

둘 다 실제로 겪었다. 카카오 콘솔 등록은 즉시 반영되지만 환경변수 변경은 **재배포가 필요**하다.
로컬용 `localhost` 항목은 지우지 말 것(여러 개 등록 가능).

| 구성 | 상태 |
|---|---|
| 브랜치 | `main`(배포) ← `develop`(통합) ← `feat/*` |
| 브랜치 보호 | `protect-main-develop` **Active** — 직접 푸시 거부 확인함 |
| CI | `verify (flags-off)` · `verify (flags-on)` · `changelog` — PR 16건에서 실제 통과 |
| 릴리스 | **`v0.2.0`** (발표일 `v1.0.0`) · main `b92ac70` (2026-08-04) |
| 배포 | **Vercel 연결됨** — Production ← `main`, Preview ← 그 외 브랜치(PR 마다 자동) |
| 로컬 | 키 없이 `npm run dev` 로 전체 플로우 동작 |

**아직 안 한 것**: 팀원 초대 · `CODEOWNERS` 아이디 채우기 · APK 리허설

⚠️ **`develop` 이 `main` 보다 26커밋 앞서 있다**(2026-08-05). 팀원 작업 3건이 들어갔고
**릴리스는 하지 않았다** — 위 "릴리스 전까지 남은 것 2건"이 먼저다.

⚠️ **Preview 배포도 같은 실 DB(production 브랜치)를 쓴다** — 환경변수를 전 환경에 넣었기 때문.
팀원이 Preview 에서 만든 테스트 모임이 실 DB 에 남으므로, 발표 전 `meetings` 정리가 필요하다.
분리하려면 Neon dev 브랜치를 파서 Preview 전용 `DATABASE_URL` 을 넣으면 된다(미실시).

**머지 이력 (전부 `v0.2.0` 릴리스로 프로덕션 반영 완료)**:
- 7/31: PR #15(Neon 이관) · #17(CODEOWNERS) → **#18 릴리스로 main 반영 완료** · #19(문서)
- 8/3 머지 (→ `b92ac70` 릴리스에 포함):
  - #20 DB 접속 실패가 빈 본문 500 으로 죽던 것 수정
  - #22 🔴 **`ENABLE_DEBUG=0` 이 디버그 잠금을 오히려 푸는 버그** — 값이 아니라 존재만 봐서
    문자열 `"0"`(truthy)에도 열렸다. 4곳 전부 `!== "1"` 로 통일.
    운영 빌드로 3값 × 4라우트 재현 확인. Vercel 환경변수엔 없던 것도 확인(CEO)
  - #21 ODsay 진단 플래그 → **이 도구로 §3-3 원인 규명 완료**
  - #23 §3-3 을 실측 결과로 전면 교체(문서)
  - #24 🔴 **ODsay 철도 매핑 + 요금 "무료" 오표시 수정** — §3-3 참고.
    요금 버그는 프로덕션에서 발생 중이던 것이라 릴리스하면 함께 고쳐진다
- 8/4 머지:
  - **#26 거점 풀 전국 확장**(12곳 → 28곳) — §3-2 참고.
    "옥천군 안남면 도덕리" 문제 해결. 남은 문제는 §3-2 의 "지방 거점 밀도"
  - **#28 ODsay 프록시 경유 + `live` 판정 실측 기반** — 아래 릴리스 순서 참고.
    `ODSAY_BASE_URL` 로 고정 IP 프록시를 거칠 수 있게 했고(비면 기존과 동일),
    `live` 가 **키 존재가 아니라 실제 결과**를 본다. 반증 테스트 통과
    (프록시 정상 → `true` / 프록시 다운 → `false`. 구버전은 둘 다 `true` 였다)
  - **#29 진단 공백 2건** — `/api/status` 에 `deploy{env,branch,commit}`·`odsayProxy`·
    `odsayProxyAuth` 추가, `lib/odsay.ts` 의 **조용히 사라지던 실패 8곳**에 경고 추가.
    둘 다 종단 확인이 막혀서 만든 것이다(아래 §1 릴리스 순서 참고)
- PR #14(날씨 스코어러) — 리뷰 🟢(비차단 🟡 4건) · 충돌 해결·push 완료 ·
  **CEO 지시로 보류 중** (머지하지 않음)

### ✅ 팀원 브랜치 3개 — **전부 머지 완료 (2026-08-05)**

브랜치만 올라와 있고 PR 이 없어 **통합 세션이 대신 PR 을 열었다**(CI 기록·리뷰 흔적을
남기기 위해). 충돌은 전부 **통합 세션의 작업 브랜치에서 해결**했고 **팀원 브랜치에는
아무것도 푸시하지 않았다.**

| 순서 | PR | 브랜치 | 머지 커밋 | 충돌 |
|---|---|---|---|---|
| 1/3 | #40 | `develop-selfhost` | `b8865ed` | 없음 |
| 2/3 | #41 | `feat/odsay-traffic-type` | `e6225b0` | CHANGELOG 1건 (양쪽 보존) |
| 3/3 | #42 | `feat/odsay-access-time` | `357397c` | 🔴 `lib/odsay.ts` 1건 (아래) |

**머지 순서는 시뮬레이션으로 정했다** — 반대 순서(수단매핑 먼저)는 충돌이 2건이고
그중 하나가 **코드**(`lib/odsay.ts`)였다. selfhost 를 먼저 넣으면 코드 충돌이 3/3 한 번으로
줄어든다.

#### 🔴 3/3 충돌 — "리팩터 vs 제자리 수정" (같은 사고 반복 주의)

| | 무엇을 했나 |
|---|---|
| develop(1/3 반영 후) | `odsayError` 를 **제자리에서 수정** — 객체형 `429`·`-98` 처리 |
| `feat/odsay-access-time` | 같은 블록을 **`lib/odsay-client.ts` 로 추출**하며 삭제 |

**추출본에 429 수정 이전 버전이 따라와 있었다**(`!Array.isArray(err)` → 배열만).

기계적으로 "브랜치 것 채택"으로 풀었다면 **1/3 의 핵심 성과가 조용히 사라졌을 것이다.**
타입검사·빌드·화면이 전부 멀쩡해서 **아무도 못 알아챈다.**

→ **해결**: 리팩터 구조는 받아들이고(`odsay.ts` 는 브랜치 버전), `odsay-client.ts` 의
`odsayError` **본문만 최신으로 이식**. 그 함수 주석에 재발 방지 경고를 박아 뒀다.

> **교훈: 리팩터(이동)와 수정(제자리)이 겹치면 git 이 "삭제 vs 수정"으로만 보여준다.**
> 옮겨간 쪽에 **옛 내용이 따라왔는지** 반드시 눈으로 확인할 것.

#### 머지 후 손실 점검 — 8항목 전부 통과 (실측)

| 출처 | 항목 |
|---|---|
| 1/3 | 429 객체형 · 근거리 700m(`ODSAY_MIN_KM`) · 속도 게이트(`rateGate`) · DB 캐시(`lib/cache-store.ts`) · `SELF_HOST_URL` rewrites |
| 2/3 | 수단 매핑(`lib/odsay-modes.ts`) · 열차명(`trainType`) |
| 3/3 | 접근시간(`lib/odsay-access.ts`) |

`tsc` ✅ · `build` ✅ · 브라우저 스모크 **4/4** ✅ · 각 PR CI 4/4 green

#### 🔴 릴리스 전까지 남은 것 2건 — **팀원에게 수정 요청 전달됨 (2026-08-05)**

**develop 머지는 안전했다**(Preview 만 배포). 아래는 **`main` 릴리스 전**에 해결되어야 한다.
`docs/버전관리.md` 릴리스 체크리스트에도 관련 조건이 걸려 있다.

1. **속도 게이트가 "실제 호출하지 않는 경우"에도 걸린다** — 실측 `0.011초 → 5.775초`
   (`lib/routing.ts:159` 의 `await rateGate()` 가 키 검사보다 앞)
2. **게이트가 ODsay 호출 3곳 중 1곳만 덮는다** — `route-detail`·`route-path` 미보호

🟡 확인 요청: 포트 3100 충돌 · `rewrites` 가 카카오 콜백까지 넘김
🔴 함께 전달: `lib/env.ts:9` `ODSAY_API_KEY` 만 `trim()` 없음 · `/api/diag:99` 프록시 미경유

> **CEO 결정(A안)**: 통합 세션이 고치지 않고 **작성자(팀원)가 수정 PR 을 낸다.**
> 상황 설명 + 할 일 5건 + 검증 방법(💰 0콜 성능 측정 포함)을 담은 프롬프트를
> 2026-08-05 에 전달했다. **PR 이 develop 으로 오면 통합 세션이 CI 4/4 확인 후 머지한다.**
> 검증 목표: 키 0개 · 4인 `mode=time` 에서 **1초 미만**(수정 전 5.775초).

---

#### (참고) 머지 전 기록 — 브랜치별 내용

| 브랜치 | 내용 | 상태 |
|---|---|---|
| **`develop-selfhost`** | ODsay 429 오진 수정 · 근거리(700m) 도보 환산 · 속도 게이트 · 자체서버 rewrites · Ollama 키 통로 · **외부 API 캐시를 DB 계층까지 확장** | 사전 검증함(아래) |
| `feat/odsay-traffic-type` | **ODsay 수단 매핑 실측 확정 — 고속·시외버스·항공 + 열차명** | 미검토 |
| `feat/odsay-access-time` | 도시간 경로의 "역까지 가는 시간" 추정 추가 | ✅ 머지 |

> ✅ `feat/odsay-traffic-type` 이 **§3 후속 항목 4 (`trafficType` 5/6/7 미검증)를 실측으로
> 닫았다.** 머지됐으므로 그 항목은 해소된 것으로 본다(코드에 `lib/odsay-modes.ts` 로 반영).

#### `develop-selfhost` 사전 검증 결과 (2026-08-05, `ac8818c` 기준)

⚠️ **이 검증은 낡았다.** 이후 `6118d82`(DB 캐시 확장)·`ced3f55` 가 더 붙었다. **재검증 필요.**

**통과**: 머지 충돌 **0건**(fast-forward) · `tsc`·`build` · 스모크 **4/4** · CHANGELOG 기입 ·
비밀 유출 0 (제거된 `Bearer "ollama"` 는 로컬용 더미)

**🔴 머지 전 수정 요청할 것 2건**

1. **속도 게이트가 "실제 호출하지 않는 경우"에도 걸린다.**
   `travelMinutesEx()` 가 `rateGate()` 를 먼저 부르고, 그 뒤 `transitRouteOdsay()` 가
   `if (!env.odsay) return null` 로 즉시 빠져나온다.
   실측(4인 · `mode=time` · **키 0개**): `develop` **0.011초** → `develop-selfhost` **5.77초**
   (24회 × 250ms). 스모크 전체도 9초 → 21초.
   ⚠️ 작성자 주석이 인정한 **최악 16초**. 우리는 `maxDuration` 미설정이라 함수 시간
   제한에 걸리면 추천이 **504 로 실패**한다(지금은 폴백으로 추정값이라도 나온다).
   → 해결: (a) 키 없으면 건너뛰기 (b) 게이트를 `odsay.ts` 의 fetch 직전으로 (c) 간격 대신
   **동시 실행 개수 제한**. **(b)+(c) 조합을 권한다** — 아래 2번도 같이 해결된다.

2. **게이트가 ODsay 호출 3곳 중 1곳만 덮는다.**
   ```
   ✅ transitRouteOdsay   (이동시간)   /api/midpoint
   ❌ transitRoutesDetail (경로 상세)  /api/route-detail
   ❌ transitPathOdsay    (폴리라인)   /api/route-path   ← 실측에서 2건 나갔다
   ```

**🟡 확인 요청 2건**

- **포트 3100 충돌** — `package.json` 의 `dev`/`start` 가 3100 이 됐는데
  `playwright.config.ts` 기본값이 이미 3100 이다. `reuseExistingServer` 가 로컬에선
  `true` 라 **`npm run dev` 켜둔 채 `npm run verify` 하면 스모크가 dev 서버를 재사용**한다.
  "빌드 결과물로 테스트한다"는 목적이 조용히 깨지는데 **CI 는 초록이라 안 잡힌다**
- **`rewrites` 가 `/api/*` 를 전부 넘긴다** — `SELF_HOST_URL` 을 켜면
  `/api/auth/kakao/callback` 도 자체 서버로 간다 → 카카오 로그인이 깨질 수 있다.
  기본값이 비면 무해하므로 차단 사유는 아니고, **자체 서버 전환 시 점검 항목**

**🟢 판단만 필요**: `.gitignore` 의 `*.log` 범위 · 근거리 도보 환산을 `real: true` 로 친 것
(`live` 배지 의미가 조금 넓어진다 — 근거는 주석에 있고 합리적)

**머지 방식**: 9커밋이 주제가 다 달라(429·근거리·rewrites·Ollama 키) **Squash 하면
이력이 뭉개진다. merge commit 권장.**

**릴리스 전 위험성 검증 (2026-08-04, 통합 세션 2 실측)** — 전 항목 통과:

| 항목 | 결과 |
|---|---|
| `db/` · `package.json` · `.github/` 변경 | **0건** (스키마 변경이 없어 롤백이 안전하다) |
| 신규 환경변수 3개 | 전부 **선택** — 비면 기존 동작 그대로(`lib/env.ts:20-22`) |
| 진단 플래그 기본값 | `NEXT_PUBLIC_FF_ODSAY_PROBE === "1"` → 프로덕션 **꺼짐** |
| 🔒 공용 파일 3개 | **순수 추가** — 기존 경로를 바꾸지 않는다 |
| 디버그 잠금(#22) | `/api/diag`·`/api/debug`·`/api/ai-trace` × (미설정·`0`) = **6/6 HTTP 403** |
| 키 없는 추천 경로 | 후보 3곳 정상 + **`live: false`** |
| 홈 렌더 · 스모크 | HTTP 200 · **4/4** |
| 로그 비밀 유출 | **0건** |

### 🚦 릴리스 순서 (통합·머지 세션 2 — 2026-08-04 재판단)

> ✅ **2026-08-05: 터널 종단 확인 — 양방향 성공.** 8/4 에 보류했던 것이 풀렸다.
> 아래 "터널 종단 확인 (2026-08-05, 완료)" 절이 최신이다.

1. ✅ **`live` 판정 수정 — 완료**(#28)
2. ✅ **Preview 터널 종단 확인 — 완료**(2026-08-05). 아래 절 참고
3. ✅ **릴리스 완료** — main `b92ac70` (merge commit, 부모 `f0a4235`+`f571a4e`) · 버전 **`0.2.0`**
   (그 뒤 `5862751` 로 문서 릴리스 추가)
4. ⏸️ **프로덕션 ODsay 환경변수 투입 — 아직 하지 않는다.** 터널이 **동작하는 것은
   확인**됐으나, 노트북 의존이라는 구조적 한계 때문에 **발표 환경에 쓸지는 미결**이다
   (아래 "구조적 한계" 참고). 판단은 CEO·멘토님 몫이다

#### 보류해도 릴리스가 옳은 이유

프로덕션은 `odsay` 키가 없어 **릴리스 전후 모두 대중교통 시간이 추정값**이다.
바뀌는 건 "추정값을 뭐라고 표시하느냐"와 "장소가 어디냐"다:

| | 릴리스 전(`f0a4235`) | 릴리스 후(`b92ac70`) |
|---|---|---|
| 지방 모임 장소 | 🔴 `옥천군 안남면 도덕리` | ✅ `대전역` |
| 시간 표시 | 🔴 추정값을 **"실 이동시간 기준"** | ✅ **"거리 추정으로 계산"** |
| 요금 | 🔴 **"무료"** 오표시 | ✅ 정상 |
| 디버그 잠금 | 🔴 `ENABLE_DEBUG=0` 이면 열림 | ✅ 닫힘 |

**나빠지는 항목이 없다.** 시간 숫자는 여전히 추정이라 지방 후보에 949분 같은 값이
나올 수 있지만, 화면이 추정임을 밝힌다.

#### ✅ 터널 종단 확인 (2026-08-05, 완료) — **양방향 성공**

Preview(`develop-selfhost` · `ced3f55`)에서 확인했다. `/api/status` 는
`odsay: true` · `odsayProxy: true` · **`odsayProxyAuth: true`** (8/4 와 결정적 차이).

| 조건 | 화면 | 판정 |
|---|---|---|
| 터널 **ON** · 성수동↔왕십리 | **"실 이동시간 기준"** · 건대입구 · 최대 10분 · **편차 0분** | ✅ 동작 |
| 터널 **OFF** · 의왕역↔모란역(새 좌표) | **"거리 추정으로 계산"** | ✅ 장애 감지 |

**8/4 는 한쪽 방향도 못 봤다. 이번엔 둘 다 봤다.**

> 🔎 **추천 결과가 실제로 달라졌다.** 같은 두 사람에 대해
> 거리순은 `왕십리`(최대 14분·편차 4분), 시간순은 `건대입구`(최대 10분·**편차 0분**).
> 이 기능이 존재하는 이유를 보여주는 대비라 **발표 자료로 쓸 만하다.**

##### 원인 규명 — 8/4 실패는 `ODSAY_PROXY_SECRET` 미설정이었다

당시 후보 3개 중 **②번이 맞았다**:

- 8/4 `/api/status` → **`odsayProxyAuth: false`** (공유 비밀 미전송)
- 프록시는 `x-proxy-secret` 를 **검사한다**(2026-08-05 프록시 코드 확인) → 401 로 거부
- 당시 코드는 `if (!r.ok) return null` 이라 **로그 없이 사라짐** → Vercel `Warning 0`

→ **"원인 미확인"이 아니라 "진단이 없어서 못 본 것"이었다.** #29 로 로그를 넣은 뒤에야
   원인을 좁힐 수 있었다.

##### 오늘(8/5) 1차 실패 — Vercel 의 `ODSAY_API_KEY` 값 오류

비밀 키를 넣어 프록시는 통과했는데 ODsay 가 `[ApiKeyAuthFailed]` 로 거부했다
(`/api/midpoint` 16건 + `/api/route-path` 2건).

**같은 프록시·터널·IP 로 키만 바꾼 대조 실험**이 원인을 확정했다(터널 세션 실측):

```
#0021  Vercel Preview 키  →  500 [ApiKeyAuthFailed]        32ms
#0022  노트북 .env.local 키 →  200 · 경로 22건 · 40분      112ms
```

Production 에 있던 값을 복사해 넣은 것이 화근이었다. **바른 키 지문: 길이 43 ·
sha256 앞자리 `3be0295c`.**

> 🔴 **관련 코드 버그**: `lib/env.ts:9` 가 **`ODSAY_API_KEY` 만 `trim()` 을 안 한다**
> (`odsayBase`·`odsayProxySecret` 은 한다). 붙여넣기에 공백·줄바꿈이 섞이면 그대로
> 전송되어 인증이 깨진다. **한 줄이면 고쳐진다 — 다음 브랜치 세션 몫.**

##### 함께 확인된 것

- **429 는 이번 건에 없었다 (0건).** 동시 8건이 나갔는데도 안 걸렸다.
  속도 게이트(`develop-selfhost`)가 막았는지, 이 규모에선 원래 안 걸리는지는 **미검증**
- 프록시 카운터: `401`·`404`·`502` **각 0건** → 앱→터널 연동(헤더·URL) 정상
- 💰 오늘 ODsay 성공 호출 **2콜**. 인증 실패 18건의 쿼터 계상 여부는 **미검증**

##### 아직 미검증

- **시외 구간**(서울↔부산 KTX 등) — 키 오류에 가려 확인 못 했다. 터널 켜고 별도 확인 필요

##### ⚠️ 구조적 한계 — 동작한다고 발표에 쓸 수 있는 건 아니다

이 방식은 **노트북이 켜져 있고 터널이 살아 있어야만** 동작한다.

| 상황 | 결과 |
|---|---|
| 노트북 절전·종료 | 배포 서비스가 조용히 추정값으로 떨어짐 |
| Wi-Fi 변경 | 공인 IP 변경 → 화이트리스트 불일치 |
| 터널 재시작 | **주소가 바뀜** → 환경변수 수정 + 재배포 |

**발표 중에 노트북을 닫으면 서비스 품질이 바뀐다.** 프로덕션 투입 여부는 **CEO·멘토님
판단 사안**으로 남긴다. 대안은 `docs/ODsay_프록시_시도기록.md` 초안 참고(저장소 외부).

---

#### (참고) 8/4 실패 당시 기록 — 위에서 원인이 규명됐다

Preview(`deploy.commit: 7dade26`)에서 **두 번 시도해 두 번 다 실패**했다:

- `/api/status` → `odsay: true` · `odsayProxy: true` · `odsayProxyAuth: false`
  (키 있음 · `ODSAY_BASE_URL` 설정됨 · 공유 비밀 없음)
- 추천 결과 → **"거리 추정으로 계산"**(`live: false`) · `최대 18분 · 편차 12분`
- **키 없는 로컬 빌드가 같은 좌표로 `최대 18분 · 편차 12분`** 을 냈다
  → Preview 가 ODsay 를 **한 번도 못 탔다**는 뜻
- Vercel 로그 `Warning 0` → 원인 미상. **이것 때문에 #29 를 만들었다**(아래)

~~**원인은 미확인이다.**~~ → **2026-08-05 규명됨: ②번이 맞았다**(위 절 참고).
당시 적어둔 후보는 ① 옛 터널 주소 ② `x-proxy-secret` 미전송 ③ IP 미등록 이었고,
"②를 먼저 보라"고 남긴 판단이 옳았다. **프록시 코드를 읽는 데 0콜이면 됐다.**

> 🔴 **정정 (릴리스 후 실측)**: `ODSAY_*` 는 **Production 스코프에도 들어가 있었다.**
> 릴리스 전에는 프로덕션이 7/31 빌드라 안 보였을 뿐이다 — **환경변수는 새 배포부터
> 적용된다.** 릴리스가 새 빌드를 만들자 프로덕션 `/api/status` 가 `odsay: true` ·
> `odsayProxy: true` 로 바뀌었다(2026-08-04 실측).
>
> **결과적으로 순서는 맞았다.** status.md 가 경고했던 사고(894분·"무료"를 실시간이라
> 표시)는 **구버전 코드에 키가 들어갈 때** 나는 것인데, #24·#28 이 같은 배포에 함께
> 들어가 그 조합이 성립하지 않았다. 릴리스 전에 프로덕션이 어떤 이유로든 재배포됐다면
> 그 사고가 났을 것이다 — **"환경변수는 넣는 시점이 아니라 배포 시점에 적용된다"** 를
> 기억할 것.
>
> **✅ 조치 완료 (실측)**: `ODSAY_*` 를 **Vercel 에서 전부 삭제 후 재배포** →
> `odsay: false` · `odsayProxy: false` · `odsayProxyAuth: false` 확인.
>
> ⚠️ **"Production 체크만 해제" 로는 3회 시도 모두 실패했다.** 스코프를 고치는
> 방식이 왜 안 먹었는지는 **미확인**(`Shared` 탭 가능성). **삭제 후 재배포가 확실하다.**
>
> ⚠️ **재배포 여부는 `deploy.commit` 으로 알 수 없다** — Redeploy 는 같은 커밋으로
> 새 배포를 만들기 때문에 값이 그대로다. **Deployments 목록의 시각**을 봐야 한다.
> 이걸 몰라 "커밋이 그대로면 정상"이라고만 안내해 확인이 한참 늦어졌다.

#### Cloudflare 재개할 때 환경변수 넣는 순서

1. **터널을 먼저 기동**해 새 주소를 확보한다 (Quick Tunnel 주소는 재시작마다 바뀐다)
2. `ODSAY_API_KEY` + `ODSAY_BASE_URL`(새 주소) 를 **Preview 만 체크**해서 추가
   (프록시가 공유 비밀을 요구하면 `ODSAY_PROXY_SECRET` 도)
3. **재배포** → Preview 에서 종단 확인 (§ 종단 확인 절차)
4. 통과한 뒤에야 Production 스코프에 추가 + 재배포

> ✅ **#26 을 빼지 말고 함께 낸다** (이전 판단 정정). #28 이 정직성을 보장하기 때문이다:
>
> | | #26 만 | **#26 + #28** |
> |---|---|---|
> | 화면 | "대전역 · 949분 · **실 이동시간 기준**" | "대전역 · 949분 · **거리 추정으로 계산**" |
> | 판정 | 🔴 거짓 표시 | ✅ 숫자는 이상해도 **추정임을 밝힘** |
>
> 그리고 #26 **이전**(= `f0a4235`, 2026-08-04 릴리스 전 프로덕션)은 "옥천군 안남면
> 도덕리 + 존재하지 않는 가게"라 **장소도 숫자도 틀린 데다 그걸 실시간이라 표시**했다.
> 셋 중 그게 가장 나빴고, 릴리스로 해소됐다.
> 949분은 ODsay 가 실제로 붙으면 실 경로값(130분대)으로 바뀐다.
> **`estMinutes` 개선(설계서 Part 2)보다 ODsay 키·프록시가 먼저다.**

#### 종단 확인 절차 (2번) — **Preview 에서 한다**

프로덕션은 main 이고 발표를 앞두고 있으니 실험대로 쓰지 않는다.

1. CEO 노트북에서 프록시 + Cloudflare Quick Tunnel 기동
2. Vercel 환경변수 3개를 **Preview 스코프에도** 넣고 재배포
3. develop Preview URL 에서 추천 실행 → 홈 중간지점 카드에 **`전원 경로 기준`** 확인
4. **터널을 끄고 다시 실행 → `일부 거리 추정` 으로 바뀌는지** ← 이게 진짜 검증

> ⚠️ **기대 문자열이 2026-08-05 에 바뀌었다.** 이전 문구는 `실 이동시간 기준` /
> `거리 추정으로 계산` 이었다 — **옛 배포(`main` `5862751` 등)를 볼 때는 그쪽을 찾아야 한다.**
> 지금 코드는 `경로 기준` / `거리 추정` 을 쓰고, 홈 카드에는 범위를 붙여
> `전원 경로 기준` / `일부 거리 추정` 으로 뜬다.

> 🔴 **반증 테스트는 반드시 "새 좌표"로 하라.** `routeCache` 에 TTL 이 없어
> **이미 캐시된 좌표쌍은 터널이 죽어도 `live: true` 로 나온다**(#28 이 스스로 밝힌 한계).
> 캐시엔 실 API 성공값만 들어가니 거짓말은 아니지만 "지금 살아있다"는 뜻은 아니다.
> 같은 좌표로 테스트하면 터널을 꺼도 `true` 가 나와 **오판한다.**

💰 종단 확인 예상 20~30콜 (무료 1,000콜/일).
- ~~ODsay 교체 예정~~ → **교체 불필요로 결론.** 실측 결과 ODsay 는 시외를 제대로
  답하고 있었다. 우리 매핑 문제다 — §3-3 참고. **ODsay 키를 다시 넣어야 한다**
  (교체 예정이라 빼뒀던 것. 현재 프로덕션은 `odsay: false` = 추정값 폴백)

### 릴리스 리뷰가 남긴 후속 작업 (별도 PR — 급하지 않음)

1. ~~DB 접속 실패 시 500 응답에 에러 본문이 없다~~ — **해결**(PR #20).
   `app/api/meeting` 의 GET·POST 를 감싸 예외를 잡고 `{ error, detail, hint }` 를 싣는다.
   실측: DB 를 끊은 상태로 모임 생성 → `HTTP 500 · 368 bytes`,
   `detail` 에 `모임 저장 실패: …` 원인이 그대로 나오고 **비밀번호 노출 0건**
   (`sanitizeDbError` 가 오류 메시지에 섞인 자격증명을 가린다. 6케이스 실측)
2. `postcss` high 2건 — `next@14` 전이 의존성. 이번 릴리스가 만든 회귀가 아니다
   (이전 main 에도 있었다). 해소하려면 next 16 승격(breaking)이라 **발표 전 보류 권장**
3. 🔴 **`/api/diag` 의 `odsay` 항목이 프록시를 안 탄다** — `app/api/diag/route.ts:99` 가
   주소를 `https://api.odsay.com` 으로 **하드코딩**해 `env.odsayBase` 를 무시한다.
   프록시가 완벽히 동작해도 그 줄은 실패로 뜨므로 **진단 도구가 오진을 만든다**
   (같은 응답의 `parsed.transit` 만 프록시를 탄다). 종단 확인 재개 전에 고칠 것
4. ✅ **`TRAFFIC_KIND` 매핑 — 실측으로 확정 완료** (2026-08-04, PR `feat/odsay-traffic-type`)

   **`5` = 고속버스 · `6` = 시외버스 · `7` = 항공.** 외부 자료가 엇갈렸으므로
   `api.odsay.com` 을 직접 7콜 호출해 확정했다. 근거는 추론이 아니라 **ODsay 가 준
   터미널 이름 자체**다:

   | 값 | 정체 | 관측된 구간 |
   |---|---|---|
   | 5 | **고속버스** | `센트럴시티터미널 → 전주고속버스터미널` · `서울고속버스터미널 → 부산종합버스터미널` |
   | 6 | **시외버스** | `서울남부터미널 → 전주시외버스공용터미널` · `동서울 → 해운대시외버스터미널` |
   | 7 | **항공** | `김포국제공항 → 제주국제공항` · `김포 → 김해국제공항` (4개 구간에서 반복) |

   `5` 출발지는 **전부** 고속 전용 터미널이고, `6` 은 시외 전용인 서울남부터미널
   출발이거나 도착지에 "시외"가 박혀 있다. → **`ourHome`·`yeoru` 가 맞고
   `capstone25-plaps` 자료가 반대로 적혀 있었다.**

   ⚠️ 버스 경로는 `pathType 12` 로 오는데 **열차가 항상 더 빨라 상위 3개에서 잘린다**
   (우리는 `limit=3`). 실측 때 상위 3개만 보면 `5`·`6` 을 못 본다 — 전수를 봐야 한다.

   > 🔴 **이 항목은 그전에 두 번 틀리게 적혔다. 경위를 남긴다.**
   > 1차: "시외/고속버스 `trafficType` 미확정 · ODsay 가 주는지 자체가 미검증"
   > 2차(PR #35): "**시외버스 = `trafficType 2` 로 확정**" ← §3-3 의 대전 실측을 근거로 삼음
   >
   > **2차가 과잉 일반화였고, 이번 실측이 그걸 확인해 줬다.** 대전 경로의 `8326` 은
   > 시내버스로 분류된 광역/직행 노선이고, **터미널 기반 시외·고속버스는 `5`·`6` 으로 온다.**
   > **교훈: 실측 1건은 "그 경우에 그랬다"이지 "규칙이 그렇다"가 아니다.**
5. 🟡 **카카오 지도가 Preview 도메인에서 안 뜬다** — Preview 에서 "지도를 불러오지
   못했어요 · 카카오 JS 키 설정 후 표시됩니다" 가 뜨는데 `/api/status` 는 `kakaoJs: true`
   다(실측). **키는 있다.** 원인은 카카오 JS 키의 **사이트 도메인 화이트리스트**로 보인다 —
   Preview 주소는 브랜치마다 달라 등록이 불가능하다(미검증). 프로덕션 도메인은
   등록돼 있어 발표에는 영향 없다. 다만 **화면 문구가 원인을 "키 없음"으로 단정**해
   ODsay 때와 같은 함정이다 — 팀원이 Preview 를 보고 키 문제로 오해한다
6. **ODsay 프록시 구현이 저장소에 없다** — CEO/종택 노트북에만 있다. 아무도 재현할 수
   없고, 노트북이 꺼지면 배포된 서비스가 조용히 추정값으로 떨어진다.
   `tools/odsay-proxy/` + 실행 문서로 올릴 것 (Cloudflare 보류 해제 후)
7. 🔴 **도시간 경로에 "역까지 가는 시간"이 통째로 빠져 있다** (2026-08-05, CEO 지적으로 발견)

   **ODsay 도시간 길찾기(`pathType 11·12·13·20`)는 역↔역 구간만 답한다.**
   녹화한 도시간 81경로 **전부** `info.totalTime` 이 탑승구간 시간의 **단순합**과
   일치했다(차이 0분). 반면 도시내(`pathType 2·3`)는 237분 vs 구간합 232분으로
   도보·환승대기가 들어 있다 — **응답 종류가 다른 것이지 우리 파싱 문제가 아니다.**

   | 요청 | ODsay 답 | 응답에 없는 구간 |
   |---|---|---|
   | 강남역 → 제주공항 | **75분** | 강남역 → 김포국제공항 **21.0km** |
   | 강남역 → 전주역 | 86분 | 강남역 → 용산 **6.6km** |
   | 센트럴시티 → 유스퀘어 | 96분 | 출발 4.5km + **도착 광주송정역→유스퀘어 8.7km** |
   | 강남역 → 제주 (복합) | 105분 = 35+70 | **오송역 ↔ 청주국제공항 연계 이동이 0분** |

   ⚠️ **이름표 문제가 아니라 추천 순위 문제다.** `travelMinutesEx`(`lib/routing.ts:78`)가
   이 값을 그대로 점수에 넣는다. 강남역 참가자에게 전주는 86분, 제주는 75분으로 계산된다.
   → §3-3 의 "추천은 이미 실제 철도 시간 기준이었다"는 기록도 **반쪽이다** —
   130분은 수서→부산 구간만이고 강남역→수서가 빠져 있었다.

   **같은 뿌리에서 나온 것 전부** (도시간 경로 기준):

   | 항목 | 지금 화면 | 실제 |
   |---|---|---|
   | 이동시간 | 75분 | 접근 21km 별도 |
   | 요금 | 탑승 요금만 | 접근 지하철 요금 미포함 |
   | 도보 | **0m** | `info.totalWalk` 가 응답에 없다 |
   | 환승 | **0회** | 지하철→공항철도→비행기 |
   | 라벨 | **"항공 직통"** | 강남역에서 직통이 아니다 |
   | 배차 대기 | 0분 | `intervalCount` 하루 **2편**인 구간 존재(용산→전주 열차, 김포→광주 항공) |
   | 폴리라인 | 직선 근사 + `real:false` | ✅ 이미 정직하다 |
   | 정거장 수 | 표기 생략 | ✅ 이미 처리됨 |

   ✅ **구현 완료 — `feat/odsay-access-time` 브랜치** (2026-08-05). `lib/odsay-access.ts` 가
   ①출발지→첫 탑승지 ②**구간 사이 연계** ③마지막 하차지→목적지를 `estMinutes()` 로
   환산해 `min` 에 더하고, 포함량을 `estimatedMin` 으로 함께 내린다. 좌표가 응답에
   이미 있어 💰 **0콜**. 화면은 고지 박스 + `역까지·환승 이동 추정 ○분 포함` 으로 밝힌다.

   **실측 효과** (replay 검증, 서울역+강남역 2인):

   | 목적지 | before | after |
   |---|---|---|
   | 부산역 | 138분 · **편차 0분** | 183분 · **편차 45분** |
   | 제주공항 | 75분 · 편차 0분 | 182분 · 편차 30분 |
   | 전주역 | 160분 · 편차 0분 | 218분 · 편차 19분 |

   출발지가 다른 두 사람의 **편차가 0으로 나오던 것**이 이 문제의 가장 뚜렷한 증상이었다.
   제주 복합 경로(`SRT 수서→오송` + `항공 청주→제주`)는 2시간 24분 → **4시간**이 되면서
   추천 1순위가 항공 직통으로 바로잡혔다 — 오송역↔청주공항 연계가 0분이라 **없는 지름길이
   1순위**였던 것이다.

   > ⚠️ **`estMinutes` 가 실측 대비 약 1.7배 과대하다**(21km→107분, 실제 55~60분대 /
   > 6.6km→38분, 실제 20분대). 그래도 0분보다 진실에 가깝다는 판단이다.
   > **모델을 새로 만들지 않은 이유**: `estMinutes` 는 ODsay 실패 시 경로 전체를 추정하는
   > 데 이미 쓰인다 — 둘로 나누면 고칠 곳도 둘이 된다. §3-2 의 `estMinutes` 개선 과제가
   > 진행되면 이 보정도 함께 좋아진다.
   >
   > 🟡 **정확도가 더 필요하면** 접근 구간을 ODsay 로 한 번 더 묻는 방법이 있다
   > (도시내 경로라 제대로 답한다). 다만 💰 **호출이 2배**가 되고, 첫 응답으로 탑승역을
   > 안 뒤에야 두 번째를 부를 수 있어 지연도 2배다. **팀 결정 사항.**
   >
   > ~~🟡 **목록 화면의 "실시간" 칩은 손대지 않았다.** 이제 그 숫자엔 추정이 섞이는데
   > 칩은 여전히 "실시간"이다. 그렇다고 `live:false` 로 내리면 지방 모임마다
   > "거리 추정으로 계산"이 떠서 **ODsay 가 안 붙은 것으로 오해**하게 된다(§ 후속 5 와 같은 함정).
   > 문구를 어떻게 할지는 화면 담당·팀 판단이라 상세 시트에서만 밝히는 선에서 멈췄다.~~
   >
   > ✅ **해소됨 (2026-08-05, `docs/verify-live-keys` · PR #46).** 당시 멈춘 이유였던
   > 딜레마("`live:false` 로 내리면 지방 모임이 통째로 추정으로 보인다")는 **판정 단위를
   > 목록에서 사람으로 내리면서 사라졌다** — `perParticipant[].real` 이라 실값인 참가자는
   > 그대로 실값으로 표시된다. 문구도 CEO 결정으로 확정됐다: **`경로 기준` / `거리 추정`.**
   > ("실시간"을 쓰지 않는 이유는 §3-0-A 참고 — 우리는 API 요청에 시각을 안 보낸다.)

   ✅ **요금 현행성은 따로 대조했다 (2026-08-04)**: 철도는 최신이다 — 서울→부산 KTX
   59,800원이 현행 운임과 일치하고, **수서발 KTX 52,900원(서울발 대비 −11.5%)** 은
   2026-05-15 KTX·SRT 연계운행에 따른 수서발 KTX 약 10% 인하가 반영된 값이다
   (수서역에 `trainType 1`(KTX)이 나타나는 이유이기도 하다). 고속버스 39,700원은
   우등 수준으로 보이나 **등급 표기가 응답에 없다.** 🔴 **항공만 위험**해서
   이번 PR 에서 표시를 뺐다(위 §3-3 · CHANGELOG 참고).

---

## 2. 이 저장소의 설계 전제

> **머지 충돌은 git 문제가 아니라 파일 배치 문제다.**
> 두 사람이 같은 파일을 안 만지게 나눠두면 충돌은 애초에 안 난다.

| 장치 | 무엇을 막나 |
|---|---|
| `lib/scoring/` 플러그인 | 관점 = 파일 하나 = 담당자 한 명. 등록은 배열에 한 줄 |
| `app/m/[code]/sections/` (15파일) | 화면 조각을 담당자별로. 전부 400줄 미만(최대 `LeaderBar.tsx` 173줄) |
| `lib/flags.ts` (`NEXT_PUBLIC_FF_*`) | 켜고 끄기를 코드가 아니라 `.env.local` 로 → 브랜치마다 값이 안 달라짐 |
| `db/migrations/` | 스키마 변경을 파일 추가로 → 여러 명이 컬럼 추가해도 충돌 0 |
| `.github/CODEOWNERS` | 소유권을 사람 기억이 아니라 GitHub 이 지킴 |
| CI 브라우저 스모크 | **빌드 통과 ≠ 화면 렌더.** 일부러 깨뜨려 잡히는 것 확인함 |

읽을 문서: `docs/팀_개발환경.md` · `docs/버전관리.md` · `docs/APK.md` · `docs/노션_통합개발환경.md`

---

## 3. ⚠️ 다음 작업

### 3-0. 🔴 `develop` 전면 검증 결과 (2026-08-05)

전문 2건 — **mock 판**과 **실키 판**이 있다. 아래 표는 mock 판이고, 각 항목의 실키 판정은
그 아래 §3-0-A 에 있다.

| | 문서 | 환경 |
|---|---|---|
| 1차 | [`docs/검증_develop_2026-08-05.md`](../docs/검증_develop_2026-08-05.md) | 키 전부 비움 · 💰 0콜 |
| 2차 | ⭐ [`docs/검증_실키_2026-08-05.md`](../docs/검증_실키_2026-08-05.md) | **실키**(화이트리스트 IP 노트북) · 💰 ODsay ~144 · 카카오 ~15 · TMAP ~16 |

키를 전부 비운 채(`ODSAY_API_KEY= TMAP_APP_KEY= KAKAO_REST_API_KEY= NEXT_PUBLIC_KAKAO_JS_KEY=
DATABASE_URL=`) 빌드·기동해 **모임 생성 → 출발지 → 거점 추천 → 투표 → 확정 → 가게 →
결과 → 예약** 전 구간을 HTTP 와 브라우저(Playwright 390×844) 두 관점으로 통과시켰다.
**유료 API 호출 0건.** 아래는 전부 이 환경 실측이다.

**흐름은 끝까지 돈다.** 4명 동시 투표에서 4표가 전부 보존됐고(쓰기 단위 규칙이 동작),
지도 폴백·모의결제 표기·알림 빈 상태·진단 엔드포인트 잠금은 전부 정직하다.

**문제는 "숫자를 믿을 수 있는가"다.**

| # | 발견 | 위치 |
|---|---|---|
| 🔴 1 | **추정값에 `실시간` 배지가 무조건 붙는다.** 칩에 조건이 없다 | `app/m/[code]/sections/TravelTimes.tsx` |
| 🔴 2 | **`많이 늦음` 이 자가신고 없이 자동 부여**된다. 지각이 아니라 거리인데 단어가 "늦음"이고, 자가신고 배지와 모양이 같다 | 같은 파일 `:38` |
| 🔴 3 | **코드만 알면 남의 표를 던지고 방장 노릇을 할 수 있다.** 비번은 `join` 에서만 검사하고, `participantId` 는 비번 없이 열리는 `GET` 응답에 들어 있다. 출발지 좌표(집·회사)도 함께 노출 | `app/api/meeting/route.ts` 전반 |
| 🔴 4 | **키 0개인데 `regions` 응답 11.29초**(2인 1.76 / 4인 3.93 / 6인 5.89 / 8인 7.85). `maxDuration` 선언 0곳 · 외부 fetch 16곳 중 타임아웃 3곳(`ai.ts` 만) | `lib/routing.ts:159` |
| 🟡 5 | **캘린더 내보내기가 항상 "다음 토요일 19시"** — `dateIso`·`timeHhmm` 는 AI 채팅 파서만 채우는데 채팅이 기본 꺼짐. `meetTime` 은 `timeText` 만 저장 | `lib/calendar.ts:11` |
| 🟡 6 | **모르는 지명이 서울 해시 좌표로 조용히 치환**된다(제주공항 → 37.594·127.129). 실패 신호 없음. 키가 있어도 카카오 0건이면 같은 경로 | `lib/geo.ts:106` |
| 🟡 7 | **실 가게 이름에 지어낸 선입금·`예약가능`.** `reservable: true` 고정 · `depositPerHead` 카테고리 상수. `(모의)` 는 버튼에만 있고 금액 줄엔 없다. mock 은 가짜 평점 ⭐까지 표시(실 API 는 안 함 — 역전) | `lib/routing.ts:346` |
| 🟡 8 | **localStorage 형식이 깨지면 3개 화면이 백지**(`Application error`). UI 로 복구 불가 | `lib/identity.ts:14` |
| 🟢 9 | `any` **49곳/12파일** · 400줄 초과 2파일 · 스코어러가 `fairness` 하나뿐인데 스플래시는 "선호도를 고려해 AI가" 라고 한다 | — |

**#1 은 팀이 이미 한 번 고친 버그다.** `app/page.tsx` 에는 `midLive` 판정이 붙어
출처를 정직하게 바꿔 표시하는데(검증 당시 문구 `실 이동시간 기준` / `거리 추정으로 계산`,
2026-08-05 이후 **`전원 경로 기준` / `일부 거리 추정`**),
**정작 제품의 본 화면 `/m/[code]` 에는 같은 수정이 안 들어갔다.**
원인은 `/api/midpoint` 만 `recommendRegionsWithMeta`(`live` 포함)를 쓰고,
`app/api/meeting` 의 `case "regions"` 는 `recommendRegions`(meta 없음)를 써서
**모임 플로우에 `live` 신호가 아예 없는 것**이다.

같은 화면에서 행을 탭하면 열리는 경로 상세 시트는 같은 숫자를 `추정값` 이라고
정직하게 밝힌다 — **한 화면 안에서 라벨이 서로 모순된다.**

**고칠 순서**: ① `실시간` 배지를 `live` 에 연결 → ② 추정 신호등 표기 분리 →
③ 속도 게이트(팀원 요청 전달됨) → ④ fetch 타임아웃 13곳 → ⑤ `participantId` 권한 →
⑥ 캘린더 날짜 없으면 경고 → ⑦ 지오코딩 실패 알림 → ⑧ 금액 줄 `모의` 표기 →
⑨ `getIdentities` 배열 검사. **①②⑥⑧⑨ 는 각각 몇 줄이라 발표 전에 넣을 수 있다.**

→ ✅ **①②⑧⑨ 는 실키 검증 PR 에서 해결됐다** (아래 §3-0-A). 남은 것은 ③④⑤⑥⑦ 이다.

---

### 3-0-A. ⭐ 실키 판정 (2026-08-05 · 화이트리스트 IP 노트북)

전문: **[`docs/검증_실키_2026-08-05.md`](../docs/검증_실키_2026-08-05.md)**

> **한 줄**: ODsay 는 살아 있었고, 그래서 문제가 더 선명해졌다.
> `실시간` 배지 결함은 mock 특유의 현상이 **아니었다** — 실키에서 한 목록 안에
> ODsay 실값 3건과 추정값 1건이 섞였고 **넷 다 `실시간`** 이었다.
> 그리고 **프로덕션은 지금 `odsay: false` 라 모든 대중교통 숫자가 추정값인데
> 화면은 전부 `실시간` 이라고 말하고 있다.**

| # | mock 발견 | 실키 판정 | 근거 |
|---|---|---|---|
| 🔴 1 | 추정값에 `실시간` 무조건 | ✅ **재현 + 프로덕션에서 발생 중** → **고침** | 자월도 참가자만 ODsay `code 3` 실패 → `31·67·29분`(실값) + `288분`(추정)이 한 목록에. 프로덕션도 `실시간 34분`/`58분` (둘 다 추정) |
| 🔴 2 | `많이 늦음` 자동 부여 | ✅ 재현 → **고침** | 자가신고 없이 빨간 `● 많이 늦음` |
| 🔴 3 | `participantId` 만으로 남의 표·방장 권한 | ⏸ **미검증** (코드 그대로) | 이번 세션 범위 밖 |
| 🔴 4 | `regions` 11.29초 · `maxDuration` 0곳 | ✅ **재현, 더 느림 — 13.87초** | 실키 4인 콜드. 웜 3.12초. `maxDuration` 여전히 0곳 |
| 🟡 5 | 캘린더 항상 "다음 토요일 19시" | ⏸ **미검증** | |
| 🟡 6 | 모르는 지명 → 서울 해시 좌표 | ⚠️ **다르게 남** | `제주공항`→33.5068,126.4927 · `포항시청`→36.019,129.343 (실키가 고침) / `asdfqwer`·오타 주소는 **해시 좌표 그대로, 경고 없음** |
| 🟡 7 | 실 가게에 지어낸 선입금·`예약가능` | ✅ 재현 | 진김밥(분식·13m)에 `예약가능`+15,000원. 금액 줄 `(모의)` **고침** |
| 🟡 8 | localStorage 깨지면 백지 | ⏸ 실키 재현 안 함 → **고침** | 원인이 명확 |
| 🟢 9 | `any` 49곳 · 400줄 초과 2파일 | 변동 없음 | |

#### 📊 B 대조표 — 발표용 (앱 실값 vs 앱 추정값)

| 구간 | 직선 km | ODsay 실값 | 앱 추정값 | 배율 | TMAP 자차 |
|---|---:|---:|---:|---:|---:|
| 왕십리→건대입구역 | 3.7 | 9분 | 24분 | 2.67배 | — |
| 강남역→홍대입구역 | 11.2 | 40분 | 60분 | 1.50배 | — |
| 서울역→잠실역 | 12.3 | 38분 | 65분 | 1.71배 | — |
| 노원역→수원역 | 43.8 | 89분 | 216분 | 2.43배 | 127분 |
| 부발역→부천역 | 67.3 | 110분 | 329분 | 2.99배 | 117분 |
| 서울역→대전역 | 142.0 | **251분** ⚠️ | 688분 | 2.74배 | **170분** |
| 서울역→부산역 | 328.5 | 138분(KTX) | **1,583분** | **11.47배** | 316분 |

**"우리 앱의 대중교통 추정값은 실제의 1.5~11배다. 수도권 안에서는 방향이 맞고,
시외로 나가면 의미가 없다."** ⏸ 카카오맵 대조는 미검증(사람 눈 필요 — 리포트 §6).

#### 🔴 실키에서 새로 나온 것 5건

1. **ODsay 가 서울↔대전에 KTX 를 주지 않는다.** 경로 3개 전부 `pathType 2` 시내/광역버스
   251·255·255분. 요금 `-1`. 그런데 `verified: true`·`live: true` 라 **화면은 이걸 실 API 값으로
   표시한다**(측정 당시 라벨 `실시간`, 지금은 `경로 기준`). ⚠️ **라벨을 고쳐도 남는 결함이다** —
   문구가 아니라 **ODsay 응답 자체가 틀렸기 때문**이다.
   **자차(TMAP)가 170분이라 "대중교통이 자차보다 81분 느린" 값이다.**
   같은 ODsay 가 2.3배 먼 부산에는 KTX 138분·59,800원을 정확히 준다.
   > ⚠️ **§3-3 의 "대전 · trafficType 2 · 237분 → 이미 정상 동작" 판정을 정정한다.**
   > 파싱은 정상이지만 **값이 틀렸다.** "우리가 파싱에 성공했다"와 "ODsay 가 맞는 답을
   > 줬다"는 다른 문제다 — 이 저장소가 두 번 겪은 유형의 사고다.
   > 원인(도시내/도시간 알고리즘 경계로 **보임**)은 **미검증**. `SearchPathType` 미시도.
2. **ODsay 실패는 캐시되지 않아 반복 과금된다.** "실 API 성공값만 캐시"(CLAUDE.md §5)는
   옳은 규칙인데, 그 결과 ODsay 가 못 푸는 참가자가 한 명 있으면 **누가 화면을 열 때마다
   후보 수만큼 실패 호출이 나간다.** 실측: 4인 모임 하나를 8번 열어 **96콜**.
   무료 한도 1,000콜/일 → **모임 하나가 하루 10%를 태울 수 있다.**
   ⏸ 실패 호출의 쿼터 계상 여부는 **여전히 미검증**.
   → 팀 논의: `code 3`(구조적으로 못 푸는 좌표)만 짧은 TTL 로 캐시할 것인가.
3. **선입금 단가가 가게 카테고리를 안 본다.** `depositPerHead` 는 검색 쿼리 순번(`qi`)을
   쓰는데 이모지는 실제 카테고리를 본다 → **☕ 카페 아이콘에 맛집 단가 15,000원**.
4. **접근시간 보정이 서울역→수서역(SRT 탑승역) 접근에 73분**을 매긴다.
   정체를 계산으로 확정했다 — 직선 13.73km → `estMinutes = 72분`, **응답의 73분과 일치.**
   즉 접근 구간도 §B 와 **똑같은 추정 모델**을 탄다. 실값은 미측정이나, §B 의 비슷한
   길이 구간(서울역→잠실역 12.3km · 실 38분 / 추정 65분 · 1.71배) 배율을 적용하면
   **약 1.7배 과대** — 기존 기록과 같은 값으로 수렴한다.
5. **Preview 배포가 Vercel SSO 로 잠겨 있다** — 계정 없는 팀원·멘토님은 링크를 못 연다.
   Preview URL(커밋 `00dd851`):
   `https://ai-bootcamp-team3-final-project-a4pul2335-sleepingkkk.vercel.app`

#### 🟡 문구 통일(2026-08-05 CEO 결정) 감사에서 나온 잔여 4건 — **다음 작업 후보**

문구를 `경로 기준` / `거리 추정` 으로 통일한 뒤 **46-에이전트 4관점 감사**를 돌렸다
(지적 42 → 반증 검증 31 확정). 코드 결함 7건은 PR #46 에 반영했고, 아래 4건은
**문구가 아니라 동작을 바꿔야 해서** 남겼다. 전부 *"같은 값이 화면에 따라 다르게 설명된다"*
는 같은 뿌리다. 전문: [`docs/검증_실키_2026-08-05.md`](../docs/검증_실키_2026-08-05.md) §5.

| # | 발견 | 왜 남겼나 |
|---|---|---|
| 잔 1 | **근거리(≤700m) 도보 환산이 `real: true`** → 목록은 `경로 기준`. 그런데 `/api/route-detail` 엔 같은 단락이 없어 ODsay 를 그대로 부르고 실패 → 시트는 `거리 추정`. **역명 출발지가 흔해 자주 걸린다** | 도보 환산을 "경로"로 볼지 "추정"으로 볼지는 **팀 결정**(이 문서가 이미 "판단만 필요"로 남긴 항목) |
| 잔 2 | **도시간 접근 추정(`estimatedMin`)이 목록에 신호가 없다.** 서울→부산 203분 중 **73분이 우리가 더한 추정**인데 목록은 그냥 `경로 기준` | 세 번째 상태를 만들려면 `perParticipant` 에 `partial` 을 더 실어야 한다 — 스코프 확대 |
| 잔 3 | **자차 + `live:false`** 면 시트가 칩만 `거리 추정` 이고 **숫자를 안 그린다** | 시트에 폴백 숫자를 그리는 건 기능 추가 |
| 잔 4 | 진단 모드에서 `⚠ 검증 안 된 원시 응답` 옆에 파란 **`추천` 칩**이 그대로 붙는다 | 배포 기본값 `0` 이라 사용자 영향 없음. 실측 세션에서만 오판 위험 |

> 🔎 **잔 1·2 는 §3-0-A ① 과 같은 계열이다** — "화면이 숫자의 출처를 정확히 말하는가".
> 이번에 **사람 단위**까지 내려갔고, 다음 단계는 **구간 단위**(한 사람의 경로 안에서도
> 실측과 추정이 섞인다)다. 발표 필수는 아니지만 같은 원칙의 연장이다.

#### 🔴 릴리스 판단에 주는 영향

- **프로덕션에 ODsay 키를 넣으면 `regions` 가 1.55초 → 약 14초가 된다**(실측 13.87초).
  참가자 수 × 후보 수에 선형이라 6인이면 게이트만으로 18초다.
  **팀원의 속도 게이트 수정 PR 이 들어오기 전에는 키를 넣지 않는 것이 안전하다.**
- `실시간` 배지 수정이 들어가기 전까지 **프로덕션 화면은 추정값을 실시간이라 말하고 있다.**

#### 🧹 정리 대상 — 이 세션이 만든 테스트 모임 (실 Neon DB)

프로덕션: **`JKD8E3`**(`[삭제요망] 실키검증_prod`) ·
로컬 실키(같은 DB): `56E6G4` · `SZJ9RJ` · `AWCSJ4` · `H3DK3N` · `C5NKRJ` · `C4G23E`

> ⚠️ **미검증**: 실 API 를 넣은 상태의 화면과 `develop` Preview 배포는 확인하지 못했다.
> 이 컨테이너가 `*.vercel.app` · `api.odsay.com` 으로 나가지 못한다.
> 위 표에서 "키 있을 때" 판단은 **코드를 읽은 결과**지 실측이 아니다.
> `develop` 에도 Vercel Preview 주소가 있으므로 **CEO 브라우저에서는 확인 가능**하다.

### 3-1. 400줄 넘는 파일 — **팀원 붙기 전에 쪼개는 게 좋다**

문서에 "팀원이 만질 파일은 400줄 미만"이라고 적었던 적이 있는데 **사실이 아니다.**
`MeetingClient` 만 쪼갰고 홈 화면은 손도 안 댔다.

| 파일 | 줄 | 누가 만지나 | 판단 |
|---|---|---|---|
| `app/page.tsx` | **1,118** | 투표·추천 담당 영역 | 🔴 제일 급함. 중간지점·투표·지도·경로가 한 파일에 |
| `app/m/[code]/MeetingClient.tsx` | 909 | 🔒 통합 담당자 | 🟡 로직 572 + 조립 337. 쪼개도 충돌 방지 효과는 없다 |
| `lib/store.ts` | 657 | 🔒 통합 담당자 | 🟡 같은 이유 |
| `lib/ai.ts` | **592** | 👤 채팅·AI 파싱 담당 | 🔴 팀원 소유인데 400줄 초과 |

`app/page.tsx` 와 `lib/ai.ts` 가 실제 위험이다 — 둘 다 팀원이 만질 파일이다.

### 3-2. 추천 알고리즘 — 구조만 있고 알맹이가 없다

**현재 점수식은 이것뿐이다** (`lib/scoring/fairness.ts`):

```
score = 최대이동시간 + 편차 × 0.8   → 낮은 순 3개
```

상권·환승·요금·모임 성격 **전부 미반영**.

- **수도권 안**: 후보 풀이 하드코딩이다. 결과가 괜찮아 보이는 건 알고리즘이 아니라
  **사람이 미리 골라둔 큐레이션** 덕분이다 — 이건 숨길 일이 아니라 이 앱이 동작하는 방식이다.
- **수도권 밖**: ✅ **해결됨 (PR #26 머지 완료 — `dcb820a`)**. 거점 풀을
  **12곳 → 28곳(전국)** 으로 넓혔다. 예전에는 12곳 중 가장 가까운 곳도 45km 를 넘으면
  `geometricCandidates()`(순수 기하 폴백)로 빠져 `옥천군 안남면 도덕리` 같은
  **모일 수 없는 좌표**가 확정되고, 거기서 존재하지 않는 mock 가게가 떴다.

**실행으로 확인한 before/after** (`lib/geo.ts` 의 `recommendRegions` 직접 호출):

| 조합 | before | after |
|---|---|---|
| 서울+부산 | 중간지점 3 / 4 / 2 | **대전역 / 청주 / 동대구역** |
| 서울+거제 | 중간지점 3 / 4 / 2 | **전주역 / 대전역 / 청주** |
| 대구 2인 | 중간지점 3 / 2 / 4 | **동대구역** (1곳) |
| 서울 3인(대조군) | 왕십리 / 종로3가 / 사당 | **왕십리 / 종로3가 / 사당** (동일 — 회귀 없음) |

곁들여 바꾼 값 두 개(둘 다 팀 결정):
- `HUB_COVER_KM` **45 → 55** — 거점을 넣어도 서울+부산은 여전히 폴백이었다.
  중심이 경북 내륙 공백지대라 최근접 대전역이 55km 였다. 45km 면 장거리 7조합 중
  4개만 정상, **55km 면 7/7**. 45 는 후보가 수도권 12곳뿐이던 시절의 값이다
- `FALLBACK_MAX_KM = 60` **신설** — "가까운 순 4곳 강제 확보"가 대구 2인 모임에
  창원·울산(각 72km)을 끌어와 후보 2·3위로 띄웠다. 💰 이동시간 API 도 후보 수만큼 쓴다

⚠️ **후보 이름은 나아졌지만 이동시간 숫자는 아직 거리 추정이다** — 위 표의 분 단위 값은
`estMinutes`(직선거리 모델)라 서울↔부산이 949분으로 나온다. 실 ODsay 경로로는 130분대다
(§3-3). **`estMinutes` 개선은 별도 작업**이고 이번 PR 범위가 아니다.

⚠️ **`geocode()` 부분 문자열 매칭의 기존 한계**(이번 변경이 만든 것 아님):
`"포항시청"` 이 `"시청"`(서울시청)에 매칭된다. 카카오 키가 없을 때만 타는 경로라
프로덕션 영향은 없지만, 별칭을 추가할 때는 다른 지명 안에 들어가는 이름인지
반드시 확인해야 한다(`"광주"`·`"순천"` 은 그래서 별칭에서 뺐다).

#### 🔴 남은 문제 — 지방 거점 밀도 (통합 세션이 PR #26 검토 중 발견)

`lib/geo.ts` 로직을 그대로 떼어 계산한 **후보 개수 시뮬레이션**이다.
PR #26 본문은 대구만 언급했지만, **같은 원인이 부산·광주에도 나타난다**:

| 조합 | 후보 | 내용 |
|---|---|---|
| 서울 3인 | 9곳 | 강남역·교대·왕십리… + `서울역` |
| 서울+부산 | 3곳 | 대전역(55) · 청주(60) · 동대구역(72) ✅ |
| 부산 2인 | 4곳 | 서면·부산역 + **창원(35km)·울산(51km)** |
| 광주 2인 | **2곳** | 광주송정역 + **목포역(56km)** |
| 대구 2인 | **1곳** | 동대구역 |

**원인은 `FALLBACK_MAX_KM` 값이 아니라 거점 밀도다.** 수도권은 14곳인데 지방
대도시는 **1곳뿐**이라, 허용반경 안에 3곳이 안 차서 "가까운 순 확보" 폴백이
발동하고 옆 도시를 끌어온다. **상한을 올리면 더 먼 도시가 오고, 내리면 대구처럼
1곳이 된다 — 어느 값도 정답이 아니다.**

→ **해결 방향(팀 결정 사항)**: 지방 대도시에 도심 거점을 2~3곳씩 추가.
   예: 대구 = 동대구역 + 반월당/동성로 · 광주 = 송정역 + 상무지구/충장로 ·
   부산 = 서면 + 부산역 + 해운대. 그러면 폴백 자체가 안 걸려 옆 도시가 안 끌려온다.

⚠️ **후보가 1곳이면 "3개 중 투표"라는 이 앱의 핵심 UX 가 성립하지 않는다.**
`recommendRegions` 는 `slice(0,3)` 이라 1곳이면 1개만 내려가고, 화면에 `regions.length`
분기가 없어 투표 항목이 하나만 뜬다(크래시는 아님, **미검증** — CI 스모크는
수도권 케이스만 돈다). 거점 밀도를 보강하면 자연히 해소되므로 화면 처리보다
밀도 보강을 먼저 본다.

💰 **수도권 API 호출 약 12% 증가**: 서울 3인 후보 풀이 8곳 → 9곳(`서울역` 진입).
최종 3곳과 수치는 동일해 회귀는 아니지만, 이동시간 호출은 참가자 수 × 후보 풀이다.

🟡 `순천역` 좌표(`34.9455, 127.5030`)가 실제보다 **약 1.2km 동쪽**으로 보인다
(통합 세션 대조도 근사치라 **미검증**). 지도에 찍어 확인 권장.


**✅ 구조는 준비됐다.** `lib/scoring/` 에 파일 하나 만들고 `index.ts` 배열에 한 줄 추가.
`decayScore()` 가 0~1 정규화를, `worstOf()` 가 "평균이 아니라 최악 기준" 조합을 준다.
방법은 `lib/scoring/CLAUDE.md`.

> develop 에 실제로 있는 것: `types.ts` · `index.ts` · `fairness.ts` · `CLAUDE.md`.
> `weather.ts` 는 **PR #14 로 만들어져 머지 대기 중**(`lib/weather.ts` 데이터 소스 포함,
> `NEXT_PUBLIC_FF_WEATHER` 플래그 뒤). `commercial.ts` · `personal.ts` 는 아직 없다.

**⚠️ `ScoreContext` 채우기 (통합 세션 몫)**: `lib/routing.ts` 가 `ScoreContext` 를 만들 때
`{ participants }` 만 넘긴다 — `when`(날짜·시간)과 `weather` 가 빈 채로 온다.
PR #14 의 날씨 스코어러는 `ctx.weather ?? getWeather(ctx.hub, ctx.when)` 자급식이라
막히진 않지만, `when` 이 없어 **"오늘 저녁(19시)" 가정**으로 돈다. 통합 세션이
`scoreCandidates()` 에서 채우면 그 값이 자동 우선된다. **개인선호 스코어러는
여전히 `ScoreContext` 를 채워야 데이터를 받는다.**

**되살릴 자산**: `docs/legacy-algo/` 에 알고리즘 8종(빌드 대상 아님).
그대로 복사하지 말고 읽고 `lib/scoring/` 규격에 맞춰 옮긴다 — `docs/legacy-algo/README.md`.
가중치 최종값은 **팀 결정 사항**(`CLAUDE.md` §5).

**🗳️ 팀 결정 안건**: PR #14 날씨 스코어러의 `weight: 0.3` 은 `TODO(팀)` 표시된
임시값이다 (fairness 는 1.0). 머지를 막을 사유는 아니고, 팀이 최종값을 정하면 된다.

**제안한 3단계** (CEO 검토 대기):
1. ~~수도권 밖 후보를 **역·터미널**에서 뽑기~~ → ✅ **다른 방법으로 해결됨**(위 참고).
   카카오 카테고리 검색(`searchByCategoryKakao("SW8")`)이 아니라 **거점 목록에 직접 넣었다** —
   역이 있다고 모일 수 있는 건 아니어서(오송·김천구미·KTX울산역은 역세권 상권이 없다)
   기계 검색보다 사람 큐레이션이 맞다고 판단했다. API 호출도 늘지 않는다
2. **상권 밀집도** 반영 — `/api/places` 가 이미 주변 상권을 센다
3. `enhanced-scoring.ts` 이식 (단위 스케일 함정 주의 — `decayScore()` 로 흡수)

### 3-3. 수단별 이동시간 (KTX·고속버스) — **수정·실키 검증 완료. 배포 투입만 남음**

> ⚠️ 이전 서술("시외 구간은 근본적으로 답을 못 낸다 · ODsay 시외 전용 엔드포인트 필요")은
> **틀렸다.** 2026-08-03 실측으로 뒤집혔다. **ODsay 교체·추가 연동은 필요 없다.**

**실측 (CEO 로컬, PR #21 진단 플래그로 4콜 — 강남역 기준)**

| 목적지 | `rawTrafficType` | 응답 | 판정 |
|---|---|---|---|
| 부산 | **4** | 수서→부산 130분 · 서울→부산 138분 | SRT · KTX (실제 소요와 일치) |
| 김천 | **4** | 수서→김천(구미) 76분 · 영등포→김천 145분 | SRT · 무궁화 |
| 대전 | 2 | 시외버스 8326 등 3회 환승 237분 · `verified: true` | **이미 정상 동작** |
| 홍대(대조군) | 1·2·3 | 지하철 40분 1,650원 | 정상 |

**원인**: `TRAFFIC_KIND` 가 `{1:지하철, 2:버스, 3:도보}` 뿐이라 **`4`(철도)를 "도보"로
뭉갰다**(`lib/odsay.ts:57,79`). 그래서 "탑승 구간이 하나도 없는 응답"으로 보여
껍데기 가드에 걸려 버려졌다. ODsay 는 처음부터 제대로 답하고 있었다.
시외 경로는 `pathType: 11` 로 온다(우리는 1·2·3 만 안다).

**즉 "시외 전체"가 아니라 "철도 경로"만 안 됐다.** 시외버스는 `2` 로 와서 통과한다.

#### 고친 것 (PR #24 — **머지 완료** `12cc335`)

1. ✅ **`4: "train"` 매핑 추가** + `kind` 타입 확장 + 열차 아이콘(🚄)·라벨.
   `pathType` 은 `1|2|3` → `number`(시외 11). **미지 타입은 `"walk"` 가 아니라 `"other"`** 로
   떨어뜨려 같은 유형의 버그가 재발하지 않게 했다 — 수단명만 모를 뿐 탑승 구간으로는 남는다
2. ✅ 🔴 **요금 "무료" 오표시** — `formatWon` 은 음수를 `"—"` 로, 대중교통 요금은 새
   `formatFare()`(0 이하 → "요금 정보 없음")를 쓴다. 통행료 0원은 진짜 무료라 그대로 뒀다
3. ✅ **판정 비대칭 해소** — `isRideType()` 하나로 통일
4. ⚠️ **열차명은 여전히 미확정.** `lane[0].name`·`busNo` 둘 다 비어 있다(실측).
   추측해 넣는 대신 수단 기본 라벨("열차")로 폴백하고, 진단 모드에서 `rawLane` 원본을
   노출해 다음 실측 때 필드를 특정할 수 있게 했다
   → ✅ **2026-08-04 해결.** `lane` 이 아니라 **`subPath.trainType`** 이었다(아래 §남은 일 1).
   `lane` 이 비어 있다는 관찰 자체는 맞았고, **볼 곳을 잘못 짚은 것**이 원인이다

**❓"추천 점수에 이미 KTX 시간이 들어가는가" → ✅ 들어가고 있었다 (확인 완료).**
수정 전 코드를 실측값으로 재구성한 응답에 대해 돌린 결과, 부산 경로에서
`transitRouteOdsay` 는 **130분(KTX)을 반환**했고 `transitRoutesDetail` 만 `null` 이었다.
즉 **추천 순위는 이미 실제 철도 시간 기준이었고 화면만 "추정값"으로 어긋나 있었다.**
→ 이번 수정으로 **추천 결과 자체는 바뀌지 않는다**(같은 130분). 화면이 진실을 따라온 것이다.
→ **§3-2 에 주는 영향 없음** — 이동시간은 이미 맞았으므로, 서울↔부산에 "김천시 개령면
신룡리" 같은 후보가 나오는 문제는 §3-3 이 아니라 **§3-2 단독 과제**다

**남은 관찰 도구**: `NEXT_PUBLIC_FF_ODSAY_PROBE=1`(기본 OFF)로 원시 응답을 볼 수 있다.
**아직 제거하지 않았다** — 열차명 필드가 미확정이라 `rawLane` 관찰이 남아 있고, 이번
수정의 실키 검증에도 캐시 우회가 필요하다. 열차명이 특정되면 별도 PR 로 제거한다
(`lib/flags.ts` 는 🔒 통합 세션 소유).
💰 켜면 캐시를 우회하므로 ODsay(1,000콜/일)·TMAP 호출이 늘어난다. 확인 후 반드시 `0`.

> **CI 매트릭스에는 넣지 않기로 했다**(통합 세션 판단, PR #21·#24 동일).
> CI 엔 ODsay 키가 없어 켜도 mock 으로 빠지므로 검증 가치가 없는데 CI 시간만 1.5배가 된다.
> 대신 **통합 세션이 머지 전 플래그 ON 으로 직접 돌린다**(#21·#24 에서 실제로 했다).

#### ⏳ 남은 일

1. ~~CEO 실키 확인~~ — **완료(2026-08-03, CEO 로컬).** 🚄 열차 표시 · "요금 정보 없음" ·
   `verified: true` 전부 확인. **열차명은 못 얻었다** — `rawLane` 이 응답에 아예 없었다.

   > 🔴 **정정 (2026-08-04). "ODsay 가 시외 열차에 노선 정보를 주지 않는 것"이라고
   > 결론냈던 것은 틀렸다.** CEO 가 "공식 문서 기준으로 맞지 않는다"고 지적해 확인했다.
   >
   > **열차 정보는 `lane` 이 아니라 `subPath` 에 직접 실린다.** 외부 구현체에서 확인:
   > ```js
   > // twowice/myway — RouteDetailBody.tsx
   > const base = TRAIN_TYPE_LABEL[Number(s.trainType)] ?? "기차";
   > const sp   = s.trainSpSeatYn === "Y" ? "(특실)" : "";
   > ```
   > ODsay 릴리스 노트에도 *"도시간 길찾기에 기차 환승 지원 추가 · 기차일 때 특실 정보
   > 포함"* 이 있다. 즉 **`lane` 이 비어 있던 건 사실이지만 정보가 없는 게 아니라,
   > 우리가 `lane[0].name`·`busNo` 만 보고 `subPath.trainType` 을 안 본 것**이다
   > (`lib/odsay.ts:128` `laneName()`).
   >
   > **교훈: "응답에 없다"가 아니라 "내가 본 필드에 없다"였다.** 원시 응답 전체를
   > 덤프해 보지 않고 특정 필드만 확인한 뒤 API 쪽 한계로 결론내지 말 것.
   >
   > ✅ **`trainType` 실측 완료 (2026-08-04, 7콜)** — 열차명이 이제 화면에 뜬다.
   >
   > | 값 | 관측 | 판정 |
   > |---|---|---|
   > | **1** | 서울→부산 138분 **59,800원**(KTX 일반실 공시운임과 일치) · 용산→전주(전라선) | ✅ **KTX** |
   > | **8** | **수서 출발에서만** 관측 · 수서→부산 130분 52,200원 · 하루 39편 | ✅ **SRT** |
   > | 3 | 서울→김천 173분 16,400원 · 특실 없음 | ⚠️ 무궁화로 **추정**, 미검증 |
   > | 6 | 서울→김천 155분 24,500원 · 특실 없음 (3보다 1.5배 비싸고 빠름) | ⚠️ ITX-새마을로 **추정**, 미검증 |
   >
   > 🛑 **3·6 은 추가 실측으로도 확정할 수 없다.** ODsay 응답에 **열차 이름 필드가
   > 아예 없어서**, 숫자→이름 대응은 공식 문서로만 닫힌다. 💰 를 더 태우지 말 것 —
   > 지금은 매핑에 넣지 않아 화면에 "열차"로 폴백한다(`rawTrainType` 으로 관찰 가능).
   >
   > ⚠️ **`trainSpSeatYn` 을 "특실"로 표시하지 않는다.** 외부 구현체(`twowice/myway`)는
   > `"Y"` 일 때 이름에 "(특실)"을 붙이는데, 실측해 보니 이 값은 **"그 열차에 특실 칸이
   > 있다"**는 뜻이지 "이 요금이 특실"이 아니다 — KTX·SRT 는 전 구간 `"Y"`, 무궁화 계열은
   > 전부 `"N"` 이었고 일반실(`payment`)과 특실(`trainSpSeatPayment`) 운임이 따로 온다.
   > 그대로 따랐으면 **모든 KTX 가 "KTX(특실)"** 이 됐다.

   → 진단 플래그(`NEXT_PUBLIC_FF_ODSAY_PROBE`)는 **이제 제거해도 된다.** 관찰 대상
   (`trainType`·`trainSpSeatYn`·`trafficType 5/6/7`)이 전부 닫혔다. 다만 제거는
   `lib/flags.ts`(🔒)를 건드리므로 통합 세션이 별도 PR 로 판단한다.

   > 🔴 **덤으로 드러난 것 — 도시간 요금 필드가 다르다 (2026-08-04 실측, 이번 PR 에서 수정).**
   > ```
   > 도시내(pathType 1·2·3)    info = { totalTime, payment, totalWalk, mapObj, ... }
   > 도시간(pathType 11·12·13) info = { totalTime, totalPayment, totalDistance, ... }
   > ```
   > 우리는 `info.payment` 만 봐서 **KTX 59,800원·고속버스 39,700원이 응답에 있는데도
   > "요금 정보 없음"** 으로 표시하고 있었다. 열차명 때와 **같은 유형**이다 —
   > *"응답에 없다"가 아니라 "내가 본 필드에 없다"*. 이제 `fareOf()` 가 둘 다 본다.
   > 도시간 응답에는 `totalWalk`·`mapObj` 도 없다(도보 0m · 폴리라인 없음이 정상).
   >
   > 🔴 **단, 항공은 요금을 표시하지 않는다.** 위 수정이 KTX·고속버스와 함께
   > **항공 146,700원(김포→제주)도 화면에 올렸는데**, 실제 편도는 평균 3만원대·
   > 최저 1만원대다(2026-08-04 외부 대조). ODsay 는 날짜·시간과 무관하게 고정값을
   > 주는데 **항공은 동적 요금이라 구조적으로 맞을 수 없고**, 응답에 등급·기준시점
   > 표기가 없어 이 숫자가 무엇인지 우리가 말할 수 없다. 정상운임으로 **보이지만
   > 그것조차 미확정**이라 그리지 않는 쪽을 골랐다(CLAUDE.md §6).
   > 원시값이 필요하면 `subPath[].payment` 를 직접 본다.
2. ⚠️ **프로덕션 ODsay 키가 빠져 있다**(§1 `odsay: false`). 배포에서 효과를 보려면
   Vercel 에 넣어야 하는데, **Vercel 은 나가는 IP 가 유동이라 ODsay 서버 키를 직접 못 쓴다.**
   → **#28 이 그 우회로를 만들었다**(`ODSAY_BASE_URL` 로 고정 IP 프록시 경유).
   투입 절차와 순서는 **§1 릴리스 순서** 참고 — ⚠️ **릴리스 이후에 넣어야 한다.**

#### 🟡 통합 세션 검토에서 남긴 것 (비차단 — 다음에 이 코드를 만질 때 참고)

- **판정 통일이 완전하지는 않다.** `transitRouteOdsay` 는 `isRideType(s.trafficType)`
  (trafficType 기반)인데 `transitRoutesDetail` 은 `l.kind !== "walk"`(kind 기반)이다.
  `trafficType` 이 숫자가 아닌 경우(누락·문자열) 갈린다 — detail 은 `"other"` 로 탑승 인정,
  route 는 제외. **관측된 적 없는 엣지**라 차단하지 않았지만, "isRideType 하나로 통일"이라고
  믿고 작업하면 오해한다
- `RouteSheet.tsx` 에 `any` 가 1개 늘었다(`:178`). 주변이 전부 `any` 인 기존 패턴을 따른
  것이고, 근본 해결은 `data` 에 타입을 주는 별도 작업이다 (§3-5 "any 46곳"에 포함)

#### 📌 PR #28 검토에서 내린 판단 (되묻지 않도록 근거를 남긴다)

- **`RegionsWithMeta` 는 `lib/routing.ts` 에 둔다** — `types.ts`(🔒)로 옮기거나
  `RegionCandidate` 에 `live` 를 넣지 않는다. **`live` 는 후보 하나의 속성이 아니라
  목록 전체의 속성**이라 후보마다 중복 저장하는 꼴이 되고, `types.ts` 는 도메인 타입의
  자리이지 특정 함수의 반환 형태를 두는 곳이 아니다. 🔒 회피가 아니라 그 자체로 옳다
- **`live` 정의 = "한 명이라도 폴백이면 `false`"** (보수적). "과반이 실값이면 true" 는
  사용자에게 설명할 수 없는 상태다("절반은 진짜예요"라고 말할 수 없다).
  CLAUDE.md §6 을 지켜온 이 앱의 기준에 맞는다
- **`routeCache` 에 TTL 을 넣지 않는다** — 근거 셋:
  ① 캐시엔 실 API 성공값만 들어가므로 `live: true` 가 거짓말은 아니다
  ② **Vercel 서버리스는 인스턴스마다 캐시가 따로고 수시로 새로 뜬다**(적중률이 낮다)
  ③ 💰 TTL 은 무료 한도 소모를 늘린다.
  대신 **반증 테스트를 새 좌표로 하라**는 주의를 §1 에 남겼다

> 3-2 와 3-3 은 같은 문제의 앞뒤다 — **함께 설계하는 것을 권한다.**
> 이동시간이 풀려도 후보 생성(§3-2)이 그대로면 서울↔부산에 "김천시 개령면 신룡리"
> 같은 모일 수 없는 좌표가 계속 나온다.

### 3-4. 남은 로직 복제 (감사에서 발견, 미수정)

- `app/votes/page.tsx` 가 `sections/VoteList.tsx` 를 안 쓰고 **같은 투표 UI 를 통째로 재구현**
  (지역·가게 두 벌, 약 90줄). 투표 UI 를 바꾸면 두 곳을 고쳐야 한다
- `myCodes()` 가 `app/page.tsx` · `meetings/page.tsx` · `members/page.tsx` · `votes/page.tsx`
  **네 곳에 한 글자도 다르지 않게 복제**돼 있다

### 3-5. 그 외

- [ ] 모임 비밀번호 **평문** — `db/schema.sql` 의 `TODO(BE)`
- [x] Vercel 배포 — **완료**(`f0a4235` → `b92ac70`). ⚠️ AI 를 되살릴 땐 `void runAiTurn()` 을
      `waitUntil()` 로 감쌀 것. 그리고 Ollama 1순위가 팀 내부망 주소라
      **Vercel 에서는 닿지 않는다** — 배포에서 AI 를 쓰려면 외부 접근 가능한 주소가 필요하다
- [ ] 폴링(`MeetingClient.tsx:118` `setInterval(load, 1800)`) → Realtime 검토
- [ ] 알림(헤더 벨, `V8Header.tsx:52`) 실데이터 연결 — 현재 빈 상태
- [ ] 구글 캘린더 **가져오기** (`lib/calendar.ts` 는 내보내기만)
- [ ] `any` 46곳 — `CLAUDE.md` §3-3 이 금지하는데 남아 있다.
      정리하면 `.eslintrc.json` 에 `next/typescript` 를 다시 켤 수 있다

---

## 4. 겪은 사고 (반복 금지)

### 타입검사·빌드로 안 잡히는 것

| 사고 | 왜 안 잡히나 |
|---|---|
| `useEffect` 를 조건부 `return` 뒤에 배치 | 화면이 통째로 미렌더. `tsc`·`build` 둘 다 통과 |
| `lib/store.ts` 호출에 `await` 누락 | Promise 를 그냥 둬도 타입이 맞다 — 조용히 무동작 |
| 모달 prop 어긋남 | 조건부 렌더라 열어보기 전엔 모른다 |

→ **브라우저로 실제로 열어야 잡힌다.** 그래서 CI 가 크로미움을 띄운다.
→ 훅 배치 사고는 이제 `npm run lint` 가 잡는다(`react-hooks/rules-of-hooks`).

### `backdrop-filter` 안에서는 `position: fixed` 가 갇힌다

`.appbar` · `.leaderbar` · `.v8-bottomnav` · `.v8-header` 가 전부 쓴다.
그 안에서 전체화면 오버레이를 띄우면 헤더 안에 갇혀 잘린다 —
**반드시 `createPortal(…, document.body)`** (`components/v8/LoginSheet.tsx` 참고).

### 내 환경에서만 되는 것

`npm run verify` 가 **방금 clone 한 환경에서 스모크 3/4 실패**했다
(`Executable doesn't exist` — Playwright 브라우저 없음). 개발 컨테이너엔
`PW_CHROMIUM_PATH` 가 있어서 안 보였다. **팀원 환경을 재현해서야 드러났다.**
→ `pretest:smoke` 로 자동 설치. **"내 환경에서 되는지"가 아니라 "빈 환경에서 되는지"를 본다.**

### 문서가 코드와 어긋나면 다음 세션이 잘못된 전제로 시작한다

v8 에서 `CLAUDE.md` 에 쓰지도 않는 Tailwind·shadcn 이 적혀 있었다.
이번 감사에서도 나왔다 — 없는 파일(`ChatSection.tsx`)을 `CODEOWNERS` 가 가리켰고,
그대로 뒀으면 **GitHub 이 그 줄을 조용히 무시**해 소유권이 안 걸렸을 것이다.

---

## 5. 한계 (의도된 범위)

결과 화면 선입금은 **모의결제** · AI 파실리테이터는 기본 비활성(`NEXT_PUBLIC_FF_AI_CHAT=1` 로 켬) ·
구글 캘린더는 내보내기만 · 알림은 미연동.

**범위 외**: 실 결제 연동 · 네이티브 재작성(Flutter·RN — PWA+TWA 로 간다) · OCR · 자동 카톡 동기화.

---

## 6. 팀 결정 사항

**DB — ✅ Neon 확정 (멘토님 추천 수용, Supabase 에서 이관 완료).**
`lib/persistence.ts` 가 격리돼 있던 덕에 드라이버(`@neondatabase/serverless`)와
스키마(`db/schema.sql`)만 갈아끼웠다. 접속은 `DATABASE_URL` 하나(서버 전용)다.

기록해 둘 트레이드오프: Neon 은 Realtime 이 없다 — 채팅·실시간 투표를 실제로
만들 거라면 **자체 WebSocket/SSE 서버가 필수**가 된다(현재는 1.8초 폴링이라 무관).
