/* 홈 — `/` 로 그냥 온 사람이 만나는 자리.

   2026-08-19: **목업(모이머 한화면.html) ① 화면의 얼개로 갈아탔다.**
   전에는 위→아래로 스크롤하는 문서였다(머리말 → 가운데 찾기 → 소개). 지금은
   **전체화면 지도 위에** 검색바와 출발지 줄이 떠 있고, 아래에서 바텀시트가 올라온다.

   뜻은 그대로다 — 로그인도 모임도 없이 **써 보면서** 무엇을 해 주는 곳인지 알게 한다.
   달라진 것은 그 '써 보는 것'이 화면의 한 칸이 아니라 화면 전부가 됐다는 점이다.

   옛 홈(`app/홈머리말.tsx` + `app/탐색/탐색.tsx`)은 이식이 끝난 뒤 **지웠다**(2026-08-20).
   되돌릴 자리로 남겨 뒀던 것인데, 아무 데서도 안 불리면서 유지비만 냈다 — `lib/넘기기.ts`
   를 고칠 때 죽은 화면도 따라 고쳐야 했다. 다시 보려면 git 이력에서 꺼낸다.

   여기서 없앤 것은 없다 — 만들기 단추 · AI 추천 · 이동 수단 · 출발지별 상세는 전부
   바텀시트 안으로 들어갔다(app/홈/시트내용_가운데.tsx). */
import 홈셸 from './홈/홈';

export default function Home() {
  return <홈셸 />;
}
