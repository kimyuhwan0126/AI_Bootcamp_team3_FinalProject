/* 홈 — `/` 로 그냥 온 사람이 만나는 자리. **옛 판 모양**이다 (예전판 기록 §1 · 사진 `예전판_02`).

   차례에 뜻이 있다: **머리말 → 가운데 찾기 → 소개.**
   가운데 찾기가 맨 위인 것이 이 화면의 뜻이다 — 로그인도 모임도 없이 **써 보면서**
   무엇을 해 주는 곳인지 알게 한다. 설명을 읽게 하는 것보다 한 번 해 보는 편이 빠르다.

   옛 판과 다른 점은 넷이다 —
     ① **갈래(카테고리) 거르개가 없다.** 사람이 뺐다
     ② **머리말 단추가 하나다.** 옛 판의 🔔 알림은 우리에게 없는 기능이라 안 그리고(논의105),
        아래 ④ 로 → 단추(모임 참가)도 없앴다
     ③ 옛 판의 스플래시가 없다 — [시작하기] 가 아무 데도 안 가는 덮개였다(기록 §1 ①)
     ④ **'초대 코드로 들어가기' 칸이 없다.** 초대는 카톡·공유로 전한 실제 링크
        (`https://.../join/<코드>`) 를 눌러 `/join/<코드>` 로 바로 들어오는 길이 전부다 —
        비로그인으로도 그 화면에서 바로 참여 폼을 채운다. 손으로 여섯 자리를 치는 사람은
        없으니 홈에 그 칸을 둘 까닭이 없다. (코드를 화면으로 부르는 옛 자리는 `코드로들어가기.tsx`
        에 남겨 뒀다 — 지운 게 아니라 이제 아무 데서도 안 부른다.)

   ⚠ **'내 모임' 요약 칸은 없앴다** (논의129). 탭바가 생기기 전에 만든 것이라,
      아래 [모임] 탭이 같은 일을 거르개까지 달고 하는 지금은 같은 것을 두 군데서 보게 됐다.
      옛 판 홈에도 이 칸이 없었던 것이 같은 까닭일 것이다 — 탭이 있으니까.
      '나한테 기다리는 게 있다' 는 알림은 **[모임] 탭의 점**이 대신한다(tabbar.tsx). */
import 홈머리말 from './홈머리말';
import 탐색 from './탐색/탐색';

export default function Home() {
  return (
    <div className="wrap">
      <홈머리말 />
      {/* 머리말이 sticky 라 `.sheet` 의 위 테두리는 두지 않는다 — 두 줄이 겹쳐 보인다 */}
      <div className="sheet" style={{ maxHeight: 'none', flex: 1, borderTop: 0 }}>
        {/* 이 화면의 주인공. 로그인도 모임도 없이 **써 보면서** 무엇을 해 주는 곳인지 알게 한다 */}
        <탐색 />

        {/* 처음 온 사람에게 — 무엇을 하는 곳인지 한 줄로만 말한다 */}
        <p className="mut" style={{ margin: '18px 0 0' }}>
          각자 출발지를 내고 가고 싶은 곳을 고르면, 가장 많이 고른 곳으로 정해져요.
        </p>
      </div>
    </div>
  );
}
