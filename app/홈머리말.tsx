'use client';
/* 홈 머리말 — **옛 판 모양**이다 (예전판 기록 §2 · §12-3).
   워드마크 `MOIMER` 하나만 남았다.

   ⚠ 옛 판에는 단추가 셋(＋ 모임 생성 · → 모임 참가 · 🔔 알림)이었는데 여기는 **0개**다.
      알림은 우리 서비스에 **없는 기능**이라 안 그리고(논의105), → 모임 참가 단추도 없앴다 —
      초대는 실제 링크(`/join/<코드>`)를 눌러 바로 들어오는 길이 전부라, 홈에서 누를 '참가'
      단추 자체가 갈 곳이 없어졌다(`app/page.tsx` 의 ④ 참고).
      ＋(모임 만들기)도 오늘 없앴다 — 바로 아래 탐색 칸에 이미 같은 곳으로 가는 **꽉 찬**
      `[모임 만들기]` 단추가 있어서, 여기 있던 작은 동그란 ＋ 는 같은 일을 하는 둘째 단추일
      뿐이었다. 눈에도 덜 띄는 작은 단추를 하나 더 두느니 없앴다.

   워드마크는 옛 판 그대로 `MOIMER` 다. 화면 안 다른 글은 다 한글인데 여기만 로마자인 것이
   어색해 보일 수 있으나, **로고는 이름이지 글이 아니다** — 옛 판을 그대로 두라는 뜻으로 받았다. */
import Link from 'next/link';
import s from './홈머리말.module.css';

export default function 홈머리말() {
  return (
    <header className={s.머리} data-slot="홈머리말">
      <Link href="/" className={s.로고} aria-label="모이머 홈">MOIMER</Link>
    </header>
  );
}
