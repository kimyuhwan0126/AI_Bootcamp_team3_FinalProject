// ─────────────────────────────────────────────────────────────
// /votes — **폐기된 라우트** (설계_v19.md §4-② · v6)
//
//  '투표함' 탭은 **모임 탭으로 통합**됐다. 투표할 것은 모임 탭 상단에 고정된다.
//  이 파일을 지우지 않고 리다이렉트로 남기는 이유:
//    · 카카오톡에 공유된 옛 링크·브라우저 북마크가 404 로 죽지 않게
//    · 발표 중에 누가 옛 주소를 눌러도 자연스럽게 이어지게
// ─────────────────────────────────────────────────────────────
import { redirect } from "next/navigation";

export default function VotesRedirect() {
  redirect("/meetings");
}
