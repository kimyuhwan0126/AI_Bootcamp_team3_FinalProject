# 모이머 실서비스 기술 스택

출처: `외부자료/2026-08-11_101011.png` (BRAINSPARK — 05 CHAPTER 기술 구현 및 아키텍처)
**변경 1건**: 데이터 `Supabase (PostgreSQL)` → **`NeonDB (PostgreSQL)`** (2026-08-11 확정)

| 영역 | 채택 |
|---|---|
| 프론트엔드 | **Next.js 14 (App Router)** · TypeScript · React |
| 지도·장소 | **Kakao Maps JS SDK** · Kakao Local 검색 |
| 이동시간 안내 | **ODsay LIVE**(대중교통) · **TMAP**(자차 경로) |
| 데이터 | **NeonDB (PostgreSQL)** ← 이미지의 Supabase 를 대체 |
| 배포 | **Vercel** |
| AI | Ollama · **GLM 5.2 (cloud)** — 스택 이미지의 'Gemini 2.5 Flash' 는 틀림(아래 참조) |

---

## 목업(현재)과의 차이

지금 `모이머 한화면.html` 은 **브라우저 하나에서 도는 단일 파일**이다. 실서비스로 옮기면 전제가 바뀐다.

| | 목업(지금) | 실서비스 |
|---|---|---|
| 저장 | `localStorage` (내 브라우저에만) | NeonDB (모두가 같은 데이터를 본다) |
| 신원 | `db.meAs` — 기기 기록 | 카카오 계정 식별자 / 이름+PIN |
| 권한 | **화면이 버튼을 안 그려서** 막힘 | 서버가 막아야 함 |
| 다른 사람의 투표 | 안 보임(혼자 도는 목업) | 실시간으로 보여야 함 |
| 지도 | 목업 SDK(`moimer-mock-kakao.js`) | 진짜 Kakao Maps JS SDK |

---

## NeonDB 로 바꾸면서 새로 정해야 하는 것

Supabase 는 **인증·권한(RLS)·실시간**이 딸려 오지만 NeonDB 는 **Postgres 그 자체**다. 셋을 따로 정해야 한다.

1. **카카오 로그인을 무엇으로 받나** — Supabase Auth 의 OAuth 공급자가 없어짐.
   후보: NextAuth(Auth.js) / 카카오 SDK 직접 + 자체 세션 / Clerk 같은 외부 인증
2. **권한을 어디서 강제하나** — Supabase RLS 정책 엔진이 없어짐.
   후보: Next.js Route Handler(서버)에서 검사 / Postgres RLS 를 직접 배선 / 둘 다
3. **다른 사람의 투표가 실시간으로 보이나** — Supabase Realtime 이 없어짐.
   후보: 폴링 / SSE / Vercel 이 지원하는 방식 / 실시간 포기하고 새로고침

---

## AI 모델 — 이미지가 틀렸다 (실측으로 확정)

스택 이미지는 `Ollama Gemini 2.5 Flash` 라고 적었지만, 실제 `.env.local` 은 이렇다:

- `OLLAMA_PRIMARY_MODEL = glm-5.2:cloud` @ `http://10.20.2.164:11434/v1` (사내망)
- `OLLAMA_FALLBACK_MODEL = qwen2.5-coder:7b` @ `http://localhost:11434/v1`

순서도 5종의 `AI(GLM 5.2)` 표기가 맞고 **이미지의 'Gemini 2.5 Flash' 가 틀렸다.**
다만 2순위는 코더 모델(`qwen2.5-coder`)이라 모임 추천용으로는 어울리지 않는다 — 따로 정해야 한다.

---

## 연결 점검 결과 (2026-08-11 · `node envcheck.js`)

| 서비스 | 결과 |
|---|---|
| **Neon Postgres** | ✅ TLSv1.3 · PostgreSQL 18.4 · `neondb` · 테이블 `meetings, participants, votes` 존재 |
| **Kakao Local** | ✅ HTTP 200 — '성수동' 검색 → `서울숲카페거리` |
| **TMAP** | ✅ HTTP 200 — 강남역→성수역 8.8km · 25분 |
| **ODsay** | ❌ `[ApiKeyAuthFailed]` — 키가 틀렸거나 **이 서버 IP 가 화이트리스트에 없음** |
| **Ollama 1순위** | ❌ 연결 시간 초과 — `10.20.2.164` 는 사내망이라 이 자리에선 안 닿음 |
| **Ollama 2순위** | ✅ 붙지만 **원하는 모델이 없음** — 설치된 것은 `gemma4:12b`, `qwen3.5:0.8b` |

기능 플래그는 **넷 다 꺼짐**: `FF_AI_CHAT=0` `FF_REALTIME=0` `FF_WEATHER=0` `FF_PERSONAL=0`

### 남은 조치
1. **ODsay** — lab.odsay.com 에서 이 개발 PC(및 Vercel)의 공인 IP 를 화이트리스트에 등록. Vercel 서버리스는 IP 가 고정이 아니라 배포 시 별도 방법이 필요하다.
2. **Ollama 2순위** — `qwen2.5-coder:7b` 를 받거나, 이미 있는 `gemma4:12b` 로 바꾸거나, 폴백을 포기.
3. **스택 이미지의 AI 칸**을 GLM 5.2 로 고쳐야 한다.

### 점검 방법
```
cd 순서도/실서비스
node envcheck.js      # 비밀값은 앞 4자만 보여주고 가립니다
```

