# 1차 · 2차 그릴링 ↔ 현재 코드 — 차이 대조

_작성: 2026-08-10 · Claude(AI) · 브랜치 `develop-grilled-session-kyh`_

> **수정 전에 차이만 적은 문서다.** 무엇을 고칠지는 §4 목록에서 팀이 고른다.
>
> - **1차 산출물** = `docs/설계도/` 순서도 HTML 5장 (v19 확정판, 2026-08-07)
> - **2차 산출물** = `docs/설계도/2차/` 동작하는 프로토타입 → 상세는 [`설계_v20_2차그릴링_대조.md`](설계_v20_2차그릴링_대조.md)
> - **현재 코드** = 이 브랜치 HEAD
>
> `설계_v19.md` 는 제가 1차 순서도를 텍스트로 옮긴 문서다. 그래서 이번엔
> **순서도 원본 5장을 직접 다시 읽어** 코드와 맞췄다 — 옮기는 과정에서
> 빠진 게 있을 수 있기 때문이다. 실제로 §2 의 A·B·C 는 원본에는 있고
> `설계_v19.md` 에는 흐릿하게만 적힌 것들이다.

---

## 0. 대조 방법

순서도 5장은 내용이 전부 `<script>` 안 문자열이라 브라우저 없이는 안 읽힌다.
한글 문자열만 뽑아 코드와 하나씩 맞췄다.

| 원본 | 뽑힌 줄 | 그 장이 정하는 것 |
|---|---|---|
| `1_전체화면_순서도_설계확정판_v19.html` | 289 | 화면 11대 · 화면별 문구 · 버튼 |
| `2_전체화면_UIUX_순서도.html` | 184 | 화면 전이 · UI 상태 |
| `3_모임생성결과_프로세스_UIUX_순서도.html` | 188 | 생성~결과 한 줄기 |
| `4_모임생성결과_프로세스_분산_순서도.html` | 134 | **역할 스윔레인 · 분기 조건 · 상시 규칙** |
| `5_기능_순서도.html` | 69 | **화면 → API → 도메인 → 저장** 대응 |

4번·5번이 가장 값지다. 4번은 분기 조건을, 5번은 액션 이름(`{addRegion}`,
`{startVote}` …)까지 적어 놔서 코드와 **한 줄씩** 맞출 수 있다.

---

## 1. 1차 = 현재 코드 (그대로 된 것)

**이미 맞다. 손대지 않는다.**

| 1차 순서도가 요구한 것 | 코드 |
|---|---|
| 6자 참여코드 · `stage: main` · `aiPhase: region` | `lib/store.ts:122` `createMeeting` |
| 참여 폼 원스텝 — 이름·출발지·이동수단·**개인 PIN**(비로그인만) | `app/m/[code]/sections/JoinGate.tsx:31,158` |
| 이름 중복 → **별칭 유도**, PIN 은 별도 복구 경로 | `lib/store.ts:223` · `case "recover"` |
| 정원 8명 초과 → 입장 거부 | `joinMeeting` |
| 1.8초 폴링 · 표는 별도 행(동시쓰기 보호) | `MeetingClient` · `castVote`/`votes` |
| 출발지 = 자동완성 좌표 그대로, 텍스트면 지오코딩 폴백 | `setOriginCoords` / `setOrigin` |
| 핑 = 누구나 · **인원당 1개** · 동 스냅 · 같은 동 병합 · 실패 시 좌표 폴백 | `addRegionCandidate` (`lib/store.ts:647`) |
| **후보 0개 시작 불가 / 1개 투표 생략 / 2개 이상 투표** | `candidateGate` (`lib/store.ts:1380`) |
| 투표 시작 = 핑 잠금 · 이후 등록·이동 불가 | `startVote` (`lib/store.ts:1393`) |
| 1인 1표 · 재탭으로 변경 · 서버는 집계만 | `castVote` |
| 진행률 `n/N` · **미투표자 이름** | `sections/LeaderBar.tsx:84,96` |
| AI 재호출 → **교체/추가 선택 팝업** | `AiMergeMode` (`lib/store.ts:865`) · `LeaderBar.tsx:121` |
| AI 로딩 문구 지역 ~25초 / 지점 ~9초 | `LeaderBar.tsx:137` |
| 확정 범위 분기 — `지역까지` 는 지점 단계 건너뛰고 result 직행 | `confirmRegion` (`lib/store.ts:429~`) |
| 반경 **700m** · 밖은 거부 · 확장 **1회 1400m · 전원 공유** | `radiusM` · `expandRadius` (`lib/store.ts:1236`) |
| 지점 후보 **상한 없음** · 방장·본인 삭제 | `addPlaceCandidate:1259` · `removePlaceCandidate:1323` |
| 경로 API 는 **확정 후에만** | 결과 화면에서만 호출 |
| 도착 신호등 — 당일만 · 노랑=지각(분) · 빨강=불참 · 시간 없으면 잠금 | `setParticipantStatus` · `ArrivalPanel` |
| 지점 카카오 링크 연결 (지점 모임만 · 결제 아님) | `ResultSection` |
| 지난 모임 — 모임일 **다음날 0시** / 시간 없으면 마지막 활동 **7일** | `archiveDueAt` (`lib/store.ts:984`) |
| 방장만 **'이 멤버로 재모임'** | `recreateMeeting` (`lib/store.ts:1041`) |
| 강퇴 = 그 사람 핑·표만 제거 · 재참여 허용 · **병합 핑은 그 몫만** | `kickParticipant` (`lib/store.ts:1142`) |
| 모임 시간 변경 — **과거 거부** | `setMeetTime` (`lib/store.ts:1012`) |
| `지역까지` → `지점도 정하기` 승격 (단방향) | `promoteToPlace` · `ResultSection.tsx:129` |
| reopen 사다리 — 확정→투표→후보 한 칸씩 · **표 유지** | `reopenStep` (`lib/store.ts:1450`) |
| 늦은 표·핑은 서버 거부 + 토스트 | `castVote`/`addRegionCandidate` 거부 + 자동 `load()` |
| 외부 API 전부 mock 폴백 · 실 성공값만 캐시 · 근사면 `real:false` | `lib/kakao·odsay·tmap·routing` |

1차 순서도가 정한 규칙 중 **대부분은 이미 코드에 있다.** 아래가 나머지다.

---

## 2. 1차 ↔ 현재 코드 — **다른 것**

### A. 미배정 핑 이관 — 순서도에 있고 코드에 없다 ❌

4번 순서도 하단에 `미배정 핑 이관` 상자가, 5번에는 홈→생성 화살표가 있다.
v19 §4-① 의 뜻은 "홈에서 찍은 출발지들을 모임으로 넘기고, **참여자가 들어와
자기 것을 고른다**".

- 코드: `app/page.tsx:1235` 에 **미구현임을 적어 둔 주석**만 있다.
  `startVote` 는 이관된 핑을 지우는 쪽(`lib/store.ts:1422`)만 이미 준비돼 있다.
- 지금 홈에서 "이 출발지들로 모임 만들기" 를 누르면 **방장 출발지 하나만** 넘어간다.
- 2차는 이걸 **`seedPings`(나머지를 그냥 지역 후보로)** 로 바꿨다 → §3-①

### B. '카카오톡 알리기' 가 결과 화면에만 있다 ⚠️

4번 순서도 상시 규칙: **"이벤트마다 '카카오톡 알리기'(상시 버튼 · 앱 설치자는 푸시)"**.

- 코드: `sections/ResultSection.tsx:302` **결과 화면 한 곳뿐**이다.
- 순서도대로면 지역 확정·지점 확정·투표 시작 같은 **이벤트마다** 눌러 알릴 수 있어야 한다.
- 발표에서 "카톡으로 굴러가는 앱"을 보여주는 장면이라 눈에 띄는 차이다.

### C. 동점 → **재투표** 갈래가 없다 ⚠️

4번 순서도: `동점?` → **`방장 — 재량 선택` 또는 `재투표`** 두 갈래.

- 코드: 재량 확정(`confirmManual`)만 있다. `재투표` 라는 말은 `lib/types.ts:263`
  주석("재투표는 덮어쓰기")에만 나오는데, 그건 **개인이 표를 바꾸는 것**이지
  방장이 판을 다시 여는 게 아니다.
- `reopenStep` 이 비슷하지만 **표를 유지**한다. 재투표는 **표만 지우고 후보는 유지**다 — 다른 동작이다.
- 2차가 이 둘을 명시적으로 분리해 놨다 → §3-③

### D. 자동 추천이 핑 자리를 먼저 차지한다 ⚠️ (설계와 코드가 부딪히는 곳)

4번 순서도의 지역 단계는 **`지도에 핑`** 으로 시작하고, AI 는 **점선(=선택)** 이다.
그래야 `후보 수? → 0개 = 시작 불가` 분기가 의미를 갖는다.

- 코드: `app/api/meeting/route.ts:333` `case "regions"` 가 출발지가 하나라도 있으면
  `recommendRegions` 로 **후보 3곳을 항상 채운다.**
- 결과: 아무도 핑을 안 찍어도 후보가 3개다 → **"0개 = 시작 불가" 분기가 실행되지 않는다.**
- 이건 버그가 아니라 **미해결 설계 충돌**이다. 1차·2차 둘 다 "사람이 먼저"인데
  코드만 "AI가 먼저"다.

### E. 반경 700m 를 **지도에 안 그린다** (사소)

4번: `반경 700m 표시`. 코드는 숫자·문구로만 알린다. `KakaoMap.tsx` 에 `Circle` 이 없다.
2차는 점선 원(`kakao.maps.Circle`)을 그린다. 작업량 작고 화면 효과는 크다.

### F. 모임 탭 필터 (사소)

4번/v19 는 "단계4 + 역할2 + 지난1 = **7종**", 코드는 `전체` 를 더해 **8칩**
(`app/meetings/page.tsx:22~29`). 실질 같음 — 2차는 두 줄 구성을 제안 → §3-④

---

## 3. 1차 ↔ 2차가 **서로 다른** 곳 (여기가 진짜 결정거리)

코드를 어느 쪽에 맞출지 정해야 하는 항목이다. 전체 13건은
[`설계_v20_2차그릴링_대조.md`](설계_v20_2차그릴링_대조.md) §2 에 있고, 여기엔
**§2 의 미구현 항목과 직접 얽힌 4건**만 뽑았다.

| | 1차(v19) | 2차 | 지금 코드 | 코드 관점 |
|---|---|---|---|---|
| ① **홈 → 생성 인계** | 미배정 핑 → 참여자가 고름 | 첫 출발지=방장, 나머지=**지역 후보 씨앗** | 방장 것만 | 2차가 **훨씬 싸다.** 고르는 화면이 아예 없어진다 |
| ② **AI 결과 자리** | 후보에 바로 | **팔레트에 담고 사람이 올림** | 바로 (자동까지) | 2차를 따르면 §2-D 충돌이 **저절로 풀린다** |
| ③ **재투표** | 동점 팝업의 한 갈래 | **되돌리기와 분리** (표만 삭제) | 없음 | 서버 액션 `revote` 하나면 된다 |
| ④ **후보 상한** | 지점 상한 없음 | **지역·지점 모두 5개** | 상한 없음 | 상수 + `n/5` 표시 |

> ①②③ 은 서로 붙어 있다. **②(AI 팔레트)를 채택하면** 지역 후보가 사람 핑만으로
> 차므로 `candidateGate` 의 `0개 = 시작 불가` 가 살아나고, 그 자리를 ①(seedPings)이
> 채워 준다. 셋을 따로 결정하면 앞뒤가 안 맞을 수 있다.

---

## 4. 수정 후보 목록 (작업량 순 · 권고 순위 아님)

### 반나절 — 값·문구만

1. **④ 후보 상한 5개 + `n/5` 표시** — 상수 하나, 등록 버튼 문구
2. **E 반경 점선 원** — `KakaoMap.tsx` 에 `kakao.maps.Circle` 한 덩어리
3. **F 필터 두 줄** · 카테고리 라벨 — 화면만

### 하루 — 규칙이 바뀜

4. **B 카카오톡 알리기 상시화** — 결과 전용 코드를 공통 부품으로 올리고 단계별 문구
5. **③ 재투표 분리** — `revote` 액션(표만 삭제·후보 유지) + 동점 팝업 두 갈래
6. **① 홈 → seedPings 인계** — 홈 출발지들을 생성 시 지역 후보로. `app/page.tsx` 주석 자리를 채운다

### 하루+ — 앞뒤가 같이 움직임 ⚠️

7. **② AI 팔레트 + D 자동 채움 해제** — `case "regions"` 가 후보를 직접 쓰지 않게 하고,
   AI 결과는 팔레트에 담는다. **`0개 = 시작 불가` 화면을 처음으로 실제로 타게 되므로
   빈 상태 화면·안내 문구가 같이 필요하다.** 6번(seedPings)과 함께 해야 빈 화면이 안 나온다

### 발표 후로 미루기를 권함

8. 이동수단 4종(타입·DB) · 바텀시트 · 결과 시트화 · 참가자 색 팔레트 충돌
   → `설계_v20_2차그릴링_대조.md` §3 마지막 두 묶음

---

## 5. 결정하고 나면

1. §4 에서 채택 항목을 고른다
2. `설계_v19.md` §13 에 **v20 행**을 추가하고 본문을 고친다
3. `CLAUDE.md` 의 기준 문서 링크를 v20 으로 바꾼다
4. 고칠 때마다 `CHANGELOG.md` 에 행을 추가하고 `npm run verify` + `check:screens` + `check:devices`

> ⚠️ 지금 상태는 **Playwright 55 통과 · 화면 11/11 · 4기기 17/17** 이다.
> §4 의 7번(AI 자동 채움 해제)은 이 세 검증을 전부 다시 돌려야 한다 —
> "후보 0개" 상태가 처음 생기므로 지금 통과하는 시나리오가 깨질 수 있다.
