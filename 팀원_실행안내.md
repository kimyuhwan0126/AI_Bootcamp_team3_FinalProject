# 모이머 — 팀원 실행 안내

작업 저장소: `https://github.com/kimyuhwan0126/AI_Bootcamp_team3_FinalProject`

> 이 문서는 **키·DB 붙이기**만 다룹니다.
> 브랜치·소유권·PR 규칙은 [`docs/팀_개발환경.md`](docs/팀_개발환경.md) 를 먼저 읽으세요.

---

## 1. 실행 방법

**Node.js 22** 가 필요합니다. `.nvmrc` 에 고정돼 있어 CI 도 같은 파일을 읽습니다.

```bash
nvm use         # .nvmrc 를 읽어 맞춰 줍니다 (nvm 을 안 쓰면 Node 22 설치)
npm install     # 처음 한 번만, 2~5분 소요
npm run dev
```

→ 브라우저에서 http://localhost:3000

**키가 하나도 없어도 전부 동작합니다.** 외부 API는 mock으로, DB는 인메모리로
자동 폴백하도록 설계되어 있어 화면·플로우 확인에는 지장이 없습니다.

> 단, 인메모리는 **서버를 재시작하면 만든 모임이 사라집니다.**
> 여러 명이 같은 모임에 들어가 투표까지 확인하려면 아래 2번(Supabase)을 설정하세요.

---

## 2. Supabase 연결 (모임 데이터 영구 저장)

### 2-1. 스키마 만들기 (한 번만)

1. Supabase 프로젝트 → 왼쪽 메뉴 **SQL Editor** → **New query**
2. 저장소의 `supabase/schema.sql` 내용을 **전체 복사해 붙여넣고 Run**
3. `meetings` / `participants` / `votes` 세 테이블이 생기면 완료
   (여러 번 실행해도 안전하게 짜여 있습니다)

### 2-2. 키 넣기

Supabase 프로젝트 → **Settings → API** 에서 복사합니다.

```bash
cp .env.example .env.local
```

```
NEXT_PUBLIC_SUPABASE_URL=https://<프로젝트ID>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon public 키>
SUPABASE_SERVICE_ROLE_KEY=<service_role 키>
```

| 키 | 성격 | 주의 |
|---|---|---|
| URL | 공개돼도 되는 값 | — |
| `anon` | 공개돼도 되는 값 | 이것만 있으면 RLS에 막혀 동작하지 않음 |
| `service_role` | **마스터 키 (RLS 우회)** | 절대 커밋·공유 금지. 서버에서만 사용 |

> 브라우저는 DB에 직접 접근하지 않습니다 — 모든 접근이 `/api/*` 서버를 거치므로
> RLS를 켜 둔 채 서버가 `service_role` 로 우회하는 구조입니다.
> `service_role` 없이 `anon` 키만 쓰려면 `schema.sql` 의 RLS를 `disable` 로 바꿔야 하지만,
> 그러면 초대 코드만 알면 누구나 DB를 직접 읽을 수 있어 권하지 않습니다.

### 2-3. 확인

```
http://localhost:3000/api/status
```

`"db": { "configured": true, "keyKind": "service_role", "ready": true }` 가 나오면 성공입니다.
`ready: false` 면 `/api/diag` 를 열어보세요 — 어떤 테이블이 왜 안 읽히는지 알려줍니다.

---

## 3. 외부 API 연결 (선택)

`.env.local` 에 값을 채웁니다. 키는 팀장(유환)에게 요청하세요.

| 변수 | 용도 | 없으면 |
|---|---|---|
| `NEXT_PUBLIC_KAKAO_JS_KEY` | 브라우저 카카오맵 표시 | 스키매틱(도식) 지도로 폴백 |
| `KAKAO_REST_API_KEY` | 출발지 검색 · 가게/주차장/역 검색 · 로그인 | HUBS mock 목록 |
| `KAKAO_REDIRECT_URI` | 로그인 콜백 (기본값 있음) | 기본값 사용 |
| `ODSAY_API_KEY` | 대중교통 실 이동시간 · 실 경로선 | 직선거리 추정 |
| `TMAP_APP_KEY` | 자차 실 이동시간 · 통행료 · 실 경로선 | 직선거리 추정 |

> ⚠️ `.env.local` 은 **절대 커밋하지 마세요.** `.gitignore` 에 이미 막혀 있습니다.
> ⚠️ **ODsay 서버 키는 호출하는 서버의 공인 IP를 화이트리스트에 등록해야 통합니다.**
> 현재 IP는 `/api/diag` 응답의 `callerIP` 로 확인하세요. 네트워크가 바뀌면 재등록해야 합니다.
> ⚠️ 카카오 JS 키는 developers.kakao.com → 플랫폼 > Web 에 도메인을 등록해야 지도가 뜹니다.

**연결 상태 한눈에 보기**
- `/api/status` — 키 설정 여부 + DB 접속 (항상 사용 가능)
- `/api-live` — 외부 API를 **실제로 호출한** 결과 (`npm run dev` 개발 모드 전용.
  운영 빌드에서는 막혀 있고, 꼭 봐야 하면 `ENABLE_DEBUG=1` 을 넣어야 합니다)

---

## 4. 화면 구성

하단 5탭 구조입니다.

| 탭 | 경로 | 내용 |
|---|---|---|
| 홈 | `/` | 출발지 검색·추가(최대 8) → 중간지점 추천 → 주변 장소 · 확정 요약 |
| 모임 | `/meetings` | 모임 생성/참여, 생성 완료 요약 + 초대 링크, 진행 중·지난 모임 |
| 투표함 | `/votes` | 거점 투표 → 가게 투표 (확정되면 마감 표시) |
| 모임원 | `/members` | 참여자 목록 + 도착 신호등 자가신고(본인만) |
| 내정보 | `/me` | 로그인, 저장 위치, 기본 이동수단 |

모임 상세는 `/m/[코드]` — 출발지 등록 → 거점 투표 → 가게 투표 → 최종 확정.
상단 단계 아이콘을 눌러 **지난 단계를 조회**할 수 있고, 상단 스위처로
**다른 참가자 시점으로 전환**해 방장/일반 참가자 화면 차이를 확인할 수 있습니다(테스트용).

---

## 5. 코드 구조

`README.md` 의 §구조 를 보세요. 요점만:

- `app/api/meeting` 이 유일한 모임 API입니다 — `GET`은 상태 폴링(1.8초), `POST`는 액션 14종
  (`create` `join` `origin` `regions` `vote` `meetTime` `status` `confirmManual`
  `reopen` `backToMain` `reserve` / AI 전용 `openChat` `chat` `aiDecide`)
- `lib/store.ts` 가 데이터 계층입니다. **모든 함수가 async** 이므로 `await` 를 빠뜨리지 마세요
  (안 붙이면 타입 오류 없이 조용히 동작만 안 합니다 — 실제로 겪은 버그입니다)
- 화면은 `lib/store.ts` 를 직접 부르지 않습니다. 항상 API 라우트를 경유합니다

---

## 6. 작업 시 주의할 점

**색상 규칙 (중요)**
- 참가자 구분색은 `app/components/KakaoMap.tsx` 의 `PIN_COLORS` **하나로만** 관리합니다.
  칩과 지도 핀이 항상 같은 색이어야 하므로 이 배열만 고치면 됩니다.
- 도착 상태가 **신호등(초록/노랑/빨강)** 을 쓰므로 `PIN_COLORS` 에는 초록·노랑·빨강 계열을
  넣지 마세요 (청록~파랑~보라~마젠타만). 신호등 색은 `lib/geo.ts` 의 `ARRIVAL_COLOR`.
- 중간지점은 빨간 **물방울 핀**이라 참가자(원형)와 모양으로 구분됩니다.

**CSS 규칙**
- Tailwind를 쓰지 않습니다. `app/globals.css` 의 CSS 변수(`:root`)와 `v8-*` 클래스를 씁니다.
- 새 컴포넌트 클래스도 `v8-` 접두사를 붙입니다.

**API 폴백 원칙**
- 모든 외부 API는 키가 없거나 실패하면 mock으로 떨어집니다. 새 연동도 이 패턴을 지켜주세요
  (팀원이 키 없이도 개발할 수 있어야 합니다).
- **성공한 실 API 응답만 캐시합니다.** 폴백을 캐시하면 키를 넣어도 계속 mock이 나옵니다.

**기능 플래그**
- 만드는 중인 기능은 플래그 뒤에 둡니다 (`lib/flags.ts`). 절반만 된 상태로 합쳐져도
  남의 화면이 안 깨집니다.
- **상수를 직접 고치지 마세요.** `.env.local` 의 `NEXT_PUBLIC_FF_*` 로 켭니다 —
  코드를 고치면 브랜치마다 값이 달라져 머지할 때마다 그 줄에서 충돌납니다.

**한 파일 400줄**
- 넘으면 쪼갭니다. 큰 파일에서는 AI 가 엉뚱한 곳을 고치고, 두 사람이 동시에 만지면
  매일 충돌납니다.

**PR 전 확인**
```bash
npm run verify      # tsc --noEmit + build + 브라우저 스모크
```
⚠️ **빌드 통과 ≠ 화면 렌더.** 타입검사·빌드를 둘 다 통과하고도 화면이 통째로 안
그려진 적이 있습니다(훅을 조건부 `return` 뒤에 배치). 그래서 스모크가 실제 브라우저로
핵심 경로를 클릭합니다. 같은 검사를 CI 도 PR 마다 돌립니다.

그리고 `CHANGELOG.md` 에 무엇을·왜 바꿨는지 한 줄 추가합니다.

---

## 7. 아직 안 된 것 (작업 후보)

- [ ] 모임 비밀번호가 **평문**입니다 — 해시 처리 필요 (`supabase/schema.sql` TODO(BE))
- [ ] Vercel 배포 — 서버리스에서 `void runAiTurn()` 이 응답 후 중단될 수 있어
      AI를 되살릴 때는 `waitUntil()` 로 감싸거나 큐가 필요합니다
- [ ] 구글 캘린더 **가져오기**(내 일정 읽기) — 후순위로 분류됨. 현재는 내보내기만 동작
- [ ] 실시간 반영을 폴링(1.8초) 대신 Supabase Realtime 으로 전환
- [ ] AI 파실리테이터 재활성화 — `.env.local` 에 `NEXT_PUBLIC_FF_AI_CHAT=1` 로 켜지지만 UI 재설계 필요
- [ ] **추천 알고리즘** — 현재 점수식이 `최대 이동시간 + 편차×0.8` 하나뿐입니다.
      구조는 준비돼 있으니(`lib/scoring/`) 관점을 파일로 추가하면 됩니다
- [ ] 알림(헤더 벨)이 아직 목업 데이터입니다
