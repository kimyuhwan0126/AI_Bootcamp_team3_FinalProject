# 모이머 v7 — 팀원 실행 안내

이 폴더는 `feat/v7-mockup` 브랜치의 스냅샷입니다. (참고용)
실제 작업은 GitHub 브랜치에서 진행해주세요 → `https://github.com/kimyuhwan0126/Moimer` (`feat/v7-mockup`)

---

## 1. 실행 방법

Node.js 18 이상이 필요합니다 (`node -v`로 확인).

```bash
npm install     # 처음 한 번만, 2~5분 소요
npm run dev
```

→ 브라우저에서 http://localhost:3000

**API 키가 없어도 전부 동작합니다.** 키가 없으면 자동으로 mock 데이터로 폴백하도록
설계되어 있어서, 화면·플로우 확인에는 지장이 없습니다.

---

## 2. 실제 API를 붙이려면 (선택)

`.env.local` 파일은 보안상 저장소/이 zip에 **포함되지 않습니다.** 직접 만들어야 합니다.

```bash
cp .env.example .env.local
```

그리고 값을 채웁니다. 키는 팀장(유환)에게 요청하세요.

| 변수 | 용도 | 없으면 |
|---|---|---|
| `NEXT_PUBLIC_KAKAO_JS_KEY` | 브라우저 카카오맵 표시 | 지도 자리에 안내 문구 |
| `KAKAO_REST_API_KEY` | 카카오 로그인 · 주소검색 · 장소검색 | mock 장소 데이터 |
| `KAKAO_REDIRECT_URI` | 로그인 콜백 (기본값 있음) | 기본값 사용 |
| `ODSAY_API_KEY` | 대중교통 실 이동시간 | 거리 기반 추정 |
| `TMAP_APP_KEY` | 자차 실 이동시간 | 거리 기반 추정 |

> ⚠️ `.env.local`은 **절대 커밋하지 마세요.** `.gitignore`에 이미 막혀 있습니다.
> ⚠️ ODsay 서버 키는 **호출 IP 화이트리스트 등록**이 필요합니다. 네트워크가 바뀌면 재등록하세요.

**키가 잘 붙었는지 확인:** http://localhost:3000/api-live — 각 API 연결 상태를 보여줍니다.

---

## 3. 화면 구성

하단 5탭 구조입니다.

| 탭 | 경로 | 내용 |
|---|---|---|
| 홈 | `/` | 출발지 검색·추가(최대 8) → 중간지점 추천 → 주변 장소 |
| 모임 | `/meetings` | 모임 생성/참여, 진행 중·결정된 모임 목록 |
| 투표함 | `/votes` | 지역 투표 → 가게 투표 → 최종 확정 |
| 모임원 | `/members` | 참여자 목록 |
| 내정보 | `/me` | 로그인, 저장 위치, 기본 이동수단 |

모임 상세 화면은 `/m/[코드]` 입니다 (AI 대화 기반 v3 화면).

---

## 4. 코드 구조

```
app/
  page.tsx                  홈 (출발지·중간지점·주변 리스트)
  meetings|votes|members|me 하단 탭 화면들
  m/[code]/                 모임 상세
  api/
    geocode/                주소 → 좌표 (카카오, 키 없으면 HUBS mock)
    places/                 주변 장소 (카페/음식점/술집/주차장/정류장·역)
    midpoint/               중간지점 (거리순 | 시간순)
    auth/kakao/             카카오 로그인 + 콜백
  components/
    KakaoMap.tsx            실제 카카오 지도 + 핀 (PIN_COLORS 팔레트)
    v7/                     헤더·하단탭·스플래시·로그인시트
lib/
  session.ts                로그인 3단계 (비회원/임시/카카오)
  geo.ts                    거리 기반 계산 + mock 데이터
  routing.ts                실 이동시간 계산 (ODsay/TMAP + 캐시)
  kakao.ts / odsay.ts / tmap.ts   외부 API 래퍼
  types.ts                  공용 타입
```

---

## 5. 작업 시 주의할 점

**색상 규칙 (중요)**
- 출발지 구분색은 `KakaoMap.tsx`의 `PIN_COLORS` 하나로 관리합니다.
  칩과 지도 핀이 항상 같은 색이 되도록 이 배열만 고치면 됩니다.
- 모임원 도착 상태가 **신호등(초록/노랑/빨강)**을 쓰기 때문에,
  `PIN_COLORS`에는 초록·노랑·빨강 계열을 넣지 마세요. (청록~파랑~보라~마젠타만 사용)
- 중간지점은 빨간 **물방울 핀**이라 참가자(원형)와 모양으로 구분됩니다.

**API 폴백 원칙**
- 모든 외부 API는 키가 없거나 실패하면 mock으로 떨어지게 되어 있습니다.
  새 연동을 추가할 때도 이 패턴을 지켜주세요 (팀원이 키 없이도 개발 가능해야 함).

**커밋 전 확인**
```bash
npx tsc --noEmit    # 타입 오류 없어야 함
npm run build       # 빌드 통과해야 함
```

---

## 6. 아직 안 된 것 (작업 후보)

- 로그인 후 홈에서 내 모임 멤버를 불러와 신호등 상태로 표시하기
- 모임원 화면의 도착 상태 편집(본인만) — 목업만 있고 저장 미연동
- 투표 결과 서버 저장 (현재 화면 내 상태만 유지)
- 구글 캘린더 연동 (후순위로 분류됨)
