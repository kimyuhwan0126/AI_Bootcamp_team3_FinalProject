/* 화면에 나가는 문구 중 **두 곳 이상에서 쓰이는 것**만 여기 둔다.
   같은 말을 파일마다 적어 두면 한쪽만 고치고 잊는다 — 논의72('표'→'선택')를 넣을 때
   실제로 ui.tsx 와 osmmap.tsx 에 똑같은 줄이 둘로 갈라져 있었다.

   한 곳에서만 쓰는 문구까지 여기로 모으지는 마라. 쓰는 자리에서 멀어지면
   무엇을 고치는지 안 보인다. */

/** 몇 명이 골랐는지 (논의72 — '표'·'투표'라는 말은 쓰지 않는다) */
export const 고른수 = (n: number) => `${n}명이 골랐어요`;

/** 아직 아무것도 안 고른 상태. 세 가지로 흩어져 있던 것을 하나로 (논의72) */
export const 아직 = '아직 안 골랐어요';
