/* 기능 스위치 — 잠시 끄고 켜야 할 것만 한곳에 모은다. 서버·클라이언트 양쪽에서
   같은 값을 보게 하려고 여기 하나만 둔다(두 군데 따로 끄면 한쪽만 잊고 안 끈 채로 남는다).

   AI 추천: 2026-08-15, 쓰던 ollama 계정의 유료 구독 기간이 끝나 모든 호출이 403 으로
   막힌다. 새 API 키를 받을 때까지 버튼 자체를 숨긴다 — 눌러도 안 되는 버튼을 보여주는
   것은 거짓말이다(논의105 와 같은 잣대). 새 키를 넣으면 이 값만 false 로 돌리면 된다. */
export const AI추천_잠시멈춤 = true;
