# 모이머(Moimer) — 변경 기록

> 이 파일의 파일을 바꾼 경우 아래에 **반드시** 기입한다.
> 형식: `날짜 | 작업자 | 대상 파일 | 변경 내용 | 사유`
> 기록 없는 변경 금지.

---

## v0.1.0 — 2026-07-30 · 새 저장소 시작

### 통합 개발 저장소로 이관 (`AI_Bootcamp_team3_FinalProject`)

팀원 4명이 각자 브랜치로 동시에 개발하고 통합 담당자가 머지를 전담하는 체제로
전환하면서 저장소를 새로 팠다. **코드는 옛 저장소의 `v8.1.0` 과 동일**하고,
히스토리도 그대로 옮겼다(31커밋).

> ⚠️ **버전 번호가 내려간 게 아니라 체계가 바뀐 것이다.**
> 옛 `v8` = "8번째로 다시 만든 시제품"(내부 반복 횟수)
> 새 `0.1.0` = "아직 정식 출시 전인 개발 버전"(SemVer 표준 의미)
> **최종 발표일에 `1.0.0`** 을 낸다. 규칙은 `docs/버전관리.md`.

| 날짜 | 작업자 | 대상 파일/폴더 | 변경 내용 | 사유 |
|---|---|---|---|---|
| 2026-08-09 | Claude(AI) | `docs/설계도/` (신규 5개) · `docs/설계_v19.md`(신규) | **그릴링 산출물을 레포에 고정** — 순서도 HTML 5종(전체화면 확정판 · 전체화면 UI/UX · 프로세스 UI/UX · 프로세스 분산 · 기능)을 `docs/설계도/` 에 보존하고, 규칙을 `docs/설계_v19.md` 로 텍스트화(화면 11대 · 상태기계 · 확정 범위 분기 · 단계 규칙 · reopen 사다리 · 역할 권한 · AI 정책 · 팝업 전수 · v2~v19 결정 이력 21건 · 폐기 목록) | 멘토 지적("세부 기초 설계 부족")에 따라 그릴링으로 뽑은 v19 스펙이 **업로드 HTML 에만 있어 세션이 끝나면 사라지는 상태**였다. 레포 코드는 그릴링 이전(`85ba6d2`)이라 폐기 결정된 것들(`password`·`Reservation`·`/votes`·`/members`)이 그대로 살아 있다. 구현 전에 기준을 코드 옆에 고정해야 다음 세션·팀원이 같은 것을 본다 |
| 2026-08-10 | Claude(AI) | `lib/store.ts` 🔒 (`setRegionCandidates`) · `app/api/meeting/route.ts` 🔒 (`regions`) · `tests/v19-flow.spec.ts` | **결과 화면에서 늦게 합류한 사람이 '이동시간 없음' 으로 남던 버그 수정** — 후보 재계산 시 ①후보 **이름 목록**이 바뀐 것과 ②**지표만 갱신**된 것을 분리해, 이름이 같아도 이동시간은 저장하도록 했다(표는 그대로 둔다). 사람이 찍은 핑(`rc_*`)·AI 후보(`ra_*`)의 지표도 함께 갱신한다. 회귀 테스트 1건 추가 | 예전엔 **이름만** 비교해 `prev===next` 면 조기 반환했다. 2명이 후보를 잡은 뒤 3·4번째가 들어오면 추천 동네 이름은 그대로라 **새로 계산한 이동시간을 통째로 버렸고**, 그 두 명은 `perParticipant` 에 영영 없다. 확정 후에는 재계산을 안 하므로 결과 화면까지 '이동시간 없음' 으로 간다 — **발표에서 팀원이 한 명씩 들어오는 순서 그대로 재현**된다(2026-08-10 브라우저 실측) |
| 2026-08-10 | Claude(AI) | `lib/store.ts` 🔒 (`setRegionCandidates`) · `tests/v19-flow.spec.ts` | **투표 중 후보 잠금(v19 §5)을 재계산 경로에도 적용** — `phaseStepOf(m) !== "region-register"` 면 후보를 건드리지 않는다. 회귀 테스트 1건 추가 | 잠금이 `addRegionCandidate` 에만 걸려 있고 자동 재계산은 뚫려 있었다. **투표 중에 누가 출발지를 고치면** 후보가 갈리며 `clearVotes` 로 **전원의 표가 사라졌다.** 발표 중 "사당이 아니라 사당역이었어요" 한 번이면 투표가 초기화된다 |
| 2026-08-10 | Claude(AI) | `lib/kakao.ts` (`redirectUriFor`·`originOfRequest` 신규) · `app/api/auth/kakao/route.ts` · `app/api/auth/kakao/callback/route.ts` · `app/api/status/route.ts` · `docs/발표_로컬시연.md` §3-2·§9 · `tests/v19-flow.spec.ts` | **LAN 시연에서 팀원 폰 카카오 로그인이 안 되던 문제 수정** — `redirect_uri` 를 `.env.local` 고정값이 아니라 **들어온 Host 기준**으로 만든다(인가·토큰교환 양쪽 동일). `/api/status` 와 `?debug=1` 이 "지금 등록해야 할 URI"를 알려준다. 런북에 확인 절차 추가. 테스트 1건 추가 | 팀원 폰이 `http://10.40.2.177:3000` 으로 들어와 로그인하면 카카오가 **그 폰 자신의 `localhost:3000`** 으로 돌려보내 로그인이 끝나지 않았다(2026-08-10 실측). `new URL(req.url).origin` 은 Next 가 호스트를 정규화해(`127.0.0.1`→`localhost`) 글자 단위 비교를 깨뜨리므로 **Host 헤더**를 쓴다 |
| 2026-08-10 | Claude(AI) | `app/page.tsx` · `app/m/[code]/sections/DebugWidget.tsx` · `tests/v19-flow.spec.ts` | **홈 CTA 버튼이 폰 모양 껍데기 밖으로 튀어나오던 것 수정** — `left:16; right:16` → 가운데 정렬 + `max-width:408`(껍데기 440 - 좌우 16). 디버그 위젯의 `right:16` 도 같은 방식으로 껍데기 안쪽에 붙인다. 넓은 화면(1280px)에서 버튼이 껍데기 안에 있는지 검사하는 회귀 테스트 추가 | `position:fixed` 는 껍데기(`.device`, 440px)가 아니라 **뷰포트** 기준이라, 노트북·태블릿에서 버튼만 화면 전체 폭으로 늘어났다(2026-08-10 팀원 제보 스크린샷) |
| 2026-08-10 | Claude(AI) | `app/globals.css` 🔒 | **`color-scheme: light dark` 선언** — 다크 팔레트를 우리가 직접 정의한다고 브라우저에 알린다 | 없으면 안드로이드 크롬의 '웹 콘텐츠 자동 다크 모드'가 우리 다크 색 위에 **자기 반전을 한 번 더** 덧칠해 색이 뭉개진다. "폰 색이 노트북과 다르다" 제보의 원인 절반(나머지 절반은 정상 — 폰이 다크 모드면 다크로 보인다) |
| 2026-08-10 | Claude(AI) | `app/globals.css` 🔒 (`.v8-cats`·`.v8-cat`) | **홈 카테고리 칩을 줄바꿈 대신 가로 스크롤로** — `white-space:nowrap` + `overflow-x:auto`(스크롤바 숨김) | 390px 폰에서 칩 6개가 한 줄에 안 들어가 글자가 칩 안에서 두 줄로 접혔다("정류장·/역"). 칩 높이가 들쭉날쭉해 깨져 보인다 |
| 2026-08-10 | Claude(AI) | `app/globals.css` 🔒 · `app/layout.tsx` · `tests/v19-flow.spec.ts` | **라이트 테마 고정** (팀 결정) — `:root` 를 `color-scheme: light` 로, 다크 팔레트는 `@media (prefers-color-scheme:dark)` → `:root[data-theme="dark"]` 로 옮겨 **지우지 않고 잠갔다**. `layout.tsx` 에 `colorScheme: "light"` 메타도 함께 선언. 기기가 다크여도 라이트로 나오는지 보는 테스트 추가 | 발표에서 노트북·팀원 폰 여러 대를 나란히 띄우는데 기기마다 다크/라이트가 갈려 **같은 화면이 서로 다른 색**으로 보였다 — 비교하며 보는 자리라 그 차이가 버그처럼 읽힌다. 다크 코드는 살려 둬서 발표 후 `data-theme` 하나로 되살릴 수 있다 |
| 2026-08-10 | Claude(AI) | `app/health/page.tsx`(신규) · `docs/발표_로컬시연.md` §8 · `tests/v19-flow.spec.ts` | **폰에서 여는 연결 점검 화면 `/health` 추가** — ①이 기기가 붙은 주소(방장이 localhost 면 경고) ②Neon/인메모리·키·AI ③**이 기기가 등록해야 할** 카카오 Redirect URI(복사 버튼) ④**기기 간 연동** — 첫 기기가 코드를 만들고 링크를 뿌리면 참여한 기기들이 1.8초 폴링으로 서로의 목록에 뜬다. 두 기기가 서로를 보는지 검사하는 테스트 추가 | `npm run check:live` 는 **노트북에서** 도는데, 발표에서 막히는 건 대부분 **폰 쪽**이다(다른 Wi-Fi·방화벽·IP 변경·카카오 미등록). 폰에서 열어 볼 화면이 하나도 없었다. ⚠️ 실제 모임 API 를 그대로 쓴다(만들기·참여·조회만 — 외부 API 0콜) — 별도 경로를 만들면 "점검은 되는데 본 화면은 안 되는" 상황이 생긴다 |
| 2026-08-10 | Claude(AI) | `scripts/dev-lan.mjs`(신규) · `package.json` 🔒 | **`npm run dev:lan` 이 실제 IP 주소를 찍는다** — 서버를 띄우기 전에 사설망 IPv4 를 골라 `http://<IP>:3000` · `/health` · **카카오 콘솔에 등록할 Redirect URI·사이트 도메인**까지 출력한다. 주소만 볼 때는 `npm run ip` | `next dev -H 0.0.0.0` 은 `Network: http://0.0.0.0:3000` 이라고 찍는다. 0.0.0.0 은 주소가 아니라 "모든 네트워크 카드로 받겠다"는 뜻이라, 그대로 폰에 치면 **폰 자기 자신**을 가리켜 아무것도 안 열린다 — 2026-08-10 실제로 여기서 막혔다 |
| 2026-08-10 | Claude(AI) | `lib/flags.ts` 🔒 · `app/m/[code]/MeetingClient.tsx` 🔒 | **디버그 위젯을 기본 꺼짐으로** — `DEV` 만 보던 조건에 `FLAGS.debugTools`(`NEXT_PUBLIC_FF_DEBUG_TOOLS=1`)를 추가 | 발표는 `npm run dev:lan`(= 개발 모드)으로 도는데 조건이 `NODE_ENV !== "production"` 뿐이라 **시연 화면에 🐞 '빠른 채우기' 버튼이 그대로 떠 있었다.** 코드는 남기고 켜는 사람만 켠다 |
| 2026-08-10 | Claude(AI) | `app/m/[code]/sections/MeetingHeader.tsx` · `app/m/[code]/MeetingClient.tsx` 🔒 · `app/page.tsx` | **시연 화면에서 신원 전환 UI 제거** — 모임 헤더의 '현재 [드롭다운]' 과 홈 투표칸의 '누구로 투표할까요?' 는 **이 기기에 신원이 둘 이상일 때만** 그린다. '+ 참가자'(모임 비밀번호를 쓰는 v2 폐기 경로)는 `FLAGS.debugTools` 전용 | 앞선 커밋에서 🐞 위젯만 껐는데, **한 기기에서 여러 참가자를 오가려고 만든 테스트 장치가 두 군데 더** 남아 있었다. 발표는 한 기기 = 한 사람이라 이게 떠 있으면 시연 화면이 테스트 도구처럼 보인다 |
| 2026-08-10 | Claude(AI) | `app/api/meeting/route.ts` 🔒 (`confirmManual`) · `tests/v19-flow.spec.ts` | **확정 시점에 그 지역의 이동시간을 전원 기준으로 다시 계산** — v19 §4-⑥("경로 API 는 최종 확정 후에만")대로, 확정된 후보 1곳 × 인원수만 계산해 저장한다. 회귀 테스트 추가 | 늦게 합류한 사람이 결과 화면에서 계속 **'이동시간 없음'** 이었다. 등록 단계 자동 재계산으로도 메워지지만, **4인분 경로 계산이 '투표 시작'보다 느려 잠금에 밀린다** — 실측 8초를 넘겨 요청이 끝나기 전에 잠겼다(2026-08-10 브라우저 추적). 확정은 한 번뿐이고 후보도 하나라 여기서 채우는 게 가장 싸고 확실하다 |
| 2026-08-10 | Claude(AI) | `lib/store.ts` 🔒 (`castVote`) · `app/m/[code]/MeetingClient.tsx` 🔒 | **"화면을 새로고침해 주세요" 문구 제거 + 실패 시 자동 재동기화** — 액션이 거부되면 클라이언트가 그 자리에서 `load()` 로 최신 상태를 당겨온다 | 단계가 바뀐 직후 옛 화면에서 누르면 나오는 안내였는데, 이 화면은 1.8초 폴링이라 **스스로 따라잡을 수 있는데도 사람에게 새로고침을 시키고 있었다.** 시연 내내 단계마다 새로고침하게 된다(2026-08-10 제보) |
| 2026-08-10 | Claude(AI) | `app/components/KakaoMap.tsx` (`MapCandidate.preview`) · `app/m/[code]/sections/MapPanel.tsx` · `app/m/[code]/sections/PlacePicker.tsx` · `app/m/[code]/MeetingClient.tsx` 🔒 · `tests/v19-flow.spec.ts` | **지점 미리보기를 지도 회색 핀으로** — v19 §4-⑧ 의 기본 동작("미리보기 핀(회색) 탭 → 후보 등록")을 실제로 구현했다. 회색 핀은 표 뱃지 대신 `+ 후보 등록` 을 달고, 탭하면 투표가 아니라 `addPlace` 로 간다. 지도 SDK 가 못 뜨는 기기에서도 같은 후보를 버튼 목록으로 그린다. 회귀 테스트 1건 | 코드 주석에는 "미리보기 핀 탭"이라고 적혀 있었지만 실제로는 **텍스트 목록만** 있었다. 지도는 이미 등록된 후보만 그려서, 설계의 핵심 상호작용이 화면에 없었다 |
| 2026-08-10 | Claude(AI) | `app/api/place-poi/route.ts` · `lib/kakao.ts` · `app/m/[code]/sections/PlacePicker.tsx` | **지점 목록을 보고 고를 수 있게** — ①행마다 **주소**(도로명)와 카테고리·도보·별점 표시 ②**[보기]** 로 카카오맵 상세(사진·메뉴·리뷰) 확인 후 **[+ 등록]** 으로 분리(행 전체 탭 → 오등록 방지) ③**샘플 목록이면 경고 칩** ④검색어로 못 찾으면 **샘플로 덮지 않고** "‘당구장’을 반경 700m 안에서 찾지 못했어요" + 반경 확장 제안 | "리스트를 저렇게 주면 방장이 뭘 보고 등록하나" · "검색이 안 먹는 느낌" (2026-08-10 제보). 실제로 검색어로 0건이면 **그 카테고리 mock 으로 폴백**해서 검색 전과 같은 목록이 남았고, 이름 말고는 판단 근거가 없었다. mock 이름(`○○ 곱창집`)이 진짜처럼 보이는 것도 `CLAUDE.md` §3-6 위반이다 |
| 2026-08-10 | Claude(AI) | `app/page.tsx` | **홈이 모임을 보여줄 때는 '이 출발지들로 모임 만들기' 를 숨긴다** | 모임 모드(홈 안 투표 칸)에서도 늘 떠 있어 화면 아래를 가리고, "보고 있는 모임"과 "새 모임 만들기"가 한 화면에서 경합했다. v19 §4-① 은 이 버튼을 **맛보기 상태의 전환 고리**로 정의한다 |
| 2026-08-10 | Claude(AI) | `app/api/place-poi/route.ts` · `app/m/[code]/sections/PlacePicker.tsx` · `tests/v19-flow.spec.ts` | **mock 은 '키가 없을 때'만** — 키가 있는데 반경 안에 0건이면 **진짜 0건**으로 내려보내, v19 §4-⑧ 의 반경 확장(700→1.4km·1회·전체 공유) 제안이 뜨게 했다. 안내 문구에 "되돌릴 수 없고 모든 참여자에게 적용"·"AI 추천" 대안을 명시. 응답 계약 테스트 1건 | "700m 안에 없으면 1.4km로 확대하라고 하지 않았나?" (2026-08-10). 확장 로직은 있었는데 **화면이 '0개'를 볼 수가 없었다** — 결과가 0이면 무조건 샘플 6개로 덮었기 때문이다. 그래서 확장 제안이 영영 안 떴고, 동시에 없는 가게가 진짜처럼 보였다 |
| 2026-08-10 | Claude(AI) | `app/globals.css` 🔒 (`.map-fs`·`.map-fsbtn`) · `app/components/KakaoMap.tsx` · `app/m/[code]/sections/MapPanel.tsx` · `app/page.tsx` · `tests/v19-flow.spec.ts` | **지도 전체화면 보기 버튼** — 지도 오른쪽 위 `⛶`. 홈·모임 상세 둘 다. Esc 로 닫힌다. `KakaoMap` 에 ResizeObserver 를 넣어 컨테이너가 커지면 `relayout()` 한다. 회귀 테스트 1건 | v19 §4-⑥⑧ 의 후보 등록·투표가 **지도 위 탭**인데 지도가 340px 카드에 갇혀 있어 핀이 서로 겹쳐 누르기 어려웠다(특히 폰). ⚠️ 카카오 지도는 컨테이너 크기가 바뀌어도 스스로 다시 그리지 않아 relayout 이 없으면 잘린 채 남는다 |
| 2026-08-10 | Claude(AI) | `lib/routing.ts` · `app/m/[code]/MeetingClient.tsx` 🔒 | **이동시간 계산 대기열 폭발 수정** — ①`travelMinutesEx` 에 **동시 진행 중복 제거**(같은 구간이 이미 날아가 있으면 그 약속을 같이 기다린다) ②후보 재계산 요청은 **방장 화면 하나만** 보낸다 | 4대 리허설에서 **확정 시점 재계산이 끝나지 않아** 늦게 온 사람이 결과 화면에서 계속 '이동시간 없음' 이었다. 원인은 `rateGate`(250ms 간격) 큐 — 참여자 기기마다 재계산을 요청하니 같은 (출발지→후보) 쌍이 기기 수만큼 쌓였다. 후보 3 × 인원 4 × 기기 4 = 48건이 250ms 씩 줄서면 12초, 그 뒤 요청은 사실상 끝나지 않는다 |
| 2026-08-10 | Claude(AI) | `scripts/check-devices.mjs`(신규) · `package.json` 🔒 · `docs/발표_로컬시연.md` §8 | **`npm run check:devices` — 4대 리허설 자동화** — 독립 브라우저 4개로 생성→초대 링크 참여→동시 투표→확정까지 화면 조작으로 밟으며 17항목 검사. 단계별·기기별 스크린샷을 `test-results/devices/` 에 남긴다 | 사람 4명을 모으기 전에 기기 간 연동을 확인할 방법이 없었다. 위 대기열 버그도 **이 스크립트로만 재현**됐다 — API 단독 호출로는 큐가 비어 있어 항상 통과했다 |
| 2026-08-10 | Claude(AI) | `lib/store.ts` 🔒 (`removeRegionCandidate`) · `app/api/meeting/route.ts` 🔒 · `app/m/[code]/sections/VoteList.tsx` · `app/m/[code]/MeetingClient.tsx` 🔒 · `tests/v19-flow.spec.ts` | **지역 후보 삭제 기능 추가** (v19 §7) — 등록 단계에서 방장은 임의 후보, 참여자는 **본인이 찍은 후보**를 지운다. 병합 핑이면 **내 몫만** 빠지고 마지막 한 명이면 후보가 사라진다(AI 후보는 유지). 사라진 후보의 표도 함께 정리. 핑 등록 토스트가 **등록된 동 이름**과 되돌리는 법을 알려준다. 회귀 테스트 2건 | v19 §7 이 "후보 삭제 — 방장: 임의 / 참여자: 본인 것" 을 명시하는데 **지점에만 있고 지역에는 없었다.** 지도를 잘못 눌러 생긴 후보를 아무도 못 지웠다 (2026-08-10 제보) |
| 2026-08-10 | Claude(AI) | `app/m/[code]/MeetingClient.tsx` 🔒 · `tests/v19-flow.spec.ts` | **지도 핑에 확인 단계** — 지도를 눌러도 바로 등록하지 않고 "여기로 등록할까요?" 시트를 띄운다. [등록] / [취소]. 시트가 동 스냅·1인 1개 규칙을 함께 설명한다 | 지도를 스크롤·확대하다 손가락이 닿기만 해도 후보가 생겼고 되돌릴 수 없었다 (2026-08-10 제보). 핑은 1인 1개라 개수가 무한히 늘지는 않지만(실측: 5번 눌러도 1개, 자리만 이동), **만들 생각이 없던 후보가 생기는 것** 자체가 문제다 |
| 2026-08-10 | Claude(AI) | `app/health/page.tsx` · `app/m/[code]/sections/VoteList.tsx` · `app/m/[code]/MeetingClient.tsx` 🔒 | **런타임 에러 2건 수정** — ①`/health` 하이드레이션 불일치(React #418·#423·#425): `navigator.userAgent` 를 렌더 중에 읽어 서버/브라우저 HTML 이 달랐다 → 마운트 후 state 로 ②**등록 단계에 절대 안 눌리는 투표 버튼**: 서버가 표를 거부하는데(v12) 목록·지도에 투표 버튼이 그대로라 누를 때마다 400 + "단계가 바뀌었어요" 만 났다 → 등록 단계에서는 투표 UI 를 그리지 않고 무엇을 기다려야 하는지 알려준다 | "런타임 에러도 뜨네?" (2026-08-10). 눌리는 것처럼 보이는데 절대 안 되는 버튼이 가장 나쁜 상태다 |
| 2026-08-10 | Claude(AI) | `scripts/check-screens.mjs`(신규) · `package.json` 🔒 · `docs/발표_로컬시연.md` §8 | **`npm run check:screens` — 전 화면 런타임 에러 훑기** — 홈·모임 탭·생성 폼·내정보·연결 점검·초대 링크·⑥⑦⑧⑨⑩ 을 열어 버튼을 눌러 보며 `pageerror`·`console.error` 를 모은다 | 스모크는 **핵심 경로**만 본다. 실제로 터지는 건 "그 화면에서 이 버튼을 눌렀을 때" 다 — 위 2건도 이 방식으로만 잡혔다 |
| 2026-08-10 | Claude(AI) | `lib/clipboard.ts`(신규) · `app/meetings/page.tsx` · `app/health/page.tsx` · `app/m/[code]/sections/ResultSection.tsx` · `tests/v19-flow.spec.ts` | **복사 기능을 http 에서도 되게** — `copyText()` 3단계 폴백(최신 API → 숨긴 textarea + `execCommand` → 실패 반환). 복사 버튼이 **성공·실패를 화면에 알린다** ("복사됐어요" / "복사가 막혀 있어요 — 길게 눌러 복사하세요"). 클립보드를 제거한 상태의 회귀 테스트 1건 | ⚠️ **`navigator.clipboard` 는 보안 출처(https·localhost)에만 존재한다.** 발표는 팀원 폰이 `http://10.x.x.x:3000` 으로 들어오는 구조라 **그 기기에서는 초대 링크 복사가 조용히 실패**한다 — 링크 공유가 이 앱의 첫 단추다. 윈도우 크롬에서는 `Write permission denied` 로 화면이 죽기까지 했다 (2026-08-10 팀원 노트북 실측) |
| 2026-08-10 | Claude(AI) | `scripts/check-devices.mjs` | **윈도우 경로 버그 수정** — `new URL(...).pathname` → `fileURLToPath()` | 윈도우에서 `pathname` 이 `/C:/…` 를 주는데 그대로 쓰면 `C:\C:\…` 가 돼 `mkdir` 이 실패한다. 팀원 노트북에서 `check:devices` 가 시작조차 못 했다 |
| 2026-08-10 | Claude(AI) | `docs/설계_v20_2차그릴링_대조.md`(신규) | **2차 그릴링 산출물 판독 + v19·현재 코드 대조** — 2차는 순서도가 아니라 **동작하는 프로토타입**(해시 라우터·localStorage 스토어·화면 7개, 33파일). 같은 것 14항목 / 다른 것 13항목을 판정(🔄뒤집힘·➕신규·⚠️충돌)과 함께 표로 정리하고, 팀이 고를 목록을 **작업량 순**으로 묶었다 | 2차 산출물이 `Downloads` 에만 있으면 세션이 끝나면 사라진다(1차와 같은 이유). 그리고 **v19 결정을 뒤집은 것이 5건**이라(후보 상한 5개 · AI 팔레트 · 홈 인계 방식 · 필터 2줄 · 결과 시트화) 무엇이 바뀌었는지 문서 없이는 구현으로 못 옮긴다. ⚠️ 참가자 색 팔레트가 도착 신호등 색과 겹쳐 `CLAUDE.md` 색상 규칙과 **직접 충돌**한다 |
| 2026-08-10 | Claude(AI) | `scripts/check-screens.mjs` · `scripts/check-devices.mjs` | 두 검증 스크립트도 **통합 모델**에 맞췄다 — 지역 '투표 시작'/'⑦ 지역 투표' 화면을 지우고, 4기기 리허설은 "찍은 표가 남의 화면에 뜨는가"를 **핑 집계와 4/4명 표시**로 본다. 확정 버튼 문구도 `로 정하기` 를 함께 찾는다 | 스크립트가 옛 단계를 밟으면 통합 모드에서 곧바로 막힌다. 검증이 제품보다 먼저 낡으면 그때부터 아무것도 못 잡는다 |
| 2026-08-10 | Claude(AI) | `lib/flags.ts`🔒 · `lib/store.ts`🔒 · `lib/roles.ts` · `lib/kakao.ts` · `MeetingClient.tsx`🔒 · `LeaderBar.tsx` · `ParticipantBar.tsx` · `app/meetings/page.tsx` | **핑 등록 = 투표로 통합** (`FLAGS.regionVoteStep` 기본 꺼짐). 지역이 **한 칸**이 된다: 지도를 누르면 그게 한 표고, 많이 찍힌 곳이 1위이며, 방장이 **전원을 기다리지 않고** 확정한다. 집계 출처가 `regionVotes` → **`contributors.length`** 로 바뀌고, 지역엔 '투표 시작'도 '재투표'도 없다(둘 다 서버가 거부하고 화면에도 안 낸다). `reopenStep` 의 지역 사다리도 한 칸 짧아진다 | 멘토링 2026-08-06 §2: "지역 투표 단계를 별도로 두지 않고 **핑 등록 = 투표로 통합** 가능 → 단계 간소화. 많이 찍힌 곳이 자동으로 지역으로 선정. **참여자는 핑 찍지 않아도 되고, 방장이 다음 단계로 강제로 넘어갈 수 있음.**" 같은 사람에게 "여기 찍어" 다음에 "이제 투표해"를 또 시키면 두 번째가 무엇을 위한 절차인지 설명할 수 없었다. ⚠️ 서버 상태기계는 **그대로 뒀다** — `stage:main` 에 머물고 `confirmRegion` 이 main 확정을 이미 허용하므로, 옛 모임 행도 마이그레이션 없이 그대로 열린다 |
| 2026-08-10 | Claude(AI) | `lib/kakao.ts` | **핑을 시·군·구로 묶는다** (`FLAGS.pingSnapDong` 기본 꺼짐 — 켜면 옛 행정동) | 멘토링 §2: "핑 찍으면 **시·군·구 단위로 자동 묶기**(또는 서울은 지하철역 기준)". 동 단위는 너무 잘게 갈려 4명이 찍으면 후보가 4개가 되기 쉽다 — 표가 흩어져 1위가 안 나온다(후보 상한 5개와 같은 이유). ⚠️ '지하철역 기준'은 역 좌표표가 필요해 이번엔 넣지 않았다 — 팀 결정 대기 |
| 2026-08-10 | Claude(AI) | `app/meetings/page.tsx` | 통합 모드에서 **지역 단계(`main`)도 '투표 중'** 으로 센다 — 목록 문구가 '참석자 모집 중' → **'지역 정하는 중'**, 🔔 상단 고정 대상에도 포함 | 핑이 곧 표라 지역 단계는 이미 의사결정이 돌아가는 중이다. 빼 두면 "지금 찍어야 할 모임"이 목록에서 안 올라와, 참여자가 들어갈 이유를 못 찾는다 |
| 2026-08-10 | Claude(AI) | `tests/{smoke,sync,v19-flow}.spec.ts` | 지역 투표를 전제하던 **테스트 20여 개를 통합 모델 계약으로 다시 썼다.** 공통 헬퍼 `startVoteIfAny`(지역의 정상 거부만 넘김)를 두고, 재투표 검사는 **지점 단계**로 옮겼으며, 새 §2 계약 테스트 4개(핑=표 · 1인 1개 이동 · 전원 미참여 확정 · 지역엔 재투표 없음)를 추가했다 | 통합은 **계약이 바뀌는 변경**이라 옛 테스트가 무더기로 깨지는 게 정상이다. 깨진 것을 조용히 지우지 않고 "그 규칙이 새 모델에서 무엇에 해당하는가"로 옮겨 적었다 — 예: '강퇴하면 그 사람 표가 사라진다' → **핑이 사라진다**, '되돌려도 표는 유지' → **핑이 유지** |
| 2026-08-10 | Claude(AI) | `lib/flags.ts`🔒 (`joinGate`) · `app/m/[code]/sections/JoinGate.tsx` · `app/m/[code]/MeetingClient.tsx`🔒 · `app/m/[code]/sections/MeetingHeader.tsx` | **초대 링크를 폼으로 막지 않는다** (`FLAGS.joinGate` 기본 꺼짐 = 새 동작). 링크를 연 사람은 먼저 모임을 **구경**하고(헤더 배지 `👀 구경 중` · '아직 참여 전' 카드), **지도를 찍는 순간** 참여 모달이 떠서 이름·출발지를 받는다 — 참여·출발지·**그 자리 핑**까지 한 번에. 지도를 안 누르는 사람은 `🙋 나도 참여` 버튼. `JoinGate` 는 폼 하나를 페이지/모달 두 껍데기로 나눠 쓴다(`asModal`·`pinned`). 구경 중에는 출발지 폼을 안 그리고 지점 단계 버튼을 잠근다(신원 없이 누르면 서버가 거부하므로). 옛 전면 폼은 `NEXT_PUBLIC_FF_JOIN_GATE=1` | 멘토링 2026-08-06 §2: "**링크 진입할 때 정보 입력 받지 말고, 핑 찍은 후 모달로 추가 정보 수집.**" 카톡 링크를 눌렀는데 첫 화면이 입력 폼이면 뭘 하는 모임인지 보기도 전에 이름·출발지를 요구받는다. ⚠️ **원스텝 원칙(참여+출발지 한 번에)은 그대로다** — 바뀐 것은 묻는 **시점**이지 항목이 아니다 |
| 2026-08-10 | Claude(AI) | `tests/v19-flow.spec.ts` · `scripts/check-devices.mjs` | 참여 순서 테스트를 새 계약으로 — '링크 열면 폼이 먼저' 1건을 **'먼저 구경한다 · 지도 찍으면 참여+핑이 한 번에' 2건**으로 교체. 4대 리허설은 기기 2·3·4 가 **지도를 눌러** 참여하고, 참여 직후 서버에 핑 3개가 있는지(`3대가 지도를 눌러 참여`) 검사한다. 참여 완료 신호를 '참여자 현황'(구경 중에도 보임) → **헤더 배지 `🙋 참가자`** 로 바꿨다 — 옛 신호는 참여가 끝나기 전에 통과돼 마지막 기기의 핑이 서버에 닿기 전 상태를 읽는 유령 실패가 났다 | 화면이 바뀌면 검사가 그 화면의 계약을 따라와야 한다. 리허설은 발표 당일 팀원이 실제로 밟는 순서(링크 → 구경 → 지도 탭 → 이름 입력) 그대로여야 의미가 있다 |
| 2026-08-13 | Claude(AI) | `tests/v19-flow.spec.ts` | **CI `verify (flags-on)` 실패 수정** — 투표 UI 를 전제로 한 화면 테스트 4개에 `test.skip(await aiChatOn(request), …)` 가드를 붙였다. 지금 서버가 무엇을 켜고 빌드됐는지 `/api/status` 의 `flags.aiChat` 에 물어보고 갈라진다(`tests/station-snap.spec.ts` 와 같은 방식) | `NEXT_PUBLIC_FF_AI_CHAT=1` 이면 **투표 UI 가 채팅 UI 로 대체된다**(둘이 같이 뜨지 않는다 — `app/m/[code]/CLAUDE.md`). 그래서 방장 바·확정 버튼·지점 미리보기를 찾는 4개 테스트가 그 빌드에서 성립하지 않는다. **앱 버그가 아니라 테스트가 빌드 조건을 안 본 것**이다 — 직전 커밋(2c9e41a)에서도 같은 4개가 실패하는 것을 로컬에서 확인했다(제 변경과 무관한 기존 문제). ⚠️ `NEXT_PUBLIC_FF_*` 는 빌드 시점에 박혀 테스트 안에서 켜고 끌 수 없다 — 그래서 CI 가 잡을 통째로 두 벌 돌리고, 테스트는 `/api/status` 로 자기가 검사할 계약을 고른다. 검증: flags-on 72통과/6스킵 · flags-off 75통과/3스킵 |
| 2026-08-13 | Claude(AI) | `app/page.tsx` · `app/components/KakaoMap.tsx` | **홈에서 출발지가 없어도 지도를 보여준다.** 예전엔 `activeOrigins.length === 0` 이면 지도 자리를 회색 안내 상자(`.v8-mapempty`)로 통째로 덮었다. 이제 지도를 그대로 그리고 안내는 **아래쪽 알약**으로 얹는다(`pointerEvents:none` — 지도 확대·이동을 막지 않는다). 아무것도 없을 때의 첫 화면 중심을 서울시청 → **협성대(화성시 봉담읍) 인근**으로 옮기고 `DEFAULT_MAP_CENTER`·`DEFAULT_MAP_LEVEL` 상수로 뺐다(level 5 ≈ 동네 몇 블록) | 팀 요청(2026-08-13). 앱을 처음 열었을 때 회색 상자만 보이면 **이 앱이 지도를 쓰는지조차 알 수 없다** — 발표에서 첫인상이 되는 화면이라 아는 동네 지도가 뜨는 편이 '지도로 중간지점을 찾는 앱'을 한 번에 보여준다. ⚠️ 좌표는 캠퍼스 근사값이다 — 정확히 맞추려면 map.kakao.com 우클릭 '좌표 복사' 값으로 바꾼다. 핀이 하나라도 있으면 곧바로 fitBounds 가 덮으므로 **아무것도 없을 때만** 쓰인다. `app/globals.css` 는 🔒 라 안내 알약은 인라인 스타일로 뒀다. 키가 없으면 기존 '지도를 불러오지 못했어요' 안내가 그대로 뜬다(원인 구분 문구 유지) |
| 2026-08-12 | Claude(AI) | `scripts/demo-record.mjs`(신규) · `docs/시연영상_제작_프롬프트.md`(신규) · `package.json`🔒(`demo`) · `.gitignore`(`/demo/`) | **시연영상 자동 촬영.** `npm run demo -- --yes` 로 4대(방장 유환·일산·자차 / 지민·노원·대중교통 / 서연·수원역·자차 / 도윤·인천·대중교통)가 모임 생성→참여→핑→확정→지점투표→결과까지 밟으면서, **각 장면마다 4대를 동시에 찍어 한 장으로 합친 사진**(자막 포함) 15장과 기기별 webm 영상 4개, 나레이션 원고를 뽑는다. 합성은 새 의존성 없이 **Playwright 로 HTML 을 그려 찍는** 방식이다. 산출물은 `/demo/` 라 gitignore 한다. 영상 제작을 다른 세션에 넘기는 프롬프트 원고도 함께 넣었다 | 시연영상은 최종발표 필수 제출물인데(공식 일정표), 사람이 4대를 손으로 조작하며 찍으면 **다시 찍을 때마다 화면·순서가 달라진다.** 자동화하면 시나리오를 고쳐 몇 번이든 같은 품질로 다시 뽑을 수 있다. `check-devices.mjs`(검증용, 최대한 빨리 돎)와 흐름은 같게 두되 **사람이 따라올 속도(PACE)**로 돌린다 — 흐름이 갈리면 영상이 실제 동작과 어긋난다. ⚠️ 첫 촬영에서 확정 버튼 클릭 실패를 조용히 삼켜 **확정 전 화면에 '지역 확정' 자막을 붙여** 찍었다. 지금은 서버 상태로 확인하고, 안 됐으면 API 로 밀어 촬영을 끝까지 가게 한다. 💰 실 API 를 쓰므로 `--yes` 없이는 실행되지 않고, 예상 호출량(카카오 10~20 · ODsay 4~8)을 먼저 보여준다 |
| 2026-08-11 | Claude(AI) | `docs/발표_시나리오_프로토타입.html`(신규) | **발표용 앱 시나리오 프로토타입** — 파일 하나. 더블클릭하면 열리고 서버도 인터넷도 필요 없다. 폰 2대(방장·참여자)를 나란히 놓고 장면 12개를 클릭으로 진행한다. **6번 장면에서는 참여자 폰의 지도를 직접 눌러** 핑을 찍을 수 있고, 그 자리가 다음 장면들에 그대로 이어진다. 핵심 장면(8번)은 방장 폰을 강조해 “한 명이 누르면 다른 폰에 새로고침 없이 뜬다”를 보여준다 | 발표장에서 **실시연 전에 흐름을 30초~1분에 설명**하는 용도다(팀 확인 2026-08-11). `docs/설계도/2차/` 의 목업은 파일 13개 + `start.cmd` 로 **노드 서버를 띄워야** 열려서 “원클릭”이 아니었고, 멘토링 이전 화면이라 지금 앱과도 다르다. 색·문구·배치는 실제 앱 토큰(`app/globals.css`)에서 가져왔다. ⚠️ **목업이다** — 실제 계산·저장·API 호출이 없다(화면 상단에 그렇게 적어 뒀다). 실제 동작은 실시연에서 보여준다 |
| 2026-08-11 | Claude(AI) | `docs/발표_중간발표_v2.html`(신규) · `docs/발표_중간발표_v2.pptx`(신규) · `scripts/build-deck-v2-pptx.js`(신규) | **중간발표 자료 v2 — 외부 청중용.** 본편 14장 + **부록 6장**(HTML 은 `A` 키로 부록 점프). 앞에 팀 소개·기획 배경·서비스 한 장 요약·기존 방식의 한계를 넣고, 기술 구조·검증·겪은 문제·한계는 부록으로 뺐다 | 발표 대상이 **우리 멘토 → 타팀 멘토·기업 현직자·타팀 팀원**으로 넓어졌다(2026-08-11 확인). 처음 듣는 사람이 있으므로 “무엇을 만들었나” 앞에 **“왜 만들었나”** 가 필요하다. 10분 발표라 엔지니어링 상세가 본편에 있으면 늘어져서, 질문이 나올 때만 펼치도록 부록으로 분리했다. ⚠️ 팀명·팀원·멘토·일정(중간 8/14 금, 최종 8/21 금)은 **팀에게 확인받은 값**이다 — 추측으로 채운 항목 없음. v1(`발표_중간발표.*`)은 우리 멘토 대상 판으로 남겨 둔다 |
| 2026-08-11 | Claude(AI) | `docs/발표_중간발표.html`(신규) · `docs/발표_중간발표.pptx`(신규) · `scripts/build-deck-pptx.js`(신규) · `scripts/shots-deck.mjs`(신규) · `docs/img/발표/`(신규) · `package.json`🔒 (`shots`) | **중간발표 자료 2종** — 방향키·클릭으로 넘기는 HTML 슬라이드 17장(파일 하나, 인터넷 없이 열림, `P` 로 인쇄용 펼쳐보기)과 같은 내용의 PPT. 화면 사진 3장은 `npm run shots` 로 다시 찍는다(고정 이름이라 HTML 은 자동 반영). PPT 는 손으로 고치지 않고 `scripts/build-deck-pptx.js` 를 고쳐 다시 만든다 | 숫자·문구가 바뀔 때 **한 곳만 고치면 되게** 하려고 스크립트로 만들었다 — 파워포인트에서 직접 고치면 다음 갱신 때 무엇을 바꿨는지 알 수 없다. 색은 앱의 실제 디자인 토큰(`app/globals.css :root`)에서 가져와 자료와 제품이 같은 색을 쓴다. 자료의 모든 숫자는 저장소에서 직접 센 값이다(테스트 78개·조건 검사 305개·소스 92파일·커밋 126개 등) |
| 2026-08-11 | Claude(AI) | `docs/설계_1차2차_현재코드_대조.md` | 지하철역 스냅 **실 키 검증 결과 기록** — 사당역 양쪽 200m 를 두 사람이 찍어 후보 1개(2명)로 병합, 봉담은 “화성시 효행구”로 폴백 | 저장소 안에서는 카카오 `place_name` 형식을 확정할 수 없어 마지막까지 열려 있던 위험이었다. 학교 노트북(키 있음)에서 `npm run check:station` 으로 닫았다. ④가 우리 학교(봉담)라 “역 없는 동네에 억지로 역을 붙이지 않는다”가 실제로 확인됐다 |
| 2026-08-10 | Claude(AI) | `scripts/check-station-snap.mjs`(신규) · `package.json`🔒 (`check:station`) | **역 스냅을 실 키로 확인하는 점검 스크립트** — `npm run check:station`. 같은 역 양쪽(사당역 ±200m)을 두 사람이 찍어 ①이름에 노선 꼬리가 없는지 ②**한 후보로 합쳐지는지** ③좌표가 역 위로 옮겨지는지 ④역 없는 동네(봉담)를 억지로 스냅하지 않는지 를 본다. 끝나면 테스트 모임을 스스로 지운다 | 이 기능의 핵심 계약(같은 역 = 한 후보)은 **이름 문자열 일치**로 판정되는데, 카카오 SW8 은 같은 역을 노선마다 따로 준다(`사당역 2호선`). 정규화가 틀리면 표를 모으려다 쪼갠다. 그런데 **CI 에는 카카오 키가 없어** 이름이 이미 깨끗한 거점표 폴백만 밟는다 — 정규화가 틀려도 전부 초록이다. 즉 이 계약은 키가 있는 기계에서만 검증되므로, 손으로 클릭하는 대신 한 명령으로 만들었다. 💰 카카오 로컬 3콜 안팎(같은 110m 격자는 30일 캐시) |
| 2026-08-10 | Claude(AI) | `lib/station-snap.ts` · `lib/store.ts`🔒 · `tests/station-snap.spec.ts` | **검증 중 발견한 것 3건 보완** — ① 역 이름 정규화가 **노선 위치에 의존하지 않게** 했다(`2호선 강남역` 도 `강남역 2호선` 과 같은 답). ② 카카오가 "반경 안에 역 없음"이라고 답한 것도 캐시한다(못 물어본 경우=키 없음·오류는 여전히 캐시 안 함). ③ `soloMine` 판정을 읽기 쉽게 정리 | ① 카카오 `place_name` 형식을 저장소 안에서 확정할 수 없다는 것이 이 기능의 가장 큰 위험이다 — 순서까지 가정하면 형식이 다를 때 같은 역이 두 후보로 갈린다. 가정을 하나 줄였다. ② 💰 역이 없는 동네(봉담·화성 — 우리 학교 근처다)에서 찍을 때마다 카카오를 다시 불렀다. 같은 자리를 열 번 찍으면 열 콜이다. CLAUDE.md §3-5 가 금지하는 것은 *폴백값* 캐시이고, "역이 없다"는 실 API 가 답한 사실이라 캐시 대상이 맞다 |
| 2026-08-10 | Claude(AI) | `lib/station-snap.ts`(신규) · `lib/flags.ts`🔒 (`pingSnapStation`) · `lib/cache-store.ts` · `app/api/meeting/route.ts`🔒 · `app/api/status/route.ts` · `.env.example` · `.github/workflows/ci.yml` | **핑을 가장 가까운 지하철역으로 묶는 옵션** (`NEXT_PUBLIC_FF_PING_SNAP_STATION=1` · 기본 꺼짐). 켜면 후보 이름이 `마포구` → **`홍대입구역`** 이 되고 후보 핀도 **역 위**로 옮겨간다. 폴백 사슬 4칸(①카카오 SW8 실제 역 1.2km → ②거점 좌표표 `HUBS` 1.2km → ③시·군·구 → ④좌표 이름)을 `snapPing` 하나로 묶어 호출부는 한 줄이 됐다. 좌표를 소수 3자리(≈110m)로 끊어 30일 캐시(`stationCache*`). `/api/status` 가 **지금 빌드가 켜고 있는 플래그**를 알려준다(빌드 시점에 박히는 값이라 밖에서 알 방법이 없었다). CI 매트릭스 `flags-on` 이 이 플래그를 켜고 한 벌 더 돈다 | 멘토링 2026-08-06 §2 의 괄호 항목 "(또는 **서울은 지하철역 기준**)". 보류 사유였던 "역 좌표표가 필요하다"는 **이미 저장소에 있었다** — `lib/hub-pool.ts` 가 홈 중간지점을 뽑을 때 카카오 `SW8` 로 실제 역을 찾고 있었고 키 없을 때의 거점표(`lib/geo.ts` `HUBS`)도 있었다. 새로 만든 것은 표가 아니라 **스냅 사슬 하나**다. "강남구에서 만나자"는 약속이 안 되지만 "강남역에서 만나자"는 약속이 된다 — 2차 지점 검색 700m 의 중심도 역세권이 된다. 💰 **켜면 핑 1개당 카카오 로컬 1콜이 는다**(캐시 적중 시 0콜) — 그래서 기본 꺼짐이고 켜는 것은 팀 결정이다(CLAUDE.md §4). ⚠️ 키가 있는데 반경 안에 역이 없으면 ②를 건너뛰고 ③으로 간다 — 카카오의 `[]`(없다)와 `null`(못 물어봤다)을 구분한다. 안 그러면 봉담에서 찍은 핑이 멀리 있는 거점으로 튄다 |
| 2026-08-10 | Claude(AI) | `lib/store.ts`🔒 (`addRegionCandidate`) · `tests/v19-flow.spec.ts` | **후보 상한에 걸려 거절당하면 내 핑이 사라지던 버그 수정** — 상한 검사를 '내 핑 옮기기'보다 **앞**으로 옮기고, 내가 혼자 쓰던 후보가 사라질 것을 미리 셈해(`soloMine`) **변형 없이** 거절한다. 회귀 테스트 1건 | 거절 경로는 `write` 를 하지 않는데, 인메모리 모드에서 `read()` 는 저장소의 **살아 있는 객체**를 그대로 준다 — write 없이도 이미 뺀 내 핑이 그대로 사라진 채 400 만 나갔다. **거절당했는데 내 표가 증발**한다. 지금까지는 후보 5개를 채우기 어려워 잘 안 밟혔는데, 역 스냅을 켜면 후보가 잘게 갈려 실제로 밟는다 (조사 워크플로가 찾음) |
| 2026-08-10 | Claude(AI) | `lib/hub-pool.ts` | 홈 중간지점 후보 이름도 **핑과 같은 함수로 정규화**(`normalizeStationName`) | 핑 경로만 노선 꼬리를 떼면, 홈·추천이 올린 `강남역 2호선` 과 사람이 찍은 `강남역` 이 **다른 이름**이라 안 합쳐진다(병합은 이름 일치 판정). 표를 모으려고 만든 기능이 그 자리에서 표를 쪼갠다 |
| 2026-08-10 | Claude(AI) | `app/m/[code]/sections/MapPanel.tsx` · `app/m/[code]/MeetingClient.tsx`🔒 | 지도 핑 안내 문구에서 **묶는 단위를 뺐다** — "동 단위로 정리" → "가까운 지점으로 정리" | 단위가 플래그에 따라 역/시·군·구/동으로 갈리는데 문구에 박혀 있었다. 어제 기본이 시·군·구로 바뀐 뒤에도 화면은 계속 "동 단위"라고 말하고 있었다 — 사용자가 본 설명과 결과가 어긋난다 |
| 2026-08-10 | Claude(AI) | `scripts/check-screens.mjs` | **서버가 정상적으로 거부한 4xx 는 런타임 에러로 세지 않는다.** `pageerror`·5xx·그 밖의 콘솔 에러는 그대로 잡는다 | 이 스크립트는 화면의 버튼을 무작정 눌러 보는데, 후보 상한(5개)이나 단계 규칙에 걸려 **의도된 400** 이 난다. 브라우저는 그것도 콘솔에 찍는다 — 세어 버리면 "예상된 거부"와 "진짜 버그"가 구분되지 않아 검증이 무의미해진다. UI 는 이미 토스트로 안내하므로 사람에게는 정상 동작이다 |
| 2026-08-10 | Claude(AI) | `app/m/[code]/MeetingClient.tsx`🔒 | **`act()` 가 실패 시 throw 대신 `null` 을 돌려준다.** 결과를 봐야 하는 호출부(`sendChat`)는 반환값으로 판단하도록 바꿨다 | 이 함수는 대부분 `void act(...)` / `onClick={() => act(...)}` 로 **띄워 보내는** 식으로 쓰인다. 그 자리에서 거부되면 **처리되지 않은 promise rejection** 이 돼 콘솔에 런타임 에러로 남는다 — 서버가 **정상적으로** 거부하는 경우(단계 변경·후보 상한)에도 그렇다. 후보 상한 5개를 넣자마자 `check:screens` 가 ⑧ 화면에서 이걸 잡았다. "예상된 거부"와 "진짜 버그"가 콘솔에서 구분되지 않으면 그 검증 자체가 무의미해진다. 호출부 20여 곳에 `.catch` 를 뿌리는 대신 **한 곳**에서 끝냈다 |
| 2026-08-10 | Claude(AI) | `app/components/KakaoMap.tsx` · `app/m/[code]/sections/MapPanel.tsx` | **반경 점선 원** — 지점 단계에서 확정 지역 중심에 `kakao.maps.Circle`(점선·연한 채움)을 깐다. 확장(700→1400)이 즉시 반영되도록 `radiusM` 을 의존성에 넣었다 | 1차 순서도 4번에 `반경 700m 표시` 가 있는데 숫자·문구로만 알렸다 — 지도에서 **어디까지가 안인지 알 수 없어**, 반경 밖을 눌러 서버에 거부당하고 나서야 알게 된다. 점선으로 그린 이유는 "정확한 경계선"이 아니라 안내라는 뜻을 담기 위함이다 |
| 2026-08-10 | Claude(AI) | `lib/types.ts`🔒 · `lib/store.ts`🔒 · `MeetingClient.tsx`🔒 · `PlacePicker.tsx` | **후보 상한 5개** (`MAX_CANDIDATES`) — 지역·지점 모두. 추천 후보도 남은 자리만큼만 채우고 멈춘다. 화면에 `후보 n/5` 를 **미리** 보여준다 | 2차 그릴링이 v19 의 "지점 상한 없음"(v7)을 되돌렸고 근거가 명확하다 — **"여섯 개를 놓고 고르라고 하면 표가 흩어져 1위가 안 나온다."** 4명이 6곳에 찍으면 1표씩 갈려 동점 천지가 된다. ⚠️ 상한 검사를 **병합·이동 검사보다 뒤**에 뒀다: 앞에 두면 다섯 개일 때 같은 동에 합류하지도, 내 핑을 옮기지도 못한다 |
| 2026-08-10 | Claude(AI) | `app/meetings/page.tsx` | **모임 탭 필터를 두 줄(상태 / 역할)로** 분리하고 줄 이름을 붙였다 | 8개가 한 줄에 뒤섞여 있었는데 `지역`(단계)과 `방장`(역할)은 **성격이 다른 축**이라, 섞으면 "지역이면서 방장인 모임"을 어떻게 보는지 알 수 없다. ⚠️ 2차 원본은 상태를 5개로 합쳤지만 `지역`/`지점` 구분은 유지했다 — 그게 이 앱의 축(확정 범위 분기)이고 합치면 목록에서 단계를 못 읽는다 |
| 2026-08-10 | Claude(AI) | `lib/handoff.ts`(신규) · `app/page.tsx` · `app/meetings/page.tsx` | **홈 → 생성 인계** — 홈에서 넣은 **첫 출발지**를 방장 출발지로, 홈에서 본 **중간지점**을 첫 지역 후보로 넘긴다. `sessionStorage` 에 담고 **읽는 즉시 지운다**. 무엇이 옮겨졌는지 생성 완료 요약에 밝힌다. 인계가 실패해도 모임 생성은 그대로 된다 | v19 §4-① 의 '미배정 핑'이 미구현이라 [이 출발지들로 모임 만들기]가 **이동만** 했다 — 방금 넣은 출발지도 방금 본 중간지점도 버려지고 모임에서 처음부터 다시 넣어야 했다. ⚠️ 2차는 "나머지 출발지를 후보 씨앗으로" 쓰는데 그대로 옮기지 않았다: **출발지는 사람이 사는 곳이지 만날 곳이 아니다.** 홍대·잠실 출발인데 후보가 '홍대'·'잠실'이면 한 명은 집 앞이고 다른 한 명은 가로질러 간다 — 이 앱이 없애려는 상황 그 자체다. 대신 중간지점을 씨앗으로 쓴다. `localStorage` 가 아닌 이유는 다음 날 새 모임에 어제 중간지점이 딸려오기 때문이다 |
| 2026-08-10 | Claude(AI) | `lib/store.ts`🔒 · `app/api/meeting/route.ts`🔒 · `lib/roles.ts` | **재투표(`revote`) 추가** — 후보는 그대로 두고 **표만 초기화**한다. 투표 칸에서는 그 자리에 머물고, 결과 화면에서는 확정을 풀고 **그 단계의 투표 칸으로** 내려간다(지역 확정은 유지 — 한 칸만 되돌린다). 등록 칸은 표가 없으니 거부. 표는 별도 행에 살기 때문에 모임 행뿐 아니라 `clearVotes` 도 함께 부른다 | 1차 순서도 4번의 `동점? → 재량 선택 **또는 재투표**` 에서 **재투표 갈래가 통째로 없었다.** 동점이면 방장이 혼자 고르는 수밖에 없었다. 멘토링 8/6 §3 도 "결과 보고 끝나는 흐름 방지 — 재투표 같은 흡수 장치 필요", "투표 마음에 안 들면 재투표"를 명시했다. ⚠️ `reopenStep`(되돌리기)과 **정반대**다 — 되돌리기는 단계를 내리고 표를 지키고, 재투표는 단계에 머물며 표를 지운다 |
| 2026-08-10 | Claude(AI) | `app/m/[code]/sections/LeaderBar.tsx` · `app/m/[code]/MeetingClient.tsx`🔒 | 재투표 버튼을 **두 곳**에 냈다 — ① 결과 화면(되돌리기 옆, 보조 문구로 `후보부터 다시` / `표만 초기화` 를 갈라 놓음) ② 투표 칸에서 **동점일 때만**. 둘 다 확인 팝업을 거친다 | 두 버튼이 나란히 있으면 무엇이 다른지 몰라 아무거나 누른다 — 그래서 문구로 갈랐다. 동점 버튼을 상시 노출하지 않은 이유는 "표를 지우는 버튼"이 늘 떠 있으면 실수로 누르기 쉬워서다. `isTied` 는 **표가 0일 때를 동점으로 치지 않는다**(아직 아무도 안 찍은 것이지 동점이 아니다) |
| 2026-08-10 | Claude(AI) | `lib/roles.ts`(신규) · `app/m/[code]/MeetingClient.tsx`🔒 | **방장/참여자 화면 분리 — 권한을 한 표로 모았다.** `isLeader &&` 가 8개 파일 39군데에 흩어져 있던 것을 `abilitiesOf(역할, 단계, 지난모임)` 한 곳으로 옮기고, 화면은 `can.startVote` 처럼 **행동 이름**으로 읽는다. 단계 판정(`stepOf`)은 서버 `phaseStepOf` 와 같은 규칙이고, `lib/store.ts` 가 DB 를 끌고 와 클라이언트에 못 들어오므로 순수 모듈로 따로 뒀다 | 멘토링 8/6 §1: "방장과 참여자 화면이 섞여 있어 **구현 시 꼬일 가능성 높음**." 실제로 같은 규칙이 화면마다 따로 적혀 있어 한쪽만 고치면 어긋났다 — 등록 단계에서 서버는 표를 거부하는데 UI 는 투표 버튼을 그리던 v12 버그가 그 형태였다. ⚠️ 이 파일은 **보이게/안 보이게** 하는 층이고, 막는 것은 여전히 서버가 한다 |
| 2026-08-10 | Claude(AI) | `app/m/[code]/sections/ParticipantBar.tsx`(신규) | **참여자 하단 바 신설** — 방장 바의 짝. 단계별로 "지금 무슨 칸인가" + "내가 할 일 / 진행률"을 보여주고, 이미 한 일은 ✓ 로 말한다(`✓ 내 핑: 역삼1동`). **버튼은 하나도 없다** | 참여자 화면 하단이 **비어 있었다**(`showLeaderbar = isLeader`). 방장 화면엔 "0/4명 · 미투표 지민"이 늘 떠 있는데 참여자는 지금 무슨 단계인지도, 뭘 해야 하는지도 알 수 없어 시연 내내 "이제 뭐 해요?"를 묻게 된다. 멘토: "방장 아닌 사람은 **핵심 투표 결과만** 보면 됨" |
| 2026-08-10 | Claude(AI) | `app/m/[code]/sections/ResultSection.tsx` | 참여자에게 보이던 **비활성 '방장만 결제할 수 있어요' 버튼을 없앴다** (렌더 자체를 안 한다) | 멘토링 8/6 §1 마지막 줄: "굳이 **비활성화 처리 불필요**." 눌리지도 않는 버튼은 화면만 어지럽히고, 참여자에게 "내가 뭔가 잘못했나"로 읽힌다 |
| 2026-08-10 | Claude(AI) | `scripts/check-screens.mjs` · `tests/v19-flow.spec.ts` | 화면 훑기에 **참여자 시점 4대**를 추가(같은 단계를 두 벌로 열어 본다)하고, 역할 분리 계약을 테스트 3개로 고정했다 — ① 참여자에게 방장 버튼이 **0개**일 것(비활성이 아니라 없을 것) ② 방장에겐 참여자 바가 안 뜰 것 ③ 등록 칸엔 확정이, 투표 칸엔 '투표 시작'이 없을 것 | 이 스크립트는 오래도록 **방장 시점만** 훑었다. 그런데 발표에서 화면 4개 중 3개는 참여자 화면이다. 두 벌로 안 열어 보면 분리했다는 걸 확인할 방법이 없다 |
| 2026-08-10 | Claude(AI) | `docs/멘토링_2026-08-06.md`(신규) | **8/6 멘토링 회의록을 레포에 저장** — 원본 그대로 옮기고, v19(8/7)·2차 그릴링(8/10)보다 **앞선 시점**이라는 주의와 이후 팀이 다르게 정한 항목(하단 탭 3개 · PWA/TWA)에 ⚠️ 를 달았다 | 멘토님이 우선순위를 한 줄로 주셨는데("힘 줄 곳: **핑 등록 + 결과 화면 지도 뷰**") 그게 채팅에만 있으면 다음 세션에서 사라진다. 실제로 이 한 줄이 §설계_1차2차_현재코드_대조 의 작업 순서를 통째로 바꿨다 |
| 2026-08-10 | Claude(AI) | `lib/flags.ts`🔒 · `app/api/meeting/route.ts`🔒 · `app/m/[code]/MeetingClient.tsx`🔒 · `sections/LeaderBar.tsx` | **지역 후보 자동 채움을 걷어내고 방장 버튼으로 옮겼다** — `case "regions"` 는 이제 후보를 만들지 않고 **이미 있는 후보의 이동시간·편차만 갱신**한다. 방장용 `suggestRegions` 액션을 새로 두고(권한·단계·병합·표 정리는 기존 `applyAiCandidates` 재사용), 추천 버튼이 **LLM 플래그와 무관하게 항상** 보이게 했다(꺼져 있으면 점수 기반으로 떨어진다). 후보 0개 화면의 안내 문구를 "계산하고 있어요…" → "지도를 눌러 등록 / 방장은 추천 버튼" 으로 바꿨다. 옛 동작은 `NEXT_PUBLIC_FF_AUTO_REGIONS=1` | 멘토링 §2: "방장은 **AI 추천 버튼으로** 지역 후보를 추가할 수 있음" — **버튼**이지 자동이 아니다. 자동으로 채우니 ① 가장 중요한 기능인 핑이 화면의 주인공이 아니었고 ② 후보가 항상 3개라 `candidateGate` 의 "0개 = 투표 시작 불가"(v19 §5)가 **한 번도 실행되지 않았다** ③ LLM 버튼은 `NEXT_PUBLIC_FF_AI_VOTE` 뒤에 숨어 있어 "AI 추천은 어디 있냐"는 제보가 나왔다. 💰 자동 호출이 버튼으로 바뀌어 이동시간 API 호출은 **줄어든다** |
| 2026-08-10 | Claude(AI) | `app/m/[code]/sections/MapPanel.tsx` | **지도 SDK 폴백에서도 핑을 찍을 수 있게** `unpos`(위치%→좌표) 역함수와 탭 오버레이를 넣고, 핑 모드에서 `.pin`·`.cpin` 장식에 `pointer-events:none` 을 줬다. 핑 안내 칩의 `!fallback` 조건도 뗐다 | 자동 채움을 걷어내자마자 드러난 구멍이다 — **카카오 키가 없거나 도메인 등록이 안 된 기기에서는 후보를 만들 방법이 아예 없었다**(그동안 자동 채움이 가리고 있었다). 발표에서 팀원 폰의 지도가 안 뜨면 지역 단계가 막다른 길이 된다(CLAUDE.md §3-4). 또 `.cpin .pulse` 는 지름 64px 이 1.7배까지 커지는 장식인데 z-index 6 으로 지도 한가운데 클릭을 삼켜, 4대 리허설에서 1대가 여기 막혀 핑이 안 찍혔다 |
| 2026-08-10 | Claude(AI) | `scripts/check-screens.mjs` · `scripts/check-devices.mjs` | 두 검증 스크립트를 **새 흐름(사람이 핑을 찍는)** 으로 고쳤다. 화면 훑기에 **'⑥ 후보 0개'** 화면을 추가하고, 4대 리허설에 **지도 핑 단계**를 넣었다. 핑 검사는 "후보가 하나라도 생겼다"가 아니라 **`contributors` 로 4/4명**을 센다. 클릭은 뷰포트 좌표(`page.mouse.click`) 대신 요소 기준(`locator.click({position})`)으로 바꿨다 | 두 스크립트가 자동 후보에 기대고 있어 플래그를 끄자마자 죽었다 — 검증이 실제 흐름이 아니라 **자동 채움을 검증하고 있었다**는 뜻이다. 느슨한 검사(`length > 0`)는 4대 중 2대가 실패해도 초록불이었고, 실제로 그 상태를 이번에 잡았다. 원인은 참여 폼을 지나온 기기가 스크롤돼 지도 아랫부분이 뷰포트 밖이라 클릭이 빗나간 것 |
| 2026-08-10 | Claude(AI) | `tests/{smoke,modals,sync,v19-flow}.spec.ts` | 테스트 4종의 **후보 시딩 경로**를 실제 제품 경로로 교체했다 — `{action:"regions"}` 로 후보가 생기던 것을 `sync` 는 **4명이 각자 핑**(`addRegion`), 나머지는 **방장 추천 버튼**(`suggestRegions`)으로 바꿨다. 등록 칸 칩 문구가 '후보 N개'로 바뀐 것에 맞춰 스모크 단언도 고쳤고, **후보 0개면 조용히 통과하던 루프**(늦은 합류자 이동시간 테스트)에 개수 단언을 세웠다 | 55개 중 12개가 자동 채움에 기대고 있었다 — 플래그를 끄자마자 무더기로 깨졌고, 이는 **테스트가 제품이 아니라 자동 채움을 검증하고 있었다**는 뜻이다. 특히 `st.regions` 가 비면 `for` 루프가 아무것도 검사하지 않고 통과해, 겉으로는 초록불인 채 회귀를 놓칠 뻔했다 |
| 2026-08-10 | Claude(AI) | `docs/설계_1차2차_현재코드_대조.md`(신규) | **1차 순서도 원본 5장을 직접 다시 읽어 현재 코드와 한 줄씩 대조** — 순서도 내용이 전부 `<script>` 문자열이라 한글만 추출해 맞췄다. 그대로 된 것 25항목 / 다른 것 6항목(A~F)을 파일·줄 번호와 함께 적고, 1차↔2차가 서로 다른 4건과 작업량 순 수정 후보 8건을 정리했다 | 수정을 시작하기 전에 **무엇이 이미 되어 있는지**부터 확정해야 한다. 실제로 대부분(핑 1개 제한·후보 0/1/2+ 게이트·PIN 복구·강퇴 병합 핑·재모임·reopen)은 이미 코드에 있었다. 반대로 `설계_v19.md` 에는 흐릿하고 **원본 순서도에만 또렷한 것 3건**(미배정 핑 이관 · 이벤트마다 카톡 알리기 · 동점 시 재투표 갈래)이 미구현으로 드러났고, `case "regions"` 의 AI 자동 채움 때문에 **'후보 0개 = 시작 불가' 분기가 아예 실행되지 않는 설계 충돌**도 찾았다 |
| 2026-08-09 | Claude(AI) | `docs/설계_v19.md` §13-A | **v19 ↔ 코드 충돌 1건 명시** — v19 는 "별점: 카카오 웹 스크래핑 유지 · 지점 정렬 별점순", 코드는 하루 앞선 PR #54·#55 에서 **스크래핑을 이미 제거**(공식 API 전환, `rating` 전부 0). 선택지 3안(A 별점 없이 · B 스크래핑 복원 · C 구글 Places)과 **A 권장** 근거를 기록, 팀 결정 대기로 표시 | v19 가 v13 의 *"PoC 는 유지, 배포 시 재검토"* 문구를 그대로 실어 날랐는데, 그 재검토가 **이미 일어났다**(Vercel 서버리스에 크롬 없음). 되돌리면 `CLAUDE.md` §3-6(가짜를 실제처럼 그리지 않는다)에 다시 부딪힌다. 스펙이 "미결정 0"이라고 선언했으므로 충돌을 덮지 않고 드러내 둔다 |
| 2026-08-09 | Claude(AI) | `tests/sync.spec.ts`(신규) · `scripts/check-live.mjs`(신규) · `package.json` 🔒 · `docs/발표_로컬시연.md` | **연결·동시성 검증 추가** ① `tests/sync.spec.ts` 5건 — **4명 동시 투표(표 유실 없음)** · 4명 동시 출발지 변경(덮어쓰기 없음) · 동시 같은 지점 등록(중복 안 생김) · **남의 표가 내 화면에 폴링으로 들어오는지**(브라우저 컨텍스트 2개 = 폰 2대) · 방장이 넘긴 단계가 참여자 화면에 반영되는지 ② `npm run check:live` — 서버에 대고 **실제로 모임을 만들어 4인·동시투표·확정까지 왕복**한 뒤 스스로 삭제. DB·카카오·ODsay/TMAP·AI 연결 상태와 **Redirect URI 포트 일치**까지 점검 ③ 런북에 절차 추가 | 지금까지 테스트는 전부 **한 명이 순서대로** 하는 것만 봤다. 그런데 `CLAUDE.md` 데이터 계층 규칙은 *"참가자·투표를 모임 행에 같이 담았더니 동시 쓰기에 표가 사라졌다"* 는 **실제 사고**에서 나온 것이라, 정작 그 사고를 재현하는 테스트가 없었다. 또 `.env.local` 은 Next 가 자동으로 읽으므로 **노트북에서 `npm run verify` 를 돌리면 이 테스트들이 그대로 Neon 에 대고 돈다** — 같은 파일이 CI(인메모리)와 실 DB 양쪽을 지킨다. `check:live` 는 조회가 아니라 왕복이라 "쓰기가 진짜 되는지"를 확인한다 — 발표 당일 아침 네트워크가 바뀐 뒤 한 줄로 점검하는 용도다. 실측: 키 없는 환경에서 설정 4건을 정확히 실패로 잡고 왕복 8건은 전부 통과 |
| 2026-08-09 | Claude(AI) | `lib/store.ts` 🔒 · `tests/v19-flow.spec.ts` | **AI 추천 후보가 재계산에 밀려 사라지던 버그 수정** — `setRegionCandidates()` 가 사람이 등록한 후보를 `rc_*` 접두사로만 보존해서, 내가 §8에서 만든 **AI 후보(`ra_*`)가 보존 대상에서 빠져 있었다.** `rc_` 또는 `ra_` 둘 다 보존하도록 고치고 회귀 테스트 1건 추가 | 자동 후보 재계산(`regions` 액션)은 **출발지가 바뀔 때마다** 돈다. 그래서 발표 시연에서 **"방장이 AI 추천 → 팀원이 참여하며 출발지 입력 → AI 후보 3곳이 통째로 증발"** 이 정확한 순서로 재현된다. v19 §8 의 AI 시연이 통째로 날아가는 경로였다. 감사(유저 여정 재검증) 중에 발견했고, 내가 넣은 코드의 결함이다 |
| 2026-08-09 | Claude(AI) | `app/m/[code]/sections/JoinGate.tsx`(신규) · `MeetingClient.tsx` 🔒 · `LeaderBar.tsx` · `ResultSection.tsx` · `app/meetings/page.tsx` · `app/page.tsx` · `tests/v19-flow.spec.ts` | **발표 시연 경로 5건 보완 (유저 여정 감사 결과)** 🔴 ① **초대 링크 참여 폼**(`JoinGate`) — 신원 없이 `/m/{code}` 로 들어오면 **참여 폼이 먼저** 뜬다. 이름·출발지·이동수단 **원스텝**(v11) + PIN(v15) + 이름 중복 시 별칭/복구 두 갈래(v4). 참여와 출발지 등록을 **한 번에** 끝낸다 🔴 ② **생성 폼 모임 시간 → `meetTime` 저장** — 자유 문구를 `prefs.timeText` 에만 넣던 것을 `datetime-local` + ISO 로 바꿔 `create` 에 실었다 🟡 ③ 홈에 **'이 출발지들로 모임 만들기'** 전환 고리(출발지 2곳 이상일 때) 🟡 ④ 확정 버튼에 **미투표자 이름**(v6) 🟡 ⑤ 결과에 **카카오톡 알리기** 상시 버튼(v16·v18) — 공유 시트, 없으면 메시지 복사 ⑥ 회귀 테스트 3건 추가(초대 링크 참여 · meetTime 저장 · 홈 전환 고리) | **감사에서 드러난 것**: 규칙(잠금·게이트·권한)은 서버 테스트로 촘촘히 고정했는데 **화면 진입 경로에는 테스트가 없었다** — 36개 중 화면 테스트가 6개뿐이었고 전부 "이미 신원이 있는" 상태를 가정했다. 그래서 ①이 통째로 빠진 걸 못 봤다(팀원이 카톡 링크를 눌러도 자기 자리가 없어 시연 3단계가 멈춘다). ②는 더 조용한 사고였다: 화면엔 시간이 보이는데 **서버는 `meetTime` 만 보므로** D-day·도착 신호등·'지난 모임' 전환이 전부 죽어 있었다. ⚠️ CTA 클릭이 테스트에서 막혀 원인을 찾다가 **첫 방문 스플래시**(`z-index:100` 전면 오버레이)가 홈 전체를 덮는 것을 확인했다 — 의도된 동작이라 코드가 아니라 테스트를 사용자와 같은 상태로 맞췄다(발표 때 팀원들은 처음 열면 '시작하기'를 한 번 눌러야 한다). verify: tsc ✅ · build ✅ · **36/36 ✅** |
| 2026-08-09 | Claude(AI) | `.env.example` | **누락된 플래그 3종 + `SELF_HOST_URL` 경고 추가** — v19 구현에서 새로 만든 `NEXT_PUBLIC_FF_AI_VOTE`(AI 추천 버튼, 💰 Ollama Cloud) · `NEXT_PUBLIC_FF_PLACE_RATING`(별점, 느려짐) · `NEXT_PUBLIC_FF_MOCK_PAY`(옛 선입금 UI)가 예시 파일에 없어 팀원이 존재 자체를 몰랐다. 함께 `SELF_HOST_URL` 항목을 신설하고 **로컬 전용이면 절대 채우지 말 것**을 명시(값이 있으면 `/api/*` 가 통째로 다른 서버로 rewrite 돼 화면은 뜨는데 API 만 전멸한다). `ODSAY_BASE_URL`·`ODSAY_PROXY_SECRET` 에도 '로컬이면 비워라' 한 줄 추가 | 학교 노트북에서 **옛 `.env.local` 을 그대로 가져다 쓴 뒤 API 테스트 29개가 전멸**했다(화면 테스트 4개만 통과). 이 패턴은 `SELF_HOST_URL` 이 남아 있을 때의 증상과 정확히 일치하는데, 그 변수가 `.env.example` 에 아예 없어서 **눈으로 대조할 방법이 없었다.** 예시 파일이 실제 변수 집합을 못 따라가면 '키가 없어도 돌아간다'는 규칙(CLAUDE.md §3-4)의 검증 기준 자체가 흐려진다 |
| 2026-08-09 | Claude(AI) | `package.json` 🔒 · `lib/env.ts` · `playwright.config.ts` · `.env.example` · `.claude/launch.json` · `docs/발표_로컬시연.md` · `memory/handover.md` · `memory/status.md` | **dev 포트 3100 → 3000 환원** — `dev`·`dev:lan`·`start` 스크립트, `KAKAO_REDIRECT_URI` 기본값, launch.json, 런북·인계 문서의 주소를 전부 3000 으로. 스모크는 **3100 유지**(`playwright.config.ts`) | 팀의 카카오 콘솔 등록값과 다른 API 설정이 **이미 3000 기준**이라 앱을 거기 맞추는 것이 맞다(사용자 확인). ⚠️ 부수 효과가 더 중요하다: `playwright.config.ts` 주석이 원래 *"스모크 전용 포트 — 개발 서버(3000)와 부딪히지 않게 따로 쓴다"* 였는데, 언젠가 dev 가 3100 으로 바뀌면서 **스모크 포트와 겹쳐 있었다.** 이 설정은 `reuseExistingServer: !CI` 라, `npm run dev` 를 띄운 채 `npm run verify` 를 돌리면 **빌드 결과물이 아니라 개발 서버를 재사용**한다 — "빌드에서만 나는 오류"를 못 잡는 상태였다. 3000 환원으로 원래 분리(dev=3000 / smoke=3100)가 복원됐고, 주석에 그 이유를 못박았다. 일괄 치환이 *"3000 vs 3100 불일치"* 를 설명하던 런북 문장 2곳을 `3000 vs 3000` 으로 망가뜨려 함께 정정했다. verify: tsc ✅ · build ✅ · **33/33 ✅** |
| 2026-08-09 | Claude(AI) | `memory/handover.md` · `memory/status.md` | **세션 인계 정리** — `handover.md` 를 v7 목업 시절 내용에서 **현재 인계**로 교체(옛 내용은 하단에 '이전 인계'로 보존): 브랜치·최신 커밋·기준 문서·새 기기 시작 절차(clone→node 22→verify)·발표 전 필수 3건(카카오 콘솔 2곳·DB·리허설)·플래그 4종 표·다음 우선순위 3건·이 세션의 판단 4건(흐름 2번 변경 / 별점 스크래핑과 빌드 / AI 강등 미채택 / 동 스냅). `status.md` 진행표를 11행으로 갱신(3번 행이 ⬜ 로 남아 있던 것 정정 + AI 추천·지도 핑 추가) | 다음 작업이 **다른 기기(학교 노트북)** 에서 이어진다. 이 세션의 맥락은 사라지므로, 무엇이 끝났고 무엇이 남았는지·왜 그렇게 정했는지를 레포 안에 남긴다. 특히 **흐름이 두 번 바뀐 것**(투표 시작 단계 신설 / 지점 후보 수동 등록)은 모르고 보면 "기능이 사라졌다"로 오해하기 쉬워 명시했다 |
| 2026-08-09 | Claude(AI) | `lib/kakao.ts` · `app/api/meeting/route.ts` 🔒 · `app/components/KakaoMap.tsx` · `app/m/[code]/sections/MapPanel.tsx` · `MeetingClient.tsx` 🔒 · `tests/v19-flow.spec.ts` | **v19 §4-⑥ — 지도 핑 UI** ① `coord2RegionKakao()` 신규 — 좌표를 **행정동(H)** 으로 스냅하고 **동 중심 좌표**를 돌려준다(찍은 자리를 그대로 쓰면 같은 동인데 핀이 흩어진다). 키 없음·오류면 `null` ② `addRegion` 액션이 **이름 없이 좌표만 오면 동 스냅**을 태우고, 실패하면 `지도 37.567, 126.978` 처럼 **소수 3자리 좌표 이름으로 폴백**(같은 자리를 두 번 찍으면 같은 이름 → 병합 규칙이 그대로 산다, v4) ③ `addRegionCandidate` 에 `participantId` 를 넘겨 **인원당 1개·병합·이탈** 규칙이 실제로 걸리게 함 ④ `KakaoMap` 에 `onMapClick` — 리스너는 **지도 생성 시 한 번만** 붙이고 최신 콜백은 ref 로 본다(폴링마다 붙였다 떼면 클릭이 씹힌다) ⑤ `MapPanel` 에 `pingMode` — 커서 crosshair + 안내 칩 ⑥ 모임 화면은 **지역 후보 등록 단계에서만** 핑 모드를 켠다(투표 시작 후엔 서버가 거부하므로 화면에서도 미리 끈다) ⑦ 회귀 테스트 3개(좌표만으로 후보 생성 · 인원당 1개 이동 · 두 사람 같은 곳 병합) | v19 스펙에서 마지막으로 남아 있던 기능 차이였다. 지금까지 지역 후보는 **검색 모달**로만 등록됐는데, v19 §4-⑥ 은 "지도 전체화면에서 누구나 핑을 찍어 등록"이다. **동 스냅이 핵심**이다 — 좌표 그대로 두면 3m 옆에 찍은 두 핑이 서로 다른 후보가 되고 투표가 갈라진다. 스냅 실패를 오류로 다루지 않고 좌표 이름으로 폴백한 것은 v4 가 정한 동작이고, 키 없이도 전체 플로우가 돌아야 한다는 규칙(CLAUDE.md §3-4)과도 맞는다. 🔒 `MeetingClient.tsx` 는 MapPanel prop 2개만 추가했다. verify: tsc ✅ · build ✅ · **33/33 ✅** |
| 2026-08-09 | Claude(AI) | `lib/types.ts` 🔒 · `lib/store.ts` 🔒 · `app/api/meeting/route.ts` 🔒 · `app/m/[code]/sections/LeaderBar.tsx` · `VoteList.tsx` · `PlacePicker.tsx` · `MeetingClient.tsx` 🔒 · `tests/v19-flow.spec.ts` | **v19 §8 — AI 추천 버튼 배선** ① `aiSuggested` 필드 신규(지역·지점) — **병합 표시**. AI 가 추천한 동이 수동 핑과 같으면 후보를 새로 만들지 않고 기존 것에 이 표시만 켠다(`source` 는 `manual` 유지, v9) ② `applyAiCandidates()` 신규 — `replace` 는 **순수 AI 후보만** 지운다(수동 병합 후보 유지, v14) · 반경 밖 AI 지점은 버린다(사람 등록과 같은 잣대, v4) · 사라진 후보의 표만 정리 ③ API `aiRecommend` 액션 — **방장 검사 + 등록 단계 검사(v8) + `NEXT_PUBLIC_FF_AI_VOTE` 게이트**, ECO 원콜, `setAiBusy` 를 `finally` 로 반드시 해제(v17), **강등(`degraded`) 결과는 후보로 넣지 않고 토스트+재시도로 돌려보낸다**(v9) ④ LeaderBar 에 방장 전용 AI 버튼 — 로딩·예상 소요(지역 ~25초/지점 ~9초)를 문구로, 재호출이면 **교체/추가 팝업**(v14) ⑤ 후보 목록에 'AI 추천' 태그(수동 제안자와 **함께** 표기) ⑥ 회귀 테스트 1개 | v19 §8 은 **"AI 는 방장이 누르는 버튼 하나뿐이고, 안 누르면 0원"** 이다. 엔진(`lib/ai-vote/`)은 이미 있었고 없던 건 이 배선이다. **강등 결과를 후보로 앉히지 않은 이유**: 스코어러 폴백은 AI 가 고른 게 아닌데 'AI 추천' 태그를 달면 거짓말이 된다 — v9 가 "실패는 토스트+재시도, 스코어러 강등 설계는 폐기"라고 못박은 지점이다. 💰 **테스트는 AI 를 부르지 않는다** — 플래그가 꺼진 상태에서 403 으로 막히는 것까지만 검사한다(CI 에서 Ollama Cloud 호출 금지, CLAUDE.md §4). 병합 규칙은 켠 상태로 사람이 눌러 확인한다. verify: tsc ✅ · build ✅ · **30/30 ✅** |
| 2026-08-09 | Claude(AI) | `app/components/v8/BottomNav.tsx` · `app/votes/page.tsx` · `app/members/page.tsx` · `app/meetings/page.tsx` · `app/me/page.tsx` · `tests/v19-flow.spec.ts` · `tests/smoke.spec.ts` | **v19 구현 7단계(마지막) — 하단 3탭 통합** ① 하단 탭 **5 → 3**(홈·모임·내정보) — '투표함'·'모임원' 제거(v6) ② `/votes`·`/members` 는 **리다이렉트만 남긴다**(모임 탭으로) — 삭제하면 카톡에 공유된 옛 링크·북마크가 404 로 죽는다 ③ 모임 탭을 통합 허브로: **투표할 것 상단 고정**(🔔 배너, 최대 3개) · **필터 7종+전체**(모집·지역·지점·확정·방장·참여·지난) · **시간순 정렬**(가까운 모임이 위, 시간 미정은 맨 아래) · 카드에 **D-day**·확정 장소 표시 · 지난 모임은 흐리게 ④ 내정보에 **기본 PIN** 추가(v19 §4-③) + 참여 폼 **자동 채움**(조건은 '내정보에 값이 있으면' 하나뿐 — v19 가 v11 을 단순화) ⑤ 회귀 테스트 4개(탭 3개·리다이렉트·필터 동작·투표 배너) | v19 §4-② 를 코드로 옮겼다. **`/members` 에 있던 도착 신호등은 8단계에서 결과 화면으로 이미 옮겨 뒀다** — 그래서 이 라우트를 걷어내도 잃는 기능이 없다. 라우트를 지우지 않고 리다이렉트로 남긴 이유: 발표 중 누가 옛 주소를 눌러도 자연스럽게 이어지게 하기 위해서다. ⚠️ 기존 스모크가 **5탭을 기대**하고 있어 깨졌다 — 설계가 바뀐 것이므로 **테스트를 3탭 기준으로 고쳤다**(가드를 되돌리지 않았다). 시간 미정 모임을 맨 아래로 보낸 이유: 언제인지 모르는 모임을 "급하다"고 말할 근거가 없다. verify: tsc ✅ · build ✅ · **29/29 ✅** |
| 2026-08-09 | Claude(AI) | `lib/types.ts` 🔒 · `lib/store.ts` 🔒 · `lib/persistence.ts` 🔒 · `app/api/meeting/route.ts` 🔒 · `app/m/[code]/sections/ArrivalPanel.tsx`(신규) · `ResultSection.tsx` · `MeetingClient.tsx` 🔒 · `tests/v19-flow.spec.ts` | **v19 구현 6단계 — 결과·신호등·지난 모임·재모임** ① `Meeting.updatedAt` 추가(DB `updated_at` 트리거 값을 도메인으로) — 시간 미입력 모임의 '마지막 활동 7일' 판정에 쓴다 ② `archiveDueAt()`·`archiveIfDue()` — **모임일 다음날 0시**부터 '지난 모임', 시간 없으면 마지막 활동 7일 후. 스케줄러가 없어 **`getState` 에서 읽을 때 판정**(전환은 한 번뿐이라 쓰기도 한 번) ③ `setMeetTime()` — 방장만·**과거 거부**(v16), 비우면 미입력으로 복귀 ④ `setParticipantStatus` 에 4중 가드: 지난 모임·'지역까지' 모임(v14)·**시간 미입력**(v7)·**당일 아님**(v16) + `lateMin`(노랑일 때만, 1~600분, 색 바뀌면 소거) ⑤ `recreateMeeting()` — 방장만, **카카오 로그인 멤버만 자동 이전**(v17), 신호등·PIN 은 안 가져옴 ⑥ `ArrivalPanel.tsx` 신규 — D-day 칩·본인 줄만 조작 가능·노랑 분 입력, 시간 없으면 **입력 유도 배너**(방장에겐 버튼) ⑦ 결과 화면에 지난 모임 블록 + '이 멤버로 재모임' ⑧ 회귀 테스트 5개 | v19 §4-⑩·§4-⑪. **신호등 UI 가 지금까지 `/members` 에만 있었다** — 9단계에서 폐기할 화면이라 결과 화면용으로 새로 만들었다. 가드를 서버에 둔 이유: 화면만 잠그면 폴링 타이밍에 빠져나갈 수 있다(v12 가 서버 거부를 못박은 이유). 자동 아카이브를 읽기 시점에 둔 이유: 크론이 없고 로컬 시연이라 백그라운드 잡을 띄울 수도 없다 — 아무도 안 보는 모임은 전환이 늦어지지만 **보는 순간 정확해진다**. 재모임이 비로그인 멤버를 못 옮기는 건 제약이 아니라 사실이다(신원이 그 사람 기기 localStorage 에만 있다). verify: tsc ✅ · build ✅ · **25/25 ✅** |
| 2026-08-09 | Claude(AI) | `lib/store.ts` 🔒 · `lib/persistence.ts` 🔒 · `app/api/meeting/route.ts` 🔒 · `app/meetings/page.tsx` · `app/m/[code]/sections/ParticipantList.tsx` · `ResultSection.tsx` · `MeetingClient.tsx` 🔒 · `tests/v19-flow.spec.ts` | **v19 구현 5단계 — 참여·인증·방장 권한** ① `recoverParticipant()` — **이름 + PIN 으로 자기 자리 복구**(비로그인 전용, v15) · **5회 실패 잠금**(v16), 성공 시 실패 횟수 리셋 ② `kickParticipant()` — 방장 강퇴(v10): 그 사람의 **핑 몫만** 빼고(마지막 한 명이면 후보 삭제) 본인 표 + 사라진 후보의 표를 정리, **재참여 허용** ③ `deleteMeeting()` — 방장 삭제(v10), `deleteParticipantRow`·`deleteMeetingRow` 신규(FK cascade 로 표까지) ④ 참여 폼을 v19 로: **비밀번호 입력란 → 개인 PIN**, 이름 중복 시 '별칭 vs 내 자리 되찾기' 두 갈래 안내(v4), 카카오 로그인이면 PIN 칸 자체를 숨김(v15) ⑤ 참여자 현황에 **정원 n/8 · 남은 자리** 표시와 방장 '내보내기' 버튼 ⑥ 결과 화면에 방장 '모임 삭제'(위험 구역) ⑦ 회귀 테스트 5개 추가(정원 8명 · 이름 중복 · PIN 복구/잠금 · 강퇴 권한과 표 정리 · 삭제 권한) | v19 §4-⑤·§7 을 코드로 옮겼다. **복구 경로가 없으면 정원만 차고 표가 갈라진다** — 쿠키가 날아간 사람이 다시 참여하면 8명 정원에서 자리를 한 칸 더 먹고, 원래 자기 표는 주인 없이 남는다. 그래서 PIN 복구를 넣었고, 4자리 숫자라 무제한 시도면 남의 자리를 가져갈 수 있어 5회에서 잠근다. 강퇴는 단순 삭제가 아니다 — **병합 핑에서 그 사람 몫만 빼야** 하고(v12), 그 결과 후보가 사라지면 거기 찍힌 **남의 표까지** 정리해야 집계가 안 깨진다. 🔒 `MeetingClient.tsx` 는 콜백 2개(강퇴·삭제 확인 팝업)만 꽂았다. verify: tsc ✅ · build ✅ · **20/20 ✅** |
| 2026-08-09 | Claude(AI) | `tests/v19-flow.spec.ts`(신규) | **v19 규칙 회귀 테스트 11개 추가** — §5 투표 시작 전 표 거부 · 시작 후 핑 잠금 · 지점 후보 0개 시작 불가 / §3 '지역까지' 지점 단계 건너뜀 · '지점도 정하기' 승격은 방장만·역방향 없음 / §4-⑧ 반경 700m 밖 거부 · 확장 1회 · 삭제 권한(방장·본인) / §6 사다리 한 칸씩·표 유지 · 경계 reopen 후 같은 동 재확정 시 지점 후보 복원 / 화면 2개(생성 폼 확정 범위 토글·'지역까지'면 카테고리 숨김·비밀번호란 없음, 지점 등록 화면 미리보기 탭 → 후보 생성) | 기존 스모크 4개는 "핵심 경로가 그려지는가"만 봐서 **v19 로 바꾼 규칙을 거의 안 덮었다** — 확정 범위 분기·게이트·사다리·반경이 전부 무검증이었다. 규칙은 서버가 최종 관문이라(v12) 대부분 API 로 검사하고, 화면이 규칙을 지키는지는 DOM 으로 2건 확인한다. 이제 `npm run verify` 한 줄로 **총 15개**가 돌아, 사람은 UX 판단(투표 시작 단계가 자연스러운가 · 지점 직접 등록이 번거롭지 않은가)에만 집중하면 된다. verify: tsc ✅ · build ✅ · **15/15 ✅** |
| 2026-08-09 | Claude(AI) | `lib/store.ts` 🔒 · `lib/place-rating.ts`(신규) · `lib/flags.ts` 🔒 · `app/api/place-poi/route.ts`(신규) · `app/api/meeting/route.ts` 🔒 · `app/m/[code]/sections/PlacePicker.tsx`(신규) · `app/m/[code]/MeetingClient.tsx` 🔒 · `next.config.mjs` · `tests/smoke.spec.ts` | **v19 구현 4단계 — 지점 단계** ① `distanceM()`·`expandRadius()`(700→1400m·**1회**·누구나·전체 공유, v15)·`addPlaceCandidate()`(전원·상한 없음·**반경 밖 서버 거부**)·`removePlaceCandidate()`(방장 임의/본인 자기 것, **그 후보의 표만** 삭제) ② `GET /api/place-poi` 신규 — 반경 내 미리보기 POI, 목적 카테고리 기본 포커싱(v6)·5종 키워드 매핑·가까운순/별점순·0개면 `canExpand` ③ `PlacePicker.tsx` 신규 — 카테고리 칩·검색·정렬·등록된 후보(삭제 권한별)·미리보기 탭 등록·0개 시 확장 팝업 ④ **별점 스크래핑 복원**(팀 결정 §13-A) — `lib/place-rating.ts`, `NEXT_PUBLIC_FF_PLACE_RATING` 뒤, 실패는 조용히 `rating 0` ⑤ **`confirmRegion` 이 지점 후보를 미리 채우지 않는다** — 사람이 등록해야 후보가 된다 | ⚠️ **빌드가 실제로 깨졌다가 고쳤다**: playwright 를 동적 import 해도 웹팩이 추적해 번들하려 들어 `chromium-bidi`·`kerberos` 미해결로 컴파일 실패 — **PR #54 가 스크래핑을 걷어낸 이유가 정확히 이것**이었다. `next.config.mjs` 의 `serverComponentsExternalPackages` 로 빼서 런타임 `require` 로 남겼다 → 크롬이 없는 팀원 로컬·CI·Vercel 에서도 빌드가 깨지지 않고 try/catch 가 받아 '정보 없음'으로 떨어진다. ⚠️ **후보 미리 채우기를 뺀 이유**: 시스템이 4개를 미리 넣으면 "후보 0개면 투표 시작 불가"(v8) 가 영영 안 걸리고 아무도 등록 안 해도 투표가 열린다. 이 변경으로 스모크의 '거점 확정 후 가게 후보 > 0' 단언이 뒤집혀, **테스트를 v19 흐름(POI 조회 → 탭 등록 → 투표 시작 → 1개면 skip → 확정)으로 다시 썼다.** verify: tsc ✅ · build ✅ · smoke 4/4 ✅ |
| 2026-08-09 | Claude(AI) | `lib/store.ts` 🔒 · `app/api/meeting/route.ts` 🔒 · `app/m/[code]/sections/LeaderBar.tsx` · `tests/smoke.spec.ts` | **v19 구현 3단계 — 단계 규칙(잠금·게이트·사다리)** ① `phaseStepOf()` 신규 — 모임이 선 칸을 `(stage, aiPhase, placeVoteOpen)` 셋으로 한 곳에서 판정(`region-register`/`region-vote`/`place-register`/`place-vote`/`result`) ② `candidateGate()` — 후보 **0개 시작불가 / 1개 투표생략 / 2개+ 투표**(v8) ③ `startVote()` 신규 — 투표 시작 = **후보 잠금**, 미배정 핑 삭제(v9), 1개면 `skipped` 반환 ④ `reopenStep()` 신규 — **사다리 한 칸씩**(확정→투표→후보), 표 유지, 지점→지역 경계에서 지점 데이터 **stash** ⑤ `confirmRegion` 이 **같은 동 재확정이면 stash 복원**(v17) — 복원 시 `setVote` 로 votes 행까지 되돌린다 ⑥ `castVote`·`addRegionCandidate` 에 **늦은 표·핑 서버 거부**(v12) ⑦ 핑 병합: 같은 동은 `contributors` 에 사람만 더하고, **인원당 1개**라 옮기면 옛 후보에서 자기 몫을 뺀다 — 마지막 한 명이 빠지면 후보 삭제(AI 후보는 유지) ⑧ LeaderBar 에 '투표 시작' 버튼(후보 수를 문구로), 되돌리기를 사다리로 교체 | v19 의 두 단계는 **똑같은 모양**(등록→잠금→투표→확정)이라 판정을 한 곳에 모았다 — 화면·API·store 가 각자 조건을 쓰면 "잠겼는데 등록이 되는" 구멍이 생긴다(v12 가 서버 거부를 못박은 이유). ⚠️ **흐름이 실제로 바뀌었다**: 예전엔 등록 화면에서 바로 투표가 됐는데 이제 '투표 시작'을 거쳐야 한다 — 스모크가 옛 흐름을 인코딩하고 있어 **테스트를 새 흐름으로 고쳤다**(가드를 푸는 대신). result 의 '처음부터'(`backToMain`)는 표를 통째로 날려 v19 의 "표는 유지된다"와 어긋나므로 버튼에서 뺐다(함수는 남겨둠). verify: tsc ✅ · build ✅ · smoke 4/4 ✅ |
| 2026-08-09 | Claude(AI) | `lib/store.ts` 🔒 · `lib/flags.ts` 🔒 · `app/api/meeting/route.ts` 🔒 · `app/meetings/page.tsx` · `app/m/[code]/sections/ResultSection.tsx` · `app/m/[code]/MeetingClient.tsx` 🔒 · `docs/설계_v19.md` · `memory/status.md` | **v19 구현 2단계 — 확정 범위 분기축** ① `confirmRegion` 이 `scope` 로 갈린다: `region` 이면 지점 단계를 건너뛰고 `stage=result`·`aiPhase=done` 직행, `place` 면 기존대로 지점 단계 + 반경 700m 리셋·`placeVoteOpen=false` ② `promoteToPlace()` 신규 — 방장 '지점도 정하기' 승격(역방향 없음, v11) ③ API `create` 가 `scope`·`purposeCategory`·`meetTime`·방장 이동수단/카카오id 수용, **비밀번호 필수 검사 제거**(v2), `promoteToPlace` 액션 추가 ④ 생성 폼에 **확정 범위 토글 + 목적 카테고리 5칩**('지역까지' 선택 시 카테고리 숨김, v15), 비밀번호 입력란 제거 ⑤ 결과 화면: `scope==="region"` 이면 경로·지점링크·선입금 전부 숨기고 지역 카드 + 방장 승격 버튼만 ⑥ **'선입금' → '지점 카카오 링크 연결'**(v17) — 옛 모의결제 UI 는 `FLAGS.mockPay`(`NEXT_PUBLIC_FF_MOCK_PAY`) 뒤로 내렸다 ⑦ 별점 팀 결정(스크래핑 복원) 문서 반영 | 분기축이 없으면 v19 의 나머지 규칙이 전부 매달릴 곳이 없다. 옛 모의결제는 **지우지 않고 플래그 뒤로** — 옛 모임 행에 `reservation` 이 남아 있을 수 있어 열어볼 길은 남긴다(기본 꺼짐, 켜도 모의결제임은 그대로). 🔒 소유 파일 사유: `MeetingClient.tsx` 는 승격 버튼 콜백 **한 곳**만 꽂았고(확인 팝업 포함), `route.ts` 는 액션 추가·입력 확장뿐이라 기존 액션 동작은 불변. `flags.ts` 는 상수 하드코딩을 피하려면 여기 말고 둘 곳이 없다(CLAUDE.md §3 — 켜고 끄는 걸 상수로 하지 않는다). verify: tsc ✅ · build ✅ · smoke 4/4 ✅ |
| 2026-08-09 | Claude(AI) | `lib/types.ts` 🔒 · `lib/store.ts` 🔒 · `lib/persistence.ts` 🔒 · `db/schema.sql` 🔒 · `db/migrations/001_v19_설계정렬.sql`(신규) · `db/migrations/README.md` · `app/page.tsx` | **v19 구현 1단계 — 도메인 타입·DB 스키마 정렬** ① 새 타입: `MeetingScope`(전체 분기축) · `PurposeCategory` 5종 + `PURPOSE_LABELS` · `CandidateSource` · `RadiusM` · 상수 `MAX_PARTICIPANTS`(8) · `MAX_PIN_FAILS`(5) ② `Meeting` 에 `scope`·`purposeCategory`·`meetTime`·`placeVoteOpen`·`radiusM`·`stashedPlaces`·`archivedAt` ③ `Participant` 에 `pin`·`pinFails`·`kakaoId`·`lateMin` ④ 후보에 `source`·`contributors`(지역) / `source`·`proposedById`(지점) ⑤ `MeetingState` 에 분기용 필드 + `isPast` ⑥ 폐기 표시(`@deprecated`): `password`·`Reservation`·`depositPerHead`·`reservable` ⑦ `createMeeting` 이 확정 범위·카테고리·모임시간·방장 이동수단/카카오id 를 받고, `joinMeeting` 은 **비밀번호 검사 제거**·정원 8명·이름 중복 거부·PIN 분기 ⑧ 마이그레이션 001 신규 + `schema.sql` 반영 | 순서도 5종이 요구하는 저장 구조를 코드에 올리는 첫 걸음. **아무것도 지우지 않았다** — 폐기 대상은 `@deprecated` 주석과 컬럼 유지로 두고 화면에서 내리는 걸 먼저 한다(돌아가는 데모를 깨지 않기 위해). 옛 행 호환: 읽기는 컬럼이 없으면 **새 모임과 같은 기본값**(`place`·700m·투표 안 열림)으로 흡수한다. 🔒 소유 파일 4개를 만진 사유: 이 4개가 **도메인 타입·저장 계층 그 자체**라 v19 를 구현하려면 우회로가 없다. 대신 시그니처는 **전부 하위호환**(새 입력은 optional, 새 필드는 기본값)이라 기존 호출부는 한 곳(`app/page.tsx` 의 가짜 참가자 객체)만 고쳤다. `normalizeMeetTime()` 이 **과거 시간을 null 로 떨어뜨린다**(v16) — 미입력이면 결과 화면이 배너로 알리므로 조용히 사라지지 않는다. verify: tsc ✅ · build ✅ · smoke 4/4 ✅ |
| 2026-08-09 | Claude(AI) | `db/migrations/README.md` | **마이그레이션 미적용 시 동작을 정정** — 초안에 "안 돌려도 앱은 뜨고 값만 기본값으로 돌아간다"고 썼으나 **사실이 아니다.** 쓰기 경로(`insert … scope, meet_time …`)는 컬럼이 없으면 SQL 오류가 나고 `saveMeeting` 이 그대로 던져 **모임 생성이 실패**한다. 읽기만 흡수한다 | 조용히 반쪽으로 도는 것보다 바로 터지는 쪽이 낫다고 판단해 코드는 그대로 두고 **문서를 사실에 맞췄다**. 반대로 적어두면 팀원이 마이그레이션을 건너뛰고 시연에 들어가 발표 중에 발견한다 |
| 2026-08-09 | Claude(AI) | `CLAUDE.md` 🔒 · `memory/status.md` | **새 기준 문서로 가는 길 뚫기** — `CLAUDE.md` 머리말에 "⭐ 지금 무엇을 만드는가 = `docs/설계_v19.md`" 블록 추가(이 파일=*어떻게* / 설계_v19=*무엇을*, 충돌 시 설계_v19 우선), §7 문서표에 설계_v19·설계도·발표_로컬시연 3행 추가. `status.md` 는 v19 구현 국면 스냅샷을 최상단에 신설(브랜치·기준문서·9단계 진행표·팀 결정 대기 1건·발표 전 카카오 등록 경고) | 🔒 `CLAUDE.md` 는 소유 파일이라 사유를 남긴다: **이 파일이 세션 시작 시 가장 먼저 읽히는 유일한 파일**이라, 여기에 링크가 없으면 다음 세션·팀원은 `설계_v19.md` 의 존재를 영영 모른 채 옛 서술(`가게`·`선입금`)대로 구현한다 — 이번에 고치려는 실패 모드 그 자체다. **기존 규칙(§1~§6)은 한 줄도 수정하지 않고** 머리말 블록과 문서표 3행만 더했다 |
| 2026-08-09 | Claude(AI) | `docs/발표_로컬시연.md`(신규) · `package.json` 🔒 · `.env.example` · `lib/env.ts` | **LAN 시연 구조 정비** — ① 런북 신규(구조도 · IP 확인 · 카카오 콘솔 등록 2곳 · 방화벽 · 비보안 출처 금지 목록 · DB 선택 · 대비책 3안 · 당일 체크리스트 · 증상별 조치표) ② `dev:lan` 스크립트 추가(`next dev -H 0.0.0.0 -p 3100`) ③ `KAKAO_REDIRECT_URI` 기본값·예시를 **3000 → 3100** 정정 | 발표 목표가 "로컬호스트로 열고 IP 로 팀원 각 기기 접속"으로 정해졌다. 실측 확인: 초대 링크는 이미 `location.origin` 기반이라 IP 접속에서 자동으로 맞고(`ResultSection:157`·`meetings/page:115`·`calendar:41`), `geolocation` 은 **코드에 아예 없어** HTTP 비보안 출처 제약을 안 받는다. 반면 **카카오맵 SDK 사이트 도메인**과 **OAuth Redirect URI** 는 IP:포트 등록이 필수다(v14 가 이미 경고한 항목). 포트 기본값이 3000 이라 `.env.local` 을 안 채우면 로그인이 `redirect_uri` 불일치로 실패했다 — dev 포트는 3100 이다. 🔒 `package.json` 은 소유 파일이라 사유를 남긴다: 기존 `dev` 를 **바꾸지 않고 별도 스크립트를 추가**만 했으므로 팀원 로컬 동작은 그대로다 |
| 2026-08-06 | Claude(AI) | `lib/geo.ts` · `next.config.mjs` | **mock 가게의 가짜 별점 제거** — `PLACE_SETS` 의 `rating` 5개(`4.5`·`4.7`·`4.3`·`4.6`·`4.4`)를 전부 **`0`**(= 정보 없음)으로. 함께 `next.config.mjs` 의 연속 빈 줄 정리(PR #54 잔재) | `lib/types.ts:66` 이 `0 = 정보 없음` 이라고 규약을 정해뒀고 **실 경로는 전부 지킨다**(카카오 로컬 `routing.ts:385` · `ai.ts:331` · `ai-vote/api-kakao.ts:59` 모두 0 — 공식 로컬 API 엔 평점이 없다). **mock 만 어겨서 역전이 생겼다**: 키를 넣으면 별점이 사라지고, 키가 없으면 그럴듯한 4.3~4.7 이 뜬다 — **존재하지 않는 가게가 실제 가게보다 더 실제처럼 보인다**(CLAUDE.md §3-6 위반). 실측 확인: 시드 모임의 `places` 4곳 `rating` 전부 0, `ai-vote` 플래그 경로도 `live:false·degraded:true` 와 함께 전부 0. **UI 는 한 줄도 안 고쳤다** — `MeetingClient:742`·`ResultSection:46`·`ManualPickModal:46`·`ai.ts:417` 이 이미 `rating > 0` 가드를 갖고 있어 0 이면 별이 저절로 사라진다 |
| 2026-08-06 | Claude(AI) | `lib/ai-vote/api-kakao.ts`(신규) · `web-kakao.ts`(삭제) · `region.ts` · `place.ts` · `index.ts` · `next.config.mjs` | **배포급 전환: 카카오맵 웹 크롤링 → 카카오 공식 API** — 검색은 로컬 키워드 API, 경로는 대중교통(publictraffic)·자동차(모빌리티)·도보 공식 API. playwright 의존 완전 제거(next.config 제외줄도 삭제), 도간 50km+ ODsay 분기는 유지 | PR #54 지적(Vercel 서버리스에 크롬 없음) 대응. 요금 검증: 대중교통·도보 10원/건(각 일 1,000건 무료)·자동차 8원/건(일 1만 건 무료) — 현재 규모(하루 ~41모임)면 0원. 로컬 실측: 상권 임계 12 유지(옥천 0·화천 2 차단, 서울역 45 통과), 지역 27.1초·가게 18.6초, 공덕 축 픽 동일, url 은 공식 place_url. 잃는 것: 별점(공식 API 미제공 → rating 0, 기존 제품 관행). 비공식 엔드포인트 리스크 소멸 |
| 2026-08-06 | Claude(AI) | `app/page.tsx` · `lib/hub-pool.ts` | **🔴 #52 가 절반만 고쳐져 있었다 — 거리순이 여전히 하드코딩 후보를 썼다.** CEO 실측(develop Preview, 노원+의정부): **시간순 `장암역`(실제 역) · 거리순 `종로3가`(하드코딩)** — 같은 화면의 두 버튼이 서로 다른 세계를 보고 있었다. 원인: `app/page.tsx:371` 이 거리순일 때 **`/api/midpoint` 를 아예 안 부르고** 브라우저에서 `recommendRegions(pseudo)` 를 직접 계산했다. 브라우저엔 카카오 REST 키가 없으니 무조건 하드코딩 폴백이다. #52 는 `/api/midpoint` **안에서** 두 모드가 후보를 공유하게 했는데 **거리순이 그 API 를 타지 않았다.** → 거리순도 `mode:"dist"` 로 서버를 거치게 했다(이동시간 API 는 여전히 안 부른다 — 늘어나는 건 후보 검색 카카오 1콜뿐). 함께: ① **솎아내기 임계값을 반경 비례로**(고정 1.5km → `max(0.5km, 반경×0.2)`) ② 후보 **10분 프로세스 캐시**(모드 토글 시 재호출 방지, 실 API 성공값만) ③ 로딩 문구를 모드별로(`경로 계산 중…` / **`중간지점 찾는 중…`**) | ①은 **첫 실사용에서 바로 드러났다**: 고정 1.5km 였을 때 노원+의정부에서 **도봉산역이 잘렸다** — 장암역이 중심에서 431m 로 먼저 채택되고 713m 떨어진 도봉산역이 제거됐다. 그런데 **도봉산역은 1·7호선 환승역**이라 의정부(1호선)에서 직통이고 장암역은 7호선 종점이라 환승이 필요하다 — **직선거리는 노선을 모른다.** 그렇다고 고정 0.6km 로 낮추면 반대쪽이 깨진다(강남↔역삼 841m · 역삼↔선릉 1,185m 라 **거의 안 걸러져** "뭐가 다른데?" 재발). 반경 비례면 둘 다 잡힌다 — 실측: 노원+의정부 반경 2,813m→임계 563m **장암·도봉산 둘 다 남음** / 8인 광역 반경 17,192m→임계 3,438m **강남 인근 5역이 강남역 하나로**. ②는 거리순이 서버를 타게 되면서 **토글할 때마다 카카오를 다시 부르는 낭비**가 생겨 넣었다. 하드코딩 폴백은 캐시하지 않는다 — 담으면 키를 나중에 넣어도 TTL 동안 계속 폴백이 나온다. ③은 거리순이 **경로를 계산하지 않기** 때문이다(후보를 찾고 직선거리로 잴 뿐) — 같은 문구를 쓰면 안 하는 일을 한다고 말하는 셈이다(CLAUDE.md §6). ⚠️ **교훈: `/api/*` 를 고쳤으면 호출부를 확인해야 한다.** #52 PR 본문에 *"두 모드가 같은 후보를 쓴다"* 고 적었는데 사실이 아니었다 — API 만 보고 화면 코드를 안 봤다 |
| 2026-08-06 | Claude(AI) | `lib/hub-pool.ts` **(신규)** · `lib/kakao.ts` · `lib/routing.ts` · `lib/geo.ts` · `app/api/midpoint/route.ts` | **🔴 중간지점 후보를 하드코딩 28곳 → 카카오 실제 지하철역으로.** 실측 근거: **노원역+의정부역 → `종로3가`**(중심에서 14.8km, 최대 98분). 중심 반경 안에 후보가 **0곳**이라 폴백이 "가까운 순 4곳"을 끌어왔는데 그게 전부 15km 남쪽이었다 — **둘 다에게서 멀어지는 방향.** 실제 정답은 **도봉산역**(중심에서 0.9km, 최대 32분). 원인은 `CANDIDATE_HUBS` 에 **서울 북부가 통째로 비어 있던 것**(노원·창동·수유·도봉…). 그런데 실제 역을 찾는 `searchByCategoryKakao("SW8", …)` 는 **이미 있었다** — `/api/places` 의 `정류장·역` 탭이 쓰고 있었고 중간지점에만 안 쓰였다. **폴백 사슬**: ① 카카오 SW8 실제 역 → ② `CANDIDATE_HUBS` 28곳(지하철 없는 지방·키 없음·API 실패) → ③ `geometricCandidates` 좌표. 함께 넣은 3가지: **(가) 기하 중앙값**(Weiszfeld) — 무게중심은 멀리 있는 한 명에게 끌려간다(실측: 서울 7명+부산 1명 → 무게중심이 **서울에서 41km 남쪽 충북 산속**, 기하 중앙값은 **2km**) **(나) 적응형 반경** — `2km` 고정이던 것을 퍼짐×0.6(2~20km, 카카오 상한)으로. 3곳 미만이면 반경 2배로 1회 재시도 **(다) 1.5km 중복 솎아내기** — 강남 중심이면 반경 안에 역이 10개 넘게 나오는데 전부 걸어갈 거리라 후보로 의미가 없다(실측: 강남·역삼·선릉·교대·양재 5역 → 강남·선릉·양재 3역). 시간순·거리순이 **같은 후보**를 쓰도록 `/api/midpoint` 가 한 번만 구해 양쪽에 넘긴다 | CEO 지적 — *"이런식으로 지도에서 보고 찾아내는 게 아니라 하드코딩된 장소로 안내하면 이걸 왜 쓰냐"*. 타당한 지적이었고, **큐레이션 목록은 다음 빈 구멍에서 똑같이 터진다**(경기 북부·인천 서부·강원 등 확인됨). 다만 하드코딩을 **지우지 않고 역할만 바꿨다** — SW8 은 **지하철역만** 주므로 전주·목포·창원·강릉에선 0건이다. ⚠️ **거리순도 함께 고쳤다** — CEO 가 신고한 종로3가 케이스가 **거리순(`lib/geo.ts` 동기 경로)** 이었다. 이 함수는 브라우저에서도 도는 동기 함수라 카카오를 직접 못 불러, `/api/midpoint` 가 후보를 미리 구해 넘기는 형태로 열었다. **두 모드가 같은 후보를 써야 한다** — 토글은 *같은 후보의 다른 계산*이어야지 목록 자체가 바뀌면 무엇이 달라졌는지 알 수 없다. 💰 **카카오 1콜**(재시도 시 2콜)이 늘지만, 후보가 13곳 → 5곳으로 줄어 **이동시간 계산이 8×13=104회 → 8×5=40회**로 준다 — ODsay 키를 넣었을 때 콜 수와 속도 게이트 대기가 그만큼 줄어 **오히려 이득**이다. ⚠️ **미검증**: 실제 카카오 SW8 호출은 컨테이너가 `dapi.kakao.com` 을 차단해 못 했다. 대신 (1) 순수 함수 4종 단위 확인 (2) 카카오 응답을 흉내 내 솎아내기→채점 전 구간 확인(도봉산역 **최대 32분** 도출) (3) **키 0개일 때 예전과 동일한 결과**(종로3가 98/40) 회귀 확인을 했다. 실 키 확인은 사람이 해야 한다 |
| 2026-08-06 | Claude(AI) | `app/components/KakaoMap.tsx` · `app/page.tsx` | **지도 실패 안내가 원인을 구분하도록.** `KAKAO_JS_KEY_SET` 상수를 내보내 홈 화면 폴백 문구를 둘로 나눴다 — ① 키가 이 빌드에 없음 → *"환경변수를 넣고 **다시 배포**하면 표시됩니다"* ② 키는 있는데 SDK 로드 실패 → *"이 주소가 카카오 개발자 콘솔의 **사이트 도메인**에 등록됐는지 확인해주세요"* | `loadSdk()` 는 **키 없음**과 **도메인 미등록·차단**을 똑같이 `false` 로 돌려주는데 화면은 한 가지 문구(`키 설정 후 표시됩니다`)만 썼다. Preview 에 지도를 띄우려면 **환경변수(Preview 스코프 + 재배포)** 와 **카카오 도메인 등록** 두 가지를 해야 하는데, 지금 문구로는 키를 넣고 재배포한 뒤에도 **같은 안내가 떠서 "키가 안 들어갔나?" 하고 엉뚱한 데를 보게 된다**(2026-08-06 Preview 실측). ⚠️ `NEXT_PUBLIC_*` 는 **빌드 시점에 치환**되므로 이 값은 "지금 환경변수가 있나"가 아니라 **"이 번들을 만들 때 있었나"** 다 — 그래서 ①의 문구에 "다시 배포"를 명시했다. 두 분기가 실제로 번들에 들어가는 것을 더미 키로 대조 빌드해 확인했다 |
| 2026-08-06 | Claude(AI) | `app/api/route-detail/route.ts` · `app/components/RouteSheet.tsx` · `app/m/[code]/sections/TravelTimes.tsx` · `app/page.tsx` | **🔴 어제 붙인 카카오맵 링크가 실제로는 동작하지 않았다 — 정정.** ① `?sName=출발&eName=도착`(이름만) 형식을 카카오맵이 **그대로 무시**해서 출발지·도착지가 **빈 채로** 길찾기 화면만 열렸다(CEO 실측 스크린샷). 공식 URL 스킴은 **좌표를 요구**한다 → `map.kakao.com/link/to/{이름},{위도},{경도}`. `RouteDetailResponse` 에 `to: {lat,lng}` 를 추가해 실어 보내고(캐시 분기에서도 **이번 요청 좌표로 덮는다** — 캐시 키가 소수 4자리 반올림이라 미세하게 다를 수 있다), 버튼 문구도 `카카오맵에서 ‘{목적지}’ 으로 길찾기` 로 바꿨다 ② 🔴 **숫자가 없는데 출처를 주장하던 것** — `perParticipant` 에 없는 참가자가 모임 상세에서 `거리 추정  —`, 홈에서 `상태 미입력` 으로 떴다. 각각 **`이동시간 없음`** 으로 통일하고 모임 상세는 숫자 자리도 비웠다 | ①은 **내가 어제 미검증인 채로 넣은 것이 실제로 틀렸음이 확인된 사례**다. PR #49 본문에 *"이 URL 형식은 눌러볼 수 없어 미검증 — 안 열리면 되돌리는 게 맞다"* 라고 적어뒀고, CEO 가 눌러 **정확히 그 경우**임을 확인했다. 이번엔 공식 스킴을 문서 예시(`link/to/카카오판교오피스,37.402056,127.108212`)로 대조했으나 **여전히 클릭 검증은 사람 몫**이다(컨테이너가 `*.kakao.com` 차단). ✅ 이 스킴은 **도착지만** 지정하는데 **그게 의도다** — CEO 결정: *"출발지는 굳이 등록하지 말고 도착지만 찾게 해. 어차피 카카오맵 안에서 내 위치 찾아주잖아."* 우리가 가진 출발지는 `압구정역` 같은 **역 이름**이지 사용자가 지금 있는 자리가 아니라, 카카오맵이 현위치를 잡는 편이 더 정확하다. 문구도 그래서 "목적지로 길찾기"까지만 한다(CLAUDE.md §6). **미완성이 아니므로 다음 세션이 출발지를 다시 넣지 않도록 주석에 명시했다.** ②의 원인은 **거점 후보를 계산한 뒤에 출발지를 등록하면 `perParticipant` 에 안 들어가고, 확정 후에는 `setRegionCandidates` 가 후보를 갱신하지 않아 영영 빈다**는 것 — 2인 모임으로 재현했다(B만 출발지 → `regions` → A 출발지 → 확정 → `perParticipant` 에 B 하나). CEO 스크린샷의 `16분` 과 재현값이 일치한다. ⚠️ **재계산 정책(확정된 후보를 다시 계산할 것인가 · 💰 호출 증가)은 팀 판단 사항**이라 이번엔 표시만 바로잡았다. `상태 미입력` 은 원래 "본인이 도착 상태를 안 남겼다"(`p.status`)는 **다른 뜻**이라 특히 오해를 만들었다 |
| 2026-08-06 | Claude(AI) | `app/page.tsx` · `app/components/RouteSheet.tsx` | **홈 지도 아래 줄에 출처 표시 + 경로 상세에 카카오맵 길찾기 링크.** ① 🔴 **홈에서 모임을 고르면 지도 아래 줄이 출처를 한 글자도 안 말했다** — `📍 중간 추천 지역: 종로3가 · 가장 균형적 — 최대 52분 · 편차 3분`. 바로 아래 확정 장소 카드는 `거리 추정 52분` 이라고 밝히는데도. 같은 파일의 `midpoint` 분기(모임 미선택)에는 `전원 경로 기준`/`일부 거리 추정` 이 붙어 있었고, **모임을 고른 분기에만 빠져 있었다.** `winnerRegion.perParticipant` 로 접어서 같은 두 단어를 붙였다 ② 경로 상세의 `!live` 경고 박스에 **`🗺 카카오맵에서 길찾기 열기` 링크** 추가 (`map.kakao.com/?sName=&eName=`) | ①은 **그 코드 바로 아래 주석이 세운 원칙을 그 자신이 안 지킨 것**이다 — *"거리순은 분 단위를 그대로 보여주면서 출처를 한 글자도 말하지 않았다… 오히려 반드시 밝혀야 하는 쪽이다"*. 접는 규칙은 `lib/types.ts` 가 정한 `perParticipant.every(x => x.real === true)` 를 그대로 쓰고 **`undefined` 는 참으로 치지 않는다**(모르면 실값이라고 주장하지 않는다, CLAUDE.md §6). 후보가 빈 배열이면 `every` 가 참이라 길이도 함께 본다. ②는 CEO 지적에서 나왔다 — ODsay 키가 없으면 `route-detail` 이 `live:false`·`transit:undefined` 를 주고 `RouteSheet` 가 `data.live && …` 로 구간을 아예 안 그려서, 탭하면 **경고문만 남아 "고장난 듯" 보인다.** 설계대로 동작하는 것이지만 *"카카오맵·네이버지도에서 확인해주세요"* 라고 해놓고 **링크가 없어** 사용자가 출발지·도착지를 다시 타이핑해야 했다 — 그 두 값은 우리가 이미 갖고 있다. ⚠️ 수단(대중교통/자차)은 URL 에 지정하지 않는다. 우리가 고른 수단으로 열린다고 말할 수 없어서 문구도 "길찾기 열기"까지만 한다. ⚠️ **이 URL 형식은 컨테이너에서 눌러볼 수 없어 미검증** — `*.kakao.com` 이 차단된다. 사람이 실제로 클릭해 확인해야 한다 |
| 2026-08-06 | Claude(AI) | `app/page.tsx` · `app/m/[code]/sections/ResultSection.tsx` · `app/m/[code]/sections/ReserveModal.tsx` · 🔒 `lib/store.ts` | **CEO 가 Preview 에서 새 모임으로 확인하다 찾은 3건 수정.** ① 🔴 **같은 화면이 중간지점을 두 개로 말했다** — 지도 자리 `중간 추천: 왕십리` · 바로 아래 카드 `중간 추천 지역: 교대`(확정한 쪽). `app/page.tsx` 의 지도 실패 폴백이 `midpoint.name`(홈이 자체 계산한 값)을 직접 읽고 있었다 → `center` 에 `name` 을 실어 **핀과 폴백이 같은 값**을 쓰게 했다 ② 🔴 **선입금 인원을 모임 정원 → 실제 참여자 수로** (`state.headcount` → `state.totalParticipants`, `reserve()` 는 `m.participants.length`). **미리보기·모달·저장 3곳을 함께** 고쳤다 ③ 🟡 `선입금 (모의)` 라벨이 `선입금 (모` / `의)` 로 줄바꿈되던 것 — `(모의)` 를 값 쪽으로 옮겼다 ④ 결과 화면 링크 이름을 `🗺️ 카카오맵에서 보기` → **`🍽 메뉴 · 사진 · 리뷰 보기`** (링크 주소·동작은 그대로) | ①은 **지도가 정상이면 안 보이는 자리**라 지금까지 안 잡혔다 — Preview 에 `NEXT_PUBLIC_KAKAO_JS_KEY` 가 없어 폴백이 뜨면서 드러났다. 확정한 거점과 다른 이름을 말하는 건 이번에 고친 `실시간` 배지와 같은 종류의 문제다(화면이 사실과 다른 것을 말한다, CLAUDE.md §6). ②는 정원 8명·참여 2명 모임에서 화면 위쪽 `참여자 2명` 과 결제 줄 `× 8명 / 합계 80,000원` 이 나란히 떠서 **오류로 읽혔다.** 정원 기준도 해석은 가능했으나 **CEO 가 참여자 수 기준으로 결정**했다. ⚠️ **3곳을 함께 고쳐야 한다** — 하나만 바꾸면 "미리보기 20,000원 → 결제하니 80,000원"이 된다. `lib/store.ts` 는 🔒 통합 담당자 소유인데 **저장값이 어긋나면 그 자체가 버그**라 같은 PR 에 넣었다(변경은 `reserve()` 안 2줄). ③은 `.kv .k` 가 72px 고정폭이라 생긴 것으로, 전역 CSS(`app/globals.css`, 🔒)를 건드리면 앱의 모든 `.kv` 에 영향이 가므로 **문구 배치로만 해결**했다. ④는 CEO 지적 — *"메뉴만 보고 싶은 사람들이 있을 텐데"*. `카카오맵에서 보기` 는 **그 안에 무엇이 있는지 안 말해줘서** 잘 안 눌린다. ⚠️ **`예약하기` 로는 만들지 않았다**: 카카오 로컬 응답에 예약 가능 여부도 메뉴도 전화번호도 없다(`lib/kakao.ts` 반환은 `name·category·path·distanceM·lat·lng·url` 뿐). 예약을 안 받는 가게에 `예약하기` 를 달면 **확인 안 된 것을 확인된 것처럼 말하는** 셈이라 방금 고친 `실시간` 배지와 같은 문제를 새로 만든다(CLAUDE.md §6). 메뉴·사진·리뷰는 그 페이지에 실제로 있는 것이라 이름으로 써도 된다. **버튼 순서 변경(주 액션 승격)은 CEO 판단으로 넣지 않았다 — 이름만 바꿨다** |
| 2026-08-06 | Claude(AI) | `memory/status.md` | **PR #46 을 `develop` 에 머지 — merge commit `d76c48b`** (main 대비 27→**37커밋**). 머리말 스냅샷의 `develop` 해시·커밋 수 갱신 · "신뢰도 결함 1건" 행을 **머지 완료**로 · "머지 전까지 프로덕션은 그대로다" → **"`main` 릴리스 전까지"** 로 범위 정정(수정은 `develop` 에만 들어갔고 `main` `5862751` 은 여전히 옛 코드다) · "이어서 할 일(세션 중단 지점)" 블록을 **머지 기록**으로 전환 · 🟡 알고 있어야 할 것 3건 신설(**옛 모임은 `real` 을 못 얻는다** — `setRegionCandidates` 가 확정 모임은 갱신 안 하고 이름이 같으면 쓰기를 생략한다 / `lib/routing.ts` 393줄 / `app/page.tsx` 1,134줄) | **squash 하지 않고 merge commit 으로 머지했다** — 커밋 7개의 주제가 검증 리포트·배지 수정·문구 통일·문서 정합성으로 서로 다르고, 특히 `cfa7978`·`5abc5f0` 은 **"실측하지 않은 숫자를 근거처럼 쓴 것을 정정한 이력"** 이라 그 자체가 기록 가치가 있다(`status.md` 의 선례와 같은 상황). 머지 전 실측 확인: `develop` 이 분기 이후 **0커밋**이라 충돌 없음 · CI **4/4 green** · `mergeable_state: clean` · 🔒 소유 파일 3개(`lib/types.ts`·`lib/scoring/types.ts`·`lib/store.ts`)는 **선택 필드 `real?: boolean` 추가와 주석뿐 로직 0줄** · `db/`·`package.json`·`app/api/meeting/route.ts`·`lib/flags.ts`·`lib/scoring/index.ts` **무변경** · DB 마이그레이션 **0건**(`perParticipant` 는 이미 `meetings.regions` jsonb 안). ⚠️ **옛 모임 항목을 남긴 이유**: 지금은 프로덕션이 `odsay: false` 라 "전부 거리 추정"이 정확하지만, **Preview 확인을 옛 모임으로 하면 수정이 안 된 것처럼 보인다** — 확인 절차가 틀리는 걸 막아야 한다. ⚠️ **`main` 릴리스는 하지 않았다** — 프로덕션에 ODsay 키를 넣으면 `regions` 가 1.55초 → 약 14초(실측 13.87초, 4인 콜드)라 **팀원의 속도 게이트 수정 PR 이 먼저다** |
| 2026-08-06 | Claude(AI) | `memory/status.md` · `docs/검증_실키_2026-08-05.md` · `docs/검증_develop_2026-08-05.md` | **문구 통일 이후의 문서 정합성 정리** — 감사가 확정한 어긋남 7곳. 원칙은 **"과거 실측 기록은 그대로 두고, 현재형 서술과 사람이 따라 하는 절차만 고친다"**. ① `status.md` ⚡스냅샷의 결함 행을 ✅해결(머지 대기)로 바꾸고 **"프로덕션은 옛 코드라 아직 `실시간` 이 붙어 있다"** 행 추가 ② 🔴 **§ 종단 확인 절차의 기대 문자열 교체** — `실 이동시간 기준`/`거리 추정으로 계산` 은 앱에서 사라졌다. 그대로 두면 **터널이 정상인데도 "실패"로 오판**한다(옛 배포용 문구도 병기) ③ § 후속 7 의 *"목록 화면의 실시간 칩은 손대지 않았다 … 문구는 팀 판단"* 을 취소선 처리 + 해소 근거(멈춘 이유였던 딜레마는 **판정 단위를 목록→사람으로 내리면서 사라졌다**) ④ "화면은 실시간이라 말한다"류 현재형 4곳을 **"라벨을 고쳐도 남는 결함"**(ODsay 응답 자체가 틀림)으로 정정 ⑤ 실키 리포트에 **§4-2 신설** — 문구 통일 근거(요청 파라미터에 시각 없음)·감사 7건·**화면 투어 7장 링크** ⑥ 감사 잔여 4건을 `status.md` §3-0-A 와 리포트 §5 에 정식 기록 ⑦ `검증_develop` 머리말에 안내 배너 + §4 표에 1·2·8·9 해결 표기 — **1번은 "여기 적은 처방과 다르게 갔다"**(목록 단위 `live` → 참가자 단위 `real`)는 이유까지 남겼다 | 이 저장소가 사고 사례로 기록한 *"문서가 코드와 어긋나면 다음 세션이 잘못된 전제로 시작한다"* 를 그대로 밟고 있었다. 특히 ②는 **사람이 따라 하는 체크리스트**라 위험도가 가장 높았다. ⑦의 "처방과 다르게 간 이유"를 남긴 것은, 그 처방(목록 단위 `live`)을 그대로 따르면 **실값인 참가자까지 추정으로 깎이기** 때문이다 — 실키 실측이 한 목록에 실값·추정이 섞이는 걸 확인했다 |
| 2026-08-05 | Claude(AI) | `app/m/[code]/sections/TravelTimes.tsx` · `app/m/[code]/sections/MapPanel.tsx` · `app/components/RouteSheet.tsx` · `app/page.tsx` · `lib/routing.ts` | **이동시간 출처 문구를 앱 전체에서 `경로 기준` / `거리 추정` 두 단어로 통일.** 사라진 표기: `실시간` · `ODsay 실시간` · `TMAP 실시간` · `실 이동시간 기준` · `거리 추정으로 계산` · `추정값` · `직선 근사`. 곁들여 고친 것 — ① **자차 툴팁 분기**(같은 목록에 자차·대중교통이 섞이는데 `isReal` 은 수단을 안 가려, TMAP 실값 행에도 "지하철·버스·열차" 툴팁이 붙었다. 경로 상세 시트는 같은 값을 "지금 출발 기준"이라 밝혀 **이번에 고치려던 모순을 새 문구가 재생산**하고 있었다) ② **"노선 시간표 기준" 주장 철회** — "실시간"을 뺀 근거가 *"확인 안 한 것은 말하지 않는다"* 였는데 그 자리에 역시 미확인 주장을 넣은 꼴이라 `실제 노선으로 계산` 까지만 쓴다 ③ **홈 거리순에도 출처 표시** — 예전엔 `criteria === "time"` 안에만 있어, 거리순으로 보면 "최대 42분 · 편차 12분"을 보여주면서 출처를 한 글자도 안 밝혔다(거리순은 정의상 100% 직선거리 추정이라 오히려 밝혀야 할 쪽) ④ **홈은 범위를 붙여 `전원 경로 기준` / `일부 거리 추정`** — 홈 `midLive` 는 목록 전체 판정인데 상세 칩은 사람 단위라, 같은 두 단어가 층이 다른 것을 가리켜 서로 반대 사실처럼 읽혔다 ⑤ **홈 결과 카드의 `예상 N분` 에도 출처** ⑥ 🔴 `travelMinutesEx` 가 **`verified === false`(껍데기 응답)를 `real: true` 로 올리던 것 차단** — 진단 모드에서 목록은 `경로 기준`, 시트는 "⚠ 검증 안 된 원시 응답" 으로 갈렸다 ⑦ 지도 폴백 `직선 근사` → `거리 추정`(세 번째 단어 제거) | **CEO 지적**(2026-08-05): *"곧 모임이 진행되는 상황이 아니라면 실시간 걸리는 시간을 추출할 필요가 없다."* 확인해 보니 코드가 그걸 증명한다 — **우리는 ODsay·TMAP 요청 어디에도 시각을 보내지 않는다**(`lib/odsay.ts` 는 `SX/SY/EX/EY/apiKey` 뿐, `lib/tmap.ts` 는 `startX/startY/endX/endY/reqCoordType/resCoordType/searchOption` 뿐). 게다가 방장이 입력한 모임 시각(`prefs.timeText`·`dateIso`)은 이동시간 계산에 **한 번도 들어가지 않는다**(`routing`·`odsay`·`tmap`·`geo`·`scoring` 전수 grep 0건). 즉 `실시간` 은 값이 추정일 때만 틀린 게 아니라 **실 API 값일 때도 틀린 이름**이었다. 앱은 이미 그걸 알고 있었다 — 같은 시트가 본문에선 "시간표 기준"이라 적고 있었다. ①~⑦ 은 **46-에이전트 병렬 감사**(4관점 지적 42건 → 반증 검증 31건 확정)가 잡아낸 것이고, 그중 ①은 **이 변경이 스스로 만든 새 모순**이었다 |
| 2026-08-05 | Claude(AI) | `docs/검증_실키_2026-08-05.md` **(신규)** · `docs/img/verify-live/*` **(신규 5장)** · `memory/status.md` | **실키 검증 리포트 — 💰 ODsay 약 144콜 · 카카오 약 15콜 · TMAP 약 16콜.** 화이트리스트 IP 노트북에서 실제 키로 mock 세션 발견 9건을 재판정했다(✅재현/❌안남/⚠️다름/⏸미검증). 🔴 **A(`실시간` 배지) = 확정 결함**: 자월도 참가자만 ODsay `code 3` 로 실패해 **한 목록에 실값 3건(31·67·29분)과 추정 1건(288분)이 섞였는데 넷 다 `실시간`** 이었다. **프로덕션(`odsay: false`)에서도 재현** — `실시간 34분`/`58분` 둘 다 추정값이고, 같은 화면 상세 시트는 `추정값` 이라 밝혀 모순이다(스크린샷). 🔴 **C = 실키 13.87초**(mock 11.29초보다 느림. 4인×후보12=48쌍×250ms 게이트). 🟡 **E = 다르게 남** — 제주공항·포항시청은 실키가 고치지만 카카오 0건 입력(`asdfqwer`·오타 주소)은 해시 서울좌표 그대로. 🟡 **D = 재현** + 선입금 단가가 카테고리가 아니라 검색 쿼리 순번을 따름. ⏸ **F Preview = 미검증**(Vercel SSO 차단). **B 대조표(발표용)**: 앱 추정값이 실값의 **1.50~11.47배**(서울↔부산 138분 vs 1,583분). 🔴 **새 발견 5건** — ①ODsay 가 서울↔대전에 KTX 를 안 주고 251분 버스 경로를 준다(자차 170분보다 81분 느린데 `verified: true`) ②ODsay 실패는 캐시 안 돼 못 푸는 참가자 1명이 화면 열 때마다 후보 수만큼 호출(4인 모임 8회 = 96콜) ③선입금 단가 근거 불일치 ④접근시간 보정 약 1.7배 과대(73분의 정체를 계산으로 확정) ⑤Preview 가 SSO 로 잠김 | 실키 검증 세션 지시 — *"mock 에서 본 흠이 실키에서도 그런가"*. 이전 세션 컨테이너가 `api.odsay.com` 으로 못 나갔고 ODsay 서버 키는 호출 IP 화이트리스트를 요구해, **등록된 IP 를 쓰는 노트북**에서만 가릴 수 있었다. ⚠️ `memory/status.md` §3-3 의 *"대전 · trafficType 2 · 237분 → 이미 정상 동작"* 판정을 정정한다 — 파싱은 정상이지만 **값이 틀렸다** |
| 2026-08-05 | Claude(AI) | 🔒 `lib/types.ts` · 🔒 `lib/store.ts` · 🔒 `lib/scoring/types.ts` · `lib/routing.ts` · `lib/geo.ts` · `lib/identity.ts` · `app/m/[code]/sections/TravelTimes.tsx` · `app/m/[code]/sections/ResultSection.tsx` | 🔴 **`실시간` 배지를 실제 출처에 연결 — 목록 단위가 아니라 사람 단위로.** `RegionCandidate.perParticipant` 와 `TravelEntry` 에 **`real?: boolean`** 추가(`travelMinutesEx` 가 이미 돌려주던 값이라 **없던 정보를 만든 게 아니라 버리던 정보를 살렸다**). `perParticipant` 가 `meetings.regions` jsonb 안이라 **DB 마이그레이션 0건**. `real === undefined`(옛 데이터)는 **`실시간` 이라 주장하지 않는다.** 곁들여 ② **추정 신호등 분리** — 자가신고는 채운 칩 `● 정시권/지체/많이 늦음`, 추정은 테두리 칩 `~ 가까움/조금 멀어요/멀어요`(지각이 아니라 거리인데 "늦음"이라 적혀 다른 사람 화면엔 자가신고처럼 보였다). ⑧ **금액 줄 `(모의)` 표기** + "카테고리별 예시값" 고지. ⑨ **`getIdentities` 모양 검사**(배열 여부 + 항목 `id` 타입 — 배열이어도 안이 문자열이면 같은 자리에서 터진다) | `docs/검증_실키_2026-08-05.md` §A·§D 에서 **실측으로 확정**한 결함이고 전부 몇 줄짜리라 발표 전에 넣을 수 있다. 🔒 공용 파일 4개를 건드린 이유: `실시간` 판정은 **데이터가 화면까지 오는 경로 전체**를 지나므로 한 파일 안에서 고칠 수 없다. `RegionsWithMeta.live`(목록 전체)는 **그대로 뒀다** — PR #28 의 판단과 어긋나는 게 아니라 층이 다르다(§4 참고). 검증은 **💰 0콜**로 했다 — L2 캐시가 키 검사보다 먼저 조회되는 성질을 이용해 키를 비운 채 혼재 목록을 재현했다 |
| 2026-08-05 | Claude(AI) | `docs/검증_develop_2026-08-05.md` **(신규)** · `memory/status.md` | **`develop` 전면 검증 리포트 — 💰 외부 API 0콜.** 키를 전부 비운 채 빌드·기동해 모임 생성→출발지→거점 추천→투표→확정→가게→결과→예약 전 구간을 HTTP·브라우저(모바일 390×844) 두 관점으로 통과시켰다. 🔴 **최상위 발견: `app/m/[code]/sections/TravelTimes.tsx` 의 `실시간` 칩에 조건이 없다** — 키 0개인데 네 참가자 전원이 `실시간 26분`·`실시간 1시간 22분`으로 표시됐다(전부 직선거리 추정). 같은 화면의 경로 상세 시트는 같은 숫자를 `추정값`이라 정직하게 밝혀 **한 화면 안에서 라벨이 모순**된다. 그 외 🔴 `참가자별 신호등 '많이 늦음'이 자가신고 없이 자동 부여`(지각이 아니라 거리) · 🔴 `코드만 알면 남의 표·방장 권한 사용 가능`(4/5 시도 성공, 출발지 좌표도 비번 없이 노출) · 🔴 `키 0개인데 regions 응답 11.29초`(maxDuration 선언 0곳, 외부 fetch 16곳 중 타임아웃 3곳) · 🟡 `캘린더 내보내기가 항상 다음 토요일 19시` · 🟡 `모르는 지명이 서울 해시 좌표로 조용히 치환`(제주공항→37.594,127.129) · 🟡 `실 가게 이름에 고정 선입금·항상 예약가능` · 🟡 `localStorage 형식이 깨지면 3개 화면이 백지` · `any 49곳`·`400줄 초과 2파일`. ✅ 정상 확인: 4명 동시 투표 4표 보존 · 지도/모의결제/알림/진단 잠금 전부 정직 | CEO 요청 — *"디벨롭 기준으로 어디까지 완성도가 있는지 · 실사용자가 어떤 문제를 겪는지 · 우리 앱 정보를 고객이 믿어도 되는지"*, **토큰·API 사용량 한도를 정해서**. 그래서 **유료 호출 0건**으로 설계했다(키를 비우면 전 경로가 mock 폴백을 타므로 실사용 흠은 그대로 드러난다). ⚠️ **`실시간` 배지 문제는 팀이 이미 한 번 고친 버그다** — `app/page.tsx` 에는 `midLive` 판정이 붙어 있는데, 정작 제품의 본 화면 `/m/[code]` 에는 같은 수정이 안 들어갔다. 원인은 `/api/midpoint` 만 `recommendRegionsWithMeta`(`live` 포함)를 쓰고 `app/api/meeting` 의 `case "regions"` 는 `recommendRegions`(meta 없음)를 써서 **모임 플로우에 `live` 신호가 아예 없는 것**. CLAUDE.md §3-6("가짜 데이터를 실제인 것처럼 그리지 않는다")을 정면으로 어기는 유일한 지점이라 최우선으로 적었다. ⚠️ 실 API 를 넣은 상태와 Preview 배포는 **이 컨테이너가 `*.vercel.app`·`api.odsay.com` 으로 못 나가 미검증** — 리포트에 그렇게 표기했다 |
| 2026-08-05 | Claude(AI) | `memory/status.md` | 머리말에 **⚡ 지금 스냅샷** 표 신설(main/develop/프로덕션/터널/열린 PR/다음 릴리스 조건을 한눈에) · 릴리스 전 남은 2건 섹션에 **CEO 결정(A안: 작성자가 수정 PR) + 팀원에게 프롬프트 전달됨(할 일 5건·검증 목표 1초 미만)** 기록 · 최종 업데이트 날짜 8/5 로 | 세션이 끝나는 시점의 상태를 파일 첫 화면에서 바로 읽을 수 있어야 다음 세션이 본문 700줄을 뒤지지 않는다. "팀원에게 전달했다"는 저장소 밖에서 일어난 일이라 여기 안 적으면 **다음 세션이 같은 요청을 또 보낸다** |
| 2026-08-05 | Claude(AI) | `memory/status.md` | **팀원 브랜치 3개 머지 완료 기록** — PR #40·#41·#42 (전부 merge commit) · 머지 순서를 시뮬레이션으로 정한 근거 · 3/3 의 **"리팩터 vs 제자리 수정"** 충돌 해결 경위 · 손실 점검 8항목 · **릴리스 전까지 남은 2건**(속도 게이트) | 충돌 해결이 **기계적으로 하면 1/3 의 핵심(429 객체형 처리)을 조용히 날리는 종류**였다. 타입검사·빌드·화면이 다 멀쩡해 아무도 못 알아챈다 — **교훈(리팩터와 수정이 겹치면 옮겨간 쪽에 옛 내용이 따라왔는지 눈으로 확인)** 을 남겨야 다음 사람이 같은 함정을 피한다. `develop` 이 `main` 대비 26커밋 앞선 상태와 그 이유도 명시했다 |
| 2026-08-05 | Claude(AI) | `lib/odsay-client.ts` | 🔴 **머지 충돌 해결 — `odsayError()` 본문을 최신으로 이식.** 이 파일이 `odsay.ts` 에서 추출될 때 **429 수정 이전 버전**(`!Array.isArray(err)` → 배열만)이 따라왔다. 리팩터 구조는 그대로 받고 본문만 갈아끼웠다 | **잘못 풀면 1/3(#40)의 핵심인 객체형 `429`·`-98` 처리가 조용히 사라진다.** 타입검사도 빌드도 그대로 통과해 **티가 안 난다** — `warnEmpty`("구간을 못 푸는 것으로 보인다")라는 사실과 다른 진단이 다시 남게 된다. 같은 사고가 반복되지 않도록 **함수 주석에 "여기가 최신인지 확인하라"는 경고를 박아 두었다** |
| 2026-08-06 | Claude(AI) | `lib/ai-vote/web-kakao.ts` · `region.ts` · `place.ts` | **시간 최적화** — ① 후보 20곳 검증을 직렬→동시 6 풀 ② 밀도 키워드 3종 병렬 ③ 카드 안정화 폴링 1초→400ms(연속 2회 불변 불변식 유지) ④ 도보·자차 고정대기 900→500ms ⑤ GLM 제안·참가자 시도 조회 병렬 | 실측: 최악 셀(스터디 가게 ECO) 60.5→18.8초(−69%), 지역 ECO 평균 52→45초. 픽 5/5 동일 — 품질 무손실. 남은 시간은 구조적 하한(렌더 ~20초·GLM 추론 ~15초)이라 여기서 수렴 |
| 2026-08-06 | Claude(AI) | `lib/ai-vote/place.ts` | ECO 원콜에 **빈약 상권 예외 조항** 추가 — 10분 이내 후보가 전무하면 빈 투표지 대신 가까운 순 최대 3곳 + 전원 초과 사실을 notes 에 명시 | 전 케이스 A/B 매트릭스(6셀)에서 발견된 유일한 ECO 품질 결함: 화천 사내면에서 규칙 1을 문자 집행해 0곳을 냈다(표준은 도구 루프로 미달 공개 투표지). 수정 후 재검 통과 — 3곳+정직 노트. 매트릭스 결론: 토큰 −47~−78%(평균 −66%), 상위 1·2위 픽 6/6 동일, 시간은 혼합(스터디 가게 셀은 ECO 가 2배 느림) |
| 2026-08-06 | Claude(AI) | `lib/ai-vote/region.ts` · `place.ts` · `index.ts` · `app/api/ai-vote/route.ts` | **ECO 모드(원콜 아키텍처)** — `?eco=1` 시 이동시간·도보를 코드가 선조회해 비용 계산·정렬까지 마친 압축표를 GLM 에 주입, 도구 루프 대신 1콜로 후보 3곳만 고르게 함. 후보 12→8곳 | 토큰 정밀계측(A/B 각 2회) 결과 풀코스 18.5k→**6.1k(−67%)**, GLM 6→3콜, 투표지 품질 동일(공덕·신촌·합정 수렴). 원리: 도구 결과가 턴마다 반복 포함되는 입력 낭비(−94%)와 표를 보고 암산하는 출력 추론(−45%)을 코드로 이전. reasoning_effort:low 는 3회 재측정 −2%로 효과 없음 판정. 표준 모드는 그대로 병행(기본값 전환은 팀 결정) |
| 2026-08-05 | Claude(AI) | `lib/ai-vote/region.ts` | **도간(道間) 구간은 ODsay 우선** — 직선 50km 초과 대중교통 구간은 `travelMinutesEx`(ODsay + L1/L2 캐시 + 속도게이트)를 먼저 쓰고 `real:true` 일 때만 채택, 실패 시 웹으로 폴백. 응답에 `legSources`(odsay/폴백/web 건수) 노출 | 카카오맵 웹은 일부 도간 구간에서 KTX 를 아예 안 준다(실측: 부산→신경주가 시내버스 199분만). 서버 실측: 도간 24구간 ODsay 전부 성공·폴백 0, 대전→김천구미 23분(KTX) 등 실측다운 수치. 서울+거제+대전(옥천 도덕리 원사고 조건)에서 옥천역·김천구미역·천안역 정상 투표지 확인 |
| 2026-08-05 | Claude(AI) | `lib/ai-vote/`(신규 5파일) · `app/api/ai-vote/route.ts`(신규) · `next.config.mjs` | **AI 투표지 생성 이식** — 하네스(W1~V12, 총 30여 라운드)에서 검증한 GLM 투표지 파이프라인을 lib 로 옮김. 지역 1차(`aiRegionVote`)·가게 2차(`aiPlaceVote`) 분리 진입점, 카카오맵 **웹**만 사용(API 0콜), 실패 시 기존 `recommendRegionsWithMeta`/`recommendPlaces` 로 강등(`live:false`), 모임 단위 잠금, 확정 지역은 **좌표 필수**(동명 사고 방지), 목적 자유텍스트 → 5종 프로파일(규칙→GLM→중립). `NEXT_PUBLIC_FF_AI_VOTE=1` 플래그 뒤(⚠️ `lib/flags.ts` 는 🔒라 라우트에서 직접 env 읽음 — 통합 시 FLAGS 이동 요청). next.config 는 playwright 번들 제외 1줄 | 투표 전 지역·가게 투표지를 AI 가 채우는 기능. 검증 근거·함정 목록은 메모리와 코드 주석에 실측 수치로 기록됨. ⚠️ `search.map.kakao.com` 은 비공식 엔드포인트(PoC) — 프로덕션 전환 전 팀 결정 필요 |
| 2026-08-05 | Claude(AI) | `.gitignore` | 루트의 AI 검증 실험 파일(`/_*.mjs` `/_*.json` `/_w*.log`)을 무시 목록에 추가 | GLM 투표지 검증 하네스가 Playwright 때문에 루트에 있는데 **Ollama 키가 하드코딩**돼 있다. 공개 저장소라 실수로 `git add .` 하면 키가 유출된다. 실험 종료 후 파일을 정리할 때까지의 안전망 |
| 2026-08-05 | Claude(AI) | `lib/cache-store.ts` · `.gitignore` · `db/migrations/001_api_cache.sql`(삭제) | L2 캐시 저장소를 **Neon → 자체 서버 디스크**로 교체. 추가 기록(append-only JSONL) + 메모리 색인 방식 (`.cache/api-cache.jsonl`) | 🔴 **내가 사용자 의도를 바꿔 해석했다.** "자체 서버에 스토리지 영역 추가"라고 하셨는데 기존 인프라 재사용이 자연스럽다고 판단해 Neon 에 만들었고, 확인을 안 했다. 되돌린다. **기술적으로도 디스크가 맞다** — ① 캐시는 빠르려고 두는 건데 네트워크 너머면 목적이 약해진다(재시작 후 540ms 중 상당 부분이 Neon 왕복) ② Neon 무료 100 CU-hours/월을 캐시 조회가 계속 갉아먹는다. 저장은 줄 단위 덧붙이기라 도중에 죽어도 마지막 줄만 깨지고, 줄이 살아있는 항목의 3배를 넘으면 임시파일→rename 으로 압축한다. SQLite 는 네이티브 모듈이라 서버에도 빌드가 필요하고 🔒 `package.json` 을 또 건드려야 해서 이 규모(수만 건)엔 과하다고 판단. **인터페이스가 같아 `lib/routing.ts` 는 한 줄도 안 바뀌었다.** Neon 테이블 2개는 drop 했다 |
| 2026-08-05 | Claude(AI) | ~~`db/migrations/001_api_cache.sql`~~ · `lib/cache-store.ts`(신규) · `lib/routing.ts` | 외부 API 응답 캐시를 **L2 계층**으로 확장 <sub>(저장소는 위 행에서 디스크로 교체됨)</sub> — `geo_cache`(TTL 30일) · `route_cache`(TTL 6시간). 조회 순서는 **L1 메모리 → L2 DB → L3 API** | 캐시가 메모리 Map 뿐이라 **서버를 재시작하면 전부 사라졌다** — 실측으로 재시작 직후 첫 조회 2,597ms(외부 14콜), 재조회 190ms. 오늘만 서버가 3번 내려갔고 그때마다 유료 호출을 처음부터 다시 했다. ⚠️ **TTL 은 필수다**: ① ODsay 약관 4.5.10 이 "사전 동의 없이 결과 데이터 저장"을 금하는데 무기한 축적은 명백한 "저장"이고 만료되는 임시 캐시는 성격이 다르다(영구 보관이 필요하면 사전 동의를 받는다) ② 대중교통 소요시간은 시간대·요일에 따라 달라져 낡은 값이 계속 나오면 안 된다. ⚠️ 개인정보는 담지 않는다 — "검색어→좌표", "좌표쌍→분" 같은 공개 지리 정보뿐이고 참가자 실제 출발지는 기존대로 `participants` 가 갖는다(모임 삭제 시 함께 삭제). DB 가 없으면(팀원 로컬) 전부 조용히 무시되고 L1 만 쓴다 |
| 2026-08-05 | Claude(AI) | `lib/odsay.ts` | `odsayError()` 가 `error` 를 **배열·객체 둘 다** 인식하도록 수정 (+ `msg` 키·숫자 code 대응) | ODsay 는 인증 실패는 `error: [ {...} ]` **배열**로, **429·-98 은 `error: { ... }` 객체**로 보낸다. 기존 코드는 `!Array.isArray(err) → return false` 라 **객체형을 통째로 놓쳤고**, 뒤에서 `result.path` 가 없다는 이유로 `warnEmpty` 가 찍혀 **"구간을 못 푸는 것으로 보인다"** 는 사실과 다른 진단이 남았다. 실제 사유는 `429 Too Many Requests` 였다 — 이 오진 때문에 원인 추적이 좌표 쪽으로 한참 샜다 |
| 2026-08-05 | Claude(AI) | `lib/routing.ts` | `travelMinutesEx` 의 실제 API 호출 직전에 **속도 게이트**(요청 시작을 250ms 간격으로 배분) 추가 | **동시 14건 발사 시 9건이 429 로 거절**되고 추정 폴백으로 떨어져 `live:false` 가 됐다. 실측(동일 14구간): 동시 → OK 4·429 9 / 순차 250ms → **OK 14·429 0**. 구간 문제가 아니라 속도 문제였다. 팬아웃이 🔒 `lib/scoring/index.ts`(거점 N개 동시) + `routing.ts`(참가자 M명 동시) **두 겹**이라 바깥을 못 건드리므로, **호출 지점 자체를 막아** 모든 호출부가 자동 보호되게 했다. 캐시 히트·근거리 단락은 게이트 앞에서 끝나 영향 없음. 응답을 기다리지 않고 시작만 벌려 14건 ≈ 3.5초(완전 순차는 9.4초) |
| 2026-08-04 | Claude(AI) | `lib/routing.ts` | 폴백 지점(추정값 반환 직전)에 좌표 포함 `console.warn` 추가 | `#29` 의 odsay warn 은 "왜"(HTTP·빈 경로)만 남기고 좌표를 안 남긴다 — 시간순 재실행에서 **"200 인데 경로 비어 있음" 9건**이 찍혔지만 어느 구간인지 알 수 없었다. 앱의 모든 추정값이 이 한 줄을 지나므로 여기서 "어디"를 남기면 두 로그를 짝지어 문제 구간이 로그만으로 확정된다. 좌표는 비밀이 아니고 URL·키는 찍지 않는다 |
| 2026-08-04 | Claude(AI) | `lib/routing.ts` | `travelMinutesEx` 에 근거리(≤700m) 단락 추가 — 대중교통이면 ODsay 를 부르지 않고 도보 시간(시속 4km, 최소 1분)으로 환산해 `real: true` 로 반환 | ODsay 는 출발-도착 700m 이내를 **`-98 "출, 도착지가 700m이내입니다."`** 로 거부한다(홍대입구역→홍대입구 거점 70m 실측). 참가자가 후보 거점의 700m 안에 있으면(역 이름 검색 UX 상 역세권 참가자는 거의 항상 해당) 그 구간만 추정 폴백 → live 정의("한 구간이라도 추정이면 false")에 걸려 **전체 목록이 "거리 추정으로 계산" 배지**를 달았다(강남·홍대 2인 실측). 도보 환산을 실값으로 치는 근거: ODsay 스스로 "경로 없음(걸어가라)" 판정하는 구간이므로 추측이 아니라 그 판정을 따르는 것. 실패 확정 콜도 절약된다. `estMinutes` 는 환승·대기 6분을 깔아 70m 에 6분이 나오므로 쓰지 않았다. 자차는 -98 대상이 아니라 기존대로 TMAP 호출 |
| 2026-08-04 | Claude(AI) | `.claude/launch.json` | 미리보기 포트를 `3000` → **`3100`** | `package.json` 과 짝을 맞춘다. 이 브랜치의 개발 서버가 3100 이므로 도구 설정도 같아야 미리보기가 붙는다 |
| 2026-08-04 | Claude(AI) | `package.json` | `dev` · `start` 의 포트를 `3000` → **`3100`** (develop-selfhost 계열 전용) | **팀원이 3000 을 쓰므로 자체 서버 개발은 3100 으로 갈라둔다** — 같은 기기에서 둘이 부딪히지 않게. 카카오 API 는 3000·3100 둘 다 등록돼 있어 어느 쪽이든 동작한다. ⚠️ 충돌 회피를 위해 `-p` 를 떼는 안도 검토했으나, 기본값 3000 으로 떨어지면 `.env.local` 의 `KAKAO_REDIRECT_URI`(3100)와 어긋나 **로그인이 조용히 깨진다.** ⚠️ `PORT` 를 `.env.local` 에 넣는 우회는 **안 먹는다**: 포트는 CLI 가 `.env.local` 로딩 전에 정한다(3005 로 넣고 3000 으로 뜨는 것 실측). 🔒 공용 파일이라 통합 담당자 확인 후 진행. develop 과 이 줄에서 충돌하며, 해결은 항상 **selfhost = 3100 유지** |
| 2026-08-04 | Claude(AI) | `next.config.mjs` | `SELF_HOST_URL` 이 있을 때만 `/api/*` 를 그 주소로 넘기는 `rewrites`(`beforeFiles`) 추가 | 백엔드를 자체 서버로 옮기면서 Vercel 은 화면 + 전달만 맡는다. **변수가 비면 빈 배열이라 기존과 100% 동일** — 자체 서버 본체와 팀원 로컬은 안 깨진다. `beforeFiles` 가 아니면 `app/api/*` 파일 라우트가 먼저 잡혀 rewrite 가 영영 안 걸린다(실측 확인). GET·POST·쿼리스트링 전부 전달되는 것과, 대상이 죽으면 화면은 200 이지만 API 는 전부 실패하는 것까지 확인 |
| 2026-08-04 | Claude(AI) | `.gitignore` | 루트 로그 파일(`dev-*.log` · `*.log`)을 무시 목록에 추가 | 기존 규칙은 `/logs/` 와 `npm/yarn-debug.log*` 뿐이라 **루트에 떨어지는 `dev-3000.log` 가 안 걸렸다.** 개발 서버를 파일로 돌리면 요청 경로·좌표·오류가 그대로 쌓이는데, 공개 저장소라 커밋되면 그 내용이 공개된다. 실제로 추적되지 않은 채 작업 트리에 있었다 |
| 2026-08-04 | Claude(AI) | `lib/ai.ts` · `.env.example` | `chatCompletion` 에 `apiKey` 파라미터 추가 · 하드코딩된 `Authorization: "Bearer ollama"` 를 `OLLAMA_{PRIMARY,FALLBACK}_API_KEY` 로 · 기본값 `"ollama"` 유지 | **Ollama Cloud 를 쓸 통로가 아예 없었다.** 로컬 Ollama 는 토큰을 검사하지 않아 더미 문자열로 충분했지만, `ollama.com` 은 진짜 키를 요구해 401 이 난다. 기본값을 `"ollama"` 로 둬서 키를 안 넣은 팀원의 로컬 동작은 그대로다. ⚠️ 클라우드 모델 ID 에 `:cloud` 를 붙이면 안 된다 — 그건 로컬 ollama 의 중계 표기이고 클라우드엔 `glm-5.2` 로 존재한다(실측). ⚠️ `glm-5.2` 는 추론 모델이라 `max_tokens` 를 낮게 걸면 사고 토큰만 쓰고 `content` 가 빈 문자열로 온다(64 로 실측) — 현재 `lib/ai.ts` 는 `max_tokens` 를 안 보내므로 무해하지만 넣을 때 주의 |
| 2026-08-04 | Claude(AI) | `lib/odsay-modes.ts` **(신규)** | ODsay 원시 코드 → 수단 매핑표를 별도 파일로 분리하고 **실측으로 확정한 값을 채웠다**: `trafficType` **`5` 고속버스 · `6` 시외버스 · `7` 항공**, `trainType` **`1` KTX · `8` SRT**. `kindOf()`(미지 값 → `other`) · `trainTypeLabel()` · `isRideType()` · `KIND_LABEL` · `MODE_ORDER` 를 함께 옮겼다 | **`5`/`6` 의 순서가 외부 구현체마다 반대로 적혀 있었다**(`ourHome`·`yeoru` 는 5=고속, `capstone25-plaps` 는 5=시외). 자료로는 못 가리므로 직접 호출해 확정했다 — 근거는 추론이 아니라 ODsay 가 준 **터미널 이름 자체**다(`5`: 센트럴시티→전주**고속**버스터미널 / `6`: **서울남부터미널**→전주**시외**버스공용터미널). 파일을 나눈 것은 `lib/odsay.ts` 가 371줄이라 표를 넣으면 **400줄 제한**(CLAUDE.md §3)을 넘기 때문이다 |
| 2026-08-04 | Claude(AI) | `lib/odsay.ts` | ① `laneName()` 이 철도 구간에서 **`subPath.trainType`** 을 읽는다 ② 요금을 `fareOf()` 로 통일 — 도시간 응답은 `info.payment` 가 아니라 **`info.totalPayment`** 다 ③ `TransitLeg.rawTrainType` 추가 ④ 매핑표는 `odsay-modes.ts` 에서 import (362줄) | ①은 "ODsay 가 열차 노선 정보를 안 준다"던 기록의 정정이다 — `lane` 이 아니라 `subPath` 에 실려 있었고, 이제 `KTX 서울→부산` 이 이름으로 뜬다. ②는 **같은 유형의 사고가 하나 더 있었다는 뜻**이다: KTX 59,800원·고속버스 39,700원이 응답에 있는데도 화면은 "요금 정보 없음"이었다. **"응답에 없다"가 아니라 "내가 본 필드에 없다"** 를 두 번째로 겪었다 |
| 2026-08-05 | Claude(AI) | `lib/odsay-access.ts` **(신규)**, `lib/odsay.ts` | 🔴 **도시간 경로에 빠져 있던 이동시간을 추정해 더한다** — ① 출발지→첫 탑승지 ② **구간 사이 연계**(오송역↔청주공항) ③ 마지막 하차지→목적지. `min` 에 포함하고 포함량을 `estimatedMin` 으로 함께 내린다 | ODsay 도시간 길찾기는 **역↔역만** 답한다(도시간 81경로 전부 `totalTime` = 탑승구간 단순합, 차이 0분). 그래서 **강남역→제주가 75분**으로 나왔고, `travelMinutesEx` 가 이 값을 그대로 점수에 넣어 **추천 순위가 왜곡**됐다. 실측 효과: 서울역+강남역 2인 부산 모임이 `138분·편차 0분` → `183분·편차 45분` 으로, 출발지가 다른데 편차가 0이던 문제가 해소된다. 좌표가 응답에 이미 있어 💰 **추가 호출 0콜**. ⚠️ `estMinutes` 는 직선거리 모델이라 **실측 대비 약 1.7배 과대**(21km→107분, 실제 55~60분대)지만 0분보다는 진실에 가깝고, 모델을 새로 만들면 고칠 곳이 둘이 되므로 기존 것을 그대로 쓴다 |
| 2026-08-05 | Claude(AI) | `app/components/RouteSheet.tsx` | 추정이 섞인 경로에 **고지 박스** + 카드마다 `역까지·환승 이동 추정 ○분 포함` 표기. `도보 0m` 는 값이 0이면 **생략** | 추정을 더해 놓고 "ODsay 실시간"으로만 두면 CLAUDE.md §6 위반이다. `도보 0m` 는 도시간 응답에 `totalWalk` 가 없어서 나오는 값이라 "안 걷는다"는 뜻이 돼 버린다 |
| 2026-08-05 | Claude(AI) | `lib/odsay-client.ts` **(신규)** | 호출 껍데기(`PROXY_INIT`)와 실패 진단 로그(`odsayError`·`warnHttp`·`warnThrown`·`warnEmpty`)를 `odsay.ts` 에서 분리 | 접근시간 보정을 넣자 `odsay.ts` 가 **408줄**이 돼 400줄 제한(CLAUDE.md §3)을 넘었다. "응답 해석"과 "어디로 어떻게 부르는가"는 관심사가 달라 여기가 자를 자리였다 (344줄로 내려옴) |
| 2026-08-05 | Claude(AI) | `lib/odsay.ts`, `lib/odsay-modes.ts` | 🔴 **항공(`trafficType 7`)이 섞인 경로는 요금을 내리지 않는다**(`fareOf()` 가 0 반환 → 화면 "요금 정보 없음"). `AIR_TRAFFIC_TYPE` 상수 추가 | 바로 앞 커밋이 만든 **회귀를 스스로 막는다.** `info.totalPayment` 를 읽기 시작하면서 KTX·고속버스 요금과 함께 **항공 146,700원도 처음 화면에 떴는데**, 김포→제주 실제 편도는 평균 3만원대·최저 1만원대다(2026-08-04 외부 대조). ODsay 는 날짜·시간과 무관하게 고정값을 주는데 항공은 동적 요금이라 **구조적으로 맞을 수 없고**, 응답에 등급·기준시점 표기가 없어 이 숫자가 무엇인지 우리가 말할 수 없다. 말할 수 없는 값은 그리지 않는다(CLAUDE.md §6). 열차·버스는 현행 운임과 일치해 그대로 쓴다 |
| 2026-08-05 | Claude(AI) | `memory/status.md` | **후속 항목 7 신설 — 🔴 도시간 경로에 접근·연계·도착 이동이 통째로 빠져 있다.** 실측: 도시간 81경로 전부 `info.totalTime` = 탑승구간 시간 단순합(차이 0분). 강남역→제주 75분인데 첫 탑승지가 21km 떨어진 김포공항 · 복합 경로는 오송역↔청주공항 연계가 0분 | CEO 지적으로 드러났다. **이름표 문제가 아니라 추천 순위 문제다** — `travelMinutesEx` 가 이 값을 그대로 점수에 넣는다. 도보 0m·환승 0회·"직통" 라벨·배차 대기(하루 2편 구간 존재)까지 같은 뿌리라 함께 적었다. 추천 점수를 바꾸는 변경이라 **팀 결정 사항**(CLAUDE.md §5)으로 남긴다 |
| 2026-08-04 | Claude(AI) | `app/components/RouteSheet.tsx` | 새 수단 3종의 아이콘·색 추가 — 고속버스 🚍 `#7c3aed` · 시외버스 🚐 `#0d9488` · 항공 ✈️ `#06b6d4` | 없으면 `KIND_ICON[l.kind] ?? "🚏"` 폴백이라 고속·시외·항공이 전부 같은 중립 기호로 뜬다. 시내버스(파랑 `#4c8df6`)와 **한눈에 갈려야** 해서 계열을 다르게 잡았다 |
| 2026-08-04 | Claude(AI) | `memory/status.md` | §3-3 과 후속 항목 4 를 **실측 결과로 갱신** — `5`/`6`/`7` 확정, `trainType` `1`·`8` 확정 / `3`·`6` 은 **미검증으로 명시**, 도시간 요금 필드 발견 기록 | 후속 항목 4 는 **두 번 틀리게 적혔던 자리**다. 이번엔 실측으로 닫되, 확정 못 한 `trainType 3`·`6` 은 "추정"이라고 남긴다 — ODsay 응답에 열차 이름 필드가 없어 **추가 실측으로도 못 좁힌다**는 것까지 적어야 다음 세션이 💰 를 또 태우지 않는다 |
| 2026-08-03 | Claude(AI) | `lib/env.ts`, `lib/odsay.ts`, `.env.example` | ODsay 호출 주소를 `ODSAY_BASE_URL` 로 빼고, `ODSAY_PROXY_SECRET` 이 있으면 `x-proxy-secret` 헤더로 보낸다 (fetch 4곳). 끝 슬래시·공백 자동 제거 | ODsay 서버 키는 **호출 IP 화이트리스트**가 필요한데 Vercel 서버리스는 나가는 IP 가 유동이라 등록이 불가능하다. 고정 IP 프록시를 거칠 수 있게 한다. **환경변수가 비면 기본값이 `api.odsay.com` + 헤더 없음이라 기존과 100% 동일** — 팀원 로컬이 안 깨진다 |
| 2026-08-03 | Claude(AI) | `lib/odsay.ts` | ODsay 의 **"HTTP 200 + `error` 본문"** 을 명시적으로 잡고 `console.warn` 으로 사유를 남긴다 | ODsay 는 인증 실패에도 200 을 준다(`[ApiKeyAuthFailed]`). `if (!r.ok)` 로는 못 잡고 뒤에서 우연히 걸러질 뿐이었다. 로그가 있어야 Vercel 에서 **"터널 장애"와 "키 문제"를 구분**할 수 있다 |
| 2026-08-03 | Claude(AI) | `lib/routing.ts` | `travelMinutesEx`(`{min, real}`) · `recommendRegionsWithMeta`(`{items, live}`) **신설**. 기존 `travelMinutes` · `recommendRegions` 는 값만 꺼내는 **래퍼로 유지** | `live` 를 알려면 이동시간이 실 API 값인지 알아야 하는데, 시그니처를 바꾸면 🔒 `app/api/meeting/route.ts`(3곳)와 👤 `lib/ai.ts` 까지 번진다. **순수 추가 방식**이라 두 파일 diff 가 0줄이다 |
| 2026-08-03 | Claude(AI) | `app/api/midpoint/route.ts` | `live: has.odsay \|\| has.tmap` → **실제 결과의 `live`** | 예전 판정은 **키가 있는지**만 봐서, 키를 넣는 순간 무조건 `true` 였다. 프로덕션에서 **실측 40분과 추정 894분이 한 목록에 섞였는데 전부 "실 이동시간 기준"** 으로 표시됐다(2026-08-03). 프록시가 죽어도 화면으로는 알 수가 없다 |
| 2026-08-03 | Claude(AI) | `app/page.tsx` | `"경로 API 키 없음 — 거리 추정으로 계산"` → `"거리 추정으로 계산"` | 키가 있어도 프록시·터널 장애면 폴백이므로 **"키 없음"은 원인 중 하나일 뿐**이다. 원인을 단정하지 않고 무엇을 보고 있는지만 밝힌다 |
| 2026-08-03 | Claude(AI) | `lib/geo.ts` | **전국 거점 16곳 추가** (`HUBS` + `CANDIDATE_HUBS` 12곳 → 28곳) — 수도권 2 · 충청 3 · 경북경남 5 · 전라 4 · 강원 2 | 후보 풀이 서울 12곳뿐이라 지방 모임이 `geometricCandidates`(순수 기하 폴백)로 빠져 **`옥천군 안남면 도덕리` 같은 모일 수 없는 좌표가 확정**되고, 거기서 존재하지 않는 mock 가게가 떴다(CEO 실측). 로직이 아니라 **데이터가 비어 있던 문제**다 |
| 2026-08-03 | Claude(AI) | `lib/geo.ts` | `HUB_COVER_KM` **45 → 55** | 거점을 넣어도 **서울+부산이 여전히 폴백**이었다 — 중심(경북 내륙 공백지대)에서 최근접 대전역이 55km 라 45km 임계를 한 걸음 차이로 넘겼다. 실측: 45km 는 장거리 7조합 중 4개만 정상, **55km 는 7/7 정상**. 45 는 후보가 수도권 12곳뿐이던 시절의 값이라 전국 풀에서는 의미가 달라졌다 (팀 결정) |
| 2026-08-03 | Claude(AI) | `lib/geo.ts` | `nearCentroidHubs` 의 "가까운 순 4곳 강제 확보" 폴백에 **절대거리 상한 `FALLBACK_MAX_KM = 60`** 추가 (상한 밖이면 최근접 1곳 보장) | 전국 거점이 생기면서 **대구 2인 모임 후보 2·3위에 창원(72km)·울산(72km)이 떴다.** 스코어러가 순위는 밀어내지만 후보 목록에는 남고 💰 이동시간 API 를 후보 수만큼 때린다(추천 1회 = 후보수 × 참가자수). 대구 2인 기준 호출 4 → 1 (팀 결정) |
| 2026-08-03 | Claude(AI) | `lib/geo.ts` | `geometricCandidates` 주석 갱신 — **최후수단**임과 "여기로 빠졌다면 거점 풀이 비었다는 신호"임을 명시 | 값(HUB_COVER_KM)을 조정하기 전에 거점 추가를 먼저 검토하도록 다음 세션에 남긴다 |
| 2026-08-03 | Claude(AI) | `memory/status.md` §3-2 | 전국 거점 확장 결과 반영 (수도권 밖 서술 · before/after) | 인계 문서가 코드와 어긋나면 다음 세션이 잘못된 전제로 시작한다 |
| 2026-08-03 | Claude(AI) | `lib/format.ts` | `formatWon` 이 **음수를 "무료"로** 표시하던 것을 `"—"` 로 · 요금 전용 `formatFare()` 신설(0 이하 → "요금 정보 없음") | 🔴 **프로덕션에서 발생 중이던 버그.** ODsay 는 철도에 `payment: 0`, 시외버스에 `payment: -1`(정보 없음)을 준다 — **4시간짜리 3회 환승 경로가 "무료"로 표시**됐다. 통행료 0원은 진짜 무료라 `formatWon` 의 0 처리는 유지하고, 뜻이 다른 대중교통 요금만 새 함수로 분리 |
| 2026-08-03 | Claude(AI) | `lib/odsay.ts` | `TRAFFIC_KIND` 에 `4: "train"` 추가 · **미지 타입을 `"walk"` 가 아니라 `"other"` 로** · `kind` 타입 확장 · `pathType` 을 `1\|2\|3` → `number`(시외는 11) · 수단명 폴백(`laneName`) · 진단 모드에서 `rawLane` 노출 | 철도(`trafficType: 4`)가 "도보"로 뭉개져 **탑승 구간 없는 응답처럼 보였고 껍데기 가드에 걸려 KTX·SRT 경로가 통째로 버려졌다.** 미지 타입을 `other` 로 두면 같은 유형의 버그가 재발하지 않는다 — 수단명만 모를 뿐 탑승으로는 남는다 |
| 2026-08-03 | Claude(AI) | `lib/odsay.ts` | 탑승 판정을 `isRideType()` 하나로 통일 (두 함수가 각자 `trafficType !== 3` / `kind !== "walk"` 로 갈려 있던 것) | **같은 응답을 추천 계산은 쓰고 화면은 버렸다.** 실제로 부산 경로에서 추천 이동시간은 130분(KTX)인데 상세는 "추정값"으로 떴다 — 판정이 한 곳에 있어야 다시 갈라지지 않는다 |
| 2026-08-03 | Claude(AI) | `app/components/RouteSheet.tsx` | 열차 아이콘·색·라벨(🚄) · `other` 중립 표시 · 요금에 `formatFare` 적용 · 정거장 0이면 표기 생략 · **"시외는 API 커버리지 밖" 안내문 삭제** | 안내문이 **사실과 달랐다** — ODsay 는 시외를 제대로 답하고 있었고 원인은 우리 매핑이었다. 틀린 설명을 남기면 다음 세션이 또 API 교체를 검토한다 |
| 2026-08-03 | Claude(AI) | `memory/status.md` §3-3 | 수정 결과 반영 · "추천에 KTX 시간이 이미 들어가는가(미검증)" → **확인 완료**로 갱신 | 인계 문서의 미검증 항목을 실행 결과로 닫는다 |
| 2026-08-04 | Claude(AI) | `memory/status.md` | PR #28 머지 반영 + 검토 판단 3건을 근거와 함께 기록 (`RegionsWithMeta` 를 `routing.ts` 에 두는 이유 · `live` 정의 · `routeCache` TTL 미도입 근거). §1 릴리스 순서를 갱신 — `live` 판정이 끝나 **#26 을 빼지 말고 함께 낸다**로 정정하고, 종단 확인은 **Preview 에서 · 새 좌표로** 하라는 절차를 명시. §3-3 실키 검증 완료 반영 | 판단 근거를 안 적으면 다음 세션이 같은 것을 다시 논의한다. 특히 **반증 테스트를 캐시된 좌표로 하면 터널을 꺼도 `live:true` 가 나와 오판**하는데, 이건 #28 이 스스로 밝힌 한계라 절차에 못 박아야 한다 |
| 2026-08-04 | Claude(AI) | `memory/status.md` | PR #26 머지 반영 + 통합 세션 검토 결과 기록 — **지방 거점 밀도 문제**(부산 2인에 창원·울산, 광주 2인에 목포가 끌려온다. 대구 1곳과 같은 원인), 후보 1곳일 때 투표 UX 미성립(미검증), 수도권 API 호출 12% 증가, `순천역` 좌표 1.2km 의심. §1 에 **릴리스 순서**(live 판정 → 릴리스 → ODsay 키) 명시 | PR #26 본문은 대구만 짚었는데 시뮬레이션에서 같은 문제가 부산·광주에도 나왔다. `FALLBACK_MAX_KM` 값을 조정해도 해결되지 않는다(올리면 먼 도시, 내리면 1곳) — **근본 원인이 거점 밀도**임을 남겨야 다음 사람이 상수만 만지다 끝내지 않는다. 릴리스 순서는 어기면 화면이 오히려 나빠진다(#26 단독 릴리스 시 "대전역 · 949분") |
| 2026-08-05 | Claude(AI) | `memory/status.md` | 🎉 **터널 종단 확인 완료 — 양방향 성공** (터널 ON → "실 이동시간 기준" · 터널 OFF + 새 좌표 → "거리 추정으로 계산"). **8/4 실패 원인 규명: `ODSAY_PROXY_SECRET` 미설정** → 프록시 401 → 당시 코드가 로그 없이 삼킴(후보 3개 중 ②가 맞았다). 8/5 1차 실패는 **Vercel `ODSAY_API_KEY` 값 오류** — 같은 프록시·터널·IP 로 키만 바꾼 대조 실험으로 확정. 🔴 **`lib/env.ts:9` 가 `ODSAY_API_KEY` 만 `trim()` 안 함** 기록. 429 이번 건 0. 팀원 브랜치 3개 현황 + `develop-selfhost` 사전 검증 결과(수정 요청 2건·확인 2건) 추가 | **"원인 미확인"이 아니라 "진단이 없어서 못 본 것"이었다** — #29 로 로그를 넣은 뒤에야 좁힐 수 있었다는 사실이 이 프로젝트의 교훈이라 경위째 남겼다. 추천 결과가 거리순 `왕십리` → 시간순 `건대입구`(편차 0분)로 **실제로 달라진 것**도 기록했다(기능의 존재 이유를 보여주는 대비, 발표 자료용). ⚠️ 동작 확인 ≠ 발표 투입 가능 — **노트북 의존이라는 구조적 한계**는 그대로라 판단 사안으로 명시했다 |
| 2026-08-05 | Claude(AI) | `docs/버전관리.md` | **릴리스 전 체크리스트 절 신설** — ① 🔴 프로덕션에 `ODSAY_API_KEY` 가 있는데 터널이 꺼져 있으면, **속도 제한(rate gate)이 들어간 릴리스에서 실패 확정 호출을 250ms 간격으로 줄 세우게 된다** → 키 제거·터널 기동·게이트 수정 중 하나 필수 ② 환경변수는 **배포 시점에 적용**되며 `deploy.commit` 으로는 재배포 여부를 알 수 없다(Deployments 시각을 봐야 함) ③ 배포 직후 확인 5항목 ④ 태그는 **웹 Releases 에서** (컨테이너는 태그 푸시가 `HTTP 403`) | 기존 문서엔 규칙만 있고 **체크리스트가 없었다.** ①은 팀원 브랜치(속도 게이트)가 머지되는 순간 현실이 되는 조건이라 **릴리스 절차에 걸어두지 않으면 놓친다** — 근거: 2026-08-05 프로덕션 실측 `[ApiKeyAuthFailed]` `/api/midpoint` 8건 + `/api/route-path` 2건. ②③④는 이번 `v0.2.0` 릴리스에서 실제로 시간을 잃은 지점들이다 |
| 2026-08-04 | Claude(AI) | `memory/status.md` | 🔴🔴 **재정정 2건 (CEO 지적 → 외부 교차검증).** ① **"ODsay 가 시외 열차에 노선 정보를 아예 안 준다"는 틀렸다** — 열차 정보는 `lane` 이 아니라 **`subPath.trainType`·`trainSpSeatYn`** 에 실린다(외부 구현체 + ODsay 릴리스 노트로 확인). 우리가 `lane[0].name`·`busNo` 만 봤을 뿐이다. ② **"시외버스 = `trafficType 2` 확정"도 틀렸다** — 외부 구현체 3곳이 **`5`·`6`(고속·시외) · `7`(항공)** 의 존재를 확인해 준다. 대전 `8326` 은 시내버스로 분류된 광역노선일 가능성이 크다. 진단 플래그는 **제거 후보에서 존치로** 변경 | **같은 항목을 두 번 틀리게 적었다.** ①은 *"응답에 없다"가 아니라 "내가 본 필드에 없다"* 였고, ②는 **실측 1건을 규칙으로 일반화**한 것이다. 둘 다 "실측한 것만 적는다"는 원칙을 형식적으로만 지킨 사례라, **결론 대신 경위와 교훈**을 남겨 다음 세션이 같은 방식으로 틀리지 않게 했다. ⚠️ `5`/`6` 순서와 `trainType` 값 목록은 **여전히 미검증** — 외부 자료끼리도 엇갈려 우리 실측이 필요하다 |
| 2026-08-04 | Claude(AI) | `memory/status.md` | 🔴 **후속 항목 4 정정** — "시외/고속버스 `trafficType` 미확정 · ODsay 가 시외버스를 주는지 자체가 미검증"은 **틀렸다.** §3-3 에 **시외버스 = `trafficType 2` 실측 기록이 이미 있었다**(강남→대전 `시외버스 8326`, 3회 환승 237분, `verified: true`). 남은 미검증은 **고속버스뿐**으로 좁혔다 | **같은 문서 안에서 §3-3 과 후속 항목이 어긋나 있었다.** 그대로 두면 다음 세션이 **이미 끝난 실측을 다시 한다**(💰 낭비 + 시간). 후속 항목을 적을 때 상세 섹션을 먼저 확인하지 않은 것이 원인이라, 재발 방지 문구도 함께 남겼다 |
| 2026-08-04 | Claude(AI) | `memory/status.md` | **태그 `v0.2.0` 부착 확인** — `5862751`(CEO가 GitHub 웹에서 생성, 실측). 위 행의 "태그 미부착"을 대체한다. 릴리스 머지(`b92ac70`)가 아니라 문서 릴리스까지 포함한 main 최신을 가리키지만 **두 커밋의 코드 차이는 0줄**이라 문제 없다 | ⚠️ **태그는 이 컨테이너에서 만들 수 없다** — git 프록시가 태그 푸시를 `HTTP 403` 으로 거부한다(브랜치 푸시는 같은 경로로 정상, 5회 시도 확인). **릴리스 태그는 사람이 웹에서 만들어야 한다**는 것을 절차로 남긴다 |
| 2026-08-04 | Claude(AI) | `memory/status.md` | **ODsay 환경변수 정리 완료를 실측으로 기록.** `odsay/odsayProxy/odsayProxyAuth` 전부 `false` 확인. ⚠️ **"Production 체크만 해제"는 3회 시도 모두 실패**했고 **삭제 후 재배포**로 해결됐다(왜 안 먹었는지는 미확인 — `Shared` 탭 가능성). ⚠️ **재배포 여부는 `deploy.commit` 으로 알 수 없다**(Redeploy 는 같은 커밋). Cloudflare 재개 시 환경변수 투입 순서 4단계 추가. 프로덕션 화면 실측 4항목 기록 | 확인이 한참 늦어진 이유가 **"커밋이 그대로면 정상"이라는 내 안내가 불완전**해서였다 — 재배포 확인 방법을 못 알려줘 같은 값을 세 번 확인했다. 다음 세션이 같은 함정에 빠지지 않게 남긴다. **"가짜 mock 가게"의 진짜 원인**(mock 이 기본이 아니라 **시골 좌표라 카카오 검색이 0건**)도 함께 밝혀 적었다 — 원인을 잘못 알면 엉뚱한 곳을 고친다 |
| 2026-08-04 | Claude(AI) | `memory/status.md` | **릴리스 `v0.2.0` 결과 기록 + 정정 1건.** 🔴 정정: `ODSAY_*` 환경변수가 **Production 스코프에도 들어가 있었다** — 릴리스 전에는 프로덕션이 7/31 빌드라 안 보였을 뿐이다. 그 외: 프로덕션 `/api/status` 실측값 · 태그 미부착(컨테이너가 태그 푸시를 막음) · **외부 API 호출에 타임아웃이 한 곳도 없음**(`AbortSignal` 0곳) · 카카오 지도는 프로덕션 정상/Preview 만 실패 | **"환경변수는 넣는 시점이 아니라 배포 시점에 적용된다"** — 이걸 몰라 "프로덕션엔 없다"고 기록해뒀는데 릴리스가 새 빌드를 만들자 드러났다. 다음 세션이 같은 착각을 하지 않게 원인까지 적었다. 실측한 것만 적고 미검증은 미검증으로 남긴다는 원칙대로, 재배포 후 복귀 확인은 "미검증"으로 표기 |
| 2026-08-04 | Claude(AI) | `package.json`, `package-lock.json` | 버전 **`0.1.0` → `0.2.0`** (`npm version minor --no-git-tag-version`) | 이번 릴리스에 `feat:` 3건(#21 진단 플래그 · #26 전국 거점 · #28 `live` 판정)이 들어 있어 `docs/버전관리.md` 의 MINOR 규칙에 해당한다. ⚠️ 태그는 `npm version` 이 만들지 않고 **릴리스 머지 후 `main` 커밋에 붙인다** — `main` 이 보호 브랜치라 직접 푸시가 막혀 있어 버전 커밋도 PR 을 거친다 |
| 2026-08-04 | Claude(AI) | `lib/odsay.ts` | **조용히 사라지던 실패 8곳에 `console.warn` 추가** — `if (!r.ok) return null` 5곳 → `warnHttp(라벨, 상태코드)`, `catch { return null }` 3곳 → `warnThrown(라벨, 오류)`, 빈 결과 2곳 → `warnEmpty(라벨)`. 로그에는 **호스트만** 싣는다 | #28 이 "Vercel 로그에서 터널 장애와 키 문제를 구분한다"고 했지만 **실제로는 구분이 안 됐다.** `console.warn` 이 "ODsay 가 HTTP 200 으로 에러 본문을 준 경우"에만 찍혀서, **프록시 403·Cloudflare 502·터널 다운이 전부 로그 없이 사라졌다.** 2026-08-04 종단 확인에서 `live:false` 가 났는데 Vercel 로그가 `Warning 0` 이라 원인을 못 좁혀 드러났다(실측). ⚠️ 전체 URL 에는 `apiKey` 가 들어 있어 **호스트만** 로그에 싣는다 — 그대로 찍으면 키가 평문으로 남는다 |
| 2026-08-04 | Claude(AI) | `app/api/status/route.ts` | 응답에 **배포 신원**(`deploy.env`·`branch`·`commit`)과 **ODsay 프록시 설정 여부**(`odsayProxy`·`odsayProxyAuth`) 추가. 값은 안 싣고 불리언·공개정보만 | ODsay 종단 확인 중 실제로 막혔다 — Preview 와 Production 의 `/api/status` 응답이 **글자 하나 다르지 않아** 어느 배포를 보고 있는지 확정할 수 없었다(`kakaoRedirect` 는 두 스코프가 같은 값이라 단서가 못 된다). `odsay: true` 인데 실패할 때 **"키 문제"인지 "프록시 미설정"인지**도 밖에서 구분이 안 됐다. ⚠️ 터널 주소(`ODSAY_BASE_URL`)와 공유 비밀은 **일부러 싣지 않는다** — 켜졌는지만 밝힌다 |
| 2026-08-03 | Claude(AI) | `memory/status.md` | §3-3 을 PR #24 머지 결과로 갱신 — "PR 대기" → 머지 완료(`12cc335`), 남은 일 2건(CEO 실키 확인 · 프로덕션 ODsay 키 재투입), 통합 세션 검토에서 나온 🟡 2건(판정 통일이 완전하지 않음 · `any` 1개 증가) 기록, 진단 플래그를 CI 매트릭스에 넣지 않는 판단과 근거 명시. §1 진행 현황에 #23·#24 추가 | "isRideType 하나로 통일"이라고만 적어두면 다음 사람이 엣지 케이스를 놓친다. 실측한 것만 적고 미완은 미완으로 남긴다는 원칙대로 검토 결과를 그대로 남겼다 |
| 2026-08-03 | Claude(AI) | `memory/status.md` | §3-3 을 **실측 결과로 전면 교체** — "시외는 근본적으로 답을 못 낸다 · ODsay 시외 전용 엔드포인트 필요"는 **틀린 주장이었다**. `rawTrafficType 4 = 철도` 확정(부산·김천 실측), 대전은 이미 정상, 고칠 것 4가지(매핑 추가 · `fare -1/0` 이 "무료"로 표시되는 버그 · 이동시간/상세 판정 비대칭 · 열차명 누락) 정리. §1 진행 현황과 `odsay:false` 설명도 갱신 | PR #21 진단 플래그로 CEO 로컬에서 4콜 실측한 결과 전제가 뒤집혔다. 낡은 주장을 그대로 두면 다음 세션이 **불필요한 API 교체 작업**을 시작한다 — 실측한 것만 적는다는 원칙대로 교체했다 |
| 2026-08-03 | Claude(AI) | `app/api/debug/route.ts`, `app/api/diag/route.ts`, `app/api/ai-trace/route.ts`, `app/api/auth/kakao/route.ts` | 디버그 잠금 판정을 `!process.env.ENABLE_DEBUG` → `process.env.ENABLE_DEBUG !== "1"` 로 (4곳 동일) | **`ENABLE_DEBUG=0` 이 잠금을 푸는 버그.** 값이 아니라 존재만 봐서 문자열 `"0"`(truthy)에도 잠금이 풀렸다 — 끈 줄 알고 넣은 값이 운영에서 `/api/diag`(외부 API 상태·서버 공인 IP)와 `/api/debug`(모임 생성)를 외부에 열어준다. CEO 가 실제로 `0` 을 넣어 발견 |
| 2026-08-03 | Claude(AI) | `.env.example` | `ENABLE_DEBUG` 안내 보강 — "끄려면 0 이 아니라 줄을 지우거나 주석 처리" · Vercel 에 넣지 말 것 · 로컬 dev 는 이 값과 무관 | 값으로 끄려는 시도가 반복될 자리다. 문서가 먼저 막는다 |
| 2026-08-03 | Claude(AI) | `lib/flags.ts` 🔒 | `odsayProbe` 플래그 1개 추가 (`NEXT_PUBLIC_FF_ODSAY_PROBE`) | ODsay 시외 응답을 **로컬에서 실측**하려면 껍데기 가드와 경로 캐시를 우회해야 한다. 상수로 껐다 켜면 브랜치마다 값이 달라져 충돌나므로(CLAUDE.md §3) 플래그가 유일한 방법이다 |
| 2026-08-03 | Claude(AI) | `lib/odsay.ts` | 진단 모드에서 껍데기 가드 우회(`verified: false` 부착) · `TransitLeg.rawTrafficType` 추가 · `TransitPathDetail.hasMapObj` 추가 · 0분 도보 구간 보존 | **시외에 ODsay 가 무엇을 주는지 관찰**하기 위함. 원시 `trafficType` 을 남기면 우리가 "도보"로 뭉개는 미매핑 수단(시외버스·열차 가능성)의 정체가 드러난다. ⚠️ `min <= 0` 은 진단 모드에서도 차단 — 이동시간 0 은 그 후보를 추천 1위로 만들어 결과를 통째로 왜곡한다 |
| 2026-08-03 | Claude(AI) | `lib/routing.ts`, `app/api/route-path/route.ts`, `app/api/route-detail/route.ts` | 진단 모드에서 경로 캐시 3종 우회 (읽기·쓰기 모두) | `routeCache`·`pathCache` 는 **TTL 이 없어** 한 번 성공하면 서버 재시작 전까지 같은 값만 돌아온다. 실측 중 "코드를 고쳐도 값이 안 바뀌는" 착시의 원인이라 우회가 필요하다 |
| 2026-08-03 | Claude(AI) | `app/components/RouteSheet.tsx` | 검증 안 된 응답에 `⚠ 검증 안 된 원시 응답` 칩 + 안내문 (기존 "ODsay 실시간" 칩 대체) | 가드를 풀면 **"ODsay 실시간 82분 · 0원 · 환승 0회"가 사실처럼 보이던 그 사고가 그대로 재현된다.** 가짜를 실제처럼 그리지 않는다(CLAUDE.md §6)를 지키려면 표시부도 함께 고쳐야 한다 |
| 2026-08-03 | Claude(AI) | `.env.example` | `NEXT_PUBLIC_FF_ODSAY_PROBE=0` + 배포 금지·비용 경고 명시 | 켠 채로 배포하면 검증 안 된 값이 사용자에게 보이고 무료 한도(1,000콜/일)를 태운다 |
| 2026-07-30 | Claude(AI) | `package.json` | `8.0.0` → **`0.1.0`** | 발표일 `1.0.0` 을 향한 SemVer 체계로 전환 |
| 2026-07-30 | Claude(AI) | `schema.sql` (루트) | 삭제 | v7 시절 **Neon용** 잔재. 아무 코드도 참조하지 않는데 팀원이 `supabase/schema.sql` 대신 잘못 실행할 위험이 있었다 |
| 2026-07-30 | Claude(AI) | `docs/legacy-algo/` (신규 8파일 + README) | 유실됐던 추천 알고리즘 8종 복원 | `memory/status.md` 가 `git show 5ff50ee:...` 로 꺼내라고 안내했는데, 그 커밋이 `main` 의 조상이 아닌 **별도 갈래**라 히스토리를 옮겨도 따라오지 않는다 |
| 2026-07-30 | Claude(AI) | `tsconfig.json` | `exclude` 에 `docs` 추가 | 위 참고용 파일들이 지금 코드와 타입이 맞지 않아 타입검사를 깨뜨린다 |
| 2026-07-30 | Claude(AI) | `package-lock.json` | `package.json` 과 동기화 (버전 `8.0.0`→`0.1.0`, `@playwright/test` 루트 devDependency 등록) | lock 이 어긋나면 CI 의 `npm ci` 가 죽는다. 팀원이 받자마자 빨간 CI 를 보게 될 자리였다 |
| 2026-07-30 | Claude(AI) | `package.json` | `next` `14.2.15` → **`^14.2.35`** | `npm audit` 에서 **critical** 취약점. 공개 저장소 + Vercel 배포 예정이라 방치할 수 없다. 14.2 안의 패치 업그레이드만 했다(16 으로 올리면 breaking change) |
| 2026-07-30 | Claude(AI) | `.nvmrc` (신규), `.github/workflows/ci.yml` | Node 버전을 `22` 로 고정하고 CI 가 `node-version-file` 로 같은 파일을 읽게 | 팀원 4명이 각자 다른 Node 로 개발하면 "내 컴에선 되는데"가 생긴다. 버전을 올릴 때 고칠 곳도 한 군데가 된다 |
| 2026-07-30 | Claude(AI) | `README.md`, `팀원_실행안내.md`, `docs/팀_개발환경.md` | 팀원 진입점 정비 — 무엇부터 읽고 무엇을 만지는지. v8 시절 서술 정정(채팅 위상·버튼 이름·구조도) | 저장소를 처음 여는 사람이 5분 안에 시작할 수 있어야 한다 |
| 2026-07-30 | Claude(AI) | `docs/팀_개발환경.md`, `docs/노션_통합개발환경.md` | 머지 방식 정정 — "Squash 만 허용" ❌ → `feat/*→develop` 은 Squash, `develop→main` 은 merge commit. 실제로 만든 룰셋 내용으로 설정표 갱신 | Squash 로만 잠그면 `develop→main` 에서 develop 커밋이 한 덩어리로 뭉개지고 두 브랜치 히스토리가 갈라져 이후 머지마다 충돌한다 |
| 2026-07-30 | Claude(AI) | `lib/supabase.ts`, `app/api/status/route.ts` | DB 접속 실패 시 **앱이 실제로 접속한 Supabase URL 과 원인 힌트**를 함께 노출 (끝 슬래시·스킴 누락·앞뒤 공백 자동 판정) | `TypeError: fetch failed` 는 원인을 전혀 말해주지 않아 `.env.local` 을 열어봐야만 오타를 찾을 수 있었다(CEO 가 실제로 겪음). 팀원 4명이 각자 키를 넣으므로 똑같이 겪을 자리다. URL 은 `NEXT_PUBLIC_` 값이라 비밀이 아니며, 키는 노출하지 않는다 |
| 2026-07-30 | Claude(AI) | `package.json` | `pretest:smoke` 추가 (`playwright install chromium`) | **`npm run verify` 가 방금 clone 한 환경에서 스모크 3/4 실패**했다(`Executable doesn't exist`). 문서가 "PR 전에 npm run verify"라고 시키는데 팀원 전원이 첫 PR 에서 이걸 만났을 것이다 |
| 2026-07-30 | Claude(AI) | `.gitignore` | `.env*.local` → `.env` · `.env.*` (+ `!.env.example`) | **공개 저장소인데 `.env` 와 `.env.production` 이 열려 있었다.** Next.js 는 그 둘도 읽으므로 키를 그 이름으로 만들면 그대로 커밋된다 |
| 2026-07-30 | Claude(AI) | `.gitignore` | `/logs/` 차단 추가 | `lib/ai.ts` 가 **참가자들의 실제 대화**를 `logs/ai-trace.jsonl` 에 기록한다. AI 채팅 플래그를 켜고 개발하면 파일이 생기는데, 공개 저장소라 실수로 커밋되면 대화가 그대로 공개된다. 지금은 채팅이 꺼져 있어 파일이 없을 뿐이다 |
| 2026-07-30 | Claude(AI) | `docs/팀_개발환경.md`, `docs/노션_통합개발환경.md`, `README.md` | **커밋 이메일 가리기** 안내 추가 (GitHub Emails 설정 2개 + `git config user.email` noreply) | 공개 저장소라 커밋에 박힌 이메일을 누구나 API 로 수집할 수 있다. 초기 커밋 9건에 개인 지메일이 남았고, **팀원 4명이 붙으면 4명 분이 더 노출된다.** 이미 올라간 것은 못 바꾸므로 앞으로를 막는다 |
| 2026-07-30 | Claude(AI) | `docs/팀원_온보딩.md` (신규) | 팀원이 **처음부터 끝까지 한 번에 읽는** 단일 온보딩 문서 — 이메일 가리기 → 세팅 → 소유권 → 첫 PR → 매일 지킬 것 → 절대 규칙 → 실제로 겪은 함정 5건 → 막혔을 때 | 문서가 README·팀원_실행안내·팀_개발환경 셋으로 흩어져 있어 팀원이 뭘 먼저 읽을지 몰랐다. 카톡·노션에 통째로 배포할 수 있는 형태 |
| 2026-07-30 | Claude(AI) | `lib/CLAUDE.md` (신규) | 데이터 계층 5규칙(store async · 쓰기 단위 · 캐시 금지) + 외부 API 래퍼 4규칙(mock 폴백 · 성공값만 캐시 · 가짜 표시 금지 · 비용 경고) + 소유권 표 | 채팅·AI 파싱 담당은 `lib/ai.ts`·`lib/parse.ts` 에서 일하는데 그 폴더에 지시서가 없었다. **`CLAUDE.md` 는 세션 시작 시 자동 로드**되므로 규칙을 프롬프트에 매번 붙이는 것보다 확실하고 드리프트가 없다 |
| 2026-07-30 | Claude(AI) | `.github/workflows/ci.yml` | **CHANGELOG 기입 확인 잡** 추가 — 코드·설정을 바꿨는데 `CHANGELOG.md` 를 안 건드린 PR 은 CI 실패. 문서만 고친 PR 은 통과 | "파일을 바꿨으면 기입"(CLAUDE.md §6)이 **기억에 의존하는 규칙**이었다. 바쁠 때 빠지고, 빠진 채로 3주가 지나면 무엇을 왜 바꿨는지 복원할 수 없다. **400줄 제한은 넣지 않았다** — `app/page.tsx`(1,118줄)·`lib/ai.ts`(592줄)가 이미 넘어서, 넣으면 담당자가 자기 파일을 아예 못 고치게 된다 |
| 2026-07-30 | Claude(AI) | `docs/팀원_온보딩.md` | `CHANGELOG.md` 충돌 해결법 추가 (양쪽 줄 다 남기기) + CI 가 기입을 막는다는 사실 명시 | 모든 PR 이 이 파일에 한 줄씩 더하므로 **구조상 자주 겹친다.** 오늘 PR 3건이 연속으로 이 충돌을 만났다. 팀원이 당황하지 않게 미리 알려둔다 |
| 2026-07-30 | Claude(AI) | `.env.example`, `lib/ai.ts` | Ollama 1순위 기본값에서 **팀 내부망 사설 IP 제거** (빈 값 → 2순위 localhost 로 폴백) | 공개 저장소에 내부망 주소가 남고, `/api/status` 의 `ai.url` 로 응답에 실려 나갔다 |
| 2026-07-30 | Claude(AI) | `app/api/diag/route.ts` | ODSAY 키의 앞 2자·뒤 2자 노출 제거 | 바로 위 주석이 "값 노출 X"라고 적어놓고 실제로는 키 조각 4자를 응답에 실었다 |
| 2026-07-30 | Claude(AI) | `package.json`, `.eslintrc.json` | `eslint` + `eslint-config-next` 설치, extends 를 `next/core-web-vitals` 로 (에러 2건 수정) | `npm run lint` 스크립트가 있는데 eslint 가 없어 실행하면 죽었다. `next/typescript` 는 기존 `any` 46곳을 전부 에러로 띄워 제외 — **React 훅 규칙이 목적**이다(조건부 return 뒤 훅 배치 사고를 lint 가 잡는다) |
| 2026-07-30 | Claude(AI) | `팀원_실행안내.md` | "Node.js 18 이상" → **Node 22** (`.nvmrc` 기준) | `.nvmrc`·README·CI 는 22 인데 이 문서만 18 이라 팀원이 18 로 맞출 수 있었다 |
| 2026-07-30 | Claude(AI) | `docs/팀_개발환경.md`, `.github/CODEOWNERS` | 존재하지 않는 `sections/ChatSection.tsx` → 실제 파일 `ChatPanel.tsx` | @TODO 자리에 아이디를 채워 주석을 풀면 **없는 경로라 GitHub 이 그 줄을 조용히 무시**해 채팅 담당 소유권이 안 걸렸을 것이다 |
| 2026-07-30 | Claude(AI) | `.github/CODEOWNERS`, `docs/팀_개발환경.md` | 경로의 `[code]` → `*` (`/app/m/*/sections/ChatPanel.tsx`) + 함정 경고 | **CODEOWNERS 는 gitignore 문법이라 대괄호가 문자 클래스로 해석된다.** `[code]` 는 `c`·`o`·`d`·`e` 중 한 글자에 매칭되고 실제 폴더 `app/m/[code]/` 에는 절대 매칭되지 않는다 — 파일명을 고쳐도 그 줄은 여전히 무효였다. `git check-ignore` 로 재현 확인 |
| 2026-07-30 | Claude(AI) | `app/m/[code]/CLAUDE.md` | "팀원이 만질 파일은 모두 400줄 미만" 주장 정정 — 실제로 `app/page.tsx` 1,118줄 · `lib/ai.ts` 590줄이 팀원 소유다 | 인계 문서의 거짓 주장은 다음 세션이 잘못된 전제로 작업하게 만든다(v8 에서 실제로 겪은 실패) |
| 2026-07-30 | Claude(AI) | `docs/노션_통합개발환경.md` | 줄 수 `1,352` → `905` 최신화 · 아직 없는 스코어러 3종에 "앞으로 만들 파일" 표시 · Code Owners 설정이 `docs/팀_개발환경.md` 와 정반대이던 모순 제거 | 멘토님께 공유하는 문서라 사실관계가 어긋나면 안 된다 |
| 2026-07-31 | Claude(AI) | `package.json`, `package-lock.json` | `@supabase/supabase-js` 제거 → `@neondatabase/serverless` 추가 (`npm install` 로 lock 동기화) | **팀 결정: DB 를 Neon 으로 이관** (멘토님 추천 수용). lock 이 어긋나면 CI 의 `npm ci` 가 죽는다 |
| 2026-07-31 | Claude(AI) | `lib/db.ts` (신규), `lib/supabase.ts` (삭제) | 서버 전용 Neon 클라이언트 — `DATABASE_URL` 하나(서버 전용, `NEXT_PUBLIC_` 금지). 접속 실패 진단(스킴·끝 슬래시·공백 감지)은 postgres:// 형식에 맞게 이식, **URL 은 비밀번호를 *** 로 가린 masked 만 노출** | Supabase URL 은 공개값이었지만 `DATABASE_URL` 에는 비밀번호가 들어 있다 — 원문 노출은 사고다 |
| 2026-07-31 | Claude(AI) | `lib/persistence.ts` | Supabase 클라이언트 호출 7종(loadMeeting·meetingExists·saveMeeting·upsertParticipant/s·setVote·clearVotes)을 Neon SQL(`insert … on conflict`)로 재작성. 쓰기 단위 3분리와 votes PK(code,target,participant_id)=1인 1표 upsert 의미 유지. timestamptz 가 Date 로 오는 차이는 `isoOf()` 로 흡수 | 저장 계층만 교체하고 도메인(store.ts 인터페이스)은 그대로 두기 위함 |
| 2026-07-31 | Claude(AI) | `lib/store.ts` | `hasSupabase` → `hasDb` (import 를 `lib/db` 로), `storeInfo.backend` `"supabase"` → `"neon"`. 로직 변경 없음 | `lib/supabase.ts` 가 사라져 import 가 깨진다 — 이관에 필연적인 최소 수정 |
| 2026-07-31 | Claude(AI) | `app/api/status/route.ts`, `app/api/diag/route.ts` | `store: "neon"` 표기, 실패 시에만 `db.url`(masked)+`urlHint` 노출 유지. diag 는 테이블 3종 개별 점검을 Neon 쿼리로, keyKind/RLS 안내 제거 | Neon 엔 anon/service_role 구분과 RLS 가 없다 |
| 2026-07-31 | Claude(AI) | `supabase/` → `db/` (폴더명 변경), `db/schema.sql`, `db/migrations/README.md` | RLS 정책·role 참조 제거 — Neon SQL Editor 에 그대로 붙여 실행 가능. migrations `NNN_*.sql` 패턴 유지, 안내 문구 Neon 콘솔 기준으로 | Neon 은 서버만 접속하므로 RLS 가 무의미하고, 남겨두면 실행은 되지만 팀원이 잘못된 전제를 갖는다 |
| 2026-07-31 | Claude(AI) | `.github/workflows/ci.yml` | changelog 잡 경로 패턴 `supabase/` → `db/` | 폴더명 변경 후에도 스키마 변경 PR 이 CHANGELOG 기입 검사를 받게 |
| 2026-07-31 | Claude(AI) | `.env.example` | Supabase 3키 → `DATABASE_URL` 하나(서버 전용) | 환경변수 통일. 키 없으면 인메모리 폴백은 그대로 |
| 2026-07-31 | Claude(AI) | `README.md`, `팀원_실행안내.md`, `docs/팀원_온보딩.md`, `docs/팀_개발환경.md`, `docs/노션_통합개발환경.md`, `docs/APK.md`, `CLAUDE.md`, `lib/CLAUDE.md`, `memory/status.md` | 스택·데이터 규칙·세팅 절차·소유권 표의 Supabase 서술을 Neon/`db/` 기준으로 정비. status.md §6 "멘토 확인 대기" → "팀 결정: Neon 확정", Neon 실접속은 **미검증** 으로 명기 | 문서가 코드와 어긋나면 다음 세션이 잘못된 전제로 시작한다(§4 에 기록된 실제 사고) |
| 2026-07-31 | Claude(AI) | `lib/db.ts`, `app/api/status/route.ts` | `maskPassword` 를 정규식 매칭 → `lastIndexOf("@")` 경계 방식으로 재작성 + status 에 "마스킹 흔적 없으면 원문 대신 안내문" 안전벨트 | **PR #15 리뷰 지적(🔴)** — 스킴 없는 `DATABASE_URL`(psql 명령 통째 복사 등)은 정규식이 매칭 실패해 **비밀번호 원문이 `/api/status` 에 노출**됐다. 하필 그 경우가 진단이 잡으라던 상황이다. `@` 포함 비밀번호의 부분 노출(🟡)도 함께 해결 |
| 2026-07-31 | Claude(AI) | `docs/팀원_온보딩.md` | 🆘 표에 "모임이 안 보이면 먼저 `/api/status` 의 `db.ready`" 행 추가 | PR #15 리뷰 지적(🟡) — 접속 실패가 "모임 없음"처럼 보여 코드 탓으로 오인하기 쉽다 |
| 2026-07-31 | Claude(AI) | `app/api/status/route.ts` | 안전벨트 판정 반전 — "@ 가 있는가" → "스킴 뗀 나머지에 콜론/@ 가 있는데 마스킹 흔적(`:***@`)이 없는가" | **PR #15 재검토 지적(🔴 잔여 엣지)** — `@` 앞에서 잘린 값(개행으로 `postgresql://user:비밀번호` 까지만 읽힌 경우)이 기존 판정을 통과해 평문 노출됐다. 의심스러우면 안 싣는 쪽으로 넘어진다 |
| 2026-07-31 | Claude(AI) | `.github/CODEOWNERS` | `/lib/supabase.ts` → `/lib/db.ts`, `/supabase/` → `/db/` 경로 갱신 | PR #15(Neon 이관) 머지로 두 줄이 없는 경로가 됐다 — CODEOWNERS 는 없는 경로를 **조용히 무시**하므로 그대로 두면 공용 파일 소유권이 안 걸린다(`ChatSection.tsx` 건과 같은 유형) |
| 2026-07-31 | Claude(AI) | `memory/status.md` | §1 진행 중 현황(PR #15 머지 완료 · #14 승인 대기 · #17 열림 · Neon 검증 대기) · §3-2 를 PR #14 이후 사실로 갱신(weather.ts 는 머지 대기, routing.ts `ScoreContext` 채우기는 통합 세션 몫) · 날씨 weight 0.3 을 팀 결정 안건으로 기록 | 문서가 코드와 어긋나면 다음 세션이 잘못된 전제로 시작한다(§4 사고 기록). 리뷰(🟡1)에서 지적된 낡은 주장 정리 |
| 2026-07-31 | Claude(AI) | `memory/status.md` | §1 에 카카오 로그인 프로덕션 동작 확인 + 도메인 바뀔 때 고칠 두 곳(Vercel `KAKAO_REDIRECT_URI` · 카카오 콘솔 Redirect URI) 표로 기록 | 한쪽만 고치면 조용히 실패한다 — 환경변수를 빠뜨리면 localhost 로 튕기고, 콘솔 등록을 빠뜨리면 KOE006 이 뜬다. 둘 다 실제로 겪었다 |
| 2026-07-31 | Claude(AI) | `memory/status.md` | §1 을 릴리스 `f0a4235` 후 실측으로 교체 — "Neon 실접속 미검증" → **로컬·프로덕션 양쪽 `db.ready:true` 실측**, Vercel 배포 완료(Production←main / Preview←그 외), `odsay:false`·`ai.ok:false` 가 의도된 상태임을 명시, Preview 가 실 DB 를 공유하는 점 경고, 릴리스 리뷰 후속 2건(500 빈 본문 · postcss) 기록. §3-5 Vercel 항목 체크 | 릴리스 전 문서에는 미검증으로 적혀 있었다. 실측한 것만 적고 확인 안 된 건 미검증으로 남긴다는 원칙대로 교체 — 다음 세션이 낡은 전제로 시작하는 것을 막는다 |
| 2026-07-31 | Claude(AI) | `app/api/meeting/route.ts` | GET·POST 를 try/catch 로 감싸 예외를 `{ error, detail, hint }` 500 으로 바꾼다 | **릴리스 리뷰 후속 1번** — `lib/persistence.ts` 는 쓰기 실패 시 throw 하는데 여기서 안 잡아 Next.js 기본 500 이 **빈 본문**으로 나갔다. `DATABASE_URL` 이 있으면 인메모리 폴백을 타지 않는 설계라(의도됨) DB 가 끊기면 이 경로로 죽는데, 화면에도 로그에도 원인이 안 남았다 |
| 2026-07-31 | Claude(AI) | `lib/db.ts` | `sanitizeDbError()` 추가 — 오류 메시지에 섞인 접속 주소의 비밀번호를 `***` 로 가린다 | 위 `detail` 을 응답에 실으려면 자격증명 노출부터 막아야 한다. 드라이버가 접속 실패 메시지에 주소를 통째로 넣는 경우가 있다. 비밀번호 쪽 패턴은 greedy 로 잡는다 — `[^\s@]*` 는 첫 `@` 에서 멈춰 `u:p@ssSECRET@host` 뒷부분이 남는다(PR #15 🟡2 와 같은 실수를 만들 뻔했고 실측에서 잡았다) |

**아래 `v8.x` 기록은 옛 저장소(`kimyuhwan0126/Moimer`)에서 이어진 것이다.**
저장소는 그대로 남아 있으니 그 이전 이력이 필요하면 거기서 본다.

---

## v8.0.0 — 2026-07-30

### v8 — 클릭 프로토타입 구현 + Supabase 영속화 (claude/moimer-v8-implementation-plan)

CEO 결정: v8 클릭 프로토타입의 방향을 채택. 프로토타입의 원본이 `feat/v7-mockup`
브랜치의 실앱이므로 그 트리를 베이스로 가져오고, 코드 네이밍을 v8로 정렬한 뒤
프로토타입과 어긋난 화면을 맞췄다. AI 채팅은 **비활성만** 하고 코드는 보존한다
(나중에 다시 넣을 수 있도록). 데이터는 인메모리 → **Supabase**로 전환.

| 날짜 | 작업자 | 대상 파일/폴더 | 변경 내용 | 사유 |
|---|---|---|---|---|
| 2026-07-30 | Claude(AI) | 전체 | `feat/v7-mockup` 트리를 main 히스토리 위에 커밋 1개로 채택 | main과 v7은 공통 조상이 없어 머지 불가. v7이 main의 lib/ai.ts·prefs·AI trace를 이미 포함 |
| 2026-07-30 | Claude(AI) | `capture-feature.mjs`, `capture-prefs.mjs` | 제거 | 데모 GIF 캡처용(puppeteer 의존) — v8 범위 외 |
| 2026-07-30 | Claude(AI) | `package.json` | 7.0.0 → 8.0.0, `@neondatabase/serverless` → `@supabase/supabase-js` | 버전 정렬 · DB를 Supabase로 결정 |
| 2026-07-30 | Claude(AI) | `app/components/v7/` → `app/components/v8/` | 폴더·`V7Header`→`V8Header`·`V7Tab`→`V8Tab` 이름 변경 | 프로토타입이 v8이므로 코드 네이밍도 v8로 통일 |
| 2026-07-30 | Claude(AI) | `app/globals.css` + 화면 6곳 | CSS 클래스 접두사 `v7-*` → `v8-*` (107곳), localStorage 키 `moimer:v7:*` → `moimer:v8:*` | 같은 사유. 미배포 상태라 키 마이그레이션 불필요 |
| 2026-07-30 | Claude(AI) | `app/components/v8/Splash.tsx` | 자동 전환 3슬라이드 → 한 화면 + 차별점 키워드 칩 3개, 하단 "시작하기" CTA | 1.8초마다 넘어가 읽기 전에 사라졌다. "공평한 중간지점"만으로는 기존 서비스와 구분 안 됨 |
| 2026-07-30 | Claude(AI) | `app/meetings/page.tsx` | 생성 완료 화면을 요약 카드로 교체 (이름·코드·정원·방장·모임 시간 + 초대 링크) | 초대 URL만 주면 입력값이 제대로 들어갔는지 확인할 데가 없었다 |
| 2026-07-30 | Claude(AI) | `app/m/[code]/MeetingClient.tsx` | `✍ 다른 후보로 정하기` 게이트를 `stage==="chat"` → `stage!=="result"` 로 수정 | 거점 투표 단계(stage=main)에서 모달이 아예 열리지 않던 버그 |
| 2026-07-30 | Claude(AI) | `app/m/[code]/MeetingClient.tsx` | 수동 확정 모달을 라디오 선택 + "이 후보로 확정" 방식으로 교체 | 후보 버튼을 바로 누르면 오클릭으로 확정됐다 |
| 2026-07-30 | Claude(AI) | `app/m/[code]/MeetingClient.tsx`, `app/globals.css` | 방장 바 문구를 투표 진행률 기반으로 변경 (전원 투표 시 "투표 종료 및 확정" 강조) | "강제 확정(방장 권한)"이 정상 마감도 월권처럼 읽혔다 |
| 2026-07-30 | Claude(AI) | `supabase/schema.sql` (신규) | meetings(+jsonb) / participants / votes 3테이블 + RLS + updated_at 트리거 | 쓰기 경합 기준으로 분리 — 참가자·투표를 모임 행에 담으면 동시 쓰기에 표가 사라진다 |
| 2026-07-30 | Claude(AI) | `lib/supabase.ts`, `lib/persistence.ts` (신규) | 서버 전용 클라이언트 + Meeting ↔ 행 매핑 | 도메인 로직을 건드리지 않고 저장 계층만 교체하기 위함 |
| 2026-07-30 | Claude(AI) | `lib/store.ts` | 전 함수 async 화 + 읽기/쓰기 경계 추가. Supabase 모드에선 인메모리 캐시 없음 | 서버리스는 인스턴스가 여러 개라 캐시를 들면 다른 인스턴스가 쓴 표가 폴링에 안 보인다 |
| 2026-07-30 | Claude(AI) | `lib/store.ts` | `setRegionCandidates` 의 후보 변경 감지를 id → **지역 이름** 비교로 수정 | id는 순위(r1·r2·r3)라 후보가 바뀌어도 그대로 → 엉뚱한 지역의 표가 남았다 |
| 2026-07-30 | Claude(AI) | `app/api/debug/route.ts` | `seedScenario` 에 빠진 `await` 추가 | Promise 가 되었는데 await 이 없어 시드가 적용되지 않았다 (타입 검사로 안 잡히는 자리) |
| 2026-07-30 | Claude(AI) | `lib/ai.ts` | 호출부 await 추가 + `search_more_places`/`evaluate_region` 결과를 `saveCandidates` 로 명시 저장 | DB 모드에선 객체를 그 자리에서 고쳐도 저장되지 않는다 |
| 2026-07-30 | Claude(AI) | `app/api/status/route.ts`, `app/api/diag/route.ts`, `lib/env.ts` | DB 연결 상태 노출 (`configured`/`keyKind`/`ready`) + 테이블별 조회 진단 | 키만 있고 스키마 미적용·RLS 차단인 경우를 구분해야 원인을 안다 |
| 2026-07-30 | Claude(AI) | `.env.example` | Neon → Supabase 전면 갱신, 키 발급 위치·주의사항 명시 | DB 전환 반영 |
| 2026-07-30 | Claude(AI) | `README.md` | v8 기준으로 재작성 | 옛 `develop` 보일러플레이트(src/·zustand·room/[id])를 설명해 실제 코드와 달랐다 |
| 2026-07-30 | Claude(AI) | `CLAUDE.md` | v8 실제 스택 기준으로 재작성 (Tailwind·shadcn·Gemini·src/ 기술 제거), 색상·데이터 계층 규칙 추가 | AI가 매 세션 처음 읽는 파일이 코드와 달라 잘못된 전제를 갖게 됐다 |
| 2026-07-30 | Claude(AI) | `팀원_실행안내.md` | v8 기준 + Supabase 준비 절차(스키마 실행·키 3종·확인 방법) 추가 | 팀원이 DB 없이 시작해 데이터가 사라지는 혼란 방지 |
| 2026-07-30 | Claude(AI) | `memory/status.md` | v8 상태로 갱신 → 이후 추천 알고리즘 현황·미착수 항목·이식 유실 버그 목록까지 인계 문서로 재작성 | 진행 상황 인계. 특히 "프로토타입이 고쳐둔 것이 실앱으로 이식되지 않은" 패턴이 반복돼 재발 방지 기록을 남김 |
| 2026-07-30 | Claude(AI) | `app/globals.css`, `app/m/[code]/MeetingClient.tsx` | `.leaderbar` 를 sticky → **fixed** 로 변경 + 하단 여백 136→148px | 브라우저 검증에서 발견: sticky+bottom:64px 는 문서 끝까지 스크롤하면 바가 흐름 위치보다 64px 위로 올라앉아 마지막 카드를 덮었다(`✍ 다른 후보로 정하기` 가 31px 가려져 클릭 불가). 하단 5탭과 같은 fixed 방식으로 통일 |
| 2026-07-30 | Claude(AI) | `lib/persistence.ts` | 참가자 조회 정렬을 `is_leader desc, joined_at asc, id asc` 로 완전 결정화 | joined_at 단독 정렬은 동순위가 가능하고(Postgres now()는 트랜잭션 시작 시각), 참가자 순서가 PIN_COLORS 색인이라 폴링마다 순서가 흔들리면 사람별 칩·핀 색이 계속 바뀐다 |
| 2026-07-30 | Claude(AI) | `app/components/v8/LoginSheet.tsx` | 오버레이를 `createPortal(document.body)` 로 이동 | `.v8-header` 의 `backdrop-filter` 가 fixed 자손의 기준 박스를 헤더(높이 56px)로 바꿔, 로그인 모달 위쪽(제목·이름칸)이 화면 밖으로 잘렸다. `+` 버튼 → 로그인 시트에서 실제로 발생 |
| 2026-07-30 | Claude(AI) | `app/globals.css` | `.v8-modal`·`.modal` 에 `max-height:calc(100dvh - 여백)` + `overflow-y:auto` 추가 | 프로토타입 html 에서 이미 고쳐뒀던 수정인데(`.proto-screen .v7-modal{max-height:100%;overflow-y:auto}`), 그게 "프로토타입 전용(실제 앱엔 없음)" 블록 안에 있어 실앱으로 이식되지 않았다. 창이 짧으면 모달이 잘리고 스크롤도 안 됐다 |
| 2026-07-30 | Claude(AI) | `app/page.tsx`, `app/globals.css` | 검색 결과 줄에 (+) 표시 이식 + `+` 칩을 "검색창으로 데려다주기"(스크롤·포커스·1.2초 강조)로 개선 | 프로토타입에서 확정한 (+) 표시("누르면 출발지로 추가된다")가 실앱 이식 때 빠졌고, `+` 칩은 포커스만 줘서 아무 일도 안 일어난 것처럼 보였다 (CEO 보고) |
| 2026-07-30 | Claude(AI) | `app/api/status/route.ts` | `kakaoRedirect`(앱이 실제로 보내는 Redirect URI) 노출 | KOE006 진단용 — 콘솔 등록값과 글자 단위 대조가 가능해진다. URL이라 비밀 아님 |
| 2026-07-30 | Claude(AI) | `app/components/v8/Icons.tsx` | 모임 참여 아이콘 화살표를 문 "안으로" 향하게 교체 | 밖으로 나가는 방향이면 탈퇴/로그아웃처럼 읽힌다 — 프로토타입·회의록에서 확정했던 방향인데 실앱만 반대로 남아 있었다 (CEO 지적) |
| 2026-07-30 | Claude(AI) | `app/api/auth/kakao/route.ts` | `?debug=1` 진단 모드 추가 (dev 전용) — 카카오에 보내는 REST키 마스킹·redirectUri·인가 URL을 JSON으로 반환 | KOE006은 카카오 페이지에서 막혀 앱이 사유를 못 보여준다. "보낸 값"을 보여주면 콘솔 등록값과 대조해 원인을 찾을 수 있다 |
| 2026-07-30 | Claude(AI) | `app/m/[code]/MeetingClient.tsx` | 내정보 프로필을 출발지 폼에 연결 — 저장 위치를 원터치 칩으로, 애용 이동수단을 기본값으로 | 내정보 탭이 "모임을 만들 때 바로 불러와요"·"새 모임에 기본 적용"이라 약속하는데 이 폼이 프로필을 읽지 않아 반영되지 않았다 (CEO 보고). 이미 등록된 출발지의 이동수단은 서버 값이 우선 |
| 2026-07-30 | Claude(AI) | `app/meetings/page.tsx` | 모임 생성·참여의 이름 입력을 선택사항으로 (비우면 로그인 이름 사용) | 이미 로그인해 이름을 준 사용자에게 매번 다시 입력하게 했고, 비우면 버튼이 비활성이었다 (CEO 요청) |
| 2026-07-30 | Claude(AI) | `lib/format.ts` (신규) + 화면 4곳 | 이동시간 표기를 `82분` → `1시간 22분` 으로. 거리·요금 포맷도 함께 통일 | 60분 넘는 값을 분으로만 쓰면 얼마나 먼지 감이 안 온다 (CEO 지적) |
| 2026-07-30 | Claude(AI) | `lib/odsay.ts` | 탑승 구간이 없거나 시간이 0인 응답을 버리고 null 반환(→ 추정값 폴백). 경로 라벨을 pathType 대신 **실제 파싱된 구간**으로 생성 | 시외 장거리(서울→김천)에서 ODsay가 빈 껍데기를 주는데 앱이 "ODsay 실시간 82분 · 0원 · 환승 0회"로 사실처럼 표시했다. CLAUDE.md §3-6(가짜 데이터를 실제인 것처럼 그리지 않는다) 위반 |
| 2026-07-30 | Claude(AI) | `app/components/RouteSheet.tsx` | 추정값일 때 경고 박스 추가 — 대중교통 API가 수도권 중심이라 KTX·고속버스가 반영되지 않음을 명시 | 값이 실제와 크게 어긋날 수 있음을 숨기지 않는다 |
| 2026-07-30 | Claude(AI) | `lib/types.ts`·`lib/routing.ts`·`lib/store.ts`·`app/api/meeting`·`app/m/[code]/MeetingClient.tsx` | **거점 후보 직접 등록** — 방장 포함 누구나 지도 검색으로 원하는 지역을 후보에 추가 (`addRegion` 액션 + `＋ 다른 후보 등록` 모달) | 자동 추천이 수도권 밖에서 기하 중간점(시골)으로 잡히는 문제의 현실적 탈출구. 추천 알고리즘 개선 전까지 사람이 후보를 낼 수 있어야 투표가 의미를 가진다 (CEO 지시) |
| 2026-07-30 | Claude(AI) | `app/m/[code]/MeetingClient.tsx` | `✍ 다른 후보로 정하기` → `✍ 다른 후보로 확정`(방장 전용)으로 개명하고, 그 옆에 `＋ 다른 후보 등록`(전원) 배치 | 두 기능은 성격이 다르다(후보를 늘리는 것 vs 마감하는 것) — 같은 이름이면 헷갈린다 |
| 2026-07-30 | Claude(AI) | `lib/store.ts` `setRegionCandidates` | 사용자 등록 후보(`rc_*`)를 자동 재계산에서 보존 | 출발지가 바뀌어 후보가 재계산될 때 사람이 낸 후보가 사라지면 안 된다 |
| 2026-07-30 | Claude(AI) | `app/m/[code]/MeetingClient.tsx` | 검색 0건일 때 "이름 그대로 등록" 폴백 (좌표는 서버가 지오코딩) | 검색이 못 찾는 지명에서 막다른 길이 되는 것을 막는다 — 프로토타입이 뒀던 안전망과 동일 |
| 2026-07-30 | Claude(AI) | `app/api-live/page.tsx` | 진단 차단 시 안내를 실행 가능하게 보강 (`npm run dev` / `ENABLE_DEBUG=1` / `/api/status` 대안 제시) | 운영 빌드에서 `/api/diag` 가 403 인데 "개발 모드 전용"만 떠 다음 행동을 알 수 없었다 |

검증: `npx tsc --noEmit` 통과 · `next build` 통과 · **실제 브라우저(Chromium)로 화면 구동 확인**
(스플래시 문구·키워드 칩 / 출발지 3개 추가 → 중간지점 "건대입구 최대 40분 편차 15분" /
시간순 전환 시 "경로 API 키 없음 — 거리 추정" 정직 표기 / 생성완료 요약 5필드 + 초대링크 /
거점 단계에서 `✍ 다른 후보로 정하기` 모달 열림·라디오 3개 / 방장 바 문구가
"0/1명 투표 중 · 지금 확정" → "1/1명 투표 완료 · 투표 종료 및 확정" 으로 전환) ·
실서버 API 전 구간 왕복
(생성 → 참여 3명 → 모임시간 → 출발지 4명 → 거점후보 → 4명 동시투표 4표 전부 기록 →
투표취소 → 비방장 확정 거부 → 거점 확정(stage=main) → 가게 투표·확정 → 자가신고 →
예약 → 최종상태). 후보 변경 시 표 무효화 / 후보 동일 시 표 유지 각각 확인.

---

## v8.1.0 — 2026-07-30

### 팀 동시 개발 기반 — 머지 충돌이 안 나는 구조로 재배치

CEO 결정: 새 저장소에서 팀원들과 각자 브랜치로 **동시에** 개발하고, 통합 담당자가
머지를 전담한다. 팀원 중 일부는 Ollama+GLM 으로 Claude Code 를 쓴다.
그래서 (1) 파일을 소유자 단위로 쪼개고 (2) 켜고 끄는 걸 코드가 아니라 환경변수로
옮기고 (3) 사람 리뷰 전에 기계가 먼저 잡도록 CI 를 세웠다.
안드로이드 배포는 **Flutter 재작성이 아니라 PWA + TWA** 로 확정.

| 날짜 | 작업자 | 대상 파일/폴더 | 변경 내용 | 사유 |
|---|---|---|---|---|
| 2026-07-30 | Claude(AI) | `lib/flags.ts` (신규) | `NEXT_PUBLIC_FF_*` 환경변수 기반 기능 플래그 4종 | 상수(`AI_CHAT_ENABLED=false`)는 켜려면 코드를 고쳐야 해 브랜치마다 값이 달라지고, 머지할 때마다 같은 줄에서 충돌난다 |
| 2026-07-30 | Claude(AI) | `app/m/[code]/MeetingClient.tsx` | `AI_CHAT_ENABLED` 를 `FLAGS.aiChat` 에서 읽도록 변경 (14곳 사용부는 그대로) | 위와 동일. 개발하려고 켠 걸 실수로 커밋하면 남의 화면까지 켜졌다 |
| 2026-07-30 | Claude(AI) | `lib/scoring/{types,index,fairness}.ts` (신규) | 점수 계산을 **스코어러 플러그인** 구조로. 파일 = 관점 하나 = 담당자 한 명 | 상권·날씨·개인선호를 여러 명이 붙이는데, 점수식이 한 줄에 있으면 전원이 그 줄에서 충돌한다 |
| 2026-07-30 | Claude(AI) | `lib/geo.ts`, `lib/routing.ts` | 복제돼 있던 점수식(`maxMin + devMin*0.8`) 2곳을 `fairnessRaw()` 한 곳으로 통합 | 같은 식이 두 파일에 있어 한쪽만 고치는 사고가 나는 자리였다 |
| 2026-07-30 | Claude(AI) | `lib/scoring/types.ts` | `decayScore()`(0~1 정규화) · `worstOf()`(평균 대신 최솟값) 제공 | 구버전 `enhanced-scoring.ts` 경고 반영 — 분 단위 점수 하나가 0~1 점수 전부를 압도했다. 공평성은 평균이 아니라 최악 기준이어야 한다 |
| 2026-07-30 | Claude(AI) | `lib/parse.ts` (신규) | 규칙 기반 한국어 파싱 — 날짜·시간·예산·목적·분위기·음주·식이 | 팀원 PC 의 Ollama 는 배포 환경·발표날 스마트폰에서 닿지 않는다. LLM 없이 항상 도는 경로가 필요 (§4 의 mock 폴백 원칙을 파싱에 적용) |
| 2026-07-30 | Claude(AI) | `tests/smoke.spec.ts`, `playwright.config.ts` (신규) | 핵심 경로(생성→출발지→거점투표→확정)를 실제 브라우저로 검증 | `tsc`·`build` 를 **둘 다 통과**하고도 화면이 통째로 안 그려진 적이 있다. 일부러 깨뜨려 이 테스트가 잡는 것을 확인함 |
| 2026-07-30 | Claude(AI) | `.github/workflows/ci.yml` (신규) | `tsc` + `build` + 스모크를 flags-off / flags-on **두 번** 실행 | 안 돌려보는 플래그는 "언제든 켤 수 있는 코드"가 아니라 "켤 수 있어 보이는 죽은 코드"다 |
| 2026-07-30 | Claude(AI) | `.github/CODEOWNERS` (신규) | 공용 파일에 통합 담당자 리뷰 강제, 기능별 담당 자리 표시 | 여러 명이 같은 파일을 고치는 것을 사람 기억이 아니라 GitHub 이 막게 |
| 2026-07-30 | Claude(AI) | `supabase/migrations/` (신규) | 스키마 변경은 `schema.sql` 수정이 아니라 번호 붙은 파일 추가로 | 세 사람이 컬럼을 추가할 때마다 같은 파일에서 충돌난다 |
| 2026-07-30 | Claude(AI) | `app/manifest.ts`, `public/sw.js`, `public/offline.html`, `public/icon-*.png` (신규) | PWA — 매니페스트 · 서비스 워커 · 아이콘 4종 | 안드로이드 APK(TWA)의 전제 조건. Flutter 재작성 없이 지금 웹앱이 그대로 앱이 된다 |
| 2026-07-30 | Claude(AI) | `public/sw.js` | `/api/*`·비GET·타 출처를 **캐시하지 않음**. 하는 일은 오프라인 안내뿐 | 1.8초 폴링으로 남의 투표를 받아오는 앱이라, 서비스 워커가 캐시하면 "투표가 반영 안 되는" 재현 어려운 버그가 난다 |
| 2026-07-30 | Claude(AI) | `app/components/ServiceWorkerRegistrar.tsx` (신규), `app/layout.tsx` | 운영 빌드에서만 SW 등록 + PWA 메타데이터 | 개발 중 SW 가 살아있으면 코드를 고쳐도 옛 화면이 나온다 |
| 2026-07-30 | Claude(AI) | `docs/APK.md` (신규) | PWA → Bubblewrap → APK 절차, Flutter 를 쓰지 않는 이유 | 발표날 팀원 폰 설치가 목표. 당일에 처음 시도하면 안 되는 작업 |
| 2026-07-30 | Claude(AI) | `docs/팀_개발환경.md` (신규) | 브랜치 모델 · 저장소 보호 설정 · 파일 소유권 · Vercel Preview · AI 개발 규칙 | 팀원이 읽고 그대로 따라 할 수 있는 한 장 |
| 2026-07-30 | Claude(AI) | `docs/팀_개발환경.md`, `docs/노션_통합개발환경.md` | 저장소 보호 절차를 최신 GitHub UI 기준으로 정정 — `Settings → Branches` → **`Settings → Rules → Rulesets`**, Secret scanning 위치(`Advanced Security` 하단) 명시 | 사이드바에 `Branches` 항목이 없어 팀원이 문서대로 따라갈 수 없었다(CEO 확인). Ruleset 은 `Enforcement: Active` 로 바꿔야 적용된다는 함정도 함께 기록 |
| 2026-07-30 | Claude(AI) | `docs/팀_개발환경.md`, `docs/노션_통합개발환경.md` | 항목 이름을 `Secret scanning` → **`Secret Protection`** 으로 정정. "버튼 글씨는 상태가 아니라 행동"(`Disable` = 이미 켜짐) 경고 추가. Dependabot·CodeQL 은 켜지 않는다고 명시 | GitHub 이 이름을 바꿔 팀원이 못 찾았다(CEO 확인). 켜져 있는 항목의 `Disable` 버튼을 "켜는 버튼"으로 오해하면 보호가 꺼진다 |
| 2026-07-30 | Claude(AI) | `lib/scoring/CLAUDE.md` (신규) | 스코어러 추가 방법 + 절대 규칙 4개 (복붙 템플릿 포함) | 컨텍스트가 짧은 모델(GLM)은 긴 루트 지시의 뒷부분을 흘린다 — 폴더별 짧은 지시가 더 잘 지켜진다 |
| 2026-07-30 | Claude(AI) | `CLAUDE.md` | 채팅=플래그 기능으로 위상 정정 · 팀 동시 개발 규칙 · 400줄 상한 · 하위 CLAUDE.md 안내 | §0 이 "대화는 카카오톡에서"라 채팅 코드를 범위 외로 오해할 수 있었다 |
| 2026-07-30 | Claude(AI) | `.env.example`, `package.json` | 플래그 4종 문서화 · `test:smoke`/`verify` 스크립트 · `@playwright/test` | — |

### 모임 상세 화면 분할 (팀원 합류 전 필수 작업)

| 날짜 | 작업자 | 대상 파일/폴더 | 변경 내용 | 사유 |
|---|---|---|---|---|
| 2026-07-30 | Claude(AI) | `app/m/[code]/sections/` (신규 **15파일**) | `MeetingClient.tsx` 1,802줄 → **906줄** (-50%). 화면 조각을 전부 분리: VoteList · ChatPanel(+PrefChips) · AddRegionModal · ManualPickModal · LeaderBar · MeetingHeader · MapPanel · OriginForm · ParticipantList · ResultSection · ReserveModal · TravelTimes · PastStepView · AddParticipant · DebugWidget | 한 파일을 두 사람이 만지면 매일 충돌난다. 담당자가 만질 파일이 전부 400줄 미만이 되는 것이 목적 |
| 2026-07-30 | Claude(AI) | `app/m/[code]/MeetingClient.tsx` | 거점 투표 행이 STAGE MAIN 과 STAGE CHAT 에 **복제**돼 있던 것을 `VoteList` 하나로 통합 | 한쪽만 고치면 같은 투표가 화면마다 다르게 보이는 사고가 날 자리였다 |
| 2026-07-30 | Claude(AI) | `lib/calendar.ts` (신규) | 구글 캘린더·.ics 내보내기 함수 3종을 화면에서 분리 | 순수 함수라 화면에 있을 이유가 없다 |
| 2026-07-30 | Claude(AI) | `app/m/[code]/CLAUDE.md` (신규) | 파일별 소유자 · 이 화면에서 났던 사고 4건 · 데이터 흐름 | 이 화면이 프로젝트에서 제일 충돌이 잦다. 폴더별 짧은 지시가 전역 지시보다 잘 지켜진다 |
| 2026-07-30 | Claude(AI) | `tests/modals.spec.ts` (신규) | 후보 등록·수동 확정 모달을 실제로 열고 눌러 확인 | 모달은 조건부 렌더라 prop 하나만 어긋나도 빌드·타입검사를 통과한 채 조용히 안 열린다 |

**남은 일**: `MeetingClient.tsx` 906줄 = 로직 572 + 조립 334. 화면 조각은 전부
나왔으므로 **소유권 분리는 끝났다**(팀원이 만질 파일은 모두 400줄 미만).
더 줄이려면 로직을 `useMeeting()` 훅으로 빼면 되지만, 그 코드는 어차피 통합
세션 소유라 충돌 방지 효과가 없다 — 급하지 않다.

**검증**: `npx tsc --noEmit` · `npm run build` · 스모크 4/4 통과
(플래그 off/on 양쪽 확인 — on 에서는 투표 UI 가 채팅으로 대체되므로 모달 테스트는 skip).
스모크를 **결과 화면까지** 확장했고, 출발지 등록은 API 가 아니라 **UI 를 직접 조작**한다.
지도·방장 바는 리팩터 영향이 커서 스크린샷으로 눈 검사까지 했다.
추천 결과가 리팩터 전과 **동일**함을 실서버로 확인
(강남역+홍대입구 → `종로3가(48/13)` → `시청(48/19)` → `사당(55/26)`, 순서·문구 그대로).
스모크의 실효성은 `MeetingClient` 를 일부러 깨뜨려 확인 — 빌드는 통과하고 스모크만 실패했다.

---

## v7.0.0 — 2026-07-28

### v7 — v3 인프라 재사용 + 피그마/목업 IA 재구성 (feat/v7-mockup)

CEO 결정: 처음부터 재구축하지 않고 v3(main)의 구현(카카오맵 SDK,
지오코딩, 모임 API, 스토어)을 베이스로 가져와 v7 피그마/목업
(jarvis-brain의 moimer_mockup_v7.html) 디자인으로 UI를 재구성.
v7에서는 투표 실로직·AI 채팅 모두 비활성(화면 플로우만).

| 날짜 | 작업자 | 대상 파일/폴더 | 변경 내용 | 사유 |
|---|---|---|---|---|
| 2026-07-28 | Jarvis(AI) | 전체 | develop 스켈레톤(src/) 제거, main(v3)의 app/·lib/·설정 이식 | v3 재사용 결정 |
| 2026-07-28 | Jarvis(AI) | `app/globals.css` | v7 컴포넌트 스타일 추가 (하단 5탭·헤더 3버튼·검색/자동완성·출발지 칩·스플래시·투표 UI·모달·내정보 카드) | 목업 v7 디자인 반영 |
| 2026-07-28 | Jarvis(AI) | `app/components/v7/` | Icons·BottomNav·V7Header·Splash 신규 | 목업의 탭/메뉴 구조 그대로 구현 |
| 2026-07-28 | Jarvis(AI) | `app/page.tsx` | v7 홈으로 재작성 — 검색 자동완성 → 출발지 칩(최대 8) → 공평 중간지점(recommendRegions 재사용) → 카카오맵 → 주변 카테고리 리스트 | 목업 비회원 화면 |
| 2026-07-28 | Jarvis(AI) | `app/api/geocode/route.ts` | 자동완성 API 신규 (카카오 키워드 검색, 키 없으면 HUBS mock) | 출발지 입력 채택안(회의록 1차) |
| 2026-07-28 | Jarvis(AI) | `app/api/places/route.ts` | 중간지점 주변 정보 API 신규 (카페/음식점/술집/교통, mock 폴백) | 중간지점 하단 카테고리(회의록 1차) |
| 2026-07-28 | Jarvis(AI) | `app/meetings/page.tsx` | 모임 탭 신규 — 목록(최근/이전) + 생성/참여 모달, 초대 URL 자동 생성·클립보드 복사 (v3 /api/meeting 재사용) | 회의록 3차 모임 생성/참여 플로우 |
| 2026-07-28 | Jarvis(AI) | `app/votes/page.tsx` | 투표함 탭 신규 — 지역/가게 투표 탭 + 스텝 트래커 + 투표 pill (로컬 상태만, 실로직 비활성) | v7 시각 플로우 (CEO 결정) |
| 2026-07-28 | Jarvis(AI) | `app/members/page.tsx` | 모임원 탭 신규 — 참여 중 모임의 참가자 목록 | 목업 하단 탭 구조 |
| 2026-07-28 | Jarvis(AI) | `app/me/page.tsx` | 내정보 탭 신규 — 저장 위치·이동수단 프리셋(localStorage), 스케줄 가져오기(후순위 안내) | 목업 마이페이지 |
| 2026-07-28 | Jarvis(AI) | `package.json` | name moimer·v7.0.0, 미사용 캡처 의존성(puppeteer/pngjs/gifenc) 제거 | 설치 경량화 |

검증: `npx tsc --noEmit` 통과 · `next build` 통과 · 프로덕션 서버 기동 후
홈(출발지 3개→중간지점 강남역 mock)·투표함·모임 생성(실제 코드 발급)·내정보
플로우 스크린샷 확인. 카카오 JS/REST 키 미설정 시 전부 mock으로 동작.

## v1.0.0 — 2026-07-21

### 프로젝트 초기화 (SyncSpot v4 → 모이머 v1.0)

| 날짜 | 작업자 | 대상 파일/폴더 | 변경 내용 | 사유 |
|---|---|---|---|---|
| 2026-07-21 | Jarvis(AI) | 전체 | SyncSpot v4 복사 → moimer 프로젝트 생성 | 프로젝트 분리 및 리네임 |
| 2026-07-21 | Jarvis(AI) | `src/` 폴더 전체 | `app/` → `src/app/`, `lib/` → `src/lib/` 가이드 스펙 폴더 구조 적용 | mds/CLAUDE.md §2 폴더 구조 규칙 준수 |
| 2026-07-21 | Jarvis(AI) | `package.json` | name: moimer, version: 1.0.0 | 프로젝트 식별 |
| 2026-07-21 | Jarvis(AI) | `package.json` | zustand, @tanstack/react-query, react-hook-form, @hookform/resolvers, zod, clsx, tailwind-merge, class-variance-authority, lucide-react, tailwindcss-animate 추가 | 가이드 스펙 패키지 맞춤 |
| 2026-07-21 | Jarvis(AI) | `src/app/globals.css` | shadcn/ui CSS 변수 토큰 (light/dark 모드) 적용 | 가이드 §3 디자인 토큰 |
| 2026-07-21 | Jarvis(AI) | `tailwind.config.ts` | shadcn 컬러 토큰 + tailwindcss-animate 플러그인 | shadcn/ui 연동 |
| 2026-07-21 | Jarvis(AI) | `tsconfig.json` | `@/*` alias → `./src/*` | src/ 기반 절대경로 |
| 2026-07-21 | Jarvis(AI) | `src/lib/utils.ts` | cn() 유틸 추가 | shadcn/ui 컴포넌트 클래스 병합 |
| 2026-07-21 | Jarvis(AI) | `src/components/ui/button.tsx` | shadcn/ui 스타일 Button 컴포넌트 | UI 컴포넌트 표준화 |
| 2026-07-21 | Jarvis(AI) | `src/components/ui/input.tsx` | shadcn/ui 스타일 Input 컴포넌트 | UI 컴포넌트 표준화 |
| 2026-07-21 | Jarvis(AI) | 앱 전체 UI 텍스트 | SyncSpot → 모이머(Moimer) 리네임 | 프로젝트명 확정 |
| 2026-07-21 | Jarvis(AI) | `mds/` | 가이드 문서(CLAUDE.md, AI-GUIDE.md, skills/) 포함 | 가이드 self-contained 구조 |
| 2026-07-21 | Jarvis(AI) | `CLAUDE.md` | 모이머 전용 AI 지시서 생성 | 이 프로젝트의 규칙 기준 문서 |
| 2026-07-21 | Jarvis(AI) | `CHANGELOG.md` | 변경 기록 파일 생성 | 가이드 §5 변경기록 규칙 준수 |
| 2026-07-23 | Jarvis(AI) | `src/app/api/transit-time/route.ts` | ODsay 응답 타입 명시(`any` 제거), transfers 음수 방지, walkMinutes/fare 반환 추가 | TypeScript 규칙 준수 및 데이터 품질 개선 |
| 2026-07-23 | Jarvis(AI) | `src/types/index.ts` | TravelTime에 transfers/walkMinutes/fare 필드 추가 | 대중교통 세부 정보 표시 준비 |
| 2026-07-23 | Jarvis(AI) | `src/lib/recommend-engine.ts` | getTransitMinutes → getTransitResult 교체, calcTravelTimes에서 환승/도보/요금 필드 수집 | TravelTime 세부 정보 반영 |
| 2026-07-23 | Jarvis(AI) | `src/lib/recommend-engine.ts` | estimateFallback 자차 로직 개선 — 도로 보정(×1.4) + 거리별 속도 적용 (5km미만 20/15km미만 30/30km미만 50/이상 70) | 자차 이동시간 추정 현실화 |
| 2026-07-23 | Jarvis(AI) | `src/app/api/car-time/route.ts` | TMAP 자동차 경로 API 라우트 생성 — 이동시간/거리/통행료 반환, TMAP 실패 시 isEstimated:true | 자차 실제 이동시간 연동 |
| 2026-07-23 | Jarvis(AI) | `src/lib/recommend-engine.ts` | getCarResult 함수 추가, calcTravelTimes에서 자차도 TMAP API 우선 사용 (실패 시 Haversine fallback) | 자차 실제 경로 기반 이동시간 적용 |
| 2026-07-23 | Jarvis(AI) | `src/types/index.ts` | TravelTime에 distanceKm 필드 추가 | 자차 경로 거리 표시 준비 |
| 2026-07-23 | Jarvis(AI) | `.env.local` | TMAP_API_KEY 추가 | TMAP API 연동 |
| 2026-07-23 | Jarvis(AI) | `src/lib/algo/travel-time-display.ts` | 이동시간 표시 유틸 — formatTravelTime, getTravelSummary, confidenceBadge, calcDepartureClock | 슬로건3 통합 |
| 2026-07-23 | Jarvis(AI) | `src/lib/algo/date-highlight-logic.ts` | 날짜 겹침 하이라이트 — computeDateHighlights, toHighlightMap | 날짜 투표 UI 고도화 준비 |
| 2026-07-23 | Jarvis(AI) | `src/lib/algo/meeting-type-scoring.ts` | 모임 성격별 카테고리 매핑 + 밀집도 점수 — commercialDensityScore, PURPOSE_TO_MEETING_TYPE | 슬로건4 통합 |
| 2026-07-23 | Jarvis(AI) | `src/app/meeting/[id]/page.tsx` | TravelBar 개선 — 실시간/추정 배지, getTravelSummary 세부 경로 요약(환승·도보·요금), formatTravelTime 포맷 | 슬로건3 UI 반영 |
| 2026-07-23 | Jarvis(AI) | `src/lib/algo/enhanced-scoring.ts` | 스코어링 고도화 — fairTime×0.70 + transitPenalty×0.15 + farePenalty×0.05 + densityBonus×0.10, normalizeTimeBy:120 | 미션A 통합 🤖 Generated with Claude Code |
| 2026-07-23 | Jarvis(AI) | `src/lib/algo/transit-strategy.ts` | ODsay 다중 경로 전략 — fastest/fewest_transfers/least_walk | 미션B 통합 🤖 Generated with Claude Code |
| 2026-07-23 | Jarvis(AI) | `src/lib/algo/yield-message-integration.ts` | 자차 양보 기여 메시지 — buildYieldMessages, createCarMinutesProvider | 미션C 통합 🤖 Generated with Claude Code |
| 2026-07-23 | Jarvis(AI) | `src/app/api/transit-time/route.ts` | 미션B 적용 — parseStrategy + pickTransit 으로 경로 선택 교체 | 다중 경로 전략 연동 |
| 2026-07-23 | Jarvis(AI) | `src/lib/recommend-engine.ts` | 미션A/C 적용 — scoreCandidateEnhanced(밀집도+세부지표), buildYieldMessages, purposeType 인자 추가 | 스코어링+양보메시지 통합 |
| 2026-07-23 | Jarvis(AI) | `src/lib/algo/car-flexible-logic.ts` | calcYieldContributions, MinutesProvider 추가 (yield-message-integration 의존성) | 미션C 완성 |
| 2026-07-23 | Jarvis(AI) | `src/types/index.ts` | RegionRecommendation에 yieldMessages?: string[] 추가 | 미션C 타입 확장 |
| 2026-07-23 | Jarvis(AI) | `src/stores/meetingStore.tsx` | buildRegionRecommendation 호출에 purposeType 전달 | 미션A 모임 성격 연동 |
| 2026-07-23 | Jarvis(AI) | `src/lib/joinCode.ts` | parseJoinTarget() 참가 파싱 유틸 추출 (BottomNav 시트 + participate 공용) | UX 재설계 미션2 |
| 2026-07-23 | Jarvis(AI) | `src/components/common/CreateActionSheet.tsx` | 통합 진입 바텀시트 신규 — 새 모임 / 모임 참가(인라인 코드 입력) | UX 재설계 미션2 |
| 2026-07-23 | Jarvis(AI) | `src/components/common/BottomNav.tsx` | 탭 5개→4개 (참가 탭 제거), 중앙 [+]를 버튼+CreateActionSheet 트리거로 전환 | UX 재설계 미션2 |
| 2026-07-23 | Jarvis(AI) | `src/lib/homeState.ts` | getHomeState() — S1신규/S2다음약속/S3액션/S4유휴 상태 판정 순수 함수 | UX 재설계 미션1 |
| 2026-07-23 | Jarvis(AI) | `src/app/dashboard/page.tsx` | 홈 화면 상태 기반 혼합형(E+F) 개편 — 상태별 히어로 카드 + INSPIRATION_CARDS 영감 카드 8개 | UX 재설계 미션1 |
| 2026-07-23 | Jarvis(AI) | `src/app/meeting/new/page.tsx` | ?purpose= 쿼리파라미터 읽어 purposeType 초기값 프리필 | UX 재설계 영감 카드 연동 |
| 2026-07-23 | Jarvis(AI) | `src/stores/meetingStore.tsx`, `src/app/meeting/[id]/page.tsx` | 채팅 실시간 반영 수정 — postgres_changes → Supabase Broadcast 전환, sendMessage 낙관적 업데이트(보낸 사람도 즉시 반영) | 새로고침 없이 실시간 채팅 |
| 2026-07-23 | Jarvis(AI) | `src/app/globals.css` | 맵 히어로 CSS 변수 토큰 추가 — 라이트/다크 + data-theme 오버라이드 (home-map-mockup.html 기준) | MapHero 컴포넌트 테마 지원 |
| 2026-07-23 | Jarvis(AI) | `src/lib/homeData.ts` | INSPIRATION_CARDS 상수 + getNextConfirmed() 유틸 신규 | 홈 데이터 로직 분리 |
| 2026-07-23 | Jarvis(AI) | `src/components/home/MapHero.tsx` | 지도 히어로 컴포넌트 신규 — CSS/SVG 정적 지도, 핀 수렴 pulse 애니, nextConfirmed 상태 분기, reduced-motion 지원 | home-screen-FINAL.md §2-1 구현 |
| 2026-07-23 | Jarvis(AI) | `src/components/home/InspirationRail.tsx` | 영감 카드 가로 스크롤 레일 신규 — INSPIRATION_CARDS, purpose 프리필 라우팅 | home-screen-FINAL.md §2-2 구현 |
| 2026-07-23 | Jarvis(AI) | `src/app/dashboard/page.tsx` | 홈 MapHero 기반 재작성 — 지도히어로+오버랩시트(CTA/참가/InspirationRail), CreateActionSheet 직접 포함 | home-screen-FINAL.md 후보2 최종 구현 🤖 Generated with Claude Code |
| 2026-07-23 | Jarvis(AI) | `src/app/my-meetings/page.tsx` | 내 모임 전용 페이지 신규 — 진행중/완료 탭, MeetingCard/SeriesGroup/StatusBadge 컴포넌트 | 홈에서 모임 목록 분리 (내 모임 탭 신설) |
| 2026-07-23 | Jarvis(AI) | `src/components/common/BottomNav.tsx` | 내 모임 탭 추가 → 5탭 (홈·일정·[+]·내모임·내정보), ListIcon 추가 | 내 모임/내 정보 탭 분리 |
| 2026-07-23 | Jarvis(AI) | `src/app/dashboard/page.tsx` | 홈 화면 정리 — 모임 목록 제거, 중복 CTA 제거, 영감 카드 4열 그리드로 재디자인 | 홈 UX 간소화 |

---

## v1.1.0 — 2026-07-21

### 채팅 AI 파싱 기능 추가

| 날짜 | 작업자 | 대상 파일 | 변경 내용 | 사유 |
|---|---|---|---|---|
| 2026-07-21 | Jarvis(AI) | `src/lib/ai/chatParser.ts` | Gemini 2.0 Flash 채팅 파싱 클라이언트 | 내부 채팅 → 모임 정보 자동 추출 |
| 2026-07-21 | Jarvis(AI) | `src/app/api/parse-chat/route.ts` | 서버 사이드 Gemini API Route, 일일 200회 제한 | API 키 보호 + 무료 티어 초과 방지 |
| 2026-07-21 | Jarvis(AI) | `src/app/meeting/[id]/page.tsx` | AI 파싱 버튼 + 제안 카드 UI 추가 | 채팅 기반 모임 정보 자동 업데이트 UX |
| 2026-07-21 | Jarvis(AI) | `.gitignore` (루트) | `src/lib/` 경로 허용 패턴 추가 | lib/ 무시 규칙 충돌 해결 |

---

## 앞으로 변경 기록 작성 방법

새 행을 아래 형식으로 추가:

```
| YYYY-MM-DD | 작업자 | 파일명 또는 폴더 | 무엇을 바꿨는지 | 왜 바꿨는지 |
```

예시:
```
| 2026-07-22 | 유환 | src/app/meeting/new/page.tsx | 모임 생성 폼 Zod 검증 추가 | 빈 폼 제출 버그 수정 |
| 2026-07-22 | Jarvis(AI) | src/lib/ai/gemini.ts | Gemini 파싱 래퍼 초안 | feat/ai-parsing 브랜치 |
```
