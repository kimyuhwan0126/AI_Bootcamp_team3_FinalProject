# ─────────────────────────────────────────────────────────────
# CODEOWNERS — 파일 소유권
#
# 여기 적힌 파일이 PR 에 포함되면 해당 소유자에게 자동으로 리뷰가 요청된다.
# GitHub 저장소 설정 → Branches → Require review from Code Owners 를 켜야
# "소유자 승인 없이는 머지 불가"가 강제된다.
#
# ⚠️ 팀원 GitHub 아이디를 아래 @TODO 자리에 채울 것.
#    아이디가 틀리면 GitHub 이 조용히 무시한다(에러가 안 난다) —
#    설정 후 PR 하나 열어서 리뷰어가 실제로 자동 지정되는지 확인할 것.
#
# ⚠️ 경로에 `[code]` 를 그대로 쓰지 말 것. CODEOWNERS 는 gitignore 문법이라
#    대괄호가 **문자 클래스**로 해석된다 — `[code]` 는 c·o·d·e 중 한 글자에
#    매칭되고, 정작 실제 폴더 `app/m/[code]/` 에는 절대 매칭되지 않는다.
#    Next.js 동적 라우트를 가리킬 때는 `*` 를 쓴다:
#        ❌ /app/m/[code]/sections/ChatPanel.tsx
#        ✅ /app/m/*/sections/ChatPanel.tsx
# ─────────────────────────────────────────────────────────────

# 기본값 — 통합 담당자가 모든 PR 을 본다
*                        @kimyuhwan0126

# ── 🔒 공용 파일: 반드시 통합 세션 리뷰 ──────────────────────────
#  여러 기능이 동시에 건드리는 곳이라, 각자 고치면 머지가 지옥이 된다.
/lib/types.ts            @kimyuhwan0126
/lib/store.ts            @kimyuhwan0126
/lib/persistence.ts      @kimyuhwan0126
/lib/db.ts               @kimyuhwan0126
/lib/flags.ts            @kimyuhwan0126
/lib/scoring/index.ts    @kimyuhwan0126
/lib/scoring/types.ts    @kimyuhwan0126
/app/globals.css         @kimyuhwan0126
/app/api/meeting/route.ts @kimyuhwan0126
/db/                     @kimyuhwan0126
/.github/                @kimyuhwan0126
/CLAUDE.md               @kimyuhwan0126
/package.json            @kimyuhwan0126

# ── 기능별 담당 ─────────────────────────────────────────────────
#  각자 자기 폴더/파일만 만지면 충돌이 물리적으로 안 난다.
#
# 추천 알고리즘 (스코어러는 파일당 1인)
# /lib/scoring/commercial.ts   @TODO-상권담당
# /lib/scoring/weather.ts      @TODO-날씨담당
# /lib/scoring/personal.ts     @TODO-개인선호담당
# /lib/weather.ts              @TODO-날씨담당
#
# 채팅 · AI 파싱
# /lib/ai.ts                   @TODO-채팅담당
# /lib/parse.ts                @TODO-채팅담당
# /app/m/*/sections/ChatPanel.tsx      @TODO-채팅담당
