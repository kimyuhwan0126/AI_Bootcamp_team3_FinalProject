# 보류 — 시작 키워드: **"기술스택 관련 설정"**

이 문서의 항목들은 사용자가 **"기술스택 관련 설정"** 이라고 말할 때 의논한다.
그 전에는 결정하지 않는다.

---

## 1. 카카오 로그인을 무엇으로 받나 (인증)

NeonDB 는 Supabase 와 달리 **Auth 가 없다.** 직접 붙여야 한다.

| 후보 | 장점 | 단점 |
|---|---|---|
| NextAuth (Auth.js) | Next.js 표준, 카카오 공급자 내장, 세션·JWT 자동 | 어댑터 설정 필요 |
| 카카오 SDK 직접 + 자체 세션 | 의존성 0, 흐름을 다 본다 | 토큰 갱신·CSRF 를 직접 만든다 |
| 외부 인증(Clerk 등) | 빨리 붙는다 | 사용자 데이터가 밖에 생기고 비용 |

**걸려 있는 것**: 신원 키(로그인=카카오 계정 식별자 / 비로그인=이름+PIN) · 방장 자격(v7 로그인 필수) · 재모임 자동 이전

## 2. 방장 전용 규칙을 어디서 막나 (권한 강제)

NeonDB 에는 **RLS 정책 엔진이 없다**(Postgres RLS 자체는 있지만 JWT 배선을 직접 해야 한다).

| 후보 | 장점 | 단점 |
|---|---|---|
| 서버 Route Handler 에서 검사 | 붙이기 쉽다 | 검사를 한 곳이라도 빼먹으면 뚫린다 |
| Postgres RLS 직접 배선 | DB 가 마지막 보루 | 세션 변수로 JWT 를 넘겨야 해 설정이 까다롭다 |
| 둘 다 | 안전 | 규칙을 두 곳에 쓴다 |

**걸려 있는 것**: 그릴링에서 나온 권한 경계표 전부(확정·투표 시작·되돌리기·강퇴·승인·삭제·마무리)

## 3. 남의 투표가 실시간으로 보이나

Supabase Realtime 이 없어졌다. 환경변수에 `NEXT_PUBLIC_FF_REALTIME=0`(끔, **미구현**)이 이미 있다.

| 후보 | 비고 |
|---|---|
| 폴링 | 환경변수 주석에 "끄면 기존 1.8초 폴링" 이라 적혀 있다 — 지금 방식 |
| SSE | 서버가 밀어 준다. 자체 서버라 연결 유지 비용 문제가 Vercel 보다 작다 |
| 실시간 포기 | 화면을 바꿀 때만 불러온다 |

---

## 배포 — 이미 정해진 것

- **Vercel 은 차후.** 지금은 **자체(로컬) 서버**를 쓴다.
- 주소: **`10.20.2.164:3100`** — 이 포트로 서버의 코드를 포함해 무엇이든 다룰 수 있다.
- 이전에 만든 **`모이머 devlop-selfhost`** 코드는 **지우고 새로 시작**한다.

### ⚠ 현재 상태 (2026-08-11 12:0x 실측)
이 PC 는 `10.20.2.214` 로 **같은 대역**인데 서버가 **응답하지 않는다**.

```
ping 10.20.2.164   → 대상 호스트에 연결할 수 없습니다 (ARP 응답 없음)
포트 3100 · 11434 · 80 · 443 · 22 → 전부 시간 초과
```

서버가 켜져야 ① 이전 코드 삭제 ② 새로 시작 ③ Ollama 1순위(GLM 5.2) 사용이 가능하다.
`devlop-selfhost` 코드는 이 PC 에는 없다(바탕화면·사용자 폴더 검색 결과 0건) — 서버 안에 있는 것으로 보인다.

---

## ⚠ 필요한 것: **Ollama API 키**

`glm-5.2:cloud` 는 Ollama 클라우드 모델이라 **API 키가 있어야 동작한다.**
현재 `.env.local` 에는 그 키가 **없다**(`OLLAMA_API_KEY` 0건).

지금 있는 것:
```
OLLAMA_PRIMARY_URL   = http://10.20.2.164:11434/v1   (사내망 · 지금 안 닿음)
OLLAMA_PRIMARY_MODEL = glm-5.2:cloud
OLLAMA_FALLBACK_URL  = http://localhost:11434/v1
OLLAMA_FALLBACK_MODEL= qwen2.5-coder:7b              (설치 안 됨 — 있는 건 gemma4:12b, qwen3.5:0.8b)
```

키를 받으면 `.env.local` 에 다음 줄을 추가한다:
```
OLLAMA_API_KEY=...
```
